# 세션 인수인계 (Session Handoff)

> **사용법**: 작업 마칠 때 이 파일 업데이트 → 다음 세션 시작 시 AI가 읽고 바로 이어받음
> 작업 시작 전 `CLAUDE.md` → `session-handoff.md` → `WORKLOG.md` 순서로 읽을 것.
> 🔴 **이 파일은 «지금 할 일»만 담는다.** 완료분은 `WORKLOG.md`에 적고 여기서 지운다 — 로그를 쌓지 마라.
> (**2026-08-17 세션 끝 정리**: 완료된 ① APT 인천 4건 묶음·검수장 왕복·동영상 스키마를 압축하고
> 맨 위에 「🔜 다음 세션 할 일 후보」 표를 신설했다. 우편함은 파일 큐(`홀덤검수/mailbox/MAILBOX.md`)로
> 전환됐다. 이전 정리 경위는 `WORKLOG.md`.)

---

## ▶▶▶▶▶▶▶▶▶ 새 세션 START HERE

## 📬 MAILBOX — 검수장·솔버와 주고받는 자리 (2026-08-16 신설 · 사장님 지시)

> **세션 시작 시 여기부터 본다.** 그동안 경로가 트랙 본문에 흩어져 있어 매번 다시 찾아야 했다.
> **받은 건 처리하고, 보낼 건 여기 적는다.** 사장님이 양쪽으로 옮겨 주신다.

| 방향 | 자리 | 규율 |
|---|---|---|
| **검수장 → 본체** (수신) | `~/Downloads/홀덤검수/reports/발신-본체-*.md` + 지시서 `fix-handoff-*.md` | 🔴 **검수 폴더는 읽기 전용.** 절대 수정하지 마라. **착수 전 `git -C ~/Downloads/홀덤검수 log --oneline -5`로 최신인지 확인** |
| **본체 → 검수장** (발신) | `docs/reply-to-review-<날짜>.md` | 본체 레포에 쓴다. 검수 폴더에 쓰지 마라 |
| **솔버 → 본체** (수신) | `~/Downloads/클로드-프로그램만들기/handoff-to-main-site/` | 🔴 「배포 대기 중」을 믿지 말고 **라이브 번들을 먼저 확인**하라 |
| **본체 → 솔버** (발신) | `docs/reply-to-solver-<날짜>.md` | 회신 경로가 없어 **낡은 요청서가 두 번 왔다**(2026-08-15 신설 경위) |

> 🆕 **08-17부터 우편함이 파일로 바뀌었다** — `홀덤검수/mailbox/MAILBOX.md`가 **유일한 큐**다.
> 세션 시작 시 그 파일에서 `🔴` 검색 → 자기 앞 행 상태를 직접 갱신(`mailbox:` 커밋, mailbox/ 한정).
> 사장님 릴레이는 더 이상 필요 없다. 아래 요약은 08-17 기준 스냅샷이다.

**📥 수신 처리 완료**: M-006(재판정 3건 전부 OK 종결·「데스크톱」 반박 수용) ✅
✅ **M-007·M-008·M-009 전부 종결** — 검수장이 **35행 전건 재판정, 무효 0**. KO 미해결 **6 → 0** · de **93**.
**📥 M-010·M-012 수신·처리 완료** (`9ec553a3`·`8aeba16f`) — 결과 전환 잔여 + Kernpunkte 1.
**📤 발신 1건 — 검수장 미확인 🔴**
· 🆕 **M-013** = `docs/reply-to-review-2026-08-18-m013.md` (M-012 반영 + 요청 3건 답)

> 🟢 **APT 인천 결과 전환 축은 이제 닫혔다.** 08-18에 글 전체를 훑어 **추가 결함 0**을 확인했고,
> 오탐 4종의 정체를 회신·WORKLOG에 기록했다(다음 사람이 같은 21곳을 다시 판정하지 않도록).
> 🔴 **약속한 것 하나**: `desc` 29편을 고칠 때 **로케일·슬러그·전후 자수 표를 회신에 첨부**한다 —
> `desc`는 검수장 원장이 «읽는» 텍스트라(추출기가 머리에 붙인다) 안 알리면 그쪽 원장에 무효가 뜬다.

> 🔴 **08-17 훅 전환이 «반쪽»이었다는 걸 검수장이 잡았다** — 메타·결과 note까지만 닿고
> **본문에 「지금 현장 등록하러 가라」는 행동 지시가 8로케일에 살아 있었다.** 08-18에 종결.
> **대회 종료 전환을 할 땐 네 자리를 함께 훑어라**(리드 · 하단 경고 · 형제 글 상호참조 · Kernpunkte).
> 상세 체크리스트는 `docs/update-calendar.md` 8/29 항목에 박아 뒀다.

> 🔴 **세 회차 연속으로 «영향 범위»가 지시서와 달랐고, 셋 다 KO 본사이트가 포함돼 있었다**
> (rangechart 8편 / pull 누수 27곳·9로케일 / 에퀴티 밴드 103곳·9로케일).
> M-009 §5에서 **지시서에 「이 결함이 언어 불변인가」 한 줄**을 요청해 뒀다.
> 그때까지는 **de 지시서를 받으면 본체가 무조건 전 로케일 + KO 레거시까지 전수를 뜬다.**
**🟢 M-002·M-003은 «미착수»가 아니다** — 08-17에 **§1-B가 반영됐다**. 남은 건 S4-S5의 구조·seoTitle 축(회신서 §4)

> 🔴 **원장 수치를 액면으로 받지 마라 — 층이 둘이다.** 08-17 실측: de 미해결 **총계 103** ↔
> **본문에 아직 살아 있음 75**(착수 시 83). 지시서의 「99」·핸드오프의 「101」과 셋 다 다르다.
> 총계는 «판정 건수»고 본체가 손댈 건 «지금도 본문에 있는 것»이다.
> 🪶 대조 도구는 `scratchpad/ledger-read.mjs`(읽기 전용) — **검수 폴더의 `ledger-check.mjs`는
> 원장을 `writeFileSync`로 덮으므로 절대 돌리지 마라.**
**📌 본체가 아직 못 보낸 답 1건**
GOP 메인 바이인 `KRW 2,7 Mio.` — **귀속 정정 + 값 재확정을 같이**(M-006에서 검수장이 UNVERIFIABLE 확인:
godsofpoker.com이 5월 대회만 싣고 있어 10/30~11/8 회차 1차가 없다). GOP 1차를 새로 파면 #55가 한 번에 닫힌다

> 🪶 **주고받을 때 지킬 것 둘** (둘 다 실제로 사고가 났던 자리)
> ① **상대가 준 대체안도 검산하라** — 08-16에 검수장 대체안이 틀려 새 결함을 7로케일에 심을 뻔했다.
> ② **「없다」는 «내 출처에 없다»일 수 있다** — Rule 85는 WSOP엔 있고 TDA·RRoP엔 없다. **둘 다 참이다.**

---

## 🔜 다음 세션 할 일 후보 (2026-08-17 세션 끝 기준 · 위에서부터)

| 순위 | 무엇 | 근거·메모 |
|---|---|---|
| 1 | 🔵 **de 축A — 회차 ①~⑤ 종결** · 회신 **M-008** + **M-009**(회차②③④) · ⑤는 **아직 미보고(M-010 필요)** | ①국적·비자 축 + 확정오류 3건(`1de9c73d`) ②`:::rangechart:::` 해체 + 게이트(`169685b8`) ③디렉티브 누수 35→0 + 게이트(`8002c216`) ④에퀴티 밴드 103곳·9로케일(`6e9c734f`) ⑤**G1 seoTitle 15편**(`6d9f42a5`). 🔴 **다음 = ⓐ desc 160자 위반 29편**(아래 별행) **ⓑ §3 클러스터 충돌 X1**(c벳 빈도 3값 — 정본 선택이 본체 결정) |
| 1-A | 🔴 **desc 160자 하드리밋 위반 — 전 로케일 29편** (08-18 발견) | §11-7 「절대 금지」인데 **`audit:hard --locale`이 메타 길이를 아예 안 본다**(프로토콜 명시 = 커버리지 구멍). en 7 · tr 4 · id 3 · pt 3 · es 2 · zh 2 · ms 2 · ar·fa·hi·ro·vi 각 1 · **최악 ms/hand-rankings 235자**. de 2편은 08-18에 이미 수정. 🔴 **순서 = 29편 수정 → 그 다음 게이트.** 게이트를 먼저 걸면 배포가 곧바로 깨진다 |
| 2 | **「홀덤사이트」 클러스터 착수 판단** — `/ranking` 14.1위·노출 28.7% | 최대 상승폭 후보. 🔴 착수 전 자동완성으로 의도 확인(플레이어 컨셉과 맞는지) + 홈 카니발 의심 확인. 아래 GSC 절 참조 |
| 3 | **GOP 1차 발굴** (귀속+값 재확정 → de `korea-marathon` #55 종결) | godsofpoker.com 본 페이지엔 10/30~11/8 회차가 없다 — events. 서브도메인 등 다른 창구를 팔 것([[data-hierarchy-mixing-error]] 참조) |
| 4 | 8/29 **EPT 바르셀로나 종료** → es 결과 전환 + D/F 검수(⓪ 잔여 1편) 8개 언어 | 캘린더 시한. 같은 파일 두 번 열지 않기 |
| 5 | 🔵 **UX·레이아웃 트랙 — 착수됨(진행 중)** · 본체 = `docs/ux-layout-brief-2026-08-17.md` | **08-17에 모바일 축이 열렸고 `/calculator` 1건 종결.** 다음 후보는 §2-A 아래 절 참조. hr 중복 12곳 · 안 보이는 박스 · 말미 4화면 · 색 부채는 그대로 남아 있다 |
| 6 | **P3·P4**(고아 클러스터 편입+게이트 · intl-footer) | `docs/nav-flow-audit-2026-08-16.md` §4. 🟢 **8/30 대기도 해제** — 귀인 보호가 폐기됐다 |

⏳ **기다리는 것**: 검수장 M-007 재판정(KO S5) · 2026 국별 엔트리 내분 발표(ja 보강)

> 🔴 **2026-08-17 사장님 결정 — P1 「9/13 판정」은 폐기한다.**
> 이유: *「개선했던 부분이 내가 알고 있는 부분이어서 굳이 개선 효과를 볼 필요가 없다.
> 무조건 개선이 되었을 것이다. 실질적으로 사이트가 좋아지는 방향으로 계속 나아간다.」*
> → **관련글 로직·클러스터 구성 재변경 금지 해제. P3·P4의 8/30 대기도 해제.**
> 잃는 것 하나만 기록해 둔다: 「관련글이 관련 없어서 안 눌린다」 가설의 **반증 기회**가 사라진다
> (`nav-flow-audit` §6). 기준선 문서는 폐기하지 말고 **참고용으로 남긴다** — 나중에 비슷한 판단을
> 할 때 「그때 얼마였나」가 필요하다.
> 🪶 **`holdem-spr` 건은 성격이 달라 그대로 둔다** — 이건 P1 귀인이 아니라 「'홀덤 spr'에서
> `/calculator`와 새 글 중 어느 쪽이 대표 URL이 되는가」라 우리 손이 아니라 구글이 정한다.
> 관련글 제외의 `until`은 만료 시 자동 복귀하므로 손댈 것 없음.

---

### 🔵 **08-17 UX 트랙 — 모바일 축 착수** · 본체 = `docs/ux-layout-brief-2026-08-17.md` §2-A

사장님 지시 「모바일 참여율이 떨어진다, 구조가 문제 같다」. **축이 셋으로 좁혀졌다 —
모바일 × 신규 × 블로그 상세.** 재방문에서는 **모바일이 데스크톱을 이긴다**(51.6 vs 47.3).

🔴 **다음에 이 축을 열 때 먼저 알 것 넷**
1. **전면 처방은 오진이다.** `/ranking`·`/solver`는 모바일이 이긴다. **자리별로 판정하라.**
2. **「답을 뒤로 미루기」는 답이 아니다.** 「한 줄 정답」이 첫 화면에 있는 건 GEO 자산이다.
   채울 것은 **「답 다음」**이다(nav-flow-audit §6의 «의도 교란»과 같은 자리).
3. ✅ **sticky 겹침 전수 스캔은 끝났다 — 회귀는 `/calculator` 1건뿐**(`node scripts/scan-mobile-fold.mjs`).
   🔴 **셸·헤더 높이를 건드린 뒤에는 이 스크립트를 다시 돌려라.** 이 유형은 «각각은 옳은 두 커밋»이
   만들어서 코드 리뷰로 안 잡힌다.
   🆕 대신 **껍데기 불일치**가 나왔다: 상단 크롬이 `/` 120 · 허브 100 · `/ranking` **56** · KO블로그 0으로
   제각각이고 **하단 탭바가 `/ranking`에만 없다.** 단 **크롬 비율로 참여율이 설명되지 않는다**
   (`/tournaments` 19%인데 74.7% ↔ `how-to-enter` 7%인데 42.2%) — **뭉뚱그리지 마라.** 상세 = 브리프 §2-A-4
4. 도구는 `node scripts/ga-device.mjs --days 28`. **(not set) 랜딩 = 봇**이니 빼고 읽어라
   (데스크톱에 9.3% 섞여 데스크톱 참여율을 눌러왔다).

✅ **종결 1건 — `/calculator` 첫 화면 회수**: 08-04 다크 그린 sticky 바가 08-05 헤더 증가로
**가시 0px**이 돼 있던 회귀를 제거 + 히어로 모바일 압축. 콘솔 547→**399px** · 계산기 가시 235→**383px**.
🪶 **`/en/calculator`엔 그 바가 없다**(08-04가 KO에만 적용). EN 히어로 압축은 **별도 판단**.

### 🆕 **08-16 내비·동선 진단 + 관련글 로직 교체 배포** (`f5a3d847`·`f727b515`) · 본체 = `docs/nav-flow-audit-2026-08-16.md`

사장님 지시 「사이트 구조 논의」로 GA4 28일 + 코드 전수 진단 → **P1(관련글·이전/다음을
클러스터 커리큘럼 기준으로) 배포 완료.** 관련 카드 유니크 목적지 **6편 → 56편**(산출물 실측).

> 🟢 **재변경 금지는 2026-08-17에 해제됐다**(사장님 결정 — 위 「다음 세션 할 일 후보」 절 아래 기록).
>    audit 문서 §2 기준선·§6 반증 조건은 **참고용으로 남긴다**(폐기하지 마라 — 나중에 비슷한
>    판단을 할 때 「그때 얼마였나」가 필요하다). 다만 **판정은 하지 않는다.**
> ✅ **P2 완료 (08-17 · `395b6a60` · 사장님 지시로 앞당김)** — 대회 가이드 5편+roadmap에
>    모바일 탭바 복구, 시리즈 칩 `hidden sm:flex` 해제. pv 목표 없음(구조 정합 복구).
> 🔜 **P3·P4 — 8/30 대기도 해제, 언제든 착수 가능**: ②고아 25편 편입 + `check:clusters`
>    게이트(**vercel.json buildCommand에도** 걸 것 — npm 훅 안 탐) ③intl-footer 링크 2개 →
>    `hubPagesFor`(`lib/hub-pages.ts`로 데이터 분리 필요). 상세는 audit §4.
> 🪶 `holdem-spr`의 관련글 제외는 `until` 만료 시 **자동 복귀** — 손댈 것 없다.
> 🪶 P3 착수 시 `apt-incheon-2026-guide`가 ①-1(훅 전환)과 같은 파일이다 — 같은 날 묶어라.

### ✅ **08-16 검수장 왕복 2바퀴 — 종결 (재판정까지 전부 OK)** · 경위는 `WORKLOG.md`

지시 2건 반영(`3fb3af6d`~) → 재판정 3건 전부 OK(M-006). 규율은 MAILBOX 절 「지킬 것 둘」에 통합했다.
🪶 하나만 추가로: **수정이 새 결함을 만든다** — 편집 후 게이트(de-style 등)를 **다시** 돌려라
(08-16 🔴 2건이 둘 다 그날 내가 들여온 것이었다).

### 🆕 **08-16 GSC 28일 실측 — 다음 작업 후보가 갱신됐다** (`81a35294`) · 경위는 `WORKLOG.md`

**총 클릭 357 · 노출 10,811 · CTR 3.30%** (2026-07-16~08-12)

> 🔴 **최대 기회 = 「홀덤사이트」 클러스터** — 노출 **3,102 = 사이트 전체의 28.7%** · 클릭 63(17.6%).
> `/ranking` **14.1위**(2,207노출·31클릭·CTR 1.4%)가 받고 **홈이 23.4위로 같은 쿼리에 함께 뜬다**(카니발 의심).
> 순위 14.7 → 1페이지까지 **4계단**이라 상승폭이 가장 크다.
> 🔴 **착수 전 자동완성으로 의도부터 확인하라** — 우리 컨셉(플레이어 관점)과 맞는지가 먼저다.
> 관련 쿼리: 홀덤 사이트 14.4(366) · 온라인홀덤 12.3(112) · 온라인홀덤사이트 10.1(50) · 해외 홀덤사이트 16.3(45)

> ✅ **「홀덤 SPR」 전용 해설 = 발행 완료** (2026-08-16 · `3392c141` · `/blog/holdem-spr`).
> 138노출·9.7위·클릭 0의 원인이 **의도 불일치**였다(상위 h1이 전부 「SPR이란 무엇일까」인데 우리는 계산기를 내밀었다).
> 🔜 **측정은 4주 뒤(9/13경)** — 「홀덤 spr」에서 `/calculator`와 새 글 중 **어느 쪽이 대표 URL이 되는지**를 본다.
> 🔴 **그때까지 이 글의 제목·H2를 건드리지 마라**(측정 오염 — (3)의 `hand-rankings` 사례와 같은 이유).
> 🪶 `3bet-pot-cbet`이 seoTitle에 「홀덤 SPR」을 갖고 있다. 지금은 미색인이라 카니발이 없지만
> **그 글이 색인되면 3자 경쟁이 된다** — 그때 역할 분리(정의 vs 실전 스팟)가 유지되는지 확인할 것.

**🔴 이 축을 다시 만질 때 지킬 것 셋** (전부 이번에 값을 했다)
1. **「0클릭」을 한 원인으로 뭉뚱그리지 마라.** 5자리를 열어보니 제목 문제는 2건뿐이고
   나머지는 **의도 불일치 · 쿼리 오염 · 측정 중 · 이미 성공한 훅**이었다.
   일괄 처리했으면 **성공 자산 2개를 망가뜨렸다.**
2. **손대기 전 `git log -S`로 «언제 바꿨는지»를 봐라.** `hand-rankings` 제목은 **8/1 변경분**이라
   지금 또 바꾸면 그 효과를 영영 못 잰다.
3. **노출이 있다고 그 노출이 우리 의도인 건 아니다.** 「킹하이」 SERP는 5위가 **주차앱**,
   8위가 **등산**이다. 구글도 의도를 못 잡은 쿼리다.

🪶 **함께 발견된 카니발 1건**: 「홀덤 확률 계산기」 — `/calculator` 16.8위(49) ↔ `/blog/holdem-odds-calculator` 28.6위(27).

### ✅ **08-16 GSC 동영상 스키마 2건 종결** (`8c079ba2`) · 경위는 `WORKLOG.md`

🔴 **사장님 몫 하나만 남음**: GSC 「동영상」 보고서에서 두 항목의 **「수정 결과 확인」** 버튼(아직 안 눌렀으면).

### ✅ ⓪ **es 대회 글 점검 — 7/8편 종결** (2026-08-15 · `c37f5da4`~`ec9b3c69`) · 경위는 `WORKLOG.md`

검수장 왕복까지 한 바퀴 끝났다. **남은 건 1편과 미결 3건뿐이다.**

**🔜 남은 1편** — `ept-barcelona-2026-guide`
→ 오늘 Gold Pass 통화만 고쳤고 **D/F 검수는 미실시**다.
**8/29 종료 → 결과 아카이브 전환 때 함께 검수**하면 같은 파일을 두 번 열지 않는다.

**🔴 남은 미결 3건 — 전부 «확정 전엔 손대지 마라»**

| # | 미결 | 확정 방법 |
|---|---|---|
| 1 | **KO `apt-incheon`의 `$3.50` 스텝 위성이 실재했나** | 현재형 문장은 전부 $20 도식으로 고쳤고 **취소선 처리된 과거 기록 행 하나만 남겼다.** de 원장 #13은 「$20 통일」로 닫혔지만 `$3.50→$35`와 `$20→$200`은 **서로 다른 경로였을 가능성**이 있다. Natural8 해당 페이지가 시즌 종료로 내려가 지금은 판정 불가 |
| 2 | **JTBC 외국인 쿼터도 추첨제인가** | 공식 페이지에 「국내 등록은 첫날 매진돼 올해부터 **추첨제**」가 있는데 **외국인 쿼터 적용 여부가 불명**이라 글에 안 넣었다. 확인되면 한 줄 보강 |
| 3 | **`vs-cash-game` 「100-300BB」 ↔ `holdem-tournament` 「초반 100-200BB」** | 전자는 «시작 스택», 후자는 «초반 레벨 깊이»라 **모순이 아니다.** 딥스택 축을 다시 손댈 때 표기를 맞출지만 판단 |

**📮 검수장 왕복 = 한 바퀴 완료**
- 인계서 `docs/handoff-to-review-2026-08-15.md` → 검수장 회신 `홀덤검수/reports/회신-본체-es대회점검-2026-08-15.md`
  (지적 6건) → **본체 회신 `docs/reply-to-review-2026-08-15.md`에 전수 반영분 + D/F 완료분**
- 🔴 **다음 세션이 받을 것**: 검수장 **재판정** — KO `apt-incheon` 3건 · `korea-poker-marathon` 2건
- 🔴 **검수폴더는 계속 읽기 전용.** 회신은 본체 `docs/`에 쓰고 사장님이 옮긴다

**🟢 이번 세션에 검증된 도구 — 다음에도 이걸로 이겼다**
- **`pdftotext -layout` 파이프라인**(`pdftotext.exe`는 WinGet Poppler에 있다). WSOP 룰북·IRS Pub 515를
  받아 조항을 축어로 셌고, **오늘 최대 성과가 전부 여기서 나왔다**(2% 수수료·Rule 4a·Matrícula·조약 25개국).
- **Playwright로 라이브 축어 확인** — 등록 페이지 3%, JTBC 등록 마감일, 솔버 네트워크 요청 6건.
- 🔴 **접근 상태 정정**: 차단은 **casinos.at 계열 4개**뿐. **spielbanken-bayern.de·isar-poker.de는 정상**
  (기존 「403」 기록은 오탐이었다).

> 🔴 **오늘 세 번 걸린 함정 — 다음 세션은 여기서 시간을 벌 수 있다**
> ① **분모를 두 번 틀렸다** — Country Exclusives 8→**9**, JTBC 마감일 4→**8**.
>    둘 다 **언어별 표기 차이로 grep이 놓친 것**이다(`September 4` / `4. September` / `9月4日`…).
> ② **PowerShell `-replace`는 기본 대소문자 무시**다 — `masterUpdated`가 `masterupdated`로 깨졌다.
>    필드명이 걸린 치환은 **`-creplace`나 Edit 도구로.** CLAUDE.md §16의 변종이다.
> ③ **PowerShell이 UTF-8을 ANSI로 읽어** 빌드 산출물 검증에서 「문항이 없다」로 오판할 뻔했다.
>    산출물 판정은 `[System.IO.File]::ReadAllText($f, UTF8)`로.

---

### (참고) 원래 지시문 — 「es 버전 홀덤대회 포스팅 점검 한번 해보자」

**단 아래 ①(8/16 APT 인천)이 시한이다. 8/16이면 ①을 먼저 닫고 이 트랙에 들어간다.**
🪶 ①에서 **es `apt-incheon`을 어차피 연다** — 그 글은 이 점검에서 «이미 손댄 글»로 취급하라(중복 왕복 방지).

**대상 8편 (2026-08-15 실측 · 전부 `lib/posts-es/`)**

| slug | date | updated | masterUpdated | EN updated | 메모 |
|---|---|---|---|---|---|
| `apt-incheon-2026-guide` | 06-18 | 08-13 | **07-28** | 08-13 | 🔴 **드리프트 후보**(master < EN) · ①에서 먼저 열린다 |
| `wpt-australia-2026-guide` | 06-18 | 08-13 | **07-25** | 08-13 | 🔴 **드리프트 후보** · 9/30 종료 예정 |
| `ept-barcelona-2026-guide` | 06-17 | 07-25 | 07-25 | 07-25 | **8/29 종료** → 곧 결과 전환. 정합 |
| `korea-poker-marathon-2026` | 06-19 | 08-13 | 08-13 | 08-13 | 정합 |
| `wsop-2026-tournament-guide` | 06-16 | 08-06 | 08-06 | 08-06 | 8/5 결과 반영 완료 |
| `holdem-tournament` | 06-16 | 08-11 | 08-11 | 08-11 | ⚠ **잔여 2건이 이 파일에 있다**(아래) |
| `holdem-tournament-vs-cash-game` | 06-11 | 08-11 | 08-11 | 08-11 | 정합 |
| `como-entrar-al-wsop` | 07-27 | 07-27 | — | **없음** | ★ **es 고유 글**(EN 마스터 없음 · es-US 트랙). 미러 규율이 아니라 **단독 검수** |

🔴 **착수 전 필독** — `REVIEW-PROTOCOL.md`(3층 게이트) + `.cursor/rules/posting.mdc`.
그리고 **손대기 전 `WORKLOG.md`를 slug로 grep**하라(이미 작업한 글인지 — 안 읽어서 8/2에 2건 오판).

**이미 열려 있는 실탄 셋 — 이 트랙에서 함께 닫으면 왕복이 준다**
- 🔴 **`holdem-tournament` 딥스택 3중 자기모순** — 표 「100–200BB+」 ↔ 본문 「200BB+」 ↔ 일반 「100–300BB」.
  남은 로케일 **en es id pt zh zh-hant**이고 **de 수정본을 옮기면 된다**(일반=100–200BB · Deepstack=**기준선 없는 상대 개념**)
- 🔴 **`holdem-tournament`의 `:::note` 출처 표기** — casinos.at·spielbanken-bayern.de를 「abgerufen」(열람함)으로
  적어 뒀는데 **실측 403인 접근 불가 출처**다. 열람 주장을 뺄지 다른 출처로 바꿀지 판단 (아래 잔여 #3)
- 🔒 **국적·비자 축** — `apt-incheon` #105·#100 (①의 3번과 같은 축. ①에서 닫히면 여기선 확인만)

**es 트랙 고유 규율** ([[es-track-latam-reorientation]]·[[es-site-concept-player-not-judge]])
- 메인 독자 = **멕시코 + US 히스패닉 + 남미**(스페인 아님). Spain 베이스 42/42는 완결이고
  **다음은 LATAM 보충 패스(Country=Mexico부터, 슬랭 우선)** — 이 점검에서 대회 글에 그 축을 볼지는 판단
- **컨셉 = 판정자가 아니라 플레이어.** 합법성은 흥미로운 팩트로만, 부정형 결론 금지.
  **톤 검수를 사실 검수와 동급으로** 다룬다
- 이벤트 글은 [[event-guide-consistency-review]] — **정확성 ≠ 내부 일관성.** 각 문장이 참이어도 문서끼리 어긋난다.
  fact-instance 전수 diff · 표 불변식 대조 · 날짜 «종류» 구분

🪶 **참고**: `npm run audit:hard` es는 **전수 🔴 0건**이다(08-15 기준). 게이트가 초록이므로
**사람은 게이트가 원리상 못 잡는 D유형(전략적 유해 조언)·F유형(사실 오류)에 시간을 쓴다.**

---

### ✅ 종결 — **GA4 분석 세션** (2026-08-15 · 커밋 `3aef37f3`) — 경위는 `WORKLOG.md` 최상단

콘텐츠 수정 0건. 사장님 질문(「다국어 참여율이 낮은 게 포스팅 방식 탓인가」)에 대한 답만 남긴다:

> **아니다. 구조는 오히려 번역본이 두껍고**(자수 ko 14,337 vs en 23,089·es 25,799 · FAQ ko 8.0 vs es 13.8),
> **번역본은 KO에서 만든 것도 아니다**(EN 44편 중 30편이 KO에 없는 독립 글).
> 낮은 이유는 **유입 경로**다 — 번역본 265세션 중 **봇이 54%**(`(direct)` 131세션 평균 6초 · `ecosia` 3초)이고
> **구글은 4세션(1.5%)뿐인데 그 4세션은 참여율 100%·3분11초**로 ko 구글보다 높다.
> 봇 제외 시 격차는 21.5%p → **14.2%p**. **번역본을 「고칠 대상」으로 읽으면 오진이다.**

🔴 **다음에 GA4를 볼 때 반드시 지킬 것 넷** (전부 이번에 값을 했다)
1. **창은 28일** ([[analytics-window-28days]]) · `/solver`는 **8/14 이후**로 자른다(그 전 자기참조 referral)
2. **「세션당 평균 참여 시간」을 그대로 읽지 마라** — 잘 넘길수록 짧아 보인다.
   `/solver` 9초(참여율 **87.5%**) · `/pub` 14초(88.4%) · `/ranking` 24초(이벤트/세션 4.51 최고)는 **전부 정상**이다.
   판단은 `node scripts/ga-page.mjs <경로>`의 **랜딩(entrance) 기준**으로
3. **GA4의 「Organic Search」에 Bing·DDG·Ecosia가 다 들어간다.** 구글인지 보려면 출처까지 쪼개라
4. **GSC 「페이지 평균 게재순위」는 허상일 수 있다** — 숨겨진 1노출 롱테일이 평균을 올린다.
   `all-in-rules`가 페이지 13.8위인데 쿼리로는 **42~71위**였다. **결론은 쿼리 단위에서 낸다**

🪶 **아직 판단 안 한 채로 남은 것 셋** (다음에 GA4를 열면 여기부터)
- **`/tournaments`** 411세션 77.6%인데 **잃은 세션 92로 절대량 1위**(규모가 커서다)
- **`holdem-tournament-how-to-enter`** 126세션 **55.6%** — 블로그 오가닉 2위인데 ko 평균 이하. **①의 4번과 같은 글**
- **EN에서 가장 가까운 자리 둘** — `holdem-drawing-odds`(`odds of flopping trips` **23.0위**/30노출) ·
  `holdem-flush-vs-straight`(**612노출 = 전 번역본의 17%** · `does a flush beat trips` 26.3위). 손대면 여기부터

---

### ✅ ① **APT 인천 종료 처리 — 4건 중 3건 종결, 1건 남음** (경위는 `WORKLOG.md` 08-17)

1(2차 훅 전환 9로케일 `82e18b0f`)·2(국적 9로케일)·4(KO S5 11건 `3aa55086`)는 **끝났다.**

✅ **#3 국적·비자 축도 종결됐다 (08-17 · `1de9c73d`)** — de `apt-incheon` #105·#71 ·
`korea-poker-marathon` #57·#68·#69 전부 반영. **①은 4/4 완결이다.**
🔴 **다만 de 두 글로 끝나지 않았다** — 착수 전 전 로케일을 세니 같은 결함이
**7로케일 × 2편 = 14자리**였고 한 회차에 같이 닫았다. **ja는 원래 옳았다(이 축의 정본).**
경위·탐지기가 두 번 틀린 기록은 `WORKLOG.md` 08-17(7).

> 🪶 이 축의 함정 기록(「거주」 3사안 구분 · zh-hant 정본 · $1,684≠$1,703 통일 금지)은
> `docs/prep-2026-08-16-apt-incheon.md`에 있다 — de를 열기 전에 그 문서를 먼저.

---

### ✅ 종결 — **de 「뮌헨에서 포커 치기」 발행** (2026-08-15 · 커밋 `d4af7480`)

`poker-turnier-muenchen` 발행 완료. 경위·1차 출처·게이트는 **`WORKLOG.md` 최상단**에 있다.
🪶 `docs/de-munich-poker-serp.md`의 **셔틀 요금 7,50 €는 폐기된 값**이다(운수사 공식은 무료).
그 문서를 다시 쓸 때 이 줄을 먼저 보라.

> 🔴 **이 글에 남은 것 셋**
> ① **`updated` 관리가 이 글의 생명줄이다.** 우리가 이긴 이유가 「경쟁이 2024년 값을 들고 있다」인데,
>    **우리도 낡으면 똑같이 당한다.** 특히 SBPM 시즌(예선 9/6·결승 10/23~25)이 지나면 **11회차 서술이 통째로 낡는다.**
> ② **셔틀 요금은 두 공식이 갈린 채로 발행했다** — 운수사 「kostenlos」 vs 카지노 페이지 「7,50 €」.
>    글에는 양쪽을 적고 「타기 전 확인」으로 넘겼다. 한쪽이 갱신되면 글도 정리할 것.
> ③ **`PP`(Poker Bundesliga 참가비 단위)의 뜻은 확인 못 했다** — FAQ 답이 동적 로드라 못 열었다.
>    글에는 **표기만 옮기고 뜻을 지어내지 않았다.** 확인되면 한 줄 보강 가능.


### ✅ 종결된 트랙 — **`/solver` 랜딩** (2026-08-14~15) · 경위는 `WORKLOG.md`

랜딩 GEO 경화(H2 5→12 · 표 5 · FAQ 15 · 본문 9,600자) · 제목 재조준 11편 · 시리즈 묶기 ·
앵커 분산 · 진입점 신설(레일·사이드바·모바일 홈) · PWA 설치 안내 — **전부 배포 완료.**

> 🔴 **이 축을 다시 만질 때 먼저 읽을 것 넷**
> ① **유입은 100% 네이버다**(28일 19세션 · 구글 0). 네이버 순위 **홀덤 gto 표 1위** ·
>    홀덤 gto 프로그램 2위 · 홀덤 솔버 2위 · gto 솔버 3위 · 홀덤 gto 5위.
>    **거의 모든 쿼리의 대표 URL이 `/solver`다** — 「랜딩이 13편과 경쟁」 우려는 네이버에선 해결돼 있다.
> ② ⚠ **GA4 표의 「활성 사용자당 평균 참여 시간」을 그대로 읽지 마라.** `/solver`가 12초로 보이지만
>    랜딩(entrance) 기준은 **3분 22초 · 참여율 68.4%**다. CTA가 새 탭이라 **잘 보낼수록 짧아 보인다.**
>    판단은 `node scripts/ga-page.mjs solver`로.
> ③ **개수를 문장에 박지 마라**(CLAUDE.md §1). 우리 수치는 배열에서 계산(`SPOT_TOTAL`·`GTO_SERIES_TOTAL`),
>    솔버 소유 수치는 하한 표현(「1만 가지가 넘습니다」). 다른 도메인이라 자동 반영이 불가능하다.
> ④ **「솔버」 단독은 방탈출 카페**, **「gto」 단독은 만화·페라리**, **`spielbank feuchtwangen` 8,100은
>    카지노 전체**(자동완성에 tanzen·dresscode). 채택 전 자동완성으로 의도를 확인하라.
> 🪶 자동완성 API는 **`oe=UTF-8` 필수** — 응답이 EUC-KR이라 없으면 한글이 다 깨진다.

**남은 것 (급하지 않음)**
- 🪶 **역방향 앵커** — 13편이 랜딩으로 거는 브랜드 앵커 14곳. 「무료 GTO 솔버」·「홀덤 GTO 표」로
  일부를 갈지는 판단 사항(글을 열 때 함께).
- 🪶 **`/hand-chart`가 네이버 미색인**이다 — 「홀덤 차트」 480을 노리는데 빠져 있다. 수집 요청 대상.
- 🪶 **PWA 성과 판정 = 4주 뒤**. 기준선 **DAU/MAU 5.9% · 재방문 9.8% · 재방문 세션 385초**.
  🔴 **비율로 판정한다** — 재방문 «명수»는 SEO가 트래픽을 늘리면 저절로 오른다(주 단위 실측:
  절대수 9→77로 8.5배인데 비율은 7~13% 제자리, 신규가 19배 늘어서다).

---

## ▶ 그 밖에 열려 있는 것

### 🔴 1차 확인이 필요한 잔여 3건 (전부 **출처 확인이 선행**돼야 한다)

| # | 무엇 | 왜 |
|---|---|---|
| 1 | 🟠 **부분 종결 (08-15)** — KO `apt-incheon`의 `$3.50` | **현재형으로 권하던 4곳은 `$20` 도식·과거형으로 닫았다.** 남은 건 **취소선 처리된 과거 기록 행 하나**뿐이다. 🔴 **«$3.50 경로가 실재했는가»는 여전히 미확정** — `$3.50→$35`와 `$20→$200`이 서로 다른 경로였을 수 있고, Natural8 페이지가 시즌 종료로 내려가 지금은 판정 불가다. **취소선 행은 «종료된 과거»로 표시돼 있어 오독 위험이 없으니 서둘러 지우지 마라** |
| 2 | **`GGPoker SuperSatellites` 고유명** | 08-13에 «주장 제거»로 중립화만 했다. ggpoker.com 404로 1차 실패 — **검수장에 근거 URL 요청** 후 고유명 승격 여부 결정 |
| 3 | ✅ **종결 (08-15)** — `holdem-tournament`의 `:::note` 출처 | casinos.at을 「abgerufen」 목록에서 빼고 「자동 접근을 막아 대조 불가 → 출발 전 직접 확인」으로 바꿨다(데이터는 유지 — 거짓인 건 «열람했다»는 주장이지 데이터가 아니다). ⚠ **spielbanken-bayern.de의 403은 오탐이었다**(정상 접속) |

### ✅ 뮌헨 발행에서 딸려 나온 것 — **둘 다 종결**

- `check-de-style` 기존 🔴 2건 → **닫았다**(커밋 `33562294`). `wpt-australia` D11 1곳 ·
  `korea-poker-marathon` D12 2곳. 전건 원문 판정 후 표기만 고쳤고 **`updated`는 갱신하지 않았다**
  (콘텐츠 불변 — 따옴표 하나로 신선도를 주장하면 신호가 거짓이 된다. 선례 `ab991c3c`와 같은 취급).
  → **de 43편 전수 🔴 0건.**
- x-default 「버그」는 **오탐이라 폐기**(아래).

> ❌ **오탐 폐기 — `intl-blog-page.tsx` x-default 「버그」는 없다** (2026-08-15 확인 후 철회)
> 내가 「18행이 `${SITE}\en\blog\` 백슬래시라 `\b`가 백스페이스로 깨진다」고 적었는데 **틀렸다.**
> 파일 원문은 `${SITE}/en/blog/${slug}` — **바이트 수준에서 백슬래시 0개**다.
> **Grep 도구 출력의 이스케이프를 원문으로 착각**한 것이다.
> 산출물도 정상: `hrefLang="x-default" href="https://www.holdemmaster.com/en/blog/holdem-tournament"`.
> 🔴 **`check:hreflang`의 0건은 옳았다 — 게이트를 의심할 이유가 없었다.**
> 🪶 교훈은 [[review-mechanize-not-repeat]]의 **거울상**이다:
> 「없다」 보고 전에 탐지방법을 의심하듯, **「있다」 보고 전에도 탐지방법을 의심하라.**
> 도구 출력은 원문이 아니다(§12-B가 WebFetch에 대해 말하는 것과 같은 유형).
> 부수 확인: 산출물 정규식이 처음에 0건이었던 것도 탐지 문제였다 — Next.js는 소문자 `hreflang`이
> 아니라 **`hrefLang` 카멜케이스**로 렌더한다. **하마터면 「hreflang이 아예 없다」로 두 번째 오판을 할 뻔했다.**

### 📬 검수장 회신 잔여 — **GOP 하나만 남았다** (2026-08-16 갱신)

✅ **`hand-rankings` #65 종결 (08-16 · `9b57aa8d`)** — WSOP 룰북을 `pdftotext`로 다시 떠서 확인했다.
**Rule 85 축어**: 「the button will begin in the seat with the **first chip stack to the dealer's right**」.
검수장의 「TDA 2024·RRoP 어디에도 없다」는 그 두 룰북 기준으로 맞고 **우리 근거는 WSOP였다** —
🔴 **출처가 다르면 둘 다 참이다. 「부재」가 아니라 「룰북별 상이」로 다뤄라.**
원문 대조에서 우리 서술이 «참이지만 불완전»한 걸 찾아 「첫 **좌석**」→「첫 **Chipstack**」으로 정밀화했다
(빈 좌석은 건너뛴다 — 노쇼 있는 Day 1에서 갈린다).

🔴 **미처리 항목은 여기 적지 마라 — 맨 위 「📬 MAILBOX」가 본체다.** 두 곳에 적으면 갈라진다.
현재 본체가 못 보낸 답 1건(GOP 바이인 출처 귀속)도 거기 있다.

✅ **08-15 재판정 5건은 종결됐다** (검수장 커밋 `51a12d4` — KO `apt-incheon` 3건 + `korea-poker-marathon` 2건).
KO `apt-incheon`은 원장에 행 2건이 신설됐다(#56 국적자격 · #57 위성표 취소선).

> 🪶 `holdem-bubble #27`(FT버블 테이블 구성)도 남아 있다 — 검수장이 룰 중립안으로 교체했다
> (구안 「an den letzten zwei Tischen」은 TDA에만 맞고 **WSOP Rule 68.a(10인 한 테이블)와 충돌**).
> **그 글을 다시 열 때 닫아라.**

### 적대 QA 잔여 **6항목** — 코퍼스 단위라 **사장님 판단이 섞인다**

> ✅ **2026-08-15 종결**: `ungerader Chip` 오역 — **4곳**이었다(이 표엔 1곳으로 적혀 있었다).
> 정본 표기는 `holdem-split-pot-rules`의 **「Odd Chip」·「übrig gebliebene」**였다.
> ⚠ 이 표가 「`hand-rankings`가 überzähliger Chip으로 정정했다」고 적어 뒀는데 **거짓**이다 —
>   그 자리(408행)는 **무늬 서열** 문장이고 홀수 칩과 무관하다.
> 🔴 **아래 「실측」 칸도 낡았을 수 있다. 손대기 전에 다시 grep해 세라.**

| 항목 | 실측 | 왜 아직인가 |
|---|---|---|
| `rabattieren` / `Rabatt` | **13곳 / 4편**(outs 5 · position-play 4 · limping 3 · strategy 1) | 네이티브: 독일어에서 **상거래 할인 전용어**(`abwerten`·`discounten`이 실사용). 하우스 표준으로 굳어 있어 **반만 바꾸면 더 나쁘다** |
| `Side Pot` ↔ `Nebenpot` | betting-actions 4 · showdown 10 vs **split-pot 18** · readnext 카드 제목까지 갈림 | 독일 실사용은 압도적 `Side Pot`. 첫 등장만 병기 권고 |
| 드로 라벨 | `Gutshot-Straße` **3편**(equity·implied-odds·**pot-odds**) vs `Gutshot (Inside Straight)`(outs) — 08-15 재실측 | 드로 표에 `-Straße`는 **완성된 족보로 오독**된다. outs 표기가 정답 |
| `Leaks` ↔ `Lecks` | **8 대 8** (08-15 재실측 — 이 표엔 「equity 1곳만」으로 적혀 있었다) | 어느 쪽으로 통일할지가 판단 |
| `outs` 19,1% ↔ 19,6% | 같은 글에 나란히 있고 **둘 다 맞다**(플랍→턴 9/47 · 턴→리버 9/46) | 사실오류 아님. 한 줄 단서를 붙일지가 판단 |
| WSOP Rule 96.a | 연속 올인 예외가 `betting-actions`에 없다 | 누락이지 오류 아님 |

🔴 **오탐 2건 — 다음 렌즈도 같은 걸 지적한다. 프롬프트의 «의도적 제외 목록»에 넣어라:**
① **통화 `€10` → `10 €`**(21곳) — DIN 5008로는 네이티브가 옳지만 **`€` 선행이 정본**이다(2026-08-11 확정).
② `equity` 하프팟 flushdraw 35% — 바로 다음 문단이 그 전제를 뒤집는 **의도적 구성**이다.

### ▶ 대기 중인 큰 트랙

| 트랙 | 메모 |
|---|---|
| 🆕 **de 축A 미해결 101건** | 🔴 **08-16 검수장 리마인드로 확인된 트랙.** 이 핸드오프에 그동안 **안 보였다**(516행이 「§6 축 B ✅ 종료」로만 적혀 있어 축A가 통째로 빠져 있었다). 본체 미인지 지시서 **3건이 검수 폴더에 실재**한다 — `fix-handoff-de-S4-S5-2026-08-12.md`(⚠ rake C44는 **철회됨**) · `fix-handoff-de-S6-2026-08-12.md`(§1-B 6건만 인지 · ⚠ bubble #27은 **룰 중립안으로 교체됨**) · `fix-handoff-ko-tournament-entry-2026-08-12.md`(①-3 국적축과 같은 묶음) |
| C. 다국어 «판 전체 재동기화» | 🔴 **사장님 판단 대기 #2.** 노출 미미(전 언어 클릭 합계 2), 정확성 명분뿐. 🪶 **오늘 실측 하나가 붙었다**: `blind-meaning` 표 헤더가 **EN·zh-hant를 뺀 23 로케일 전부** 같은 결함인데 08-14에 5곳만 고쳤다 — 나머지 18곳(`master=07-11`)이 이 트랙 소관이다 |
| es LATAM 보충 패스 | Spain 베이스 42/42는 완결. **Country=Mexico부터, 슬랭 우선** |

---

## 🔴 사장님 판단 대기

| # | 안건 | 메모 |
|---|---|---|
| 0 | 🆕 **검수장 요청 — 대회 글 3편을 원장에 신규 편입할지** (08-15) | `wsop-2026-tournament-guide` · `como-entrar-al-wsop` · `korea-poker-marathon-2026`이 **어느 원장에도 없다.** 그래서 카드 수수료·등록 마감 같은 축이 «판정 0건»이었던 것이지 축이 빠진 게 아니다. 편입하려면 **행 추가가 아니라 신규 1편씩(주장 전수 추출 + 판정)**을 열어야 한다. 🪶 오늘 회신 §1·§8이 이미 판정해 둔 게 있어 **그게 그대로 첫 행들이 된다** |
| 1 | 🔴 `starting-hands-chart #13` — **수치를 고칠지 핸드 목록을 고칠지** | UTG **13%** 수치와 핸드 목록이 충돌한다(다른 4곳은 그 핸드들을 폴드하라고 한다). 조합수로는 13%가 맞다(144콤보 = 10.86%). **SEO·전략 판단이라 본체가 못 정한다.** 이것만 정해 주면 C층에서 유일하게 막힌 자리가 풀린다 |
| 2 | 다국어 «판 전체 재동기화» — **할지 말지부터** | 위 표 참조. 착수 시 본체는 `docs/multilang-sync-plan.md` |
| 3 | de 콘텐츠 신설 | 칩 분배 계산기(`verteilung rechner` LDA**7** · 비대체 자산 · KO에도 없어 역수입 가능) · 세금 FAQ 흡수(승인됨, 미착수 · BFH X R 8/21 1차출처 확보) · 툴 브랜드 대응(gto wizard 2,400 · equilab LDA3) → **솔버 다국어화 시 함께 결정** |
| 4 | 🪶 PDF 용지 규격 — ko·ja·zh도 A4 시장이다 | `generate-beginner-pdf.mjs`가 `@page { size: Letter }` 하드코딩이었다. 08-10에 언어별 `pageSize` 도입, **de만 A4**. 재생성은 `node scripts/generate-beginner-pdf.mjs ko ja zh` + CONTENT에 `pageSize: "A4"` 한 줄 |
| 5 | 🪶 PDF 수요는 족보 밖에도 있다 | `poker wahrscheinlichkeiten pdf` · `poker strategie pdf` 10/18 · `poker starthände tabelle` 110/**LDA5** |
| 6 | `es` 국기 판단 | |

---

## ⏰ 시한이 박힌 것

**본체는 `docs/update-calendar.md`다.** 현재 살아 있는 것 —

| 시점 | 무엇 |
|---|---|
| **8/16(일)** | **APT 인천 종료 → 위 4건 묶음** |
| **8/29** | EPT 바르셀로나 종료 → `ept-barcelona-2026-guide` 8개 언어 결과 아카이브 전환 |
| **9/초 (8/29~9/5)** | ★★ **GTO 13편·`/solver` 색인 성과 확인 → T24(솔버 다국어) 착수 판단.** 볼 것 셋은 캘린더에 있다. 🔴 **GA4로 솔버 유입을 볼 거면 구간을 8/14 이후로 잘라라** — 그 전은 자기 참조 referral로 오염돼 있다(8/14에 사장님이 「원치 않는 추천」 설정 완료) |
| **9/5경** | `/hand-chart` 색인 해제 효과 재측정 |
| **9/12** | 🆕 **de 43편 색인 재조회**(8/15 기준선에서 4주). 기준선 = ✅색인 **4** · **미크롤 36** · unknown 1(뮌헨 당일발행). 명령·판정 규칙·**오탐 2건 기록**은 캘린더에 |
| **9/10경** | 족보 필라 앵커 정비 효과 확인 — 아래 절 |
| **9/6 · 10/23~25** | de `holdem-tournament` SBPM 예선 마감 / 결승. 지나면 「11. Auflage」·개런티 갱신 |
| **10/초** | JOPT 오사카 바이인 공개(글에 약속함) 🔴 **단 대상 파일이 발행 취소돼 없다** — 글을 새로 쓰는 판단이 먼저 |
| **12/31** | K-ETA 면제 만료 — **한국 대회 글 전 언어 영향.** 가장 광범위하다 |

### ⏰ 9/10경 — 족보 필라 앵커 정비 효과 (`/hands` 재측정은 취소됨)

08-13에 §17 금지어 계열 7건을 걷어내고 그 자리를 **「홀덤족보」 붙여쓰기**로 채웠다(0건 → 7건).

```bash
node scripts/gsc-query-pages.mjs "홀덤족보" --days 7        # 8,100 ★우리 진짜 헤드텀(붙여쓰기)
node scripts/gsc-query-pages.mjs "홀덤 족보 순위" --days 7  # 390 (기준선이 있는 쿼리)
```

**기준선(2026-08-09 GSC 7일)**: 필라 「홀덤 족보 순위」 63노출 **12.1위** · 「포커 족보」 23노출 27.8위.

🔴 **「포커 족보」 18,100을 헤드텀으로 세지 마라** — 자동완성 21개에 **홀덤이 하나도 없다**
(마운틴·7포커·5포커·RDR2까지 섞인 **다른 게임들의 볼륨**). 33.7위인 건 **실패가 아니라 의도 불일치**다.
★ 세븐포커 의도는 `holdem-vs-7poker-hand-rankings`(8.9위)가 받을 자산이다.
⚠ 앵커 변경은 재크롤에 시간이 걸린다 — 4주에 안 움직여도 실패로 단정하지 말고 **8주까지** 볼 것.

---

## 🔴 손대면 안 되는 것 / 되살리지 마라

| 무엇 | 결론 |
|---|---|
| **`/hands` 색인** | 🔴 **noindex 유지.** 08-13에 열었다가 같은 날 되돌렸다. 필라가 29.4위 → **12.1위**로 오른 건 «실측된 성과»이고 도구가 9.4위를 회복한다는 건 «가설»이다. **실측된 성과를 가설에 걸지 않는다.** 양쪽 논거는 `app/hands/page.tsx` 상단 주석에 **전부 보존**돼 있다 — 열기 전에 반드시 읽어라. **「도구가 9.4위였다」는 이미 계산에 들어간 관찰이라 새 근거가 아니다** |
| **트레이너 랜딩 `/trainer` 신설** | 🟠 **기각.** ①노릴 키워드 10개 전부 볼륨 데이터 없음 ②이 축은 `/solver`가 이미 노림(홀덤 gto 390 등) ③`/solver` GSC가 쿼리 1·노출 1·32위 — **있는 진입로가 아직 아무것도 못 잡았다.** 대신 **랜딩에 흡수**했다(섹션 1 + FAQ 2문항, 새 URL 0개). 🪶 **재론 조건**: 「gto 트레이너」류에 실측 볼륨이 잡히거나, `/solver`가 「홀덤 gto」(390)에서 실제 노출을 얻은 뒤 |
| **`/pub/*` 11개** | ⏸ **사장님 지시로 보류 — 건드리지 말 것** |
| **`app/home-client.tsx`** | 🪶 **죽은 파일**(어디서도 import 안 됨 · 현재 홈 = `community-home`). 삭제할지 판단 대기. 08-13에 죽은 `schema` prop만 걷어냈다 |

---

## 🔗 솔버 프로그램과의 인계

솔버는 **별도 배포**(`solver.holdemmaster.com`)이고, 그쪽 세션이
`~/Downloads/클로드-프로그램만들기/handoff-to-main-site/`에 요청서를 놓는다.

- 🔴 **요청서의 「배포 대기 중」을 믿지 말고 라이브 번들을 먼저 확인하라.** 지난번엔 이미 배포돼 있었다
  (`curl` → 번들 grep). **순서를 반대로 잡으면 같은 불일치를 반대 방향으로 만든다.**
- 🪶 요청서의 분모·전제도 실측과 갈렸다(「13편 전부」 → 실제 11편 · 「`%팟` 표기」 → 0건).
  **검수장 지시서와 같은 규율을 적용하라 — 수치는 직접 센다.**
- 🪶 역방향 링크는 솔버 쪽 `ARTICLE_URLS`가 갖고 있다 — **포스팅 slug를 바꾸면 그쪽에 알려야 한다.**
- ✅ GA4 「원치 않는 추천」 = **2026-08-14 완료**(사장님 직접). 유입 측정 신뢰 구간은 **8/14부터**다.

### 📮 **회신 경로** — `docs/reply-to-solver-*.md` (2026-08-15 신설 · 경로 표는 맨 위 「📬 MAILBOX」)

🔴 **요청서가 낡은 채로 두 번 왔다**(EV 표기·H1은 08-13에 닫혔는데 08-15 갱신판에도 「급함」으로 있었다).
원인은 **회신 경로가 없어서**다 — 본체 판정이 이 파일에만 있어 저쪽이 못 본다.
→ **판정은 `docs/reply-to-solver-<날짜>.md`로 남기고 사장님이 전달한다.**

**최신 회신**: 🆕 **`docs/reply-to-solver-2026-08-17.md`** — 「«로그인 전엔 전송 안 함» 삭제」 요청 **반영 완료**
(`d3296472`). **요청서는 `faq.ts:71` 한 곳을 지목했는데 실측에서 두 곳이었다** — 110행은 08-15에
본체가 «같은 요청서의 직전 판»을 반영하며 만든 문장이라 요청서에 있을 수 없었다.
🔴 **요청서의 대상 목록은 «요청서가 쓰인 시점»의 것이다. 그 뒤 우리가 만든 자리는 우리가 찾는다.**
🪶 그쪽에 **확인 요청 1건 동봉**(솔버 앱 내 동일 유형 재점검 — 표현이 달라 단일 문자열 grep으로는 놓친다).

**직전 회신**: `docs/reply-to-solver-2026-08-15.md` — 전달 5건 + **설치 안전성 요청 1건** 판정
(#1 PWA ✅완료 · #2 EV 표기 **이미 닫힘(급하지 않다)** · #3 H1 **이미 닫힘** · #4 트레이너 랜딩 **기각 유지** · #5 GA4 ✅)

✅ **`요청_설치안전성_FAQ.md` 반영 완료** (08-15 · `483596c4`) — **FAQ 15 → 17문항**
(「홈 화면에 설치하면 안전한가요?」 + 「삼성 인터넷에서 «안전하지 않은 앱 차단됨»이 뜹니다」).
선택 문항이던 삼성 건도 **넣는 쪽으로 판단**했다 — FAQ는 설득하는 자리가 아니라
**그 문구로 검색해 들어온 사람에게 답을 주는 자리**다.
> 🟢 **문구를 옮겨 적지 않고 라이브로 검증한 뒤 썼다** — 첫 로드 네트워크 요청 **6건 전부 자기 도메인**
> (외부·애널리틱스·트래커 0건) · manifest에 **권한 선언 없음** · GitHub **Public·AGPL-3.0·222 커밋**.
> 그래서 「안전합니다」 대신 **「개발자도구 네트워크 탭을 열면 요청이 전부 solver.holdemmaster.com 안에서만 오갑니다」**로 썼다.
> ⚠ **「크롬으로 열면 경고가 안 뜬다」는 본체가 확인 못 했다**(삼성 기기 없음). 솔버 세션 보고를 근거로 채택했고,
> **코드 주석에 「반증이 나오면 이 문장부터 뺀다」**를 박아 뒀다. 그쪽 확인이 추정이면 알려 달라고 회신에 적었다.

**PWA 트랙 종결** — 요청서 `docs/handoff-to-solver-pwa.md` → 솔버가 배포(커밋 `611653b`) →
본체가 랜딩 설치 안내 + FAQ 반영. 라이브 확인분:
`manifest.webmanifest`(`start_url=/?view=trainer` · standalone) · `sw.js` **`Cache-Control: no-cache`** ·
`apple-mobile-web-app-capable`.
> 🔴 **「오프라인으로 GTO 계산 가능」이라고 쓰지 마라.** 오프라인은 **교육 예제 열람 + 트레이너 풀이**뿐이고
> 커스텀 계산은 엔진을 한 번 받은 뒤에만 돈다. **라이브를 열어봐도 확인되지 않는 종류의 오류**다.

**UTM 대응표** (측정할 때 쓴다 · `utm_source=solver` · `utm_medium=referral`)

| `utm_content` | 도착지 | 이 값이 크면 |
|---|---|---|
| `navbar` · `about-credit` | 홈 | — |
| `about-landing` | `/solver` | **앱에 온 사람이 설명을 더 원한다** |
| `preset-card` · `preset-preview` | 해설 13편 | 예제 → 해설 흐름이 돈다 |
| `trainer-feedback` | 해설 13편 | **트레이너 → 블로그 순환이 돈다** |
| `static-fallback` | 홈·`/solver`·해설 | JS 미실행 유입 |

🪶 **솔버엔 GA4 태그가 없다** — 설치·재실행·솔버 체류는 측정 불가(사장님 보류 판단).
   붙이려면 `G-CHSBJJDC14` + 교차 도메인이 먼저다.

---


## ▶ 백로그 (두 트랙 밖)

| # | 할 일 | 메모 |
|---:|---|---|
| 1 | 프로덕션 E2E 마감 | 검수 세션에 `BASE=https://www.holdemmaster.com`으로 요청 |
| 2 | 다국어 정책 페이지 | `/privacy`·`/terms`·`/contact`가 한국어뿐인데 링크는 영어 라벨 |
| 3 | 리버 승부 해설 | 복기 표에 승률만 있다. 「키커로 갈렸다」를 `score5`의 키커로 자동 생성 가능 |
| 4 | 🪶 FAQ 섹션이 아예 없는 2편(`ru`·`tr` 각 1편) | `check:faq-schema` 커버리지에 «미판정»으로 출력된다. **FAQ를 새로 쓸지는 SEO 판단** |
| 5 | 🪶 저가치 스키마 잔여 | `/about` `/contact` `/privacy` `/terms` `/login`에 WebPage/AboutPage/ContactPage 정도. **검색 수요가 거의 없어 우선순위 최하** |

---

## 📚 계속 유효한 규율 (트랙과 무관하게)

> 상시 함정·렌즈 운용 = `REVIEW-PROTOCOL.md` 맨 아래 · 다국어 경화 교훈 = `docs/lessons-i18n-hardening.md`.
> **트랙 착수 전 둘 다 읽어라.**

### ⚙ 미러 작업 4+1단계

0. 🔴 **어느 로케일이 «정답»을 갖고 있는지 먼저 찾는다. EN이라는 보장이 없다** —
   `equity`·`flush-vs-straight`·`all-in-rules`는 **de 정본**, `positions`·`implied-odds`는 **ja 정본**,
   `texas-…-beginners` #28·#29도 **de 정본**이었다(EN 포함 24곳이 안 따라와 있었다).
   정답 보유가 EN이 아니면 **EN을 그쪽에 맞춰 고친 뒤** 전파한다.
1. **EN 마스터에서 자리를 특정한다.** EN도 같이 고치고 `updated` 갱신.
2. **전 로케일 «동일 상대행»에서 원문을 뽑는다** — 번역본은 EN과 문단 순서가 평행하다.
   단 **FAQ 구역은 로케일마다 개수가 달라 어긋난다** → 문자열로 잡을 것.
3. **로케일별로 개별 작성.** 각 로케일의 **기존 어휘를 재사용**해 최소 침습으로.
4. **결함 문자열을 grep해 «잔존 0»을 확인.** 그 다음 게이트 → 빌드 → 커밋.

### 🔴 반복해서 값을 한 규율 다섯

1. **「N곳」을 가정하지 마라.** 실측에서 계속 깨졌다 — `split-pot #21` **ja 1곳** · `fish #30` **pt 1곳** ·
   `short-stack #1` 7곳(EN이 이미 옳았다) · `card-counting #20` **8로케일 × 2자리** ·
   `beginners #28·#29` **24로케일 × 2자리**. **언제나 직접 grep해 세고, 로케일별로 개별 판정하라.**
2. 🔴 **미탐을 «부재»로, 히트를 «존재»로 읽지 마라.** 08-14 하루에 세 번 걸릴 뻔했다 —
   betting-actions에서 ar·fa·he·pl·th 5곳이 「없음」으로 나왔으나 **전부 보유**했고(영문 `big blind`만 본 정규식),
   번카드 스캔의 fr=6·tr=4는 **전건 오탐**이었다(tr `yaklaşık`=대략). **그 자리를 직접 열어라.**
3. 🔴 **`masterUpdated` 스탬프를 신뢰하지 마라.** 같은 07-19 스탬프를 단 **de·es·zh가 두 건 다 미반영**이었다.
   스탬프는 «그 리비전을 봤다»는 뜻이지 «전부 반영했다»가 아니다 — **드리프트는 문자열로 세라.**
4. 🔴 **지시서·원장도 낡는다.** C층 잔여 6편 중 **셋이 이미 닫힌 건**이었다. **고치기 전에 실측하라.**
   그리고 **「N곳이 비어 있다」를 결함으로 읽기 전에 «정본이 그렇게 정한 것 아닌가»를 먼저 보라**
   (선례 커밋을 `git log -S`로 판다 — 이 레포는 커밋 메시지에 «왜 안 바꿨는지»를 적는다).
5. 🔴 **한정어를 «덧붙이는» 정정은 금지** — 거짓 주장 문자열이 살아남아 게이트에도 안 잡힌다.
   단 기존 서술이 «참이지만 불완전»하면 덧붙이는 게 맞다. 그리고 **기존 서술을 «빼는» 정정이 더 안전하다**
   (근거를 새로 지어내다 사고가 난 게 누적 4건).

> 🔴 **부재는 «누락»이 아니라 «해당 없음»일 수 있다 — 미러에서 새 문항을 만들지 않는다.**
> 판정이 끝난 자리는 **`docs/locale-intentional-diffs.md`**(append-only)에 한 줄씩 쌓는다.
> 그 파일에 **없다 = 「아직 판정 안 함」**이지 「차이 없음」이 아니다.

### 🔴 검수장이 준 대체 수치도 검산하라

「라이브는 핸드당 약 2분」의 근거로 WSOP Rule 126.c·TDA RP-8 B를 쓸 뻔했는데, 그 2분은
**버블 핸드포핸드에서 «시계를 깎는 규정값»**이지 실측 소요시간이 아니다.
**규정값을 실측으로 둔갑시키면 §12-B 위반이다.** → 이미 검증된 형제 로케일의 실무 범위를 채택했다.
🪶 같은 유형: **TDA RP-8 B 원문은 「2-minutes per hand」**로, 지시서의 「최대 3분」과 다르다.

### de 키워드 조사 도구 주의

🔴 라쿠 **서제스트는 시드 언어를 못 따라간다**(`vierling` → 미국 DB로 새서 미시간 식당·야구선수).
**지역·언어를 명시하는 `search-volume-history`(German/Germany)가 정답이다.**
그리고 조회한 독일 포커 키워드가 **12개월 −23~−69%로 전부 하락 추세**다 — 신규 투자 판단 시 감안할 것.

### 1차 출처 — 확보해 둔 경로 (재조사 금지)

> 🟢 **de 관련은 §5 「다시 파지 마라」 13축을 먼저 읽어라**
> (`홀덤검수/reports/fix-handoff-de-S1-S3-2026-08-12.md`) — 검수장이 «찾아봤고 없다»를 **근거와 함께**
> 박아 뒀다. **재조사에 시간 쓰지 마라.**
> ⚠ 1차 접근이 여럿 막혀 있다: `worldpokertour` 403 · `pokerstars` 404 · `casinos.at` 403 ·
> `spielbanken-bayern.de` 403 · `ggpoker.com` 404. **막힌 걸 「확인했다」로 적지 마라**(위 잔여 #3이 그 사고다).

- **WSOP 2026 공식 룰북**: `wsop.gg-global-cdn.com/wsop/pdfs/2026-WSOP-Tournament-Rules.pdf`
  → `pdftotext -layout`. 쓴 조항: 72·75·84·85·90(a/b.1/d)·95·96·97·99·100.b·103·116·117·119 + 용어집 DEALER BUTTON
  · **126 전문**: 126.a 「the clock being **paused**」 · 126.c 「each hand will run **2 minutes** off the clock,
  **regardless of how long the hand actually takes**」 · 126.d 배치 아닌 **매 핸드** · 126.e 「Blinds will **continue to increase**」
- ⚠ **`pokertda.com`은 이 환경에서 접속 불가**(curl·Playwright·WebFetch 전부 ECONNREFUSED).
  TDA 원문은 **버지니아 주 규제기관 공개 PDF**로 확보했다 —
  `ris.dls.virginia.gov/uploads/11VAC20/dibr/Poker Tournament Directors Assoc_2019 Rules-20221228170122.pdf`
  (2019 Longform + Illustration Addendum). 쓴 조항: 12·14·16·17·18-A/B·19·20·68 + Addendum 16/18 · **RP-8**
- ⚠ **Robert's Rules는 1차 출처로 쓰지 않았다** — 전문 호스트 `pokercoach.us`가 **Betsperts Media 소유**라
  «저자 원본»이 아니다. 깨끗한 호스트를 찾으면 캐시 캡 「a bet and three raises」를 되살릴 수 있다.

### 🔴 룰 인용 시 두 판을 갈라 써라

- **R1 캡**: 「3 raises → 4」는 **캐시게임에선 3이 맞다**(해제 조건도 캐시는 «핸드» 헤즈업이라 WSOP와 정반대).
- **무늬 서열**: WSOP 용어집 DEALER BUTTON의 「highest card by rank **and suit**」는
  「at the start of a **new game**」(= 캐시 신설 테이블)에만 적용되고 **Rule 85가 토너먼트를 명시적으로 배제**한다.
  → **「캐시 신설 테이블」로 한정해 쓸 것.**

### 적대 검수

- **렌즈 «종류»가 성과를 가른다.** 4렌즈에서 겹친 건 3건뿐이고 나머지는 전부 단독 검출.
  ★**룰북 축어 대조 렌즈**를 따로 두면 «내가 방금 박은 조항 번호»가 걸린다(12곳 중 2곳 불일치, 둘 다 그날 내가 넣은 것).
- **2차 교열 패스는 매번 값을 한다.** 반영이 새 결함을 만든 게 한 세션에 3+3+1건.

### 다음 언어 경화 6단계 (de 6/6에서 확립)

①정본 재확인 → ②**기계 게이트 먼저** → ③편집 → ④**되읽기 패스**
→ ⑤**현지 네이티브 적대 QA 2렌즈**(네이티브 + 교열, 교열엔 `git diff`) → ⑥빌드·게이트·커밋·WORKLOG

★**여섯 세션 전부에서 최대 ROI는 「이름표」 유형이었다** — 콘텐츠는 이미 있고 **현지 검색자가 쓰는
낱말만 없는** 상태. H2·표 이름·태그 교체로 닫히고 **§13 리스크가 0**이다.
실적: `blinds tabelle` · `Kombinationen` 4,400 · `karten reihenfolge` 880 ·
`poker begriffe` 260(vs `glossar` 10 = **26배**) · `pokerturnier` **720**.

> 🔴 **볼륨표는 리서치의 «요약»이지 대체물이 아니다. 글마다 현지 SERP를 실제로 봐라** —
> SERP는 볼륨표가 못 주는 «경쟁사가 무엇을 제공하는지»와 «어떤 낱말을 쓰는지»를 준다.
> 한 세션이 이걸 건너뛰어 `pokerblätter` 720과 PDF 어포던스를 통째로 놓쳤다.

---

## 📊 상태 (2026-08-15 실측 — 전 게이트 재확인)

| 지표 | 값 |
|---|---|
| build | **631 페이지 성공** (69 blog + **458** intl + static 60) — 08-15 de 신규 1편 반영 |
| `audit:hard` KO | **41/41 · 🔴 0건** (🟠 4건은 확률 글 표 헤더 대조 건 — 기존) |
| `check:faq-schema` | **456/456 성립 · 🔴 0편** |
| **네이버 색인** | GTO 13편 **8/13** · `/solver` ✅ · `/calculator` ✅ · 🔴 **`/hand-chart` 미색인** |
| **`/solver` 네이버 순위** | 홀덤 gto 표 **1위** · 홀덤 gto 프로그램 2위 · 홀덤 솔버 2위 · gto 솔버 3위 · 홀덤 gto 5위 |
| **재방문 기준선** | DAU/MAU **5.9%** · 재방문 **9.8%** · 재방문 세션 **385초**(신규 167초의 2.3배) |
| 🆕 **구글 색인 (de 43편)** | ✅색인 **4** · **미크롤 36**(`Discovered`, 크롤 시각 없음) · unknown 1(뮌헨 당일발행). **9/12 재조회** |
| 🆕 **번역본 유입 (GA4 28일)** | 265세션 중 **봇 54%** · **구글 4세션(1.5%)**. GSC 28일 번역본 구글 클릭 **합계 2** |
| `audit:hard` 로케일(en de es id ja pt zh zh-hant) | **전수 🔴 0건** (08-15 세션 종료 시점 재확인) |
| 🆕 **`/solver` 랜딩 FAQ** | **17문항** (설치 안전성 2문항 추가 · 산출물 `Question 17` 확인) |
| 🆕 **미러 드리프트** | es **6 → 4**(apt-incheon·wpt-australia 종결) · en 0 · ja 6 · zh 6 · zh-hant 7 · de 5 · id 4 · pt 6 |
| `check:hreflang` | 481페이지 · 50세트 · 🔴 0건 |
| 🆕 `check:rangechart` | **8/8 일치 · 🔴 0건** · 셀프테스트 **7/7**. `prebuild` + **`vercel.json buildCommand`** 양쪽 등록(훅이 프로덕션에서 안 도는 문제 회피) |
| 🆕 `check:directives` | 산출물 626개 · **`:::` 마커 잔존 0**(직전 35페이지) · 셀프테스트 **6/6**. `postbuild` + **buildCommand** 양쪽. 🔴 **산출물을 본다** — 소스의 `:::pull` 은 정상이고 결함은 «렌더 후에도 남는 것»이다 |
| `canonical:check` | KO 104 라우트 · 🔴 0건 |
| `audit:hard:selftest` | **76/76** · `check:de-style` 셀프테스트 **104/104** (de 43편 = 실제 43편, 누락·유령 0) |
| **구조화 데이터** | ✅ **66페이지 완료** (허브 10 + 로케일 50 + 도구 6). 실제 `schema={}` prop 잔존 **0건** |
| **검수장 재검수 원장** | 42편 2,988주장 · OK 2,558 · WRONG 30 · RISKY 116 — **순위1~5 종결.** 남은 WRONG은 🔒8/16 국적축 5건뿐 |
| **§6 축 B (de 키워드)** | ✅ 전부 종료 |
| **C층 트랙** | ✅ **종결 2026-08-14** — 잔여 6편 중 셋은 이미 닫혀 있었고 셋을 고쳤다(총 65곳) |
| **미러 전파 / de 구 검수 / 스키마 / 허브** | ✅ 전부 종료 — 경위는 `WORKLOG.md` |

> ⚠ **검수 폴더(`~/Downloads/홀덤검수`)는 검수장이 동시에 작업 중이다.** 읽기만 하고 **수정하지 마라.**
> 착수 전 `git pull` 필수. 원장을 볼 때는 최신인지 확인할 것.

## 📦 과거 로그는 아카이브로

**`docs/handoff-archive-2026-08-09.md`** — 2026-07-13 ~ 08-08 세션 로그 전체(약 3,980행).
그 이후 세션 로그는 **`WORKLOG.md`가 본체**다.
완료 트랙의 방법론은 `docs/gto-solver-series-spec.md`·`docs/kr-remediation-spec.md` §4에 남겼다.
