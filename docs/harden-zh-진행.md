# 🇨🇳 zh(간체) 경화 진행 파일

> 규격 = `docs/hardening-protocol.md` §8. 절차 본체는 그 파일 — 여기엔 **상태·미결·갈림·요청**만.
> 레인 = `Holdem-zh` / 브랜치 `harden-zh` / 담당 `lib/posts-zh/` 42편(대상 밖 13+1편은 프로토콜 §7-A).

## 1. 회차 표

| 회차 | 클러스터 | 편수 | 상태 | 커밋 | 렌즈 지적/반영 | 날짜 |
|---|---|---:|---|---|---|---|
| 1 | 규칙 | 6 | ✅ 완료 | `29aa0480` 머지됨 | 4종 32 / 반영 27 · 2차 교열 5 / 반영 5 | 2026-09-06 |
| 2 | 족보 | 6 | ✅ 완료 | `dd5fd538` · 머지 `28669901` | **5종 51 / 반영 37 · 기각 3 · EN-먼저 11** · **2차 교열 6 / 반영 5 · 자가검출 1** | 2026-09-07 |
| 3 | 확률 | 7 | ✅ 완료 | `7e839eb2` · **머지 `59aa52cd`**(충돌 0) | **5종 61 / 반영 46 · EN-먼저 7** · **2차 교열 15 / 반영 15** | 2026-09-07 |
| 4 | 전략 | 8 | ✅ 완료 | `818c08d0` · **머지 `706df5da`**(충돌 0) · sitemap `1177ede5` | **5종 83 / 반영 52 · EN-먼저 9 · 이관 4** · **2차 교열 13 / 반영 13** | 2026-09-08 |
| 5 | 토너먼트 | 9 | ✅ 완료 | `78fffc53` (헤드 머지 대기) | **5종 88 / 반영 62 · 기각 5 · EN-먼저 8 · 이관 4** · **2차 교열 23 / 반영 23 · 자가검출 2** | 2026-09-08 |
| 6 | 용어 | 6 | ✅ 완료 | `6f2f8c6c` · **머지됨**(main 포함 확인 2026-09-09) | **5종 104 / 반영 93 · 기각 9 · EN-먼저 2** · **2차 교열 15 / 반영 15 · 자가검출 1** | 2026-09-09 |
| **7** | **트랙 종결**(클러스터 아님 — 회차 1~6이 넘긴 미결 + EN 드리프트 8편) | **23** | ✅ 완료 | `e0f3fa7a`(B) · **`28714f97`**(C) · **머지 ff `90b59bd0`**(2026-09-09 · 헤드 대기열 39 전파 19편 + EN-먼저 판정은 다음 헤드 커밋) | **5종 42 / 반영 27 · 기각 4 · EN-먼저 5** · **2차 교열 15 / 반영 15 · 🔴 오출처 1 자가검출** | 2026-09-09 |

> 🔴 **회차 7은 클러스터가 아니다.** 42/42는 회차 6에서 끝났고, 이 회차는 «다음 회차 / 트랙 종결 회차»로 넘어온
> 미결 + **회차 중에 헤드가 두 번 고친 EN 드리프트 8편**을 닫는다.
> A 구간 산출 = 뱅크 `docs/keyword-bank/zh-track-close.md` · 브리프 `docs/harden-brief/zh-트랙종결.md`.
> **A 구간이 넘겨받은 처방 7건 중 3건을 실측으로 뒤집었고**(브리프 §14 ①②③), **C 구간의 렌즈가 A 구간 판정 3건을 다시 뒤집었다**(§10-A) — 이 회차의 성격은 «처방을 그대로 실행하지 않는 것»이다.
> 🔴 **커밋이 둘이다**: B 구간을 `HARDEN_SKIP_SYNC=1`로 중간 저장(`e0f3fa7a`)한 뒤 `git merge main`(충돌 0) → C 반영을 별도 커밋. 사유는 §2 «회차 7이 남긴 미결» ①.

**착수점(2026-09-06 헤드 실측)**: `audit:hard --locale=zh` 56/56 🔴 0 · 🟠 2 · `check:drift` 🔴 13 · §13 미검사 27편.
**회차 1 마감 시점**: `audit:hard` 56/56 🔴 0 · 🟠 2(변동 없음) · `check:drift` 🔴 **9**(규칙 4편 해소) · 빌드 70 blog + 525 intl.
**회차 3 마감 시점**: `audit:hard` 56/56 🔴 0 · 🟠 2(변동 없음) · `check:drift` 🔴 **8**(확률 7편은 원래 ✅ · 🟠 추적불가 **1 → 0**) · cjk 0 · meta 0 · seo-sync 0 · 빌드 exit 0 · 708 페이지 · 70 blog + 525 intl.
**회차 2 마감 시점**: `audit:hard` 56/56 🔴 0 · 🟠 2(변동 없음) · `check:drift` 🔴 **7**(족보 2편 해소 — kicker·tiebreak) · cjk 0 · meta 0 · seo-sync 0 · 빌드 exit 0 · 596 페이지.
**회차 6 마감 시점**: `audit:hard` 56/56 🔴 0 · 🟠 2(변동 없음) · `check:drift` 🔴 **2 → 0 · ✅ 56/56**(미러가 EN 마스터를 처음으로 완전히 따라잡았다) · cjk 0 · hangul 0 · meta 0 · seo-sync 0 · 거울쌍 6편 중 갈림 2편(둘 다 zh 고유 절) · 빌드 exit 0 · 708 페이지 · 70 blog + 525 intl. 🔴 **이 회차도 드리프트 이식은 0이었다** — 작업량 전부가 직답 블록 44개 + 오역·용어 정정이었다.
**회차 5 마감 시점**: `audit:hard` 56/56 🔴 0 · 🟠 2(변동 없음) · `check:drift` 🔴 **2**(fish·glossary = 회차 6 몫 · **토너먼트 9편은 착수 시점부터 ✅**) · cjk 0 · hangul 0 · meta 0 · seo-sync 0 · intl-links 525편 통과 · 빌드 exit 0 · 708 페이지 · 70 blog + 525 intl. 🔴 **이 회차는 드리프트가 0인 첫 회차**다 — 작업량 전부가 직답 블록·키워드·표기였다.
**회차 4 마감 시점**: `audit:hard` 56/56 🔴 0 · 🟠 2(변동 없음) · `check:drift` 🔴 **2**(전략 5편 해소 — 3bet·continuation-bet·limping·position-play·starting-hands-chart) · cjk 0 · hangul 0 · meta 0 · seo-sync 0 · 빌드 exit 0 · 708 페이지 · 70 blog + 525 intl.

## 2. 미결

- 🟠 `holdem-card-counting`에 `masterUpdated` 필드가 없다 → 회차 3에서 EN diff 실물 대조 후 필드 신설.
- 🟠 C2 형제 대조 중 **`3bet`↔`continuation-bet` 1쌍**이 «행 키 불일치»로 자동 대조 불가 → 회차 4에서 육안 대조.
  (`game-order`↔`flush-vs-straight` 1쌍은 **회차 1에서 해소** — 족보 순위 5=同花·6=顺子로 두 글이 일치, 결함 없음.)
- 🟠 **비정본 라벨 `快速答案` — 회차 2에서 2편 해소**(`holdem-reading-the-board`·`holdem-split-pot-rules`).
  남은 것은 `ept-barcelona-2026-guide` **1편뿐**(회차 5 토너먼트 몫). 정본은 `快速回答`(프로토콜 §7-B).
- 🟠 **`tldr`에 `**`가 든 zh 글 2편**: `ace-paired-board-strategy` · `blind-battle-connected-board`.
  `{post.tldr}`는 마크다운 파서를 안 타므로 **별표가 화면·JSON-LD에 글자로 나간다**(회차 1 실증 — §3 참조).
  두 편 모두 회차 1 대상 밖이라 손대지 않았다. → 해당 회차 또는 헤드 일괄.
- 🟠 **`blind-meaning` 최상단 직답(파일 L26)이 `tldr`과 거의 같은 문장**이다. tldr은 렌더 시 별도 aside로
  이미 출력되므로 첫 화면에 같은 말이 두 번 나온다. **원본 구조 유래**이고 다른 zh 글에도 같은 패턴이
  있을 수 있어 회차 1에서 손대지 않았다 → **전 로케일 구조 문제로 헤드 판정 필요**.
- ✅ 용어6 신마 변이 재점검(`translation-terms-zh.md` §0.5) — **회차 6에서 종결**. 6편 전수 대조 결과 §0.5 硬규칙은 전부 지켜지고 있었고(`cooler`·`bad beat`·`set over set`·`straddle` 영어 유지 · `爆冷`·`鱼`·`跟注站` 주词 유지 · `概率`·`四条` 간체 정본), **빠져 있던 것은 `冤家牌` 하나뿐**이라 cooler 첫 등장에 병기했다. 🔴 함께 나온 결함: 번체 `手風`(1자) · `抓头注`(정본에 없는 변형 → `抓头`) · `资金盘`(화어권에서 폰지 판을 뜻함 → `bankroll（资金）`).
- 🪶 「德州扑克为什么叫德州扑克」(라쿠 실측 질문문) FAQ 후보 — **본문에도 EN에도 기원 서술이 없다.**
  근거 없이 쓰지 않았다. 넣으려면 EN-먼저.

### 회차 2가 남긴 미결
- 🔴 **`二四法则` vs `2 和 4 法则` vs `4-2 法则` 3파전 — 회차 3에서 일괄 판정**(회차 1 뱅크 §4가 넘긴 것에
  세 번째 변이가 확인됐다). 정본 `translation-terms-zh.md` §8-3 = **`二四法则(=四二法则)`** ·
  레포 지배 표기 = `2 和 4 法则`(probability·pot-odds·outs 20건+) · `holdem-flush-vs-straight` L127만 **`4-2 法则`**.
  회차 2에서 flush 한 편만 고치면 클러스터가 갈리므로 **손대지 않았다.** 본거지는 확률 클러스터다.
- 🟠 **태그 `牌型概率` 소유자 미확정**: 회차 2가 `hand-rankings`에 `德州扑克牌型概率`를 넣었다가
  `holdem-probability`의 기존 태그 `牌型概率`와 같은 의도임을 렌즈가 잡아 **`德扑牌型胜率`로 회피**했다
  (자동완성 실측 「德州扑克牌型胜率」 존재). 회차 3에서 probability 쪽을 재판정할 것.
- 🟠 **`打公共牌`(kicker·reading 21건) ↔ `打公牌`(tiebreak 16건) ↔ `打牌面`(split-pot 3·reading 1) 표기 갈림.**
  정본 `translation-terms-zh.md` §8-2는 **둘 다 수록**(`play the board=打公牌/底牌无效`)이라 결함이 아니고,
  각 글 안에서는 일관되다. 회차 2는 **병기로 이었다**(tiebreak 블록에 `（play the board）` 1회 · kicker 블록에도 있음).
  🔴 전면 통일은 23건+ 치환이라 **레인 범위를 넘는다 — 헤드 판정 필요**. 네이티브 렌즈는 정본대로 `打公牌`,
  SEO 렌즈는 코퍼스 지배대로 `打公共牌`을 권해 **두 렌즈가 갈렸다**(둘 다 근거 있음).
- 🪶 **短牌 하우스 룰 「三条 > 顺子」** — 딜러 렌즈가 신마·마닐라 숏덱에 흔하다고 제기했으나
  **1차 출처 확인 실패**(triton.com.au 접속 불가)로 **기각**했다. `hand-rankings` L415 「其余牌型顺序两者一致」는
  그대로 뒀다. 근거를 잡으면 EN-먼저로 올릴 것.

### 회차 3이 남긴 미결
- 🔴 **인용부호 관례가 두 갈래다** — 본문은 CJK 안에서 ASCII `"…"`(편당 11~17회), **이번 회차가 만든 직답 블록은 `「」`**(implied-odds 8 · pot-odds 4 · card-counting 2 · equity 2 · outs 1 · probability 2 · drawing-odds 0). 인접 `texas-holdem-rules-for-beginners`는 또 `“…”`다. 네이티브 렌즈 판정: 정본이 `「」`를 허용하는 것은 «한 글 안에서 섞어도 된다»는 뜻이 아니다.
  🔴 **전면 통일은 회차 1·2 산출물까지 걸려 레인 범위를 넘는다 — 헤드 판정 필요**(§5-9). 간체 표준은 `“”`이고 `「」`는 대만/홍콩 관례라 zh-hant 전환 계획과도 얽힌다.
- 🟠 **`街道`(=시가지 도로) 오역이 `holdem-position-play`에 9곳 남아 있다.** `implied-odds` 9곳은 이번 회차에서 `街`/`后面几条街`로 정정했다(street의 중문은 `街`·`轮`이지 `街道`가 아니다). position-play는 **회차 4 몫**.
- ✅ **`对手们` 오역(`holdem-fish`) — 회차 6에서 해소.** EN 원문은 `Three distinctions **competitors** constantly blur`이고 여기서 competitors는 «경쟁 교학 글»이다 → `别的教学文章`. 회차 3의 `顶部页面`과 같은 유형.
- 🟠 **`pot-odds`에 比率↔百分比 절이 두 개**(L70·L161 · 표도 두 벌 1:1~6:1 / 2:1~5:1). 원본 유래이고 이번에 양쪽에 직답 블록이 붙어 같은 환산을 네 번 말하게 됐다(16.7/17% 표기 불일치도 이 이중 구조가 낳았고, 이번에 16.7%로 통일했다). **통합은 절 삭제가 걸려 회차 범위 초과 — 헤드 판정.**
- 🟠 **`德州扑克赔率表`(pot-odds 신설) ↔ `底池赔率计算`(pot-odds 신설)이 근사 중복**이다. 명사형/질문형 차이라 남겨 뒀으나 볼륨 실측으로 하나를 정리할 것. SEO 렌즈 확신도 «낮음».
- 🟠 **`drawing-odds` 단일 카드 기준이 형제 글과 다르다** — drawing-odds는 17%/8.5%(÷47), pot-odds 표는 17.4%/8.7%(÷46). **각 글 안에서는 일관하고 둘 다 산술적으로 옳으나 기준을 밝히지 않는다.** `outs` 표는 이번에 열 라벨을 「翻牌 → 转牌（1 张）」로 명시해 해소했다 — 같은 처방을 두 글에 적용할지 판정 필요.
- 🪶 `holdem-card-counting` 「德扑算牌和 21 点差在哪？对照表」 절에 직답 블록이 없다. **브리프가 «표가 이미 직답»으로 ⛔ 지정한 것**이나, SEO 렌즈는 태그 `德扑算牌和21点区别`가 정확히 이 쿼리라 AI 개요 자리를 비워 뒀다고 본다(확신도 중간~높음). 다음 회차 또는 트랙 종결 회차에서 재판정.
- 🪶 `holdem-implied-odds` 「实战算一遍：转牌圈的同花听牌」도 명사형 + 블록 없음이다. **절차 실연이라 의도된 것**이며, 다음 회차가 재지적하지 않도록 여기 기재해 둔다.

### 회차 4가 남긴 미결
- 🔴 **`扑克里什么时候该弃牌？` FAQ 질문이 `holdem-bubble`에도 있다.** 회차 4가 `holdem-strategy` 쪽을
  「新手最常见的弃牌错误是什么？」로 바꿔 **strategy↔when-to-fold 충돌은 해소**했으나, `bubble`은 회차 5(토너먼트) 몫이라
  손대지 않았다. 지금은 **when-to-fold ↔ bubble 2편이 같은 Question을 JSON-LD로 동시 발행**한다 → 회차 5에서 bubble 쪽을 바꿀 것.
- 🟠 **`劫机位`(glossary L121-122) ↔ `劫持位`(positions) 표기가 글 간에 갈린다.** 둘 다 Hijack의 역어로 정확하지만,
  이 두 글자 차이가 회차 4에서 잡은 사실오류(`starting-hands-chart`가 6맥스 UTG를 「劫机位」라 적은 것)의 토양이었다.
  glossary는 회차 6 몫 → ✅ **회차 6에서 `劫持位`로 통일**(glossary 2자리). 근거 = 코퍼스 실측(`劫持位`가 `holdem-positions` 15자리 이상 지배 · `劫机位`는 그 두 줄뿐) · 자동완성은 **둘 다 0**이라 SEO 손실 없음.
- 🟠 **블록↔본문 최장 축어 13~16자가 7편에 1건씩 남았다**(strategy 16 · positions 16 · starting-hands-chart 16 ·
  position-play 15 · continuation-bet 15 · when-to-fold 15 · limping 13). **20자 이상은 0건**으로 내렸다.
  SEO·2차 교열 두 렌즈가 모두 «강행 처방 아님»으로 둔 구간이고, 더 줄이려면 «이동» 원칙 자체를 버려야 한다 → **다음 회차 계량 대상**.
- 🟠 **1차 렌즈가 놓친 축어 2건을 2차 교열이 발견**했다: `holdem-limping` L152(22자) · `holdem-positions` L199(16자).
  둘 다 회차 4 반영분이 건드리지 않은 자리라 손대지 않았다 → 다음 회차 또는 트랙 종결 회차.
- 🪶 `holdem-continuation-bet` 태그 `什么时候不 c-bet` ↔ `k-high-board-cbet` 태그 `什么时候不该 c-bet`가 **Dice 0.84**로
  근사 중복이다(「该」 한 글자 차이). 자동완성 0인 축이라 회차 4는 손대지 않았다. **소유권은 GTO 시리즈 쪽이 자연스럽다 — 헤드 판정**.
- 🪶 `holdem-positions` 블록 2개가 규격(60~110자)을 넘는다(位置英文 154자 · 그 외 25/70블록이 110자 초과).
  SEO 렌즈는 「길지만 인용 가능해 좋다」로 **판단 유보**, 네이티브 렌즈는 「표 복창」으로 축약 권고 → 두 렌즈가 갈렸다.
  회차 4는 **네이티브 쪽을 채택**해 축약했다. 나머지 110자 초과는 열거형이라 손대지 않았다.

### 회차 5가 남긴 미결
- 🟠 **`德州扑克现金桌` 태그의 본문 근거가 얇다** — 뱅크 §3이 지시한 「본문 1회 병기」는 했으나 등장이 `快速回答` 블록 안 괄호 1회뿐(`现金局（也叫现金桌，英文 ring game）`). 서제스트 5~6건짜리 축의 근거로는 얇다. H2 `德州扑克里的现金局是什么？`를 `德州扑克现金桌（现金局）是什么？`로 병기하면 착지한다 → 다음 회차 또는 트랙 종결 회차.
- 🟠 **`短码策略`·`短码怎么打` 태그가 앵커 없는 `短码` 단독**이다(뱅크 §1-E 자릿수 함정 = 문자 단축번호). 기존 태그이고 이번 diff 밖이라 손대지 않았다 → 트랙 종결 회차.
- 🟠 **`wpt-australia` 卫星赛 표 1행↔2행이 완전 중복**이고, EN에 있던 검산 가능한 행(`$10K High Roller Direct Qualifier · Sep 22, 1-in-10 · $1,050` — `10 × ($1,050−$50) = $10,000` ✅)이 zh에선 「其余卫星赛」로 뭉개졌다. **diff 무변경 = 기존 이월 결함**이라 이번 회차 밖. EN이 옳다.
- 🟠 **`ept-barcelona`의 `:::note[关于来源]`이 「2026-08-31 시점 PokerStars 공식 미게시」라고 적는다** — 오늘(09-08) 기준 **재확인 시한이 지났다**. `docs/update-calendar.md` 대상(헤드).
- 🟠 **`六选一` 풀어쓰기가 절반만 됐다** — `wpt-australia` 블록에서 `1/6 卫星赛`만 「六选一」로 풀었고 표의 `1/5`(Poker Cup)·`1/8`(Mini)은 그대로다. 표 안에서는 분수 표기가 읽히므로 급하지 않다.
- 🟠 **태그 배열 안 표기 규칙이 로케일 안에서 갈린다** — `holdem-icm`은 `德州扑克 icm 意思`처럼 띄웠는데 `korea-poker-marathon`은 `春川马拉松2026报名`(붙임)·`韩国马拉松 2027`(띄움)이 섞여 있다. 🔴 **표기 하나가 320 대 0을 가르는 축**(메모리 `keyword-harvest-wildcard-positions`)이라 **실측 없이 건드리지 마라** → ✅ **회차 6에서 판정**(뱅크 `zh-glossary.md` §7): 같은 질의를 붙임/띄움으로 던지면 결과가 갈린다 = 구글이 완전 정규화를 하지 않는다. **CJK↔라틴은 띄우고 CJK↔CJK는 붙인다**(확신도 높음). `holdem-icm`의 띄움은 맞았고, **숫자 경계는 이 클러스터에 표본이 없어 미결 유지**.
- 🪶 **회차 4가 넘긴 「블록↔본문 최장 축어」 2건**(`holdem-limping` L134 22자 · 그 외)은 **계량 기준의 결함이었다** — §6 방법론 4 참조. 진짜 문장 중복은 `limping` 1건뿐이고 트랙 종결 회차 몫.
- 🪶 **`快速回答` 블록 안 `==**…**==` 중첩 볼드가 상단 리드 4개에 있다**(apt·ept·wpt·korea). **HEAD에도 있던 자리**이고 렌더러 검증 없이 손대면 하이라이트가 깨진다 → 헤드 판정. 🪶 이번에 신설한 블록 103개는 `**` **0건**(전수 확인).

### 회차 6이 남긴 미결
- 🔴 **`超对 vs 9 张 outs 同花听牌 ~65%（2:1）`는 과대다 — EN-먼저**(§5-23 ①). 전수열거로 9-out 조건의 상한은 **63.43%(1.73:1)**이고, 68.99%(2.22:1)가 나오는 배치는 오버페어가 그 무늬 A를 들어 드로가 **8 outs**로 줄 때뿐이다. zh·EN 동형이라 로케일에서 고치지 않았다.
- 🟠 **string bet 역어가 zh 코퍼스에 3갈래다** — `串注`(betting-actions L257 FAQ · 이번에 glossary를 여기로 통일) · `分段下注`(all-in-rules L65·L71) · 옛 `拉线下注`(제거됨). **회차 1 클러스터(규칙)가 이미 지나간 자리**라 all-in-rules는 손대지 않았다 → 트랙 종결 회차 또는 헤드 일괄.
- 🟠 **`holdem-tiebreak-rules`를 부르는 앵커 표기가 zh 코퍼스에 12갈래다**(`分胜负规则` 2 · `平局判定规则` 2 · `德州扑克平局怎么判` 2 · 그 외 8종 각 1). 이번 회차 6편 중 2자리만 고쳐도 통일이 아니라 셔플이라 **손대지 않았다**. 🪶 네이티브 렌즈는 local-voice §7 실측(zh는 「平局/怎么比大小」로 검색)에 따라 `平局` 계열을 권한다 → 트랙 종결 회차에서 코퍼스 전체 판정.
- 🟠 **`rake`에 신마 현장 대조점이 없다** — 정본 §0.5-3이 「RWS 센토사·겐팅의 rake 구조를 명시해도 된다」고 열어 뒀는데 글의 사례는 미국 `$1/$2·10%·封顶 $3–$6`뿐이다. zh 독자 중 «현장에서 매주 내는» 층은 신마다. 🔴 **구체 수치(5%·S$·RM)는 라이브 재확인 없이 쓰지 마라** — 이번 회차는 확인할 1차 출처를 확보하지 못해 넣지 않았다.
- 🟠 **`fish` 剥削표 1행 ⊃ 3행(수치상 포함관계)이 완전히는 안 풀렸다.** 2차 교열 반영으로 **행동 조건을 앞세워**(「어떤 사이즈에도 거의 弃牌 안 함」 ↔ 「작은 벳은 跟, 큰 벳은 弃」) 판별은 가능해졌지만, VPIP/PFR 구간 자체는 여전히 3행이 1행의 부분집합이다. 배타적 구간을 주려면 **없는 임계값을 만들어야** 해서 멈췄다.
- 🪶 **`bad-beat`의 첫 내부링크가 필라(`holdem-glossary`)가 아니라 `holdem-cooler`다**(CLAUDE.md §8 위반). **EN도 같은 순서**라 zh만 바꾸면 링크 구조 1:1 원칙이 깨진다 → §5-23 ②로 EN-먼저 등재. 나머지 4편은 필라가 첫 링크다.
- 🪶 **`straddle`에 `房规`가 13회 나온다.** 종류 표에 「看房规」 열이 이미 있어 본문 반복은 잡음에 가깝다(네이티브 렌즈). 이번 회차는 문장 파손 쪽을 우선해 손대지 않았다.
- 🪶 **《The Cooler》(2003)의 중문 개봉명을 확정하지 못했다.** zh 고유로 「幸运赌鬼」가 적혀 있었으나 **豆瓣·1905 둘 다 Playwright 접속이 막혀 1차 출처 확인 실패** → EN·zh-hant와 동일하게 **영문 제목만 남기고 삭제**했다. 근거를 잡으면 되살릴 수 있다.
- 🪶 **`深跑（Deep run）`는 직역투라는 지적이 있었으나 채택 유지**했다 — 자동완성 실측(`德州扑克 深跑 意思`)에 살아 있고 `holdem-tournament-vs-cash-game`이 이미 3회 쓰는 **코퍼스 공용어**다. 바꾸려면 전 코퍼스 동시에.

### 🔴 회차 7 2차 교열이 잡은 것 — **1차 반영이 A급 9건을 새로 만들었다**

프로토콜 §5-⑤(2차 교열 필수)가 네 회차 연속 값을 냈다. **가장 큰 것은 «오출처 이식»이다**:

- 🔴 **`holdem-bubble`에 `holdem-icm`의 문장을 잘못 이식했다.** EN 커밋 `af6ca275`의 diff를 읽으며
  「prize money is **spread across several places**」가 bubble 것이라고 봤는데, 실측하니 **EN bubble은 날짜만 바뀌었고**
  (`updated` 1줄) 그 문장은 `lib/posts-en/holdem-icm.ts` L49의 것이었다.
  → 이식이 ① bubble의 stone bubble 정의(「한 명 나가면 그 순간 전원 확정」)와 **정면 충돌**하고
  ② 같은 글 L199의 「奖金头重脚轻」과 **한 글 안에서 갈리고** ③ 뒤 문장의 인과 사슬을 끊었다.
  **되돌렸고, 진짜 대상인 `holdem-icm` L52에 이식했다.**
  🔴 **교훈: 다중 파일 커밋의 diff를 읽을 때 «어느 파일의 hunk인가»를 파일명으로 확인하라.**
  `git diff --stat`의 줄 수(bubble 6줄 = 3+/3−)를 «내용 변경»으로 읽은 것이 발단이다 — **날짜 2줄도 6줄로 센다.**
- 🔴 **방향 정정이 같은 글 다른 자리와 반대가 됐다**(`pot-odds`): 정정한 L242는 「所需胜率을 과대평가해 弃牌」인데
  L237 블록은 「每一个都会把本该弃的注变成跟」이라 5개 중 1번만 반대다. → 블록에 예외를 명시.
- 🔴 **정정하며 산수가 한 항 어긋났다**(`pot-odds` L192): 「자기 콜만」 빼면 분모는 80(`20 ÷ 80 = 25%`)인데
  두 베팅을 다 뺀 값(`20 ÷ 60 = 33%`)을 썼다. 세 줄 위 불릿이 그 33%를 「**두 笔**를 뺀 값」으로 이미 정의한다.
- 🔴 **노출면을 6곳 중 4곳만 고쳤다**(`icm` 「已锁定的奖金」 L108 블록·L245 FAQ 누락 · `apt` 「送出 74」 L145 누락).
  회차 5 방법론 6을 스스로 어긴 것이 이번 회차만 **세 번**이다(wpt 표↔블록 포함).
- 🔴 **이식이 절반만 돼 노트가 거짓이 됐다**(`ept`): 출처 노트를 「09-09 공식 대조 완료」로 갈았는데
  来源 절에는 「撰稿时 PokerStars **尚未** 공개」가 남아 **「见文末来源」이 가리킬 대상이 없었다.**
  EN은 来源에 3번째 불릿을 신설했다 → 그 불릿을 이식하고 옛 줄을 삭제.
- 🔴 **되돌리면서 원문에 없던 것을 새로 붙였다**(`wpt` 「赛前：」). `git log -S`로 회귀 이전 정본(`519240b6`)을
  확인하니 그 라벨이 없었다. **되돌리기는 «원문 복원»이지 «내가 다시 쓰기»가 아니다.**
- 🔴 **새 예외가 자기 절의 예시를 무효화했다**(`tournament` L164 블록의 유일 예시 «20인 1석 위성»이
  새로 이식한 winner-take-all 예외 그 자체) · **표만 고치고 블록은 옛 무조건형**(`beginners` L327 ×4 법칙).
- 🟠 B급 6건도 전건 반영: `limping` 블록 축어 재복사 + 「唯一」↔「四种情形」 개수 충돌 ·
  `drawing-odds` 블록→본문 복창 + **표 헤더 불변식 파손**(「多久一次」인데 전 행이 산식 → `算式`으로) ·
  `apt` 표 열 규칙(6015만 서술형 + 6001과 목적지 중복) · `bubble` `====` 중첩 · `icm` 블록 단정형.

### 회차 7이 남긴 미결

- 🟠 **`HARDEN_SKIP_SYNC=1`을 1회 썼다**(프로토콜 §1 탈출구 · 보고 의무 이행). 경위: B 구간 커밋 직전에
  헤드가 `lib/posts-zh/holdem-reading-the-board.ts`를 고쳐(`e47a3f2f`) pre-commit이 막았고, **로컬 변경 때문에
  `git merge main`도 거부**되는 교착이었다. 중간 저장 → 즉시 merge(충돌 0) 순서로 풀었다.
  🪶 **게이트 오작동이 아니라 제 일을 한 자리다** — 다만 «커밋도 머지도 못 하는» 교착이 실재하므로,
  훅 메시지의 처방(`git stash`)이 **레인 워크트리에서는 위험하다**는 점을 헤드가 알아야 한다(스택이 창 간 공유다).
- 🔴 **`check:drift` ✅ 56/56은 «날짜 진실»이지 «내용 진실»이 아니다** — 이번 회차 최대 발견(뱅크 §6).
  `holdem-3bet`·`holdem-position-play`는 `masterUpdated`가 EN `updated`와 **이미 같았는데 내용이 안 와 있었다**
  (EN 08-20 `cd9ec3c0`·`1d0245c1`). 한 번 날짜를 올려 버리면 **diff 방식으로는 영영 못 찾는다.**
  → 처방 = **드리프트 판정에 «구조 계수»를 한 겹 더**(EN에 있고 나에게 없는 링크·표 행·리스트 항목 수). 전 코퍼스 0.5초.
  🔴 **남은 «✅ 54편»도 같은 이유로 «검증»이 아니다** — 다음 로케일·다음 트랙이 계수로 재차 훑어야 한다.
- 🟠 **`holdem-limping` 최장 축어의 1등은 한 번도 판정 대상에 오른 적이 없었다**(SEO 렌즈). 회차 4→5가 지목한
  L155는 **2등(10자)**이었고 1등은 L157(13자 · 블록↔본문)이었다. 이번에 둘 다 처리했으나,
  **«계량 대상을 고르는 절차»가 두 회차 연속 1등을 놓쳤다**는 것이 교훈이다 → 계량은 **전 자리 정렬 후 상위부터**.
- 🟠 **`drawing-odds` AKs 「331:1」만 기준이 다르다**(수학 렌즈). 같은 표 다른 3행은 정확한 odds-against인데
  AKs만 1/p(331.5)의 반올림이다(정확값 **330.5:1**). **EN 동형 → §5-30 EN-먼저**. 이번에 손대지 않았다.
- 🟠 **`holdem-3bet` ↔ `3bet-pot-cbet` 수치 모델이 다르다**(딜러·수학·SEO 3렌즈). 3bet은 「3bb 오픈/9bb 3bet」
  모델(19.5bb·SPR 4.7), cbet은 솔버 실측 「2.5bb 오픈/11bb 3bet」(22.5bb·SPR 4.0). **양쪽 다 산술은 맞다.**
  이번에 zh는 **전제를 축어로 명시**해 오독을 막았으나(zh-hant 선례), 두 글의 모델 통일은 EN-먼저다(§5-30).
- 🪶 **thumb을 뗀 것은 «EN 1:1» 규율을 따른 것이고, zh 코퍼스 관례와는 갈린다.** 다른 zh 글은 같은 슬러그를
  thumb으로 부른다(`blind-battle-cbet`·`donk-bet-strategy`). 3렌즈가 규율 위반으로 지적해 **규율을 우선**했다 →
  **«thumb도 EN 1:1인가»는 헤드 판정 대상**(전 로케일 공통).
- 🪶 **`德州短码` 태그를 하루 만에 되돌렸다.** A 구간 자동완성 실측으로 채택했으나, 교열·네이티브 두 렌즈가
  ① 온페이지 등장 **0회** ② `德州`가 그 자체로 지명(산둥 德州市) ③ 뺀 `短码策略`이 **형제 글이 이 글을 부르는 앵커**
  (`holdem-tournament` L223·L410)이자 **자기 FAQ의 축어**임을 잡았다. → **자동완성 실측만으로 태그를 갈지 마라.
  «온페이지 근거»와 «형제 글 앵커»를 같이 세라.**
- 🪶 `holdem-community-event-guide.ts`(KO)에 `emoji` **중복 키**(TS1117) — 범위 밖 · 헤드 요청 §5-32.

## 3. 되돌리지 마라 (헤드가 넘긴 것 + 회차 1이 확정한 것)

### 헤드가 넘긴 것
- `holdem-3bet` A5s 블로커 대상 = **AA·AK**(KK 아님) — `1c0dc8dc`(2026-09-06) 정정본.
- `holdem-3bet`·`holdem-pot-odds`·`holdem-split-pot-rules`는 **2026-09-06 EN 역이식(`4522532a`)**이 이미 들어가 있다.
- `apt-incheon` 「개막 당일 수관」은 EN 「before the festival」을 **일부러 안 따른** 편차.
- 2·4법칙 링크 앵커는 도착 글 표기 「二四法则」로 통일돼 있다.
- GTO 13편·wsop-2026은 대상 밖.
- 🆕🟠 **헤드 통지(2026-09-08 · ja 회차 4 머지에서 파생) — GTO 역링크 2자리가 조건을 충족했는데 안 열렸다.**
  `locale-intentional-diffs.md` 08-27 두 행의 해소 조건은 「그 언어에 해당 GTO 편이 발행되면 재저작해 넣는다」인데,
  **zh는 GTO 4편을 전부 보유**(`a-high-board-cbet`·`3bet-pot-bet-sizing`·`3bet-pot-cbet`·`low-board-check-raise`)하면서 링크는 **1자리뿐**이다.
  실측(2026-09-08 헤드): `continuation-bet` ✅(2자리) · **`position-play` ❌**(⑤ `low-board-check-raise`) · **`3bet` ❌**(③ `3bet-pot-cbet`).
  🪶 형제 비교 = en 3/3 · ja 3/3(회차 4가 열었다) · zh-hant 3/3(회차 4가 열었다) · **zh 1/3** · es 0/3(레인 없음 → 헤드 미결).
  🔴 **회차 4(전략)가 이미 지나간 클러스터다** — 회차 5 마감 때나 트랙 종결 회차(7)에서 소급하라. 헤드는 손대지 않았다(§6-0).
- 🆕🔴 **헤드 통지(2026-09-08) — `holdem-starting-hands-chart`의 `/zh/hand-chart` 링크가 라이브 404다.**
  라우트는 `app/hand-chart`(한국어)와 `app/en/hand-chart` **둘뿐**이다(실측: /hand-chart 200 · /en/hand-chart 200 · **/zh/hand-chart 404**).
  형제 실태 = de·id·ja는 `/hand-chart`(한국어 도구로 떨어짐) · es는 `/en/hand-chart` · **zh-hant·pt는 아예 뺐다**(회차 4 판정 · `docs/locale-intentional-diffs.md` 2026-09-08 행).
  🔴 **헤드가 손대지 않았다** — 프로토콜 §6-0(레인 소유 파일). **회차 5 마감 때 zh 레인이 판정해 처리하라.**
  선택지 = ⓐ 링크 제거(zh-hant·pt 선례) ⓑ `/hand-chart`로 강등(de·id·ja 선례 · 한국어 화면) ⓒ `app/zh/hand-chart` 신설(헤드 몫 · 별건 회차).
  🪶 **이건 «갈림»이 아니라 결함이다** — 404는 어느 선례로 가든 지금보다 낫다. 다만 어느 쪽인지는 zh 레인 판정.

### 회차 1이 확정한 것
- 🔴 **`tldr`에 마크다운을 넣지 마라.** `components/intl-blog-post-client.tsx`가 `{post.tldr}`를 **파서 없이**
  `<p>`에 꽂고 `lib/intl-blog-page.tsx`가 JSON-LD `abstract`로 내보낸다 → `**`가 글자로 나간다.
  회차 1에서 game-order·showdown에 넣었다가 되돌렸다. **다시 넣지 마라.**
- 🔴 **`holdem-showdown-rules` FAQ 「盖了还能反悔吗」의 死牌 서술** = TDA 제14조 정본
  (「荷官가 弃牌堆로 밀어 넣어 100% 식별·회수 불가가 되어야 死牌 · 엎어 둔 것만으로는 자동 무효 아님」).
  옛 문장 「牌一旦触底就成了死牌」은 **룰북과 정반대**였다(zh 고유 커밋 `12c960a0` 유래, EN·zh-hant에 없음).
  같은 글 본문 「牌说话」 절이 이미 TDA 14를 정확히 인용하고 있었다 — **자기모순이었다. 되돌리지 마라.**
- 🔴 **`holdem-all-in-rules`의 「重新打开下注」 규칙에 TDA 조항 번호를 달지 마라.** 회차 1에서 렌즈가
  「TDA 47-A」를 제시해 넣었다가 **2차 교열이 되돌렸다** — EN 마스터가 일부러 번호 없이 쓰고,
  레포에 TDA 원문 사본이 없으며, 전 20개 로케일에서 zh만 번호를 달게 되기 때문이다. → §5 헤드 요청.
- **용어 정본 통일(회차 1 적용)**: `table stakes` = **`桌上筹码为准`**(「桌面注码」 폐기 · 港澳 색채) ·
  `cards speak` = **`牌说话（以牌面为准）`**(「牌面说话」·「以牌为准」 단독 사용 폐기) ·
  `all in` 구어 = **`推`/`全下`**(「俗称 push、shove、jam」처럼 중국어 속칭 없이 라틴만 나열 금지).
  근거 = `translation-terms-zh.md` §8-1 L257 · §1 L61.
- **`德州扑克谁先下注` 축의 소유자 = `holdem-game-order`**(seoTitle이 그렇다).
  `德州扑克谁先开牌` 축의 소유자 = `holdem-showdown-rules`. 다른 글에 이 태그를 다시 넣지 마라.
- **`holdem-betting-actions`의 「5 个下注动作」이 정본**이다. `game-order`의 7행 표는
  「기본 5개 + 3bet·all in 두 특례」로 화해시켰다 — 「七种下注动作」으로 되돌리지 마라.

### 회차 2가 확정한 것
- 🔴 **`holdem-split-pot-rules` 「假平分」 절의 踢脚 서수** = 보드 `A♦ K♠ 9♣ 6♥ 2♠` · A♠Q♦ vs A♥J♦에서
  **제1踢脚 K(공유) → 제2踢脚 Q vs J(여기서 승부) → 제3踢脚 9(공유)**. 옛 문장은 「前两张踢脚 K、9 …
  分胜负的是第三张踢脚」로 **2번과 3번을 뒤집어** 적고 있었다(원본 유래 · 수학·SEO·교열 3개 렌즈가 독립 검출).
  같은 회차에 고친 tiebreak 一对 FAQ(「牌面的 Q 跳到了 9 的前面」)와 정면으로 어긋나던 자리다. **되돌리지 마라.**
- 🔴 **短牌(6+)에서 바뀌는 것은 «同花 > 葫芦» 하나뿐이다.** 옛 문장 「很多短牌桌也把同花排到顺子之上、
  比标准玩法更靠前」은 **헛말**이다 — 표준 홀덤에서도 同花는 이미 顺子 위다. `flush-vs-straight` 3자리
  (직답 블록·본문·FAQ)를 「同花跳到葫芦之上 / 同花对顺子这一档没有变化」로 고쳤다. **EN에 없는 zh 고유 오류였다.**
- 🔴 **`holdem-reading-the-board` FAQ 7选5 예시의 베스트5는 `A-A-K-Q-J`**(5장)다. 옛 문장은 「A-K-Q-J」로
  **4장만** 적었고 보드도 `A-K-Q-J-x`라 **x=10이면 보드 자체가 Broadway가 되어 예시가 무너졌다**.
  보드를 `A♠ K♦ Q♣ J♥ 7♠`로 확정했다. 형제 글 tiebreak L198은 원래부터 `A-A-K-Q-J`로 옳게 적고 있었다.
- 🔴 **3연속 보드에서 홀카드 «한 장»으로는 스트레이트가 안 된다.** 위험 보드 표의 「三连面上一张 J 或 6 就能成顺」은
  거짓이었다(9-8-7 보드에서 J-10 또는 6-5 **두 장**이 필요하다). **되돌리지 마라.**
- **`德州扑克顺子 12345`·`都是X怎么比大小` 계열이 zh FAQ 질문 형태소 정본**이다(자동완성·dpskill 제목 실측).
  EN 「Who wins if…」 직역인 「谁赢」형으로 되돌리지 마라 — **자동완성에 「谁赢」형이 아예 없다**(뱅크 §1 실측).
- **`铁支`는 «华语牌桌上也叫» 라벨로 1회만 병기**한다. 主词는 `四条`(terms-zh §2). 「港台俗称」은
  딜러 렌즈 지적으로 완화했다(铁支는 대만 十三支 계열 용어라 신마 테이블에서는 거의 안 들린다).
- **`拆平局` 폐기 → `打破平局`**(简体 표준). 클러스터 5건 교체.

### 회차 3이 확정한 것
- 🔴 **`二四法则`이 정본이다. `2 和 4 法则`·`4-2 法则`·`4 和 2 法则`은 폐기.** 레포 잔존 0건(클러스터 21건 + `flush-vs-straight` 1 + `beginners` 2 = **27건 전량 교체**).
  근거는 «우리가 많이 쓰던 표기»가 아니라 **실측**이다: 자동완성에 `德州扑克 二四法则`·`二四法则` 실재, `4-2法则`은 `4 2法则` 하나만 돌려주고, **`2 和 4 法则`은 자동완성·경쟁 글 제목 어디에도 없다**(EN "rule of 2 and 4"의 직역). 경쟁 글 5편이 제목에 `二四法则`/`四二法则`을 쓴다. 정본 `translation-terms-zh.md` L372(`二四法则(=四二法则)`)와도 일치.
  🪶 `holdem-outs` 二四法则 절 첫 등장의 `（也叫"四二法则"）` **1회 병기는 의도된 것**이다.
- 🔴 **`所罗门法则`은 zh 고유 확장으로 채택했다**(EN에 없음 · 거울쌍 갈림으로 보고하지 마라). `translation-terms-zh.md` L372가 이미 용어로 수록하는데 코퍼스에 0건이었다.
  **1차 출처 대조 + 직접 검산 완료**: `X×4 −(X−8)` = `3X+8`, 적용 조건 **X ≥ 9**(=「超过 8 张」). X=9 → 35(진값 34.97) · X=12 → 44(44.96) · X=15 → 53(54.12). 🔴 **조건을 빼면 안 된다** — X=4에 적용하면 20%(진값 16.5%)로 오히려 나빠진다.
  🔴 **WebSearch 요약은 이 공식을 `4n−(8−n)`으로 틀리게 줬다**(9 outs가 37%가 된다). 원문(legendpoker.cn/xw/jx/2851.html)을 직접 열지 않았으면 오답을 실었다 — §12-B의 실증.
- 🔴 **`equity`의 표면 용어는 `胜率` 하나다.** `权益`(FAQ·tldr·stripe 등 20여 곳)·`赢率`을 전부 `胜率`로 통일했다. 남은 `赢率`은 **seoTitle 「你的赢率并不等于你到手的」 하나뿐이고 이는 일부러 남긴 CTR 훅**이다(§17 — 훅은 살리고 키워드만 보강). `胜率兑现`(개념)과 `兑现率`(비율)은 **일부러 구분**한다.
- 🔴 **`equity` FAQ의 「补牌超过大约 14 张」은 거짓이었다 → 「超过 8 张」.** 같은 답변 뒷문장의 「超过 8 张时…减去(补牌−8)」와 자기모순이었고, 14에는 근거가 없다. 오차는 단조 증가라 불연속점이 없다(X=8 +0.55 · 9 +1.03 · 12 +3.04 · 15 +5.88). **딜러·수학·SEO 3개 렌즈가 독립 검출.**
- 🔴 **`pot-odds` 「一步步教你」 절(zh 고유 확장)에 국면 전제를 넣었다.** 8 outs 两头顺에 ×4(32%)를 적용해 「32% > 25% → 跟」으로 닫고 있었는데, **같은 글 8줄 위에서 「후속 벳이 있으면 단일 카드로 계산하라」고 못 박은 것과 정면 충돌**했다. 실제로 이 콜이 사는 것은 转牌 한 장 = 8÷47 = **17.0%**로 25%에 못 미친다. 딜러 렌즈만 잡은 D유형이다. **전제 없는 「跟」으로 되돌리지 마라.**
- 🔴 **`outs` 표 열 라벨은 `翻牌 → 转牌（1 张）`이다**(옛 `仅转牌（1 张）` 폐기). 값은 9÷47 = 19.1%인데 라벨이 「转牌」만 말해 같은 글의 转牌→河牌 19.6%(9÷46)와 혼동됐다. **산술 오류가 아니라 라벨 오류**였고 수학·SEO 2렌즈가 수렴했다. 본문 검산문도 두 값을 함께 밝힌다.
- 🔴 **`implied-odds`의 street 역어는 `街`/`后面几条街`다. `街道`(시가지 도로)로 되돌리지 마라.** 형제 글 `pot-odds`·`drawing-odds`는 원래부터 `街`·`一条街`로 옳게 쓰고 있었다.
- 🔴 **`card-counting`에서 하우스를 가리킬 때는 `牌房`이다**(`不管谁赢，牌房只抽水（rake）`). 같은 글에서 `庄家`는 **21점 딜러**를 가리키고 정본 §4에서도 `庄家`=Dealer/Button이라, 신마 독자에게 「버튼이 레이크를 뗀다」로 읽혔다. 🪶 21점 맥락의 `庄家`는 정당하니 그쪽은 바꾸지 마라.
- **번역투 정정(되돌리지 마라)**: `多数牌`→`把补牌数多算`(overcount가 뜻을 잃었던 자리) · `顶部页面`→`排在前面的那些教学文章`(SERP 상위 글의 직역) · `加时赛判官`→`决胜关键`(tiebreaker) · `反向`→`不利`(odds against · 글의 主题가 反向隐含赔率이라 오독을 낳았다) · `启发式经验`→`经验值` · `发明牌`→`有明牌` · `poker 里`→`德扑里` · `赢家玩家`→`赢钱的牌手` · `肥数字`→`大数字` · `发薪日`→`送钱上门` · `一副透镜`→`一副眼镜`.
- **`drawing-odds` 「十三倍」는 同花 행에서만 참이다.** 표 전체로 일반화하면 거짓이 된다(连张 행은 ~7.7배, 口袋对子 행은 听牌 열이 없다). 「两张同花牌…十三倍，连张也差着近八倍」로 한정했다. 暗三条↔明三条는 **8.74배**라 「近九倍」다(「将近十倍」 아님).
- **태그 소유**: `隐含赔率` = **implied-odds 단독**(pot-odds에서 제거 — 유일한 exact collision이었다) · `二四法则`·`德州扑克outs计算` = **outs** · `德州扑克概率表`·`概率表完整版`·`德州扑克概率计算` = **probability** · `德州扑克胜率表`·`德州扑克翻牌前胜率` = **equity**.
  🔴 **`扑克胜率计算器`는 제거했다 — 도구 의도라 글로 못 이긴다**(SERP 1위가 계산기 위젯 페이지 `dongpaidi.com/calc.htm`, 본문 한자 77자). `算牌器`·`计算器在线`도 같은 이유로 채택하지 않는다.
  🪶 **`牌型概率`(probability) ↔ `德扑牌型胜率`(hand-rankings)는 카니발이 아니다** — 두 문자열이 다르고 자동완성에 둘 다 실재한다. 회차 2 미결 **종결**.

### 회차 4가 확정한 것
- 🔴 **`holdem-strategy` VPIP는 `15–22%`(6인 25% 상하) · PFR `12–18%`다.** 옛 「25–30%」는 **같은 문장의 「弃 80%」(=VPIP 20%)와
  산수로 모순**이었고, 같은 글 L251(「超过五分之一就是打太多」)·형제 글 `holdem-starting-hands-chart`(15–20%)·
  `holdem-fish`(15–22% / PFR 12–18%)와도 어긋났다. **딜러·수학 2개 렌즈가 독립 검출.** 되돌리지 마라.
- 🔴 **`holdem-starting-hands-chart` AA 만석 승률은 `34.5%`(약 35%)다.** 옛 「30.87%(약 31%)」는 **상대 수 off-by-one** —
  30.87%는 상대 **9명(10인 테이블)** 값이고 「满9人桌」은 상대 8명이라 **34.5%**다(수학 렌즈 MC 40만 회 · AA vs 1 = 85.28로 검증).
  🔴 **zh 단독 수치였다**(`grep -rn "30.87" lib/` → 이 한 줄뿐). 되돌리지 마라.
- 🔴 **6인 테이블의 UTG는 `lojack（LJ）`다. `劫机位`(HJ)가 아니다.** EN 원문 L146이 「UTG here is really the **lojack**」이고,
  같은 클러스터 `holdem-positions`(3자리)와 이번 회차 신설 직답도 전부 LJ다. 좌석 하나를 통째로 밀어 놓은 오역이었다.
- 🔴 **UTG 확장 범위에 `AJo`를 넣지 마라.** EN L114는 「a few offsuit hands like **AQo**」로 AQo만 든다.
  같은 글 퀴즈(「AJo 进不了 UTG 的门槛」)·형제 글 `holdem-position-play`(「在 UTG 弃掉 AJo …你多半打对了」)와 정면 충돌했다.
- 🔴 **`when-to-fold` 세트 vs 넛플러시 「七张补牌」의 내역은 「葫芦（三张5·三张2）**或四条（剩下的那张9）**」다.**
  옛 문장은 7장 전부를 葫芦라 적었는데 **9♦는 四条**다. 수치(34%)와 결론은 옳았고 족보 명칭만 틀렸다.
  🪶 34%는 **전수 열거로 확인**했다: 미확인 45장 · C(45,2)=990 · 히어로 승 341 = **34.44%**(수학·딜러 2렌즈 독립 일치).
- 🔴 **`continuation-bet`의 GTO 역링크 2문단은 zh에 있어야 한다.** `locale-intentional-diffs.md` 2026-08-19·08-26 두 행이
  「🔜 각 언어에 그 편이 발행되면 그때 재저작해 넣는다」로 건 **조건이 충족됐다**(zh `a-high-board-cbet`·`3bet-pot-bet-sizing` 09-03 발행).
  🔴 **이미지 경로는 `-zh.webp`다**(EN판 `-en`을 끌어오지 마라) · **태그 `德州扑克 范围优势`는 `a-high-board-cbet` 소유**라
  continuation-bet에 넣지 않는다 — **링크로만 받는다**.
- 🔴 **`A-7-2 98.2%`는 「跟注方가 领打(donk)를 포기한다」는 국면이다.** 「加注方의 c-bet 빈도」로 읽히면 근거가 어긋난다 —
  이번 회차가 「放弃领打（donk）」 한 구를 넣어 국면을 밝혔다. 딜러 렌즈 단독 검출.
- **번역투 정정(되돌리지 마라)**: `唯一最赚钱/唯一最大`(*the single most* 축자역 — 중문에서 「唯一」과 최상급은 충돌한다) → 최상급 단독 ·
  `均衡器`(음향 이퀄라이저로 읽힌다) → `在 OOP 唯一能扳平的武器` · `位置自动驾驶` → `有位置就自动开火的惯性` ·
  `坍缩`(물리 용어) → `退化`/`只剩推或弃` · `跛入`·`不要跛` → `平跟`(클러스터 7편이 전부 `平跟`인데 이 글만 갈렸다) ·
  `看起来假装得很弱` → `伪装得很弱` · `面对你的进攻继续` → `顶着你的进攻继续跟下去` · `你对他们的翻后位置` → `翻后你相对他们是先动还是后动`.
- **태그 소유**: `什么时候 3bet` = **3bet 단독**(strategy에서 제거 — 유일한 exact collision) · `什么时候弃牌` = **when-to-fold** ·
  `c-bet 策略` = **continuation-bet** · `德州扑克 谁先行动`은 **제거**(자동완성 0건 · 실검색형은 전부 남의 글 소유).
  🪶 **`德州扑克 位置 顺序`(positions 신설)는 `holdem-game-order`와 충돌하지 않는다** — game-order의 축은 `下注顺序`·`谁先下注`(행동 순서),
  신규 태그의 주어는 `位置`(좌석 순서)이고 positions 본문이 명시적으로 game-order로 넘긴다(SEO 렌즈 확신도 높음).

### 회차 5가 확정한 것
- 🔴 **`bubble`·`泡沫`은 zh 검색어가 아니다 — 主词는 `钱圈`이다.** 자동완성 실측: `bubble` 단독 = bubblemon 전자담배·bubble tea·bubble 韩国(K-pop 앱) · `泡沫` 단독 = 스티로폼·泡沫经济·邓紫棋 노래 · `冒泡` = 冒泡排序 · `德州扑克泡沫`·`泡沫期` = **0**. 살아 있는 것은 **`钱圈`(8건 + `锦标赛 钱圈率`)** 하나다(뱅크 §1-D). `holdem-bubble`의 seoTitle·title·desc·tldr·H2·FAQ 질문·태그를 재조준했고, **`泡沫圈`은 전 `lib/posts-zh/` 0건**으로 내렸다(형제 `holdem-when-to-fold`·`holdem-position-play` 4자리 포함). 🔴 **본문 산문의 `bubble` 라틴은 의도적으로 남겼다**(전문 복합어 `bubble factor`·`stone bubble`·`hand-for-hand`). `泡沫系数`·`吃泡`는 `translation-terms-zh.md` L434 정본이니 **바꾸지 마라.**
- 🔴 **상위어/하위어를 갈랐다**: `钱圈` = **상위어**(3종 전체) · 하위 종류는 **`进钱 bubble`**(옛 `钱圈 bubble`). H2 「你会遇到的 3 种钱圈：进钱 vs 决赛桌 vs 卫星赛」가 정본. **되돌리지 마라.**
- 🔴 **泡沫系数 3.0의 필요승률은 `75%`다**(BF ÷ (1+BF) = 3÷4). 🔴 **딜러 렌즈가 계산은 75%로 해 놓고 처방 문안엔 「70% 上下」라고 썼고, 1차 반영이 그 불일치를 그대로 옮겼다** — 2차 교열이 잡았다. 「70%」로 되돌리지 마라. 옛 문장 「胜率够 60% 以上」은 **표의 1.5(=진钱 bubble) 값**을 chip leader 국면에 갖다 붙인 것이었다.
- 🔴 **`apt-incheon` 「1,393」은 `人次`이지 `人`이 아니다.** 산술 증명: `3,008,880,000 ÷ 1,393 = 2,160,000` 나머지 0 = 엔트리당 상금풀 기여액(수수료 13.6%). EN은 세 자리 모두 「1,393-**entry** field」로 옳았고 zh만 흘렸다. 같은 글 「参赛人次要会读」 주석과 정면 모순이었다. desc·tldr·note 3자리 전부 교체.
- 🔴 **`apt-incheon` 비자 직답 블록에 中国内地를 명시했다.** 옛 블록은 면제 목록에 港澳台·일본·싱가포르만 넣고 내지를 「其余国家」로 밀어 **「K-ETA 신청하라」**고 말했는데, **내지 여권은 K-ETA 대상이 아니라 C-3-9 비자 대상**이다(같은 글 표·⚠️ 콜아웃이 정반대). 간체 글의 1순위 독자가 첫 화면만 읽고 항공권을 끊는 사고였다. **되돌리지 마라.**
- 🔴 **`korea-poker-marathon` 제주 동선에 경고를 넣었다.** 「多数国籍免签」 뒤에 「从济州飞首尔」을 권하고 있었는데, 형제 글 `apt-incheon` L390·L396이 **「济州岛免签不适用于仁川——济州入境后不得转飞首尔、釜山等本土」**라고 못 박는다. 두 글이 같은 사실에 정반대 동선을 줬다. 방안 B 표에도 전제를 달았다.
- 🔴 **`korea-poker-marathon`은 「2027 템플릿」 프레임으로 전환했다.** desc·tldr·리드 블록·赛程表 블록·요약 4자리에서 「9 月 4 日截止」·「请趁早报名」을 **전부 «2026 년 외국인 등록 창은 모두 닫혔다»로** 바꿨다. 🪶 이 4자리는 **EN 동형이라 원래 EN-먼저 등재분**이었으나, 오늘(09-08) 기준 desc·tldr이 이미 거짓이고 새 블록과 한 글 안에서 충돌해 **로케일에서 먼저 닫았다.** EN-먼저 등재는 §5에 그대로 유지한다(헤드가 EN + 나머지 7로케일).
- 🔴 **`holdem-tournament` 상금 블록**: 「钱圈率约 12–15%」는 **표 어느 행도 지지하지 않았다**(실제 12.0~13.0%). 「最低奖 1.5–2 倍」도 표의 2,000명 행(1.7–2.2)을 덮지 못했다. → 「（钱圈率），下表四档都落在 12–13%；最低奖 1.5–2.2 倍」. **「实测」이라 쓰지 마라** — 표는 «典型» 열이고 실측은 WPT 한 건뿐이다.
- 🔴 **`/zh/hand-chart`는 404다**(라우트 `app/hand-chart`·`app/en/hand-chart` **둘뿐**). 헤드 통지(09-08)에 대한 zh 레인 판정 = **`/en/hand-chart`로 통일**(`holdem-starting-hands-chart` L118 · `texas-holdem-rules-for-beginners` L503 두 자리). 근거 = ①404 해소 ②zh 코퍼스 안 표기 통일(두 자리가 갈려 있었다) ③신마 화교 독자는 영어를 읽는다(한국어 도구로 강등하는 de·id·ja 선례보다 낫다) ④es 선례와 일치. 🪶 헤드 통지의 「zh-hant·pt는 아예 뺐다」는 **`starting-hands-chart` 한 편 얘기**다 — zh-hant `beginners`에는 `/hand-chart`가 남아 있다(실측).
- **번역투·문장 정정(되돌리지 마라)**: `机械概念`(mechanical의 축자역) → `一套机制` · `每套制胜方案都挂在其上的、按阶段的骨架`(좌분지 과적) → `每套制胜方案都挂在同一副骨架上，下面按阶段拆开` · `对它遗漏的东西要诚实`(Be honest about 직역) → `它漏掉的东西，值得一条条摆出来` · `从中掉出两条实战规则`(fall out of 직역) → `由此引出` · `押在固定奖金上的彩票`(動賓 충돌 — 彩票는 사는 것이지 押하는 것이 아니다) → `一张对着固定奖金表的彩票` · `按数学不是这样`(the math says otherwise 직역) → `数学上并非如此` · `没有它的话` → `没有 hand-for-hand`.
- **개수 선언 정정**: `规则三条`(본문 4개) → `场馆规则四条` · `重点 12 场`인데 6개만 나열 → `主轴是这 6 场…完整 12 场见下表，全系列共 36 场`.
- **`holdem-short-stack` 요약은 `推得宽、跟得紧、动手早`다.** 1차 반영이 쓴 「推、别跟、趁早」는 **같은 글이 정면으로 반박하는 조언**이었다(L114·L123: 大盲位 43.9% 门槛 · 「小对子和弱 A 正是跟注范围的核心」). D유형. **되돌리지 마라.**

### 회차 6이 확정한 것

- 🔴 **`glossary`의 「按钮位在你右手第一个」은 거짓이었다 → 「按钮位是庄家按钮所在的那个座位」.** 버튼은 독자의 상대 위치가 아니다(9인 테이블에서 8자리에 대해 거짓). 같은 글 아래 포지션 표(「按钮位 = 庄家位」·「CO 是按钮位右边」)와 정면 모순이었다. **EN glossary에는 이 표 자체가 없는 zh 고유 확장**이다. 딜러 렌즈 단독 검출.
- 🔴 **구두 선언 관련 두 줄을 TDA 원문으로 고쳤다**(`docs/sources/tda-2022-shortform-rules.txt`).
  ① 「면대주에 「过牌」을 외치면 荷官이 «벨 게 없는 것»으로 처리」는 거짓 → **「只剩跟注或弃牌，不能再加注」**(Rule 55 축어: *"A player declaring 'check' when facing a bet may call or fold, but cannot raise."*).
  ② 「荷官按你实际投入的动作判定」은 거짓 → **「轮到你时的口头声明是有约束力的，而且先说的算」**(Rule 40-A: *"whichever is first defines the bet"*). 같은 글 `串注` 행이 「先喊「加注」再分次推码就是合法的」로 이미 구두 구속력을 전제하고 있어 **한 글 안에서 자기모순**이었다.
  ③ 🔴 **2차 교열이 잡은 것**: ②를 고치며 쓴 「推的钱不够一个最小加注时就只算跟注」도 **Rule 43과 어긋난다** — *"raises 50% or more … must make a full minimum raise. If less than 50% it is a call"*. → 「**하한의 절반 미만이면 콜, 절반 이상이면 최소 가주로 보충**」으로 재정정.
  🔴 **조항 번호는 넣지 않았다** — 회차 1이 `holdem-all-in-rules`에서 확정한 판정(「EN이 번호 없이 쓰는데 zh만 달게 된다」)을 따랐고, 실제로 **거울쌍 게이트가 `Rule55`를 zh 단독 토큰으로 잡았다**. 근거는 이 문서에 남긴다.
- 🔴 **string bet 역어는 `串注`다**(옛 `拉线下注` 폐기). 1차 반영이 `分次下注`로 바꿨다가 **같은 셀의 「先喊「加注」再分次推码就是合法的」와 글자가 겹쳐**(반칙 이름 = 합법 행위 이름) 2차 교열이 되돌렸다. 코퍼스 정본은 `holdem-betting-actions` L257 FAQ 「什么是 string bet（串注）？」다.
- 🔴 **`牌局`이 «업장»을 가리키던 17자리를 `牌房`으로 고쳤다**(title·desc·tldr 포함). `牌局`은 «한 판/판국»이지 하우스가 아니고, 특히 `一间牌局`은 量词 충돌로 비문이었다. EN은 처음부터 "the cardroom / the house"로 옳았다 — **EN이 맞고 zh 번역이 흘린 유형이라 로케일에서 고쳤다**(회차 2 방법론 4). 남은 `牌局` 5건은 전부 «게임/판» 뜻이다.
- 🔴 **`资金盘`을 쓰지 마라 → `bankroll（资金）` 또는 `一份…资金`.** 화어권(신마·대륙 공통)에서 `资金盘`은 **폰지·투자사기 판**을 가리킨다. 도박 자금 관리 문단에서 최악의 연상이었고, 같은 클러스터의 glossary·bad-beat은 이미 `bankroll（资金）`로 옳게 쓰고 있었다.
- 🔴 **`下风狗`(underdog 축자역)·`星号`(asterisk 직역)·`桌布对面`(=식탁보 맞은편)·`尼特`(=NEET)·`磨谷者`·`路场赌徒`·`让他自己上吊`·`牌河`(존재하지 않는 말·5자리)·`背运输`(背+运输로 읽힌다)·`发出走法`·`一副牌能击败`(=카드 한 벌)·`被惨败`(비문)를 되돌리지 마라.** 전부 신마 네이티브 렌즈 단독 검출이고, **카드·확률·규칙 술어가 아니라 관용구·비유 계열**에 몰려 있었다(회차 3의 `街道`·`顶部页面` 계열의 재발).
- 🔴 **번체 `手風`(bad-beat 1자)을 `手风`으로.** 6편 중 유일한 번체 혼입이었고 `check:hangul`·`check:cjk` 어느 게이트도 원리상 못 잡는다.
- 🔴 **`fish` 剥削표는 «행동»이 판별축이고 수치는 보조다.** 1차 반영이 3행을 「差距没到 25」로 고쳤다가 **수학 렌즈가 공집합임을 증명**했다(VPIP ≥ 40 ∧ gap ≤ 25 ⟹ PFR ≥ 15인데 같은 글 자가진단표는 鱼 = PFR < 10). → 1행 「**어떤 사이즈에도 거의 弃牌 안 함**」 / 3행 「**작은 벳은 跟, 큰 벳은 弃**」로 갈랐다. **「差距没到 25」로 되돌리지 마라.**
- 🔴 **`fish`의 「45% 手牌인데 5%만 加注」는 «전체 핸드의 5%»(PFR 5)다.** 옛 「只加注**其中的** 5%」는 45%×5% = **PFR 2.25%**로 읽혀 같은 글 다른 자리·EN(`raising only 5% of all hands dealt`)과 어긋났다.
- 🔴 **`rake`의 「底池抽 5% ≠ 你只亏 5%」 절 예시는 `$60 底池 → $3(5%) → 净利 $30 의 10%`다.** 옛 예시는 `$6(10%) → 20%`여서 **H2가 말하는 5%와 어긋났다**(EN에 없는 zh 고유 절이라 로케일에서 고쳤다). 🪶 배수 서술도 정밀화했다 — 이 배수를 정하는 것은 底池 크기가 아니라 **네가 그 底池에 넣은 비율**이다.
- 🔴 **`bad-beat` BBJ 자격의 「两张底牌」 요구는 «输家»에만 무조건이다.** FAQ가 「输赢双方…缺一条就领不到」로 단정했으나 같은 글 본문과 EN L112는 「往往赢家的也要」(**often**)로 하우스별이라고 말한다. → 「输家의 두 장은 필수 · 赢家 쪽은 房마다 다름」으로.
- **`德州扑克 意思` 축의 표제어 3개를 glossary 표에 신설**(`隔离加注（Isolation raise / iso）`·`深跑（Deep run）`·기존 `bad beat 大奖池` 행에 `JP` 약칭 병기). **EN에 없는 zh 고유 확장이라 거울쌍 갈림으로 보고하지 않는다**(회차 3 `所罗门法则` 선례).
- **`cooler`의 중문 역어는 제목·도입·태그가 쓰는 `撞牌`가 주(主)이고 `冤家牌`가 병기다.** 1차 반영이 블록에서 순서를 뒤집었다가 2차 교열이 되돌렸다. 🪶 `德州扑克 撞牌`는 자동완성 실측에 살아 있고 `德州扑克 冤家牌`는 0이다 — 그래도 정본 §0.5-6이 `冤家牌`를 수록하므로 **병기는 유지**한다.

### 회차 7이 확정한 것

- 🔴 **`holdem-3bet` 요약 6번 항목의 「죽은돈이 늘면 배수가 4배로 간다」는 부호가 반대다 — 되살리지 마라.**
  배수 = (2B+D)/(2R+D)이고 **∂/∂D < 0**이라 죽은돈이 커지면 배수는 **반드시 작아진다**(수학 렌즈 전수표:
  D=0 → 3.00 · D=1.5 → 2.60 · D=2.5 → 2.41). 4배에 닿는 조건은 **무위치 대형 3bet**이다.
  🔴 **EN·ja는 아직 틀린 채이고 zh-hant가 이미 고쳐 놓았다**(`「換成不利位置的大尺寸…」`) — **zh-hant가 정본**이다.
  🪶 zh-hant의 「多一個跟注者的死錢」도 엄밀히는 4배에 못 간다(3-way = 2.89)라 zh는 그 절을 뺐다.
- **이 항목의 계산 전제는 「3bb 개池 · 버튼 3배 3bet to 9bb · 100bb 深」이다**(zh-hant 선례를 따라 축어로 박았다).
  이 전제 없이는 링크 대상 `3bet-pot-cbet`의 22.5bb·SPR 4.0과 모순으로 읽힌다. **전제를 지우지 마라.**
- **`wpt-australia` 위성 표 1행은 두 구간을 분리해 적는다**(`$550：7/24–9/25` · `$290：7/30–9/9`).
  EN은 「Pre-festival, from Jul 24 | $290 or $550」로 뭉뚱그리지만, **그러면 같은 글 블록·FAQ와 어긋난다**
  (4개 렌즈가 독립 검출). 🔴 **EN 파리티를 이유로 다시 합치지 마라** — 정밀도가 파리티보다 앞선다(§5-30 EN-먼저).
- **`$10K 豪客赛`가 이 글의 정본 표기다**(일정표 L145 `豪客赛`·`apt-incheon`도 `豪客赛`). `高额赛`로 되돌리지 마라.
- **`holdem-limping` 「现场低级别 vs 线上/GTO」 절의 본문은 «기제 설명»이 아니다.** 기제(封顶 range → iso-raise →
  OOP 열세)는 **12줄 위 불릿 3개가 이미 한다** — 재작성본이 그걸 반복해 2렌즈가 잡았다. 이 절의 일은
  **«온라인에서는 그 세 수법이 기본값»과 GTO 기준선(100BB open-limp 금지)**이다.
- **`holdem-card-counting` 직답 블록은 «21점 vs 홀덤의 정보 구조 차이»만 말한다.** outs·블로커·card removal 열거는
  **표 뒤 마무리 문단(L82)과 다음 H2 블록이 이미 한다** — 블록에 넣으면 세 자리가 같은 말을 한다(3렌즈 독립 검출).
  🪶 이 글의 정본 용어는 **`outs`**(35회 · L92에서 `==out（出路牌）==`로 정의)다. `补牌`을 쓰지 마라.
- **`holdem-drawing-odds` 표 4행의 「多久一次」 열은 `312 ÷ 1,326`이다.** 「几乎每 4 手一次」로 되돌리면
  블록의 「분모가 전부 같은 1,326」 선언이 **4행 중 3행에서만 참**이 된다(교열 렌즈).
- **`holdem-tournament-vs-cash-game`은 H2가 병기를 맡고 블록은 주词만 쓴다.** H2 `现金桌（现金局）` ↔ 블록
  `现金局（英文 ring game）`. 🔴 블록에 `也叫现金桌`를 되살리면 **괄호가 두 줄 만에 뒤집혀** 주词가 안 정해진다.
  🪶 코퍼스 실측 `现金局` 30건+ vs `现金桌` 1건 — **본문 주词는 现金局**이고, `现金桌`는 자동완성 실측(SG 5·MY 4)이
  살아 있어 **H2·태그에서만** 쓴다. 두 판정이 갈린 자리를 이렇게 갈랐다.
- **`holdem-blind-meaning` FAQ 질문은 `大盲`·`小盲` 엔티티를 유지한다.** 이 글 태그가 `德州扑克大盲小盲顺序`이고
  FAQ 질문은 FAQPage 스키마로 나가는 exact-match 표면이다(교열 렌즈). 축은 `翻前 vs 翻牌后`로 좁히되 엔티티는 남긴다.
- **`holdem-short-stack` 태그 `短码策略`은 되돌린 것이 맞다** — 위 §2 회차 7 미결 참조.
- 🪶 **`holdem-straddle` L222의 「一切都取决于房规」 삭제는 정정이었다**(딜러 렌즈). 그 문장이 붙은 주장은
  「翻牌之后的顺序永远跟着按钮位走」인데 **포스트플롭 액션 순서는 房规가 아니라 불변 규칙**이다. 되살리지 마라.
- **EN 드리프트 이식 8편은 헤드의 EN-먼저 판정과 GPT 교차검수 팩 결과다**(`e47a3f2f`·`af6ca275`). 되돌리지 마라:
  ① `hand-rankings` 스캔 라벨 = **`同花 → 顺子 → 对子`**(`花色` 아님 — 스캔 대상은 무늬가 아니라 동화 성립 여부)
  ② `pot-odds` 「콜을 분모에서 빼면 **所需胜率을 과대평가해 콜해야 할 자리를 폴드한다**」(옛 「价钱을 좋게 본다」는
     방향이 반대였다 · zh는 L192·L242 **두 자리**가 같은 오류였고 L182는 이미 옳았다 — 한 글 안 모순이었다)
  ③ `beginners` ×4 행 라벨 = 「**且能不再付一笔注就看完剩下两张（例如全下）**」
  ④ `apt` 74석은 **保证**(送出 아님) · 6015 버스는 **明洞/忠正路/孔德/麻浦**(江南/良才 아님) · AREX Express는 **1·4호선** 환승
  ⑤ `ept` 현금 신고는 **문턱이 없다고 단정하지 않는다**(EPT 페이지 「任何现金交易」 ↔ 카지노 양식 **€10,000 이상**)
  ⑥ `ept` 출처 노트 = **2026-09-09 PokerStars 공식 완전결과 페이지와 대조 완료**(회차 5 미결 «시한 경과» 동시 종결)
  ⑦ `bubble`·`icm`의 「已锁定的奖金」 → **「高于已锁定名次的那部分权益」**(잠긴 최저액 자체는 여전히 내 것이다)
  ⑧ `tournament` 위성 전략에 **winner-take-all 예외**(단일 席位 위성은 chip EV로 1등을 노린다) · 奖金分配表는
     **등록·리바이 마감 후에야 최종 확정** · ITM 밖에서도 **赏金/PKO는 별도 수령**

## 4. 거울쌍 갈림 (🇹🇼가 받을 것)

| 슬러그 | 자리 | 내 값(zh) | 상대 값(zh-hant) | EN 원문 | 판정 |
|---|---|---|---|---|---|
| `holdem-all-in-rules` | 全押 摊牌 규칙 FAQ | **TDA 第 16 条**(번호 명시) | 「TDA 規則」만 — **번호 없음**(L252·L260) | L259 「TDA **Rule 16**」 | 🔴 **zh가 맞다.** zh-hant가 조항 번호를 빠뜨렸다 → 🇹🇼가 보충할 것 |
| `holdem-game-order` | pct 토큰 4개 | 89%·91% | 92%·2% | — | ✅ **게이트 오탐.** 둘 다 결함 없음 → §5 헤드 요청(게이트 수정) |
| `holdem-hand-rankings` | 「易混对决造成 90% 的新手错误」 | **90% 있음** | **문장 자체가 없음**(요약 문단으로 대체) | L287 「cause **90%** of beginner mistakes」 | 🔴 **zh가 맞다.** zh-hant가 EN의 이 문장을 빠뜨렸다 → 🇹🇼가 보충할 것 |
| `holdem-tiebreak-rules` | EN 08-30 드리프트 전부 | **반영 완료**(花色 H2 신설 + Rule 73 인용 + Broadway 문단 + FAQ 5 재작성 + 표 1행) | **미반영**(파일 282줄 · 花色 H2 없음 · Rule 73 없음) | `c96af74d` 2026-08-30 | 🔴 **zh가 앞섰다.** 🇹🇼 회차 2가 같은 드리프트를 받아야 한다 |

**갈림으로 보고하지 않는 것(zh 고유 확장 — 결함 아님)**: `holdem-kicker`(H2 13개 vs EN·zh-hant 7개 — zh 고유
롱테일 절 6개) · `holdem-split-pot-rules` 零头 계산 예시($1000÷3 · EN에 없음) · `holdem-tiebreak-rules`
打公牌 3인표와 counterfeit FAQ · `holdem-reading-the-board` 위험 보드 표·坚果会变 절. 전부 EN에 없는 zh 자체
확장이라 거울쌍이 갈리는 것이 정상이다. 회차 2에서 §13 전건 검산해 통과시켰다.

🪶 참고로 🇹🇼에 넘길 판정 하나 더: **zh는 §14-A 질문형 H2 70%를 적용한다**(회차 1 A 구간 실측 근거 —
현지 상위 글이 전부 명사형인데 질문형 수요는 자동완성·라쿠 양쪽에 실재, 아무도 H2로 세우지 않았다).
**zh-hant는 반대 판정**(명사형 유지)이므로 거울쌍이라도 이 축은 일부러 갈린다 — 갈림으로 보고하지 마라.

### 회차 3(확률 클러스터) 판정 — 갈림은 «zh가 앞섰다»가 대부분

| 슬러그 | 자리 | 판정 |
|---|---|---|
| `holdem-outs` | zh-hant에만 `19.6%` | 🔴 **내 쪽 결함이었다 — 수정 완료.** 二四法则 절의 「9 × 2 = 18%（真值 19.6%）」 검산문을 B 구간이 요약체로 바꾸며 §13 수치를 통째로 날렸다. `pot-odds`에서도 같은 문장이 같은 방식으로 사라진 것을 찾아 **두 곳 다 복구**했다. **거울쌍 게이트가 아니었으면 못 잡았다** |
| `holdem-pot-odds` | 갈림 13(`8♠9♦K♣` · `$80/$160` · pct 4종) | ✅ **zh 고유 확장**(H2 11개 vs zh-hant 8개 — 「一步步教你」·「5:1 换算」·「分母」 3절이 EN·zh-hant 둘 다에 없다) |
| `holdem-probability` | zh-hant에만 82%·46%·70% | ✅ **zh-hant 고유 확장**(翻前 all in 대결 FAQ 2개 · EN에도 zh에도 없다) |
| `holdem-drawing-odds` | zh-hant에만 `5♠ 5♦` | ✅ 경험담 카드 표기 차이. zh는 「一对 5」로 카드 토큰 없음 · EN에 대응 문장 없음 |
| `holdem-equity`·`holdem-implied-odds` | zh에만 pct·bb | ✅ 이번 회차 직답 블록이 실은 수치 |
| **표기** | zh `二四法则` ↔ zh-hant `四二法則` | 🪶 **정본이 둘 다 수록**(`二四法则(=四二法则)`)이라 결함 아님. 🇹🇼가 통일할 때 **zh는 `二四法则`을 정본으로 골랐다**는 것만 알아 둘 것(근거 = 뱅크 §2 자동완성·경쟁 글 실측) |

🪶 **회차 3은 zh-hant에 넘길 «상대 쪽 결함»을 찾지 못했다.** 확률 클러스터는 zh-hant가 아직 경화 전이라 갈림 대부분이 «zh가 앞섰다»로 설명된다.

### 회차 4(전략 클러스터) 판정 — 갈림 전부 «zh가 앞섰다»

`check:mirror-pair --locale-pair=zh,zh-hant`를 8편 전건 실행. **zh-hant만 갖고 zh에 없는 토큰은 0편** —
즉 **회차 3형 «내 쪽 수치 증발» 사고는 이번에 없다**(회차 3에서 `outs` 19.6%가 그렇게 사라졌다).

| 슬러그 | 갈림 | 판정 |
|---|---|---|
| `holdem-position-play` | **0** | ✅ 완전 일치 |
| `holdem-strategy` | zh에만 `30%` | ✅ 이번 직답 블록이 실은 수치 |
| `holdem-positions` | zh에만 `$14` | ✅ 경험담 금액(EN L29와 축어 동일 · zh-hant 미경화) |
| `holdem-starting-hands-chart` | zh에만 `30.87% 31%` · `1/8` | 🔴 **내 쪽 결함이었다 — 수정 완료**(AA 만석 off-by-one → 34.5%). 거울쌍이 아니라 수학 렌즈가 잡았다 |
| `holdem-limping` | zh에만 `85% 49%` | ✅ AA 다인 승률(수학 렌즈 재계산 통과 — AA vs 5 = 49.23%) |
| `holdem-3bet` | zh에만 `15bb` | ✅ squeeze 사이징(zh 고유 표 확장) |
| `holdem-continuation-bet` | zh에만 `10♥` · `98.2% 98.4%` · `1/3` | ✅ **이번 회차 신설 GTO 2문단**. `10♥`는 게이트의 `T♥` 정규화 |
| `holdem-when-to-fold` | zh에만 `67% 50% 60% 40%` | ✅ MDF FAQ(zh 고유 확장 · 수학 렌즈 4행 전건 재계산 통과) |

🪶 **회차 4도 zh-hant에 넘길 «상대 쪽 결함»을 찾지 못했다.** 전략 클러스터는 zh-hant가 아직 경화 전이다.
🔴 다만 **🇹🇼가 이 클러스터를 받을 때 반드시 승계할 것 3가지**(zh 고유 정정이라 게이트로는 안 잡힌다):
① AA 만석 = **34.5%**(30.87% 아님) ② 6인 UTG = **lojack** ③ VPIP **15–22%**.

### 회차 5(토너먼트 클러스터) 판정 — 갈림은 전부 «zh가 앞섰다» 또는 게이트 오탐

`check:mirror-pair --locale-pair=zh,zh-hant` 9편 전건 실행.

| 슬러그 | 갈림 | 판정 |
|---|---|---|
| `holdem-tournament-vs-cash-game` · `holdem-short-stack` | **0** | ✅ 완전 일치 |
| `holdem-tournament` | zh만 `13%` · zh-hant만 `10bb` | ✅ 13%는 이번 블록이 실은 수치. `10bb`는 **표기 차이**(zh 「10 个大盲」 ↔ zh-hant 「10bb」) — 정보 손실 아님 |
| `holdem-icm` | zh만 `60% 67%` | ✅ 이번 블록 신설. **재검산 통과**(1.5→60% · 2.0→2÷3=66.7%→67%) |
| `holdem-bubble` | zh만 `75%` | ✅ 이번 정정분. **재검산 통과**(3÷4=75%) |
| `apt-incheon` · `ept-barcelona` · `wpt-australia` · `korea-poker-marathon` | date 5·5·11·9 | 🔴 **전건 게이트 오탐** — §5-17 참조 |

🪶 **회차 5도 zh-hant에 넘길 «상대 쪽 결함»을 찾지 못했다.** 토너먼트 클러스터는 zh-hant가 아직 경화 전이다.
🔴 다만 **🇹🇼가 이 클러스터를 받을 때 반드시 승계할 것 4가지**(zh 고유 정정이라 게이트로는 안 잡힌다):
① `bubble`·`泡沫`은 zh 검색어가 아니다 → **主词는 `钱圈`**(단, **번체권은 별도 실측 필요** — 대만 표기는 다를 수 있다)
② 泡沫系数 3.0 = **75%**(70% 아님)
③ APT 인천 「1,393」은 **人次**(人 아님)
④ 中国内地 여권은 **K-ETA 대상이 아니라 C-3-9 비자 대상** · 제주 免签은 본토 이동 불가

### 회차 6(용어 클러스터) 판정 — 갈림 2편 · 둘 다 «zh 고유 절»

`check:mirror-pair --locale-pair=zh,zh-hant` 6편 전건 실행(반영 전·후 두 번).

| 슬러그 | 갈림 | 판정 |
|---|---|---|
| `holdem-bad-beat` · `holdem-cooler` · `holdem-fish` | **0** | ✅ 완전 일치 |
| `holdem-glossary` | 반영 중 `Rule55` 1건 발생 → **0으로 되돌림** | 🔴 **내 쪽에서 잡았다.** TDA 조항 번호를 넣었다가 게이트가 zh 단독 토큰으로 띄웠고, 회차 1 판정에 따라 번호를 뺐다 — **거울쌍 게이트가 «표기 규율 위반»도 잡는다는 실증** |
| `holdem-rake` | zh만 `$60` `$30` | ✅ **zh 고유 절**(「底池抽 5% ≠ 你只亏 5%」는 EN·zh-hant 어느 쪽에도 없다 · EN H2 6개 실측 확인) |
| `holdem-straddle` | zh만 `$200` · `100bb 50bb` | ✅ **zh 고유 절**(「别人抓了 straddle，我的起手范围该怎么改？」는 EN·zh-hant 둘 다 없다) |

🪶 **회차 6도 zh-hant에 넘길 «상대 쪽 결함»을 찾지 못했다.** 용어 클러스터는 zh-hant가 아직 경화 전이다.
🔴 다만 **🇹🇼가 이 클러스터를 받을 때 반드시 승계할 것 5가지**(zh 고유 정정이라 게이트로는 안 잡힌다):
① 버튼 정의 = **庄家按钮所在的座位**(「你右手第一个」 아님)
② 면대주 「过牌」 오선언 = **跟注 아니면 弃牌**(TDA 55) · 구두 선언은 **순서에 맞으면 구속력** + **50% 미만은 콜, 50% 이상은 최소 가주 보충**(TDA 40·43)
③ string bet = **串注**(`拉线下注`·`分次下注` 아님)
④ 하우스는 **牌房**이지 `牌局`이 아니다 · **`资金盘`은 폰지 판을 뜻하니 쓰지 마라**
⑤ 「45% 手牌인데 5% 加注」의 5%는 **전체 핸드 기준**(「其中的 5%」 아님)

### 회차 7(트랙 종결) 판정 — 이번 회차가 만든 갈림은 **1편뿐**

`check:mirror-pair --locale-pair=zh,zh-hant` 전건 실행: 공통 56편 · ✅ 동일 24 · 🔴 갈림 32.
🔴 **갈림 32편 중 31편은 선재다** — zh-hant는 회차 6까지 왔고 zh는 회차 7(+EN 드리프트 8편)을 했으니 당연히 벌어진다.

| 슬러그 | 자리 | 내 값(zh) | 상대 값(zh-hant) | 판정 |
|---|---|---|---|---|
| `wpt-australia-2026-guide` | 위성 표 | **`$1,050` · `9/22`**(=$10K 豪客赛 십선일 행) | 없음 — 아직 옛 catch-all `賽事期間其他衛星賽 · $270–1,050` | 🔴 **zh-hant가 뒤처졌다.** EN 마스터 L109에 이 행이 있다(`$10K High Roller Direct Qualifier · Sep 22, 1-in-10 · $1,050`) → **🇹🇼가 받을 것** |

🪶 **`holdem-3bet`은 갈림이 안 떴다** — zh-hant가 6번 항목을 먼저 갖고 있었고 19.5bb·7.5bb가 일치한다.
🪶 `holdem-position-play` check-raise 링크도 zh-hant에 이미 있다 → **이 두 자리는 zh가 뒤처졌던 쪽**이었다(뱅크 §6).
🔴 **그래서 이번 회차의 거울쌍 값은 «내 결함 발견»이 아니라 «zh-hant 결함 발견»으로 뒤집혔다** —
거울쌍은 «누가 앞섰나»를 양방향으로 잰다는 실증이다.

🔴 **🇹🇼가 회차 7을 열 때 반드시 승계할 것**(zh 고유 정정이라 게이트로는 안 잡힌다):
① 3bet 6번의 **「죽은돈 → 4배」는 부호 반대**(zh-hant는 이미 옳다 — 되돌리지 마라)
② wpt 위성 표 **1행 두 구간 분리** + **$10K 豪客赛 행 신설** + 분수 풀어쓰기(`六选一`류)
③ `pot-odds` **콜 누락 시 방향**(所需胜率이 과대평가된다 — zh-hant도 확인할 것)
④ `hand-rankings` 스캔 라벨 **`同花`**(`花色` 아님)
⑤ EN 드리프트 8편(`e47a3f2f`·`af6ca275`) — **zh-hant도 미수신 상태다**

## 5. 헤드 요청

> 🆕 **헤드 통지 2026-09-11 (6) — 직답 블록 «길이 규격» 판정 완료 · 다음 회차(8) = 길이 소급 (사장님 착수 지시 대기)**
> 🔴 **규격 = zh 60~130자**(게이트 `check:answer-echo` 로케일표 · 정본 `docs/settled-decisions.md` §3 「직답 블록 길이 규격 로케일표」 · 프로토콜 §4). 09-10 (4)의 60~110은 추정치였고 실측 세 갈래(ja×0.74 = 126 · p90 129 · 라틴·구두점 비중)가 130에 수렴해 **위로** 갈랐다. 되돌리지 마라.
> **대상 = `npm run check:answer-echo -- --locale=zh` 🟠 length 33자리 / 22편**(그중 **미달 3** — 미달은 «늘리기»가 아니라 «자기완결 여부» 판정이다 · 초과는 «삭제»가 아니라 «재서술»). 🔴 echo는 0을 유지해야 한다.
> **방법 = ja 회차 10 브리프 `docs/harden-brief/ja-회차10-직답길이.md` 방식 복제**(A 구간이 `docs/harden-brief/zh-회차8-직답길이.md`로 쓴다): §0 규율 5줄 그대로(재서술·LCS 재실행·§13 불변·링크 보존·«되돌리지 마라» 자리) · §1 자리별 «남길 형태소·자를 자리·echo 주의» · §2 보존표 · §3 §13 손검산 · §4 종료 조건.
> 🔴 **C 구간 렌즈는 네이티브가 1순위**(ja §3-K 1 — 압축은 조사·양사·«的/了» 같은 기능어를 먼저 깨뜨리고 수학·딜러·SEO·교열은 그걸 원리상 못 본다) · 렌즈에게 **게이트와 같은 산식**을 줘라(§3-K 4 · 안 주면 길이 지적은 오탐) · 보존 대상은 반영 전후 `grep -c`(§3-K 6) · «같은 유형» 고친 뒤 diff의 + 줄 전수 재주사(§3-K 7).
> 거울쌍: zh-hant는 같은 규격에서 9자리/6편(미달 6)만 걸린다 — 같은 절이 zh-hant에서는 규격 안이면 그쪽 문안이 «압축의 참고»가 되지만, 🔴 간체↔번체 기계 변환은 금지(§7-D)다.
> 🪶 이 회차는 `masterUpdated`를 올리지 않는다(EN 델타가 아니라 길이 규격) · `updated`는 C 구간에서 «실제로 손질한 편만».

> **헤드 처리 2026-09-07 (7)**: 1 ✅(`a069430a`) · 2-B **11건 종결**(회차 (5) · 채택 8 · 기각 1 = ⑪ 172조는 캐시 룰북 `Live Action Rules` L854 실재 — 검수장 발췌 §B) · **2-C EN-먼저 7건 → 헤드 대기열 등재**(판정은 EN 원문·TDA 원문으로 별도 회차) · 3 ✅(회차 (1) ②③) · 4 ✅ · 5 ✅ **`docs/sources/tda-2022-shortform-rules.txt` 자산화** · 6·9 헤드 판정 대기열(표기 통일 · 인용부호 — 둘 다 zh-hant 트랙과 얽혀 한 번에) · 7 미결 유지 · 8·10 ✅ sitemap · 11 접수(중첩 볼드 오탐 — posting.mdc 「본문 형식 금지 3종」 문구에 «병렬 강조는 해당 없음» 주석 후보).
> 🔴 **회차 4(전략) 시작 전 `git merge main`**(HARDEN.md 첫 명령) — main에 프로토콜 §5-⓪(C 구간 전 merge main 재실행)·§6-0 · `check:hangul` 게이트 · 족보 4편+tiebreak 헤드 정정이 들어 있다.

1. 🔴 **게이트 수정 — `scripts/check-mirror-pair.mjs:46`**
   `pct: /\d+(?:[.,]\d+)?\s?[%％]/g` 가 **URL의 percent-encoding을 백분율로 오인**한다.
   실례: 위키백과 링크 `%E5%BE%B7%E5%B7%9E%E6%89%91%E5%85%8B` → `5%`·`7%`·`89%` 추출.
   zh는 `德州扑克`, zh-hant는 `德州撲克`를 인코딩하므로 바이트가 달라 **정상인데 «갈림»으로 뜬다.**
   → 처방: 토큰 추출 전 URL(`https?://...`)을 마스킹하거나, `%` 앞 두 글자가 hex면 제외.
2-B. 🔴 **EN-먼저 11건 — 회차 2 (족보 클러스터)**. 전부 EN 마스터에 같은 문장이 있어 zh에서 고치지 않았다.
   렌즈 5종이 1차 출처(2026 WSOP 규칙 PDF · TDA 2022/2024 PDF)로 대조한 것이며, **헤드가 EN을 먼저 고치고
   전 로케일에 전파**해야 한다. 확신도 순:

   | # | EN 파일·자리 | 무엇이 문제인가 | 근거 |
   |---|---|---|---|
   | ① | `holdem-tiebreak-rules` L196 (두 쌍 FAQ) | 「K-K-9-9-5 보드에서 **every player already holds the same two pair** → 최대 底牌가 가져간다」는 **거짓**. 포켓 QQ·JJ·TT는 `K-K-Q-Q-9`로 **더 큰 두 번째 페어**를 만들고, K나 9를 든 사람은 **葫芦**다 | 수학·네이티브 2렌즈 독립 검출 · 직접 조합 검산 |
   | ② | `holdem-tiebreak-rules` L153/155 vs L232 | **자기모순**: 「Suits do exactly one job … Hold'em uses it for neither」라고 닫아 놓고, 같은 글 FAQ가 「there **is** one place suits are used(버튼 추첨)」라고 말한다 | 교열 렌즈 · zh는 괄호 한 줄로 임시 화해시켰다(§3) |
   | ③ | `holdem-split-pot-rules` (零头 규칙) | **TDA Rule 20 첫 단계가 통째로 빠졌다**: *"First, odd chips will be broken into the smallest denomination in play."* EN엔 denomination 언급이 0건이라 독자가 $333/$333/$334를 받고 딜러에게 항의하게 된다 | 딜러 렌즈 · TDA 2022 PDF 축어 |
   | ④ | `holdem-hand-rankings` L185 (`:::tiebreak` 표) | 「Royal Flush\|**Always a tie (split pot)**」 — EN tiebreak는 08-30에 「보드가 로열일 때만 撞车」로 고쳤는데 EN hand-rankings는 안 고쳐 **EN 안에서 크로스문서 불일치**. 🔴 **zh도 그 상태를 그대로 복제하고 있다**(zh tiebreak는 08-30 드리프트를 받아 정밀화, zh hand-rankings는 EN을 따라 유보). 2차 교열이 「한쪽만 두지 마라」고 했으나 **EN을 앞질러 고치면 임의 소급**이라 현행 유지했다 — 헤드가 EN 두 글을 동시에 맞춘 뒤 전 로케일 전파할 것 | 딜러·SEO·2차 교열 3렌즈 |
   | ⑤ | `holdem-kicker` L109 | 打公共牌 서술이 「everyone uses the same five cards and the pot is split」로 **무조건 단정**. 같은 글 FAQ는 08-26에 「If nobody can improve on the board」 조건절을 받았다 | SEO 렌즈 · zh 블록은 FAQ에 맞춰 조건절을 넣었다 |
   | ⑥ | `holdem-split-pot-rules` L93 (`:::tip`) | 팟오즈 임계값(만池 2/3 · 半池 1/2)이 **헤즈업 전제**인데 그 전제가 안 적혀 있다. 3인 팟이면 되찾는 건 「절반」이 아니라 1/3 | 딜러 렌즈(산수 자체는 정확 — 나도 재검산 통과) |
   | ⑦ | `holdem-reading-the-board` L172 | 「announce that, not …」 — 카지노는 cards speak다. TDA Rule 12는 구두 선언이 **구속력 없고 고의 오신고는 제재** 대상이라 초보에게 «신고해라»로 읽히면 위험 | 딜러 렌즈 · TDA 2022 PDF |
   | ⑧ | `holdem-reading-the-board` L51 (7선5 표) | 헤더가 **"How common"**(빈도)인데 셀 값은 C(7,5)=21가지 **부분집합 개수**(10/10/1)다. 「打公共牌이 21번에 1번」으로 오독된다. 수치 자체는 정확 | 수학 렌즈 |
   | ⑨ | `holdem-hand-rankings` L35 (别称 열) | 「Royal Flush = **Broadway flush**」·「Straight Flush = **steel wheel**」 — 둘 다 그 족보 **전체의 별칭이 아니다**(steel wheel은 A-5 동화순 하나만). 같은 글 본문은 범위를 좁혀 옳게 쓴다 | 수학 렌즈 |
   | ⑩ | `holdem-kicker` L176 (两对 FAQ) | 예시 보드가 「Q-7-7」 **3장**이라 摊牌이 성립하지 않는다(결론은 정확) | 수학·딜러 2렌즈 |
   | ⑪ | `holdem-split-pot-rules` L91 | 「WSOP live-action Rule 172」는 **이번 패스로 검증 불가**. 2026 WSOP 토너먼트 규칙 PDF는 **129조에서 끝난다** — 172조는 별도 cash game 문서다. 「없다」가 아니라 「이 문서로는 못 잰다」 | 수학 렌즈 · 원문 직접 확인 |

2-C. 🔴 **EN-먼저 7건 — 회차 3 (확률 클러스터)**. 전부 EN 마스터에 같은 문장·같은 구조가 있어 zh에서 고치지 않았다.
   **헤드가 EN을 먼저 고치고 전 로케일에 전파**해야 한다. 확신도 순:

   | # | EN 파일·자리 | 무엇이 문제인가 | 근거 |
   |---|---|---|---|
   | ① | `holdem-card-counting` L107 note | 「做记号的牌·串通·线上实时用 solver」를 한데 묶어 **전부 「欺诈」**라 부른다. 층이 다르다 — 串通/마킹은 도박법·대회규칙 위반(**TDA 2022 Rule 69** 축어: *"Chip dumping and other forms of collusion will result in disqualification."*)이지만 **온라인 실시간 solver(RTA)는 사이트 약관 위반**이라 계정 정지·자금 몰수로 끝나고 형사 사기가 아니다. 절 제목이 「算牌违规吗」라 중국어 독자에게 같은 법적 층위로 읽힌다 | 딜러 렌즈 · TDA 2022 shortform PDF 원문 |
   | ② | `holdem-card-counting` L104·L167 | 「100% 合法」·「没人有理由拦你」를 반복하면서 **현장에서 실제로 벌점이 나오는 규칙을 한 번도 말하지 않는다.** **TDA 2022 Rule 5** 축어: *"Betting apps and charts may not be used by players with live hands … Violations of this paragraph are subject to penalties in Rule 71."* 🔴 독자층이 토너먼트 참가자라 실전 손해가 구체적이다(핸드가 살아 있는 채로 오즈 앱을 켜면 Rule 71 벌점) | 딜러 렌즈 · TDA 원문 |
   | ③ | `holdem-implied-odds` L84·L86 | A♥K♥ / Q♥7♥2♣3♠ 예시를 **9 outs로만 센다.** 같은 절이 상대를 「顶对를 내는 深筹码 상대」로 규정하는데, 그렇다면 A·K 오버카드 6장이 살아 **15 outs = 32.6%**로 필요치 25%를 이미 넘는다 → 「即时价格说弃牌, 그래서 隐含赔率이 필요하다」는 **절의 전제 자체가 무너진다.** EN·zh 모두 같은 문단 | 딜러 렌즈 · 직접 조합 검산 |
   | ④ | `holdem-equity` L105–113·L206 | 半诈唬 EV 계산이 「跟注 후에도 두 장을 본다(35%)」를 전제한다. 상대가 翻牌에서 콜하면 转牌에 또 벳이 오므로 **같은 사이트가 6편에서 반복하는 「×4는 all in일 때만」과 충돌**. 산수는 맞다($52) — 전제가 낙관적이다. 🪶 단일 스트리트(19.1%)로 재계산해도 **+$33**이라 결론(听牌 半诈唬는 크게 +EV)은 견고하다 | 딜러 렌즈 · 재계산 |
   | ⑤ | `holdem-probability` L106 | set-mine 요구치를 「超过 7.5 比 1 的回报」로만 제시한다. 그런데 형제 글 `implied-odds`는 바로 그 숫자를 두고 *「拿 7.5 这个数字当你实战桌上的准绳，就是一个慢性漏钱口」*이라 하고 `drawing-odds`도 「7.5:1은 理论下限, 15:1 이상이라야」라고 쓴다. **필라 글에만 이론 하한이 홀로 서 있다**(EN 안의 크로스문서 불일치) | 딜러 렌즈 |
   | ⑥ | `holdem-equity` L204 | **`弃牌率`이 한 글에서 두 뜻**이다 — H2·블록은 fold **equity**(내가 얻는 지분), FAQ는 fold **frequency**(상대가 弃牌하는 빈도 33%). 중문에서 `弃牌率`의 자연스러운 독해는 후자다. EN도 "How much fold equity do I need"로 같은 혼동을 갖고 있다 | SEO 렌즈 |
   | ⑦ | `holdem-equity`·`holdem-implied-odds` 전편 | 🔴 **EN 마스터 자체가 «H2 직후 굵은 리드 문장 = FAQ 답변»으로 축어 중복**이다(실측: equity L39↔FAQ0 **65%/LCS 56자**, L119↔FAQ6 46%/59자; implied-odds L136↔FAQ4 **78%/LCS 55자**, L49↔FAQ2 69%/40자). zh는 그 리드 문장을 직답 블록으로 **옮겼을 뿐**이라 중복이 그대로 따라왔다(zh equity 블록↔FAQ 평균 21.3% · implied-odds 13.0%). 🔴 **zh만 고치면 다음 로케일에서 재발한다.** 🪶 `pot-odds`·`probability`·`drawing-odds`의 EN에는 이 결함이 **없고**, 그래서 zh에서도 그 3편의 FAQ 중복이 낮다 — **인과가 맞는다** | SEO 렌즈 · 계량 |

3. **EN-먼저 2건** (회차 1 · 프로토콜 §0 — 임의 소급 금지, 헤드가 EN 먼저 고치고 전 로케일 전파)
   - `holdem-betting-actions` **「Mistake 2 — The string raise」**(EN L178): 이 사례는 WSOP 제103조 정의상
     **string bet이 아니다**. 103조는 「사전 구두 선언 **없이** 여러 동작으로 칩을 내미는 것」이고,
     「跟注」라고 말한 뒤 더 내미는 것은 **제90.d조(구두 선언 구속력)**로 콜 확정되는 별개 사안이다.
     EN 본문도 다른 자리에서 이 구분을 명시한다 → 라벨이 자기 글과 충돌.
     🔴 zh는 **직답만 정정하면 H3와 갈리므로 H3도 같이 고쳤다**(「错误 2——说出口的"跟注"改不回来」).
     zh 본문 L153이 이미 구분을 명시하고 있어 zh 자체 근거가 있었다. **EN·타 로케일 사본 전수 확인 필요.**
   - `texas-holdem-rules-for-beginners` **「weak aces often make second-best one-pair hands」**(EN L376):
     실전에서 약한 A가 지는 이유는 「작은 페어」가 아니라 **A를 맞춰 톱페어 약한 키커가 되는 것**이다.
     EN 동형이라 zh에서 고치지 않았다.
4. **`docs/locale-intentional-diffs.md` 등재 요청** — 「zh는 질문형 H2 70% 적용 / zh-hant는 명사형 유지」.
   회차 1 A 구간에서 근거를 실측했고 `docs/local-voice/zh.md` §3에 판정을 남겼다. 정본 등재는 헤드 몫.
5. ✅ **TDA 원문 확보 경로가 생겼다**(회차 2 수학 렌즈). 회차 1에서 「레포에 사본이 없다」고 적었던 항목이다.
   - **TDA 2022 shortform**: `https://pokercalendar.asia/wp-content/uploads/2022/12/2022-Poker-TDA-shortform-rules.pdf`
     → `pdftotext`로 **Rule 12·14·19·20 축어 추출 성공**(딜러 렌즈가 실제로 썼다).
   - **TDA 2024 Longform**: TDA 공유폴더 → Wayback 스냅샷 경유. 🔴 `curl`은 Dropbox 인터스티셜에 막히니 **Playwright 필요**.
   → 헤드가 `docs/`에 자산화하면 all-in 「重新打开下注」에 조항 번호를 달 수 있다(회차 1에서 근거 부족으로 되돌림 — §3).
   덤으로 **2026 WSOP 토너먼트 규칙 PDF는 129조에서 끝난다**는 것도 실측됐다(§5-2-B ⑪).
6. **`打公共牌`/`打公牌` 표기 전면 통일 판정** — §2 미결 참조. 레인은 병기로만 이었다(23건+ 치환은 범위 초과).
   정본은 둘 다 수록하고 렌즈 둘이 서로 다른 쪽을 권했다. **헤드가 한쪽으로 정하거나 «둘 다 정본»을 확정해 줄 것.**
7. 참고: **`快速答案` 잔존 1편(`ept-barcelona-2026-guide`) · `tldr` 마크다운 2편**은 §2 미결 참조.
8. 🔴 **머지 후 `npm run build`로 `public/sitemap.xml`을 갱신해 함께 push할 것.**
   이 레포는 sitemap을 커밋해 쓴다(Vercel `buildCommand`가 npm 훅을 건너뛰므로 — 메모리
   `vercel-buildcommand-skips-npm-hooks`). 회차 2는 6편의 `updated`를 **2026-09-07**로 올렸으므로
   sitemap `lastmod` 6줄이 바뀐다(회차 2에서 빌드해 확인 후 되돌려 뒀다 — 12줄 diff).
   레인은 프로토콜 §5-⑧ 커밋 목록에 sitemap이 없어 커밋하지 않는다.

9. 🔴 **인용부호관례 판정** — §2 미결 참조. 본문 `"…"` ↔ 이번 회차 직답 블록 `「」` ↔ `beginners` `“…”`로 세 갈래다. **전면 통일은 회차 1·2 산출물까지 걸려 레인 범위를 넘는다.** 간체 표준은 `“”`이고 `「」`는 대만/홍콩 관례라 zh-hant 트랙과도 얽힌다 — 헤드가 한쪽으로 정해 줄 것.

10. 🔴 **머지 후 `npm run build`로 `public/sitemap.xml`을 갱신해 함께 push할 것**(§5-8과 같은 이유). 회차 3은 **9편**의 `updated`를 2026-09-07로 올렸다(확률 7편 + `holdem-flush-vs-straight`·`texas-holdem-rules-for-beginners`는 표기 통일 때문에 회차 2에서 이미 09-07이었다) → sitemap `lastmod`가 바뀐다. 레인은 커밋하지 않는다.

11. 🪶 **회차 1 「beginners 직답 블록 중첩 볼드」는 오탐이었다 — 미결에 올리지 않았다.** 렌더러 정규식 `/\*\*(.+?)\*\*/g`가 뒤집는 것은 **바깥 볼드가 안쪽을 감싸는 진짜 중첩**일 때인데, 실측하니 13개 블록 전부 `**A**…**B**` 병렬 강조였다(굵은 구간 2~27자 · `**` 개수 짝수 · 빈 볼드 0). **첫 실행에 걸린 건은 전건 원문 판정**(메모리 `gate-tuning-loop-is-the-work`)의 실증.

12. 🔴 **EN-먼저 9건 — 회차 4 (전략 클러스터)**. 전부 EN 마스터에 같은 문장·수치가 있어 zh에서 고치지 않았다.
   딜러·수학 두 렌즈가 1차 출처(자사 솔버 글 · EN 원문)로 대조한 것이며, **헤드가 EN을 먼저 고치고 전 로케일 전파**해야 한다. 확신도 순:

   | # | EN 파일·자리 | 무엇이 문제인가 | 근거 |
   |---|---|---|---|
   | ① | `holdem-position-play` L213 (c-bet 빈도 표 2행) | 「OOP(**盲注의 3bet 底池**) 约 **40–50%**」인데 **자사 솔버 글이 같은 국면을 97.8~98.4%로 실측**한다(`3bet-pot-bet-sizing` Q♥T♥7♠ 98.4% · `3bet-pot-low-board` 8-5-2 97.8% · `ace-paired-board-strategy` L215 「3bet 方下注 98%–100%」). 3벳터는 낮은 SPR + 레인지 우위로 c-bet 빈도가 **매우 높다** — 40–50%는 «3벳을 당한 쪽» 값에 가깝다. 🔴 표가 1행=싱글레이즈드 / 2행=3벳팟으로 **팟 종류를 섞어** 「위치 때문에 갈린다」로 오독시킨다 | **딜러·수학 2렌즈 독립 검출** · 자사 솔버 3편 대조 |
   | ② | `holdem-position-play` L179 ↔ `holdem-limping` L169 | **같은 국면(전원 폴드 후 SB)에 정반대 지시**. position-play 「~40%(raise-or-fold) · **never flat**」 ↔ limping 「**completing the small blind is one of the most defensible limps**」. 「never flat」은 «오픈에 대한 콜»에는 맞으나 표의 문맥이 «弃牌到你时»라 컴플리트 금지로 읽힌다 — 어느 쪽이 옳은가가 아니라 **국면 전제가 빠진 것**이 결함 | 딜러 렌즈 · 두 글이 서로를 내부링크로 건다 |
   | ③ | `holdem-limping` L98 | 「**Shallow-stacked late position** — solvers develop button open-limping ranges at short stacks」가 **몇 bb인지, 캐시인지 토너인지 안 밝힌다**. 같은 클러스터 `position-play`는 「1/2 현장에서 버튼 limp는 최악」이라 못 박는다. 100bb 캐시 버튼과 15bb 토너 버튼은 완전히 다른 얘기인데 「浅码」 두 글자가 그 구분을 삼켰다 | 딜러 렌즈 · 신마 라이브는 100bb+ 딥이 표준 |
   | ④ | `holdem-continuation-bet` L127 | 「In **tournaments**, lean a touch smaller — often **half pot or less**」인데 바로 앞에서 干牌面 = **1/3**을 제시했다. **半池(50%)는 1/3(33%)보다 크다** — 「토너 = 더 작게 = 반팟」으로 읽으면 드라이 보드에서 사이즈를 **올리게** 된다. 어느 단을 줄이는지 명시해야 한다 | 딜러 렌즈 · 🪶 zh는 이번에 직답에서 이 절을 뺐다(증폭 회피) |
   | ⑤ | `holdem-starting-hands-chart` L114 ↔ L124 | **EN 안의 자기모순**: L114는 UTG 13% 레인지가 「**77–99**까지 넓어진다」, L124는 「**77, 88** — fine from the button, **marginal from UTG**」. 소제목이 「該弃的牌」라 지시가 정반대다. zh도 같은 상태 | 딜러 렌즈 · zh에서 단독 수정 안 함 |
   | ⑥ | `holdem-when-to-fold` L112·L117 | 「On the *flop* the same set is still ~34% against a made flush, **so that's a call**」이 **가격 없이** 서술된다. 34%는 팟사이즈 벳(33% 필요)엔 콜이지만 **플랍 올인 대응까지 콜로 읽히면** 100bb를 34%에 밀어넣는다. 🪶 완화 사유: 34%는 «최악의 상대 핸드» 값 | 딜러 렌즈(확신도 낮음) |
   | ⑦ | `holdem-starting-hands-chart` L197·L278 | 「two suited cards make a flush by the river only **~6.5%**」 — 재계산 결과 **6.40%**(C(11,3)·C(39,2) + C(11,4)·39 + C(11,5) = 135,597 ÷ C(50,5) = 2,118,760). 🪶 같은 줄의 翻牌 0.8%(0.842%)·플러시드로 완성 35%(34.97%)는 정확 | 수학 렌즈 · 0.1pt |
   | ⑧ | `holdem-3bet` L202 | 실전 핸드에서 CO가 $18 3벳에 **4bet $55 = 3.06x**인데, 같은 글 FAQ는 「有位置 2.2–2.5x、无位置 **再大一点点**」이라 한다. 2.5x의 «조금 더»는 2.7x 안팎 — 자기 FAQ의 상단을 넘는다 | 수학 렌즈(확신도 낮음 · 실전 레인지 안이긴 하다) |
   | ⑨ | `holdem-positions`·`holdem-position-play` 상단 블록 | **EN 마스터가 zh보다 «블록↔tldr» 축어중복이 심하다** — 실측: EN positions 블록↔tldr **73자**·↔FAQ **39자** / EN position-play 블록↔tldr **50자**·↔FAQ **61자** (zh는 각각 55·20 / 35·27). **zh는 번역하며 오히려 축어를 줄였다.** 처방은 «tldr = 전체 요약 / Quick answer = 첫 H2의 직답»으로 역할 분리 | SEO 렌즈 · 계량 |

13. 🔴 **`德州扑克怎么打` 태그 귀속 판정** — 자동완성에 실재하나 `texas-holdem-rules-for-beginners`·`holdem-game-order`(`德州扑克怎么玩` 보유)와 근사다. 회차 4는 **보류**했다.

14. 🔴 **`holdem-blind-meaning` L205 축 좁히기** — SEO 렌즈 판정: zh 코퍼스에 「SB·BB 누가 먼저 행동하나」 답변 패시지가 **4개**이고 그중 2개를 회차 4가 새로 만들었다(positions 블록 · position-play FAQ). 회차 4는 **`翻牌后` 접두로 갈랐고 태그로도 안 올렸다**(브리프 지시 준수). 그러나 소유주인 `blind-meaning` L205는 접두가 없어 여전히 정면 경쟁한다 → **처방은 소유주 쪽을 「盲注的行动顺序：翻前 vs 翻牌后」로 좁히는 것**. 회차 4 파일 밖이라 손대지 않았다.

15. ✅ **`劫机位` → `劫持位` 통일 완료(회차 6).** 헤드 처리 불필요.

16. 🔴 **머지 후 `npm run build`로 `public/sitemap.xml`을 갱신해 함께 push할 것**(§5-8·10과 같은 이유). 회차 4는 **8편**의 `updated`를 2026-09-07로 올렸다 → sitemap `lastmod`가 바뀐다. 레인은 커밋하지 않는다.

17. 🔴 **게이트 수정 2종 — `scripts/check-mirror-pair.mjs:51` `date` 정규식**(기존 요청 1과 같은 계열이다).
   현행: `/\d{4}-\d{2}-\d{2}|(?:1[0-2]|[1-9])\s?月\s?(?:3[01]|[12]?\d)\s?日|(?<![\d/])(?:1[0-2]|[1-9])\/(?:3[01]|[12]?\d)(?![\d/])/g`
   - **(a) 범위 날짜를 비대칭으로 잡는다.** 재현(node 실측): zh 「8 月 8–9 日」 → **`null`**(못 잡음) / zh-hant 「8/8–9」 → **`["8/8"]`**(잡음). `月…日` 패턴은 月 뒤 숫자 다음에 日이 바로 와야 하는데 범위 대시가 끼면 깨지고, 슬래시 패턴은 뒤에 `–`가 와도 lookahead를 통과한다. → **이벤트 가이드 4편이 전건 «zh-hant만 N건» 오탐**(apt 5 · ept 5 · wpt 11 · korea 9).
   - **(b) 분수를 날짜로 오인한다.** `1/6 卫星赛`(= 1-in-6 위성) → `["1/6"]`. wpt zh만 `1/6 1/5 1/8`로 뜬다.
   - 처방: `月…日` 패턴에 범위(`(?:\s?[–—-]\s?\d{1,2})?`)를 허용하고 슬래시 쪽도 같은 방식으로 묶거나, **로케일별 날짜 표기 규약을 정해 한쪽만 인정**하는 것이 근본이다. 분수는 뒤에 `卫星赛/衛星賽`류가 붙으면 제외.
18. 🔴 **EN-먼저 8건 — 회차 5 (토너먼트 클러스터)**. 전부 EN 마스터에 같은 문장·수치가 있어 zh에서 고치지 않았다.

   | # | EN 파일·자리 | 무엇이 문제인가 | 근거 |
   |---|---|---|---|
   | ① | `korea-poker-marathon-2026` L125·L130 (+ desc·L153 H2) | **죽은 행동 지시** — *"apply early before it fills"*. 등록 채널이 닫혔다(라이브 실측: worldsmarathons 이벤트 위젯이 `Notify me` · 열려 있는 2027-03 서울마라톤은 등록 버튼 7개·`Notify me` 0). 🪶 **zh는 한 글 안 모순 때문에 먼저 닫았다**(§3 회차 5) — EN + 나머지 7로케일이 남았다 | Playwright 대조군 |
   | ② | `holdem-tournament` L201 | 「**Short stack (under 20BB):** Push-or-fold **takes over**」가 **같은 글 L89**(「below 20 you're *edging into*, by 15 it's your main mode」)와 어긋난다. zh L96은 L89를 정확히 옮겼고 L223이 L201의 모순을 복제했다 | EN 내부 모순 |
   | ③ | `holdem-bubble` L139 | 「(WSOP Rule **126.a** and **126.c**; TDA RP-8)」 — 🔴 **레포 1차 사본에 없다.** `docs/sources/wsop-2026-rules-발췌.md`·`tda-2022-shortform-rules.txt` 어디에도 126조도 hand-for-hand 조항도 없다(직접 grep). 내용은 업계 통용 절차와 맞지만 **조문 번호를 달 자격이 없다** | `docs/sources/README.md` 규율 |
   | ④ | `wpt-australia-2026-guide` L260·L261 | ETA 표가 **6개국만 열거**하고 나머지를 「$250·수 주」 catch-all로 묶는다. 실제 601 적격국 독자가 오도된다. 🔴 **「말레이시아·대만이 601 적격」이라는 렌즈 주장은 검증 실패** — 호주 내무부 페이지가 Playwright·WebFetch 둘 다 **403**. §12-B대로 사실 주장은 쓰지 않았고 **구조 결함만** 등재한다 | 라이브 검증 실패 |
   | ⑤ | `apt-incheon-2026-guide` L341 | 7일 예산 합계 `$2,500–$5,000`가 명세 8행의 합과 안 맞는다(재계산: 최저 **$1,425**·최고 **$4,200**). 하한은 1.8~3배, 상한은 19% 높다 | 수학 렌즈 재계산 |
   | ⑥ | `apt-incheon-2026-guide` L155 vs L68/L89/L339 | 같은 主赛 참가권이 **$1,684**(Country Exclusives)과 **$1,703**(그 외)로 갈린다(환율 1,485 vs 1,468). 출처가 다른 것으로 보이나 한 글 안이라 독자에겐 모순 | 수학 렌즈 |
   | ⑦ | `holdem-short-stack` L111 ↔ `holdem-bubble` L129 | 같은 «10bb 全下 vs BB 跟 9bb» 국면에 **底池가 20.5bb ↔ 12bb**로 다르다(ante 유/무 전제가 갈리는데 어느 쪽도 안 밝힌다). 각각은 산술적으로 옳다(43.9% / 42.9%) | 수학 렌즈 |
   | ⑧ | `ept-barcelona`·`wpt-australia`·`korea`·`apt` 상단 리드 블록 ↔ tldr | **EN 쪽 축어 중복이 zh보다 크다** — ept **zh 48자 vs EN 111자** · wpt 48/49 · korea 36/43 · apt 28/49. 리드 블록은 편집 전부터 있던 것이고 EN 동형이라 이번 회차에서 손대지 않았다 | SEO 렌즈 계량 |

19. 🔴 **`德州扑克 锦标赛`(bare 머리 어휘)를 `wsop-2026-tournament-guide`가 소유한다** — 뱅크 §3은 「소유자 = `holdem-tournament`」로 판정했는데 태그상 소유는 wsop다(Dice 0.75~0.86). wsop는 **경화 대상 밖**이라 레인이 못 건드린다 → 헤드가 wsop 쪽을 `WSOP 锦标赛 2026`류로 좁히고 필라에 bare를 줄지 판정할 것. 🪶 덤: wsop는 비정본 라벨 `> **一句话总结**`을 쓴다.
20. 🟠 **`Paradise City Casino 扑克` 표기 소유가 뱅크 판정과 뒤집혔다** — apt가 서제스트 **0**인 이 문자열을 유지하고 kpm이 실측형 `Paradise City 赌场`를 가져갔다. exact 충돌은 해소됐고 Dice 0.69라 게이트엔 안 걸린다. 뱅크 §3 축어는 「apt도 `仁川赌场`/`paradise city 赌场`으로 갈아타는 편이 낫다」였고 apt엔 이미 `仁川赌场`이 들어갔다 → 교체 여부는 헤드 판정.
21. 🔴 **머지 후 `npm run build`로 `public/sitemap.xml`을 갱신해 함께 push할 것**(§5-8·10·16과 같은 이유). 회차 5는 **11편**의 `updated`를 2026-09-08로 올렸다(토너먼트 9 + `holdem-starting-hands-chart`·`texas-holdem-rules-for-beginners`). 추가로 `holdem-when-to-fold`·`holdem-position-play`는 본문만 고치고 `updated`를 **올리지 않았다**(표기 통일 4자리뿐 · 판단 필요하면 헤드가).
22. 🟠 **`docs/locale-intentional-diffs.md` 등재 요청 2건**: ① **「zh 토너먼트 축은 질문형 H2 70%를 적용하지 않는다」** — 자동완성 실측에서 `什么是…`·`怎么打`·`为什么…` 접두가 **거의 전부 0**이고 「명사 + 意思/报名费/时间/攻略」만 산다(뱅크 §0). 회차 1의 «zh 질문형 70%» 판정은 **규칙·족보 축에서 참**이고 토너먼트 축에서는 아니다 — **축별로 갈린다**는 것이 이번 실측이다. ② **zh의 `/en/hand-chart` 링크**(§3 회차 5) — 라우트가 생기면 되돌릴 자리.

23. 🔴 **EN-먼저 3건 — 회차 6(용어 클러스터).** 전부 EN 마스터에 같은 문장이 있어 zh에서 고치지 않았다.

   | # | EN 파일·자리 | 무엇이 문제인가 | 근거 |
   |---|---|---|---|
   | ① | `holdem-bad-beat` L92(표) + L98(각주) | 「Overpair vs a flush draw … **~65% (2:1)** … their **9-out** flush draw」 — **9 outs 조건에서 도달 불가능한 값**이다. 전수열거(C(45,2)=990) 결과 오버페어 equity는 **61.9~63.4%**, 오즈 **1.63~1.73:1**. 대표값 AA vs 8♦3♦ / K♦Q♦2♣ = **63.43%(1.73:1)**가 상한. 68.99%(2.22:1)가 나오는 유일한 배치는 오버페어가 그 무늬 A를 들어 드로가 **8 outs**로 줄 때다 → 「~63%（약 1.7:1）」이 맞다 | 수학 렌즈 · 7장 평가기 2종 교차검증 8/8 |
   | ② | `holdem-bad-beat` L112(불릿) ↔ L118–121(표) | **한 글 안에서 갈린다.** 불릿은 2026-08-26에 「the other players **dealt into that hand** split the rest」로 고쳐졌는데 **바로 아래 표는 「Others at the table ~25%」 그대로**다. zh도 같은 모양이라 **EN 유래**다(zh L128 「拿到底牌的其余玩家」 ↔ L136 「牌桌上其他人」) | A 구간 실측 · EN·zh 동형 |
   | ③ | `holdem-bad-beat` 도입 문단(L20~22 대응) | 첫 내부링크가 필라 `holdem-glossary`가 아니라 `holdem-cooler`다(**CLAUDE.md §8** 「클러스터는 필라를 첫 내부링크로」). **EN이 그 순서라 zh만 바꾸면 «링크 구조 EN 1:1» 원칙이 깨진다.** 나머지 4편(cooler·fish·rake·straddle)은 전부 필라가 첫 링크 ✅ | SEO 렌즈 · EN 원문 대조 |

24. 🟠 **게이트 신설 제안 — «간체 로케일의 번체자 누출».** `bad-beat`에 번체 `手風`(1자)이 있었는데 **`check:hangul`·`check:cjk`·`audit:hard` 어느 것도 원리상 못 잡는다**(한글도 아니고 개행 문제도 아니다). 간체 로케일(zh)에 번체 전용자가, 번체 로케일(zh-hant)에 간체 전용자가 섞이면 잡는 게이트가 있으면 좋겠다. 🪶 이번 회차는 **임시 스크립트로 30자 쌍을 훑어 0건을 확인**했다(風/機/會/這/個/來/買/籌/對/實/點/際/說/請/撲/蓋/贏/輸/錢/張/場/們/當/過/還/兒/專/變/據/價).

25. 🟠 **string bet 역어 통일이 레인 범위를 넘는다** — `串注`(betting-actions L257·L259 FAQ · 이번에 glossary를 여기로 통일) ↔ `分段下注`(all-in-rules L65·L71). **all-in-rules는 회차 1(규칙) 클러스터라 이미 지나갔다** → 헤드가 일괄로 `串注`로 통일할지 판정할 것.

26. 🔴 **머지 후 `npm run build`로 `public/sitemap.xml`을 갱신해 함께 push할 것**(§5-8·10·16·21과 같은 이유). 회차 6은 **6편의 `updated`를 2026-09-09로** 올렸고, `masterUpdated`는 **glossary·fish 2편을 2026-08-30으로** 올렸다(드리프트 게이트 🔴 2 → **0 · ✅ 56/56**).

30. 🔴 **EN-먼저 5건 — 회차 7(트랙 종결).** 전부 EN 마스터에 같은 문장·수치가 있어 zh에서 고치지 않았거나,
   zh만 고쳐서 로케일 간 갈림이 남은 자리다.

   | # | EN 파일·자리 | 무엇이 문제인가 | 근거 |
   |---|---|---|---|
   | ① | `holdem-3bet` L301 | 🔴 **「more dead money pushes it toward 4×」는 부호가 반대다.** 배수 = (2B+D)/(2R+D) · **∂/∂D < 0** — 죽은돈이 커지면 배수는 **반드시 작아진다**(D=0 → 3.00 · D=1.5 → 2.60 · D=2.5 → 2.41). 4배 조건은 **무위치 대형 3bet**이다. 🔴 **`lib/posts-ja/holdem-3bet.ts` L332도 같은 오염**(「デッドマネーが増えれば4倍近く」) · 🟢 **zh-hant L332는 이미 옳다** → **zh-hant를 정본으로 EN·ja 동시 정정** | 수학 렌즈 · 전 파라미터 조합 단조성 실측 |
   | ② | `wpt-australia-2026-guide` L104 | 위성 표 1행이 `Pre-festival, from Jul 24 \| $290 or $550`로 **두 창을 뭉뚱그린다.** 그러면 같은 글 블록(L94 `$290: Jul 30–Sep 9`)·FAQ(L306)와 **어긋난다** — $290이 7/24부터로 읽힌다. 🔴 **4개 렌즈가 독립 검출.** zh는 두 구간을 분리해 되돌렸다(§3) → EN도 같이 | 딜러·수학·네이티브·SEO 4렌즈 |
   | ③ | `holdem-3bet` L301 ↔ `3bet-pot-cbet` L136·143 | **두 글이 다른 수치 모델을 쓰는데 한 문장이 이어 붙인다.** 3bet = 3bb 오픈/9bb 3bet(19.5bb·SPR 4.7) ↔ cbet = 솔버 실측 2.5bb/11bb(22.5bb·SPR 4.0). **양쪽 다 산술은 맞다.** zh는 전제를 축어로 명시해 임시 봉합했다(zh-hant 선례) — EN은 전제도 없다 | 딜러·수학·SEO 3렌즈 |
   | ④ | `holdem-drawing-odds` L164 | 「A-K suited **331:1**」만 기준이 다르다. 같은 표 다른 3행(220:1 · 16:1 · 3.25:1)은 정확한 **odds-against**인데 AKs만 1/p(331.5)의 반올림이다 — 정확값 **330.5:1**. 업계 통용 표기라 «오류» 단정은 애매하나 **한 표 안에서 기준이 갈린 것**은 사실 | 수학 렌즈 |
   | ⑤ | `holdem-3bet` L301 | 「SPR drops to about 4.7, **which is why** the three-bettor often bets its entire range」가 SPR을 원인으로 못박아 «SPR 낮음 → 레인지 벳»이라는 **보드 무관 규칙**으로 굳기 쉽다. 정작 링크 대상 글이 「얕은 스택이 이유가 아니다」를 경고 박스로 못박는다(`3bet-pot-cbet` L209·L295) | 딜러 렌즈(확신도 낮음) |

31. 🔴 **판정 요청 — «thumb도 EN 1:1인가»** (전 로케일 공통). 이번에 zh가 `holdem-3bet`·`holdem-position-play`의
   신설 링크에 thumb을 달았다가 **3렌즈가 「EN에 없다 → 규율 위반」으로 지적해 뗐다.** 그런데 **zh 코퍼스 관례는
   같은 슬러그를 thumb으로 부른다**(`blind-battle-cbet` L138 · `donk-bet-strategy` L212). 규율(메모리
   `translation-link-structure-equals-en`)과 코퍼스가 갈린다 → **헤드가 «thumb은 파리티 대상인가»를 정해 줄 것.**

32. 🪶 **범위 밖 발견** — `lib/posts/holdem-community-event-guide.ts` L5·L15에 `emoji` **중복 키**(TS1117).
   KO 파일이라 레인이 못 건드린다. 무해하나 실재한다.

33. 🔴 **머지 후 `npm run build`로 `public/sitemap.xml`을 갱신해 함께 push할 것**(§5-8·10·16·21·26과 같은 이유).
   회차 7은 **23편**의 `updated`를 2026-09-09로 올렸다 → sitemap `lastmod`가 바뀐다. 레인은 커밋하지 않는다.
   🪶 레인 빌드 실측: **708 페이지 · 70 blog + 525 intl · exit 0**.

34. 🟠 **`wpt-australia`의 `$290` 창(7/30–9/9)이 오늘(2026-09-09) 마감이다.** 내일부터 L100 블록·L123 인용·L336 FAQ
   **세 자리가 동시에 낡는다.** → `docs/update-calendar.md` 등재 대상(헤드).

## 6. 자산 축적 체크 (회차 마감마다)

| 회차 | ① 키워드뱅크 | ② 편차 판정 요청 | ③ voice/판정 정본 |
|---|---|---|---|
| 1 | ✅ `docs/keyword-bank/zh-rules.md` 신설 — 자동완성 전량 수확 · 배정/양보표 · **§3 태그 카니발 판정 4건** · §4 `二四法则` 표기 미결(회차 3 이월) | ✅ §5-3 (질문형 H2 축) | ✅ `docs/local-voice/zh.md` **신설** — §1 검색 표기 실측 · §2 라쿠 질문 DB 특성 · §3 현지 경쟁 골격 판정 3개 · §4 라벨·길이 계량 단위 · **§5 직답 블록 실패 유형**(겹침 42~62% 실측·처방 5개·tldr 금지) |
| 2 | ✅ `docs/keyword-bank/zh-hand-rankings.md` 신설 — 자동완성 47시드 전량(**SG·MY 완전 동일 재확인**) · 라쿠 3시드 · 배정/양보표 · **§4 함정 6건**(同花顺=증권 앱 · 铁支·机率=港台 표기 등) · **「谁赢」형이 자동완성에 없다는 실측**(EN 직역 금지의 근거) | ✅ §5-2-B (EN-먼저 11건 표) · §5-6 (打公共牌 표기 판정) | ✅ `docs/local-voice/zh.md` **§6 신설** — 아래 방법론 |

| 3 | ✅ `docs/keyword-bank/zh-probability.md` 신설 — 자동완성 46시드 전량(**SG·MY 완전 동일 — 세 번째 실증**) · 배정/양보표 12행 · **§2 「二四法则」 3파전 종결 표** · §4 함정 5건(도구 의도 · `机率` 범위 정정 · 번체 오염 · **WebSearch 요약이 공식을 틀리게 준 실증**) · §6 경쟁 글 6편 Playwright 실측 · **§8 라쿠는 이 축을 커버하지 않는다는 실측** | ✅ §5-2-C (EN-먼저 7건 표) · §5-9 (인용부호 판정) | ✅ `docs/local-voice/zh.md` **§8 신설** — 「확률 축은 명사로 검색하고 질문으로 클릭한다」 |

| 5 | ✅ `docs/keyword-bank/zh-tournament.md` 신설 — 자동완성 전량(SG) + **`gl=MY` 검증표본에서 «SG·MY 완전 동일»이 처음 깨졌다**(`首尔 马拉松`) · 배정/양보표 27행 · **§4 함정 6건**(bubble/泡沫=전자담배·스티로폼 · 라틴 3글자 단독 · Balatro 盲注 · ptt/澳门 · 筹码=실물 칩) · **§5 경쟁 글 4편 Playwright 실측**(3편이 H2 골격 자체가 없다) · **§6 1차 출처 대조**(JTBC 등록 위젯 대조군) · §7 시한 자산 상태 | ✅ §5-17(게이트 2종) · §5-18(EN-먼저 8건) · §5-19~22 | ✅ `docs/local-voice/zh.md` **§10 신설** — 아래 방법론 |

| 4 | ✅ `docs/keyword-bank/zh-strategy.md` 신설 — 자동완성 46시드 전량(**SG·MY 완전 동일 — 다섯 번째 실증**) · 배정/양보표 26행 · **§0 축별 서제스트 표**(策略·c-bet·什么时候弃牌 = **0건**) · §4 함정 5건 · §5 경쟁 글 5편 Playwright 원문 실측 | ✅ §5-12 (EN-먼저 9건 표) · §5-13~15 (태그 귀속 · blind-meaning 축 · 劫机位 표기) | ✅ `docs/local-voice/zh.md` **§9 신설** — 「전략 축은 머리 키워드가 없다 — 관문 3편으로 먹는다」 |

| 6 | ✅ `docs/keyword-bank/zh-glossary.md` 신설 — 자동완성 전량(SG) + `gl=MY` 검증표본 7건 · **§0 「意思」 축 발견**(시드 하나가 표제어 15개를 준다) · 배정/양보표 6행 · **§4 함정 6건**(bad beat=의류 브랜드 · cooler=PC 쿨러 · **抽水=粤语 «등쳐먹다»** · 爆冷=축구 · 机率 재확인 · 鱿鱼 오염) · **§5 경쟁 글 6편 Playwright 실측** · **§7 태그 띄어쓰기 판정**(회차 5 미결 종결) | ✅ §5-23(EN-먼저 2건) · §5-24~26 | ✅ `docs/local-voice/zh.md` **§11 신설** — 아래 방법론 |

| 7 | ✅ `docs/keyword-bank/zh-track-close.md` 신설 — 자동완성 SG **26시드** + `gl=MY` 검증표본 4건 · **§0 넘겨받은 미결 7건 재판정표**(3건 뒤집힘) · **§2 함정 5건**(踢脚牌=인테리어 걸레받이 · 短码怎么打=断码/短信验证码 · 串注=串珠 · 牌型一样谁赢=牌告/牌品) · **§4 앵커 전수 실측**(12갈래 · 후보 전부 0 → SEO 축 아님 판정) · **§5 EN↔zh 링크 계수 전 56편 기계 대조** · **§6 «drift ✅는 날짜 진실이지 내용 진실이 아니다»** | ✅ §5-30(EN-먼저 5건) · §5-31(thumb 파리티 판정) · §5-32~34 | ✅ `docs/local-voice/zh.md` **§12 신설** — 아래 방법론 |

### 회차 7이 남긴 방법론 (다음 트랙·다음 로케일이 그대로 쓸 것)

1. 🔴 **`check:drift` ✅는 «날짜 진실»이지 «내용 진실»이 아니다.** `masterUpdated`를 한 번 올려 버리면
   **diff 방식으로는 영영 못 찾는다.** 이번에 EN 08-20 커밋 2건(`cd9ec3c0`·`1d0245c1`)이 zh에 안 와 있었는데
   게이트는 계속 ✅였다(zh masterUpdated 09-06·08-27). → **드리프트 판정에 «구조 계수»를 한 겹 더 붙여라** —
   «EN에 있고 나에게 없는 링크·표 행·리스트 항목의 개수». 전 코퍼스 0.5초이고, 날짜도 diff도 못 보는 자리를 본다.
   🔴 **회차 6의 «미러가 EN을 처음으로 완전히 따라잡았다»는 선언은 이 계수를 안 돌린 상태의 선언이었다.**
2. 🔴 **한 회차 안에서 판정이 두 번 갈릴 수 있다 — A가 실측으로 뒤집고, C의 렌즈가 A를 다시 뒤집는다.**
   이번에 넘겨받은 처방 7건 중 3건을 A가 뒤집었고, 그중 2건을 C가 되돌렸다(`现金桌` 층 분리 · `德州短码` 원복).
   원인은 **A가 «검색이 무엇을 부르나»만 보고 «우리 코퍼스가 무엇을 쓰나»를 안 본 것**이다.
   → **태그·표기 판정에 «온페이지 등장 횟수»와 «형제 글이 이 글을 부르는 앵커»를 필수 입력으로 넣어라.**
3. 🔴 **«0»을 한 덩어리로 다루지 마라 — 함정형/근사중복형/축부재형 셋이다**(local-voice §12-2).
   근사중복형만 제거 대상이고, **축부재형은 건드리면 셔플이지 개선이 아니다.**
4. 🔴 **EN 파리티가 정밀도를 이기지 못한다.** `wpt` 표를 EN 구조로 되돌렸더니 같은 글 블록·FAQ와 어긋나
   **4개 렌즈가 독립 검출**했다. zh의 옛 셀이 EN보다 정확했다. → **EN이 뭉뚱그린 자리는 로케일이 정밀한 채로 두고
   EN-먼저로 올린다.** «EN 1:1»은 구조·대상·개수의 원칙이지 «EN이 덜 정확해도 따라가라»가 아니다.
5. 🔴 **거울쌍은 양방향이다 — 이번엔 zh가 뒤처진 쪽이었다**(회차 1~6은 전부 「zh가 앞섰다」였다).
   `3bet`의 부호 오류를 **zh-hant가 이미 고쳐 놓았고** EN·ja는 아직 오염이다. → **상대 레인이 정본을 갖고 있는지
   먼저 보라.** 이번 정정은 zh-hant 문안을 정본으로 삼아 이식한 것이다.
6. 🔴 **계량은 «상위부터»** — 회차 4→5가 지목한 `limping` 자리는 **2등**이었고 1등은 두 회차 연속 판정 대상에
   오르지 않았다. **눈에 띈 자리로 고르지 말고 전 자리를 정렬한 뒤 상위부터.**
7. 🔴 **직답 블록의 새 실패 유형 = «표 재인쇄».** 뒤가 표인 H2에서 블록이 표를 요약해 48자(하한 미달)가 났다.
   회차 5 방법론 3의 「뒤가 표면 파손 0」은 **«본문 파손»이 없다는 뜻이지 «블록이 성립한다»는 뜻이 아니다.**
   → **뒤가 표면 «표에서 읽어 낼 결론»을 써라.**
8. 🔴 **노출면 목록에 «그 표를 요약하는 블록»을 추가한다**(회차 5 방법론 6의 확장). 표 행 하나를 갈았더니
   절 상단 블록이 새 행을 안 담고 사라진 행을 계속 가리켰다 — **표만 보고 블록을 안 봤다.**
9. **렌즈 5종이 일곱 번째로 값을 냈고, «한 렌즈만 잡은 항목»이 이번에도 결정적이었다**:
   수학 = **부호 반대**(전 파라미터 단조성을 미분해야 보인다 — 다른 넷은 「19.5÷7.5=2.6 통과」로 넘겼다) ·
   네이티브 = **把자문 파손**·`平跟` 이중 의미·**지시어로 시작하는 직답**(local-voice §5 명문 위반) ·
   교열 = **블록↔표 노출면 누락**·**zh-hant 거울쌍 미이식**·`短码策略`이 형제 앵커라는 것 ·
   딜러 = **`straddle` L222는 원래 틀린 문장**이었다는 판정 · SEO = **계량 1등이 판정 대상이 아니었다**는 것.
   **4개 렌즈가 겹쳐 검출한 것은 하나뿐**(wpt 날짜)이고, 나머지 값은 전부 단독 검출에서 나왔다.
10. 🪶 **B 구간을 «중간 저장»으로 끊어야 하는 교착이 실재한다.** 헤드가 레인 자기 자리를 고치면
   pre-commit이 커밋을 막고, 동시에 로컬 변경 때문에 `git merge main`도 거부된다. 탈출구가 유일한 길이었다.
   🔴 훅 메시지가 제안하는 `git stash`는 **레인 워크트리에서 위험하다**(스택이 창 간 공유) — WIP 커밋이 맞다.

### 회차 6이 남긴 방법론 (회차 7이 그대로 썼다)

1. 🔴 **«축마다 다시 재라»가 세 번째 형태를 냈다.** 회차 1·2(규칙·족보)=**질문형**, 회차 5(토너먼트)=**명사+접미**, 회차 6(용어)=**「德州扑克 + <용어> + 意思」**. 세 번 다 앞 회차 판정을 그대로 쓰면 틀렸을 자리다. → **A 구간에서 축을 재는 것을 건너뛰지 마라.**
2. 🔴 **이 축에는 «표제어 커버리지를 검색으로 감사하는» 시드가 있다.** `德州扑克 意思` 하나가 독자가 실제로 찾는 표제어 15개를 통째로 돌려줬고, 우리 표에 없던 3개(`深跑`·`iso`·`jp`)를 그렇게 찾았다. → **사전형 글은 «내가 뭘 빠뜨렸나»를 자동완성으로 물어볼 수 있다.**
3. 🔴 **«개수를 선언하는 블록»은 새 결함 유형이다 — 4편에서 동시에 났다.** 「三步」(실제 4) · 「三个动作就够」(실제 5 · 「就够」가 나머지를 적극 부정) · 「三种收法」(H2가 부른 dead drop을 빼고 4→3) · 「7 组…这 8 组」. **요약하면서 세는 순간 본문과 어긋난다.** → **블록에 숫자를 쓸 거면 그 자리에서 본문 항목을 세라.** 3개 렌즈(딜러·SEO·네이티브)가 독립 검출했다.
4. 🔴 **«이동»이 만드는 파손의 반대편 — «과잉 삭제»도 있다.** 예고문(「现在讲那个…真相。」)을 지우는 것은 옳았지만, 같은 회차가 cooler에서는 **답을 지우고 예고만 남겼고**(「下面那一句就是它」), rake에서는 **「얼마인가」를 묻는 H2의 직답에서 숫자를 전부 뺐다.** → **블록은 «답»이다. 본문을 가리키게 만들지 마라**(회차 4 방법론 3의 쌍둥이).
5. 🔴 **1차 반영이 만든 새 결함을 «다른 렌즈»가 잡았다 — 2차 교열만으로는 못 잡았을 것이다.** `fish` 剥削표 3행을 딜러 렌즈 지적대로 고쳤더니 **수학 렌즈가 그 조건이 공집합임을 증명**했다(VPIP≥40 ∧ gap≤25 ⟹ PFR≥15). → **렌즈 지적을 반영할 때, 그 지적이 «수치 조건»을 건드리면 수학 렌즈에 다시 걸어라.**
6. 🔴 **«코퍼스에 이미 있는 역어»를 확인하지 않고 새 표기를 만들었다.** `拉线下注`를 고치며 `分次下注`를 새로 만들었는데, 레포에는 이미 `串注`(betting-actions FAQ)와 `分段下注`(all-in-rules)가 있었고 **그 글이 바로 이 행이 링크로 가리키는 심층 가이드**였다. → **표기를 바꾸기 전에 «이 글이 링크로 넘기는 글»의 표기를 먼저 grep하라.**
7. 🔴 **거울쌍 게이트가 «표기 규율 위반»도 잡는다.** TDA 조항 번호를 넣었더니 `Rule55`가 zh 단독 토큰으로 떴고, 회차 1이 확정한 「zh만 번호를 달지 마라」 판정에 걸렸다. → **§5-⑥을 «수치 대조»로만 쓰지 마라**(회차 3 방법론 2의 확장).
8. 🔴 **자동완성이 돌려준 문자열의 공백은 «구글의 정규화»이지 «사용자의 타건»이 아니다.** A 구간의 배정표(§3)가 실측 원문의 공백을 그대로 태그로 옮겨 §7 판정과 **한 문서 안에서 갈렸고**, SEO 렌즈가 그걸 잡았다. → **뱅크 안에서 «실측 원문»과 «채택 표기»를 다른 칸에 적어라.**
9. **렌즈 5종이 여섯 번째로 값을 냈고, «한 렌즈만 잡은 항목»이 이번에도 결정적이었다**: 딜러 = **버튼 정의가 거짓**·구두 선언 룰 2자리(다른 넷은 룰북을 안 본다) · 수학 = **fish 표의 공집합**과 65%/2:1 과대(전수열거 없이는 안 보인다) · 네이티브 = **`资金盘`이 폰지 판을 뜻한다**·`牌河`·`尼特`(오역 12종 — 다른 어느 렌즈도 못 봤다) · SEO = **블록이 H2가 부른 항목을 빼먹은 것**과 축어 2층 계량 · 교열 = **개수 선언 4건**과 선행사 소실 5곳. **하나라도 빼면 그 항목은 통과한다.**
10. **A 구간 §13 선검산이 세 번째로 값을 냈다.** cooler의 set-over-set을 A에서 전수열거(990 런아웃)해 두었더니 C의 수학 렌즈가 **독립적으로 같은 43/990에 도달**했고, 그 덕에 수학 렌즈의 시간이 «아직 안 본 표»(bad-beat 승률표·rake bb/100 체인)로 갔다 — 거기서 실제 결함 3건이 나왔다.

### 회차 5가 남긴 방법론 (회차 6이 그대로 쓸 것)

1. 🔴 **«질문형 H2 70%»는 로케일 상수가 아니라 «축»마다 다르다.** 회차 1이 규칙·족보 축에서 실측해 확정한 판정을 이번에 토너먼트 축에 던졌더니 **질문형 접두 시드가 거의 전부 0**이었다(`什么是卫星赛`·`什么是 icm`·`钱圈 是什么`·`锦标赛 怎么打`·`短码 怎么打`·`为什么锦标赛` = 전부 0 · 반면 `德州扑克锦标赛` 10건 · `钱圈` 8건 · `德州扑克 icm` 4건). → **클러스터마다 A 구간에서 질문형/명사형을 다시 재라.** 로케일 판정을 상속하지 마라.
2. 🔴 **«최장 축어»는 고유명사를 빼고 재라.** 회차 3·4가 「평균이 아니라 최장으로」를 확립한 것은 옳았지만, 필터가 없으면 **좌석 나열(`UTG、LJ、HJ、CO、BTN、SB、BB` 25자)·용어 라틴 병기(`紧凶（tight-aggressive，TAG）` 24자)·URL·상품 구성**이 상위를 다 먹어 진짜 결함을 가린다. 회차 4 파일을 다시 재니 20자 초과 7건 중 **6건이 그 유형**이고 진짜 문장 중복은 1건이었다. → **① 원본 ② 라틴런·숫자·통화·URL 마스킹한 «순수 산문» 두 층으로 재라.** 이번 회차 산문 기준 평균 **9.1자** · 20자 초과 **1건**(그것도 기존 리드 블록).
3. 🔴 **«이동»이 파손을 만드는 자리에는 규칙성이 있다 — 블록 뒤가 산문 단락인 곳이다.** 네이티브 렌즈 전수 점검: 블록 107자리 중 뒤가 **표·리스트·`<div>` 카드**인 43자리는 **파손 0건**(옮길 문장이 없었으니까), 파손 9곳 + 반복 11곳은 **전부 뒤가 산문인 자리**(개념 4편 + korea)에 몰렸다. → **B 구간은 «뒤가 산문인 H2»부터 손대고, 거기에 검수 시간을 배분하라.**
4. 🔴 **렌즈의 «계산»과 «처방 문안»이 어긋날 수 있다 — 처방을 그대로 옮기지 마라.** 딜러 렌즈가 「BF 3.0 → 3 ÷ (1+3.0) = **75%**」라고 계산해 놓고 처방 문안엔 「**70% 上下**」라고 썼고, **1차 반영이 그 불일치를 못 잡고 그대로 옮겨 새 §13 오류를 만들었다.** 2차 교열이 잡았다. → **렌즈 처방에 수치가 있으면 채택 전에 그 렌즈의 계산과 대조하라.** 「전건 원문 판정」(프로토콜 §5-④)은 렌즈의 *결론*뿐 아니라 *처방*에도 걸린다.
5. 🔴 **«중복 제거»가 이번에도 원점 복귀를 만들었다 — 6자리.** 2차 교열 23건 중 **6건이 「블록↔본문 중복을 지우려다 본문이 블록을 복사」**였다(회차 4는 4건). 유형이 굳어졌다: 선행사가 사라진 자리를 메울 때 **가장 가까운 문장(=블록)을 베끼는 것**이 기본값이 된다. → **본문을 되살릴 때는 «블록에 없는 정보»를 넣어라.** 같은 명제를 다른 말로 쓰는 것도 부족하다 — 블록이 결론이면 본문은 **검산·사례·기제**여야 한다.
6. 🔴 **한 자리를 고치면 그 문장이 걸린 «노출면»을 전부 세라.** `korea-poker-marathon`에서 블록만 「已全部关闭」로 바꿨더니 **desc·tldr이 「9 月 4 日截止」로 남아 SERP 스니펫과 페이지 상단이 서로 반대**가 됐다. 2차 교열이 잡았다. → 치환 체크리스트: **desc · tldr · seoTitle · imageAlt · figcaption · readnext 카드 · 相关文章 카드 · 형제 글이 이 글을 부르는 라벨**. (회차 4의 「같은 문장 안부터 세라」의 확장판이다.)
7. 🔴 **표기 재조준은 «상위어/하위어»를 먼저 정하고 시작하라.** `bubble → 钱圈` 승격이 반쪽에서 멈춰, `钱圈`이 **상위어(3종 전체)**와 **하위어(머니버블)** 두 뜻으로 한 글에 공존했다(2차 교열이 표로 전수 제시). 해소는 **하위어에 새 이름을 주는 것**(`进钱 bubble`)이었다. → 개명 착수 전에 «이 말이 지금 몇 층을 가리키나»를 먼저 세라.
8. **렌즈 5종이 다섯 번째로 값을 냈고, «한 렌즈만 잡은 항목»이 이번에도 결정적이었다**: 교열 = **apt 비자 블록의 K-ETA 오지시**(간체 1순위 독자에게 틀린 절차 · 다른 넷은 못 봤다) · 딜러 = **제주 免签 동선이 형제 글과 정반대** · 수학 = **`1,393 人` → `人次`**(3,008,880,000 ÷ 1,393 = 2,160,000 나머지 0이라는 산술 증명) · 네이티브 = **문장 파손 9곳**(다른 어느 렌즈도 「由 Dan Harrington 推广，」이 주어 없는 조각이 된 걸 못 봤다) · SEO = **`钱圈` 재조준이 FAQ 질문 축에서 멈춘 것**과 축어 2층 계량. **하나라도 빼면 그 항목은 통과한다.**
9. **A 구간에서 §13을 미리 검산해 두면 C가 가벼워진다(회차 4 방법론 8의 재확인).** ICM 표·泡沫系数 표를 A에서 손으로 검산해 브리프에 「✅ 검산 완료」로 적었고, C의 수학 렌즈가 **독립적으로 같은 결론**에 도달했다. 그 덕에 수학 렌즈의 시간이 «아직 안 본 표»(상금 구조·예산 3방안·환율 정합)로 갔고 거기서 실제 결함 2건이 나왔다.

### 회차 4가 남긴 방법론 (회차 5가 그대로 쓸 것)

1. 🔴 **«이동»은 콜론에서 끊으면 반드시 파손된다.** 이번 회차 문장파손 11곳이 **전부 같은 손동작**이었다 — 원문 「…一个直接的修正：==**내용**==」에서 **콜론 뒤만 블록으로 올리고 콜론 앞을 본문에 남긴 것**. 남은 문장은 「하나의 수정으로 수렴했다」로 끝나 **그 수정이 무엇인지 끝까지 말하지 않는다.** 같은 유형: 「这是策略图表不会告诉你的秘密。」(비밀을 예고만 함) · 「让你成为赢家的思维转变就在这里。」(这里가 가리킬 것 없음) · 「"大部分"是多少？」(인용 대상 소실).
   → **블록으로 올릴 때 «콜론 앞»도 같이 올리거나, 본문에 결론을 한 구 남겨라.** 대명사·인용부·「这N件事」가 남으면 그 자리는 깨진 것이다.
2. 🔴 **반영이 만드는 새 결함이 «축어중복 지우기»에 몰린다.** 2차 교열 13건 중 **4건이 «원점 복귀»**였다 — 렌즈가 지적한 축어를 지우려고 본문에 문장을 되살렸더니 **되살린 문장이 블록을 그대로 복사**했다(최악 18자). 특히 **하이라이트 자산 복원 2건이 정확히 이 함정에 빠졌다**.
   → **문장을 되살릴 때는 «같은 뜻, 다른 말»로 써라.** 블록에서 잘라온 문자열을 그대로 되돌리면 반영 전으로 돌아간다.
3. 🔴 **중복을 피하려다 직답 자체를 표로 위임하지 마라**(2차 교열 2건). `starting-hands-chart`는 H2가 「10대 랭킹」인데 블록이 3개만 대고 「看下面这张表」로 넘겼고, `positions`는 H2가 예/아니오 질문인데 답 대신 「下面这张表把它拆开了」로 넘겼다. **블록은 AI 인용이 집어가는 패시지다** — 표로 미루면 그 자리가 비는 것이지 중복이 해소되는 게 아니다.
4. 🔴 **치환은 «같은 문장 안»부터 세라.** `跛入`→`平跟`을 구결에는 적용하고 **같은 문장 뒤쪽**(`别用跟注跛入`)과 **tldr**은 놓쳤다. 이 레포의 반복 사고이고, 회차 3(`庄家`·`赢家玩家`)에 이어 **두 회차 연속**이다.
5. 🔴 **최상급 정리 같은 «단순 치환»이 오타를 만든다.** `牌桌上唯一最赚钱` → `牌桌上牌桌上最赚钱`(이미지 title = figcaption으로 **독자에게 보인다**). 손으로 고친 자리는 **렌더되는 자리인지** 확인하라 — `render-markdown.ts`는 이미지 title을 `<figcaption>`으로 낸다.
6. 🔴 **평균 중복률은 4회차 연속 합격인데 최장 축어는 4회차 연속 터졌다.** 이번 실측: 평균 6-gram 블록↔본문 **15.8%**(합격) ↔ **최장 38자**, 70블록 중 **40개가 12자 초과**. 반영 후 **20자 이상 0건**으로 내렸으나 13~16자가 7편에 1건씩 남았다. → **다음 회차도 평균이 아니라 최장으로 재라.**
7. **렌즈 5종이 네 번째로 값을 냈고, «한 렌즈만 잡은 항목»이 이번에도 결정적이었다**: 딜러 = VPIP 25–30%·6맥스 UTG=劫机位·AJo(전부 D유형) · 수학 = **AA 만석 off-by-one**(30.87%가 상대 9명 값이라는 것은 재계산 없이는 안 보인다) · 네이티브 = **문장파손 11곳**(다른 어느 렌즈도 「这一点」이 빈 손가락이 된 걸 못 봤다) · SEO = 최장 축어 계량과 **FAQ 질문 exact 충돌**(strategy↔when-to-fold) · 교열 = 「三件事」↔5항목 개수 불일치. **네이티브를 빼면 독자가 못 읽는 문장 11개가 그대로 나갔다.**
8. **A 구간에서 §13을 미리 검산해 두면 C가 가벼워진다.** 회차 4는 A에서 3핸드(3bet·cbet 2국면)를 손으로 검산해 브리프에 「✅ 검산 완료」로 적었고, C의 수학 렌즈가 **독립적으로 같은 결론**에 도달했다. 교차검증이 공짜로 생긴다.

### 회차 3이 남긴 방법론 (회차 4가 그대로 쓸 것)

1. 🔴 **«우리가 많이 쓰는 표기»는 실측의 대상이지 근거가 아니다.** `2 和 4 法则`은 zh 코퍼스 **19건**으로 압도적 지배 표기였는데 자동완성·경쟁 글 제목 어디에도 없는 EN 직역이었다. 회차 1 뱅크가 «미결»로, 회차 2가 «본거지는 확률 클러스터»로 두 번 이월한 것을 이번에 자동완성 한 번으로 끝냈다. → **경화 회차마다 그 클러스터의 지배 표기를 한 번씩 의심하고, 자동완성으로 재라.**
2. 🔴 **거울쌍 게이트는 «내 쪽 결함»을 잡는 데 쓸 수 있다.** 회차 3에서 게이트가 「zh-hant에만 19.6%」를 띄웠고, 파고드니 **이번 B 구간이 §13 검산 수치를 요약체로 바꾸며 날린 자리**였다(같은 유형이 `pot-odds`에도 있었다). 상대 레인이 아직 경화 전이라 «zh가 앞섰다»로 넘길 뻔한 갈림 안에 내 결함이 섞여 있었다. → **§5-⑥을 «상대에게 넘길 것 찾기»로만 쓰지 마라.**
3. 🔴 **직답 블록을 만들면서 본문 검산 문장을 «요약»하지 마라.** 「9 × 4 = 36%（真值 35.0%）」 같은 자리는 §13 자산이고 EN parity 항목이다. 블록은 **결론**을, 본문은 **검산**을 맡는다. 둘이 겹치면 블록에서 덜어내는 것이지 본문을 지우는 것이 아니다.
4. 🔴 **반영이 만드는 새 결함이 회차마다 늘고 있다**(회차 1 **5** → 회차 2 **6** → 회차 3 **15**). 반영량에 비례한다. 유형: ① 치환이 절반만 됨(`庄家`·`赢家玩家`) ② 복구한 본문이 5행 뒤 문장과 충돌(`20×` vs `15–20×`) ③ 블록에서 덜어내려다 오히려 새 문장을 덧붙임 ④ 선행사를 지움(`这类听牌`) ⑤ H2 키워드를 넣으랬더니 **질문을 되묻는 형태**로 넣어 직답이 지연됨. → **2차 교열은 «반영분 델타»만 보게 하라**(두 diff를 주고 차이를 뽑게 하면 정확도가 올라간다).
5. 🔴 **평균 중복률은 합격인데 최장 축어가 터진다.** 회차 3 실측: 블록↔본문 6-gram 평균 **6.3%**(전 파일 20% 미만 = 합격)인데 **최장 일치가 6건에서 12자 초과**(최악 24자). 회차 2가 「LCS를 같이 재라」고 남긴 것이 그대로 값을 냈다 — **평균만 봤으면 「전편 합격」으로 닫혔다.**
6. **블록↔FAQ 중복의 뿌리는 EN 구조다**(회차 2가 「회차 3에서 계량하라」고 넘긴 항목). 계량 결과 zh `equity` 21.3% · `implied-odds` 13.0%로 높은데, **그 두 편은 EN 마스터가 이미 «리드 문장 = FAQ 답변»을 65~78%로 겹치고 있었다.** 반대로 EN에 그 결함이 없는 `pot-odds`·`probability`·`drawing-odds`는 zh에서도 낮다. → **로케일에서 고칠 문제가 아니다**(§5-2-C ⑦).
7. **렌즈 5종이 다시 값을 냈고, 한 렌즈만 잡은 항목이 이번에도 결정적이었다**: 딜러 = 五步 예시의 국면 누락(D유형) · 수학 = 「七个锚点」 개수 · **네이티브 = `街道`·`多数牌`·`顶部页面`·`庄家` 오역 4종**(다른 어느 렌즈도 못 봤다) · SEO = 최장 축어 계량과 태그 exact collision · 교열 = 선행사 소실과 「5% 法则」 증발. **네이티브 렌즈를 빼면 뜻이 깨진 문장 4개가 그대로 나갔다.**

### 회차 2가 남긴 방법론 (회차 3이 그대로 쓸 것)

1. 🔴 **§13 «승자 검산»과 «서술 검산»은 다른 것이다.** 회차 2에서 나는 7장→베스트5 평가기를 짜서
   45개 시나리오를 전건 돌렸고 **승자는 전부 맞았다**. 그런데 `split-pot`의 「前两张踢脚 K、9 …
   分胜负的是第三张踢脚」은 **키커 서수가 뒤집힌 오류**였다 — 평가기는 승자만 보므로 원리상 못 잡는다.
   렌즈 3종(수학·SEO·교열)이 독립적으로 잡았다. → **베스트5를 «몇 번째 카드에서 갈리는가»까지 서술한
   문장은 그 서수를 손으로 세라.** 메모리 「게이트 0건 + 렌즈 0건이라야 0건이다」의 실증.
2. 🔴 **직답 블록을 «신설»하면 본문과 겹치고, «이동»하면 안 겹친다.** 회차 2 실측: 굵은 선도문장을
   블록으로 옮긴 자리(`-` 삭제줄이 짝을 이룬 곳)는 중복률 20% 미만인데, **아무것도 빼지 않고 요약만 앞에 붙인**
   자리가 전부 35~52%로 나왔다. 최장 축어 일치는 회차 1의 25자에서 **30자로 오히려 악화**했다.
   → **B 구간에서 블록을 쓸 때마다 «본문에서 무엇을 뺄까»를 같이 정하라.** 뺄 게 없으면 블록을 넣지 마라.
3. 🔴 **렌즈를 5종으로 늘린 것이 값을 냈다**(회차 1은 4종). 수학·§13 렌즈를 딜러 렌즈에서 분리했더니
   딜러는 TDA 원문 4개 조항을 새로 확보했고, 수학은 WSOP 규칙 PDF를 직접 받아 Rule 73 축어를 대조했다.
   **한 렌즈만 잡은 항목**: 딜러=短牌 「排到顺子之上」 헛말 · 수학=7선5 표 헤더/别称 열 · SEO=태그 카니발과
   패시지 선점 · 네이티브=번역투 3건과 「拆平局」 · 교열=고아 문장과 花色 자기모순. **하나라도 빼면 통과한다.**
4. 🔴 **«EN 동형이면 무조건 EN-먼저»가 아니다 — 번역 정확화는 로케일에서 한다.**
   회차 2 판정 실례: EN "mucked hand is dead"의 zh 번역이 「盖掉」(덮다/폴드)여서 TDA 14와 어긋났다 →
   **「推进弃牌堆（muck）」로 고쳤다**(EN은 원래 맞았고 zh 번역만 넓었다). 반대로 EN 문장 자체가 틀린 11건은
   손대지 않고 §5-2-B에 등재했다. **판별 기준 = EN 원문이 맞는데 zh가 흘렸나(고친다) / EN이 틀렸나(등재한다).**
5. **두 렌즈가 갈리면 그것이 진짜 쟁점이다** — `打公共牌`/`打公牌`에서 네이티브는 정본을, SEO는 코퍼스
   지배를 근거로 서로 반대를 권했다. 둘 다 근거가 있어 **레인이 판정하지 않고 헤드로 올렸다**(§5-6).
   프롬프트 장치 「확신도 필수」가 이 갈림을 드러냈다.
6. 🔴 **1차 반영이 새 결함을 만든다는 것이 회차 2에서도 재현됐다**(회차 1은 5건 · 회차 2는 **6건**).
   유형이 회차 1과 다르다 — 회차 1은 «용어 개명 절반만»류였는데 회차 2는 **«고치다가 옆 문장을 틀리게 함»**이
   3건이었다: ① 三连面을 고치며 「两连面差一张」(맞던 문장)을 「还差两张」으로 **과잉교정**
   ② 「另有专篇」으로 축을 양보했는데 **바로 밑 표에 그 두 대결이 그대로** ③ 「这部分才是…」를 블록으로 올려
   본문 「在这里」의 선행사가 사라짐. → **문장을 고칠 때 «그 문장이 참조하던 것»과 «그 문장을 참조하던 것»을
   양쪽 다 보라.** ①은 2차 교열 전에 내가 재검산으로 먼저 잡았다(파이썬 평가기로 두 연속 보드를 직접 돌렸다).
7. **직답↔FAQ 중복은 게이트 밖이다** — 2차 교열 실측: 직답↔본문은 전부 27% 이하로 내려갔는데
   **직답↔FAQ는 33~40%**(reading `30秒` 39% · kicker `什么是踢脚牌` 40%). 「H2마다 직답 블록」 구조가
   구조적으로 만드는 것이라 회차 2에서는 손대지 않았다. → **회차 3에서 계량 항목에 넣을 것.**

### 회차 1이 남긴 방법론 (다음 회차가 그대로 쓸 것)

1. 🔴 **`check:drift`의 🔴 개수를 작업량으로 믿지 마라.** 회차 1에서 게이트는 4편 드리프트라 했지만
   **diff 실물로 재니 2편은 내용이 이미 와 있었다**(`masterUpdated`만 안 올라감). 프로토콜 §3-③이
   경고한 함정의 **반대 방향**이다. → A 구간에서 편마다 `git diff <masterUpdated 직전> HEAD -- lib/posts-en/<slug>.ts`.
2. 🔴 **경화의 본체는 드리프트가 아니라 «직답 블록»일 수 있다.** 회차 1은 6편 전부에 직답이 없었고
   그것이 작업량의 8할이었다. A 구간에서 `grep -c 快速回答`로 먼저 재라.
3. 🔴 **렌즈 4종은 서로 다른 것을 잡는다** — 회차 1 실증: 딜러/TD 렌즈만 TDA 14 정반대 서술을,
   교열 렌즈만 tldr 마크다운 렌더 버그를, SEO 렌즈만 직답 두 개의 상호 반박을,
   네이티브 렌즈만 `一刀切`·`注码` 오용을 잡았다. **하나라도 빼면 그 항목은 통과한다.**
4. 🔴 **2차 교열 패스가 실제로 값을 낸다** — 회차 1에서 1차 반영이 **새 결함 5건**을 만들었고
   (용어 개명 절반만·조건 과잉교정·직답만 고치고 소제목 방치·표기를 통일하려다 하나 더 늘림·근거 없는 조항 번호),
   2차 교열이 전건 잡았다. 프로토콜 §5-⑤를 건너뛰지 마라.
5. **렌즈 지적은 정본 대조 후 채택한다** — 회차 1에서 3건 기각(정본과 어긋남 1 · EN 동형 2).
   특히 표기 제안은 `translation-terms-zh.md`에 그 항목이 실제로 있는지 **grep으로 확인**하라.

---

## 7. 회차 2 마감 보고 (2026-09-07)

```
회차 2 · 족보 6편 · 커밋 dd5fd538
드리프트: 미반영 2편 → 반영 2 / 이미 반영 4편 / 편차 0
키워드: winnable 21건 흡수 · 카니발 4건 양보(踢脚牌 3파전 해소 · 牌型概率 회피) · 함정 6건 기록
렌즈 5종: 지적 51 · 반영 37 · 기각 3 · EN-먼저 11
2차 교열: 지적 6 · 반영 5 · 1건은 반영 전 자가검출로 이미 수정
§13 손검산: 6편 60여 자리 + 확률표 16칸 + 팟오즈 임계값 3개 — 전건 일치(파이썬 7장 평가기 교차검증)
거울쌍: 갈림 6편 판정 · 내 쪽 결함 0 · 상대 쪽 등재 2(hand-rankings 90% 누락 · tiebreak 08-30 미반영)
게이트: audit 🔴 0(56/56) · cjk 0 · seo-sync 0 · meta 0 · drift 족보 6/6 ✅ · 빌드 exit 0(596 페이지)
FAQ 스키마: 빌드 산출물 Question 개수 = 소스 Q 개수, 6편 전부 일치(22/11/15/20/16/15)
헤드 요청: §5-2-B EN-먼저 11건 · §5-5 TDA 원문 확보 경로 · §5-6 打公共牌 표기 판정 · §5-8 sitemap
```

**이 회차가 실제로 무엇을 했나**: 드리프트는 2편뿐이었고(tiebreak가 무거웠다 — 신설 H2 1 + FAQ 5 재작성),
작업량의 대부분은 **직답 블록 57개 신설·전환**과 **FAQ 질문 형태소의 현지화**(「谁赢」형 → 「都是X怎么比大小」계열)였다.
회차 1의 판정(「경화의 본체는 드리프트가 아니라 직답 블록일 수 있다」)이 다시 맞았다.

**가장 값진 검출**: `split-pot` 「假平分」 절의 **키커 서수 뒤집힘**(원본 유래). 승자는 맞고 서술만 틀려서
기계 검산으로는 원리상 안 잡히는 자리였고, 렌즈 3종이 독립적으로 잡았다. 같은 회차에 고친 tiebreak 一对 FAQ와
정면으로 어긋나 있었다 — **클러스터를 한 회차에 묶어서 본 것이 이 모순을 드러냈다.**

---

## 8. 회차 3 마감 보고 (2026-09-07)

```
회차 3 · 확률 7편 · 커밋 7e839eb2
드리프트: 미반영 0 / 이미 반영 6 / 해당없음 1 — 🟠 추적불가 1 → 0(card-counting masterUpdated 신설)
키워드: winnable 9건 흡수 · 카니발 2건 양보(隐含赔率 → implied-odds · 数 outs 补牌 제거) ·
        도구 의도 1건 기각(扑克胜率计算器) · 함정 5건 기록 · 「二四法则」 3파전 종결(27건 전량 교체)
렌즈 5종: 지적 61 · 반영 46 · 기각 8 · EN-먼저 7
2차 교열: 지적 15(전부 «반영이 새로 만든 것») · 반영 15
§13 손검산: 7편 19개 표 + 카드 문단 11 — 전건 일치(조합식 재유도 · 所罗门法则 4행 · EV·隐含赔率 실전 산수)
거울쌍: 갈림 6편 판정 · 🔴 **내 쪽 결함 1 수정**(outs 19.6% 소실 — 이번 B 구간 유래) · 상대 쪽 등재 0
게이트: audit 🔴 0(56/56) · cjk 0 · seo-sync 0 · meta 0 · drift 확률 7/7 ✅ · 빌드 exit 0(708 페이지 · 70 blog + 525 intl)
FAQ 스키마: 빌드 산출물 Question 개수 = 소스 Q 개수, 7편 전부 일치(16/11/11/11/12/14/8)
헤드 요청: §5-2-C EN-먼저 7건 · §5-9 인용부호 관례 판정 · §5-10 sitemap · §5-6 打公共牌(회차 2 이월)
```

**이 회차가 실제로 무엇을 했나**: **드리프트가 0이었다** — 7편 전부 EN 최신 내용이 이미 와 있었고, `implied-odds`는
「날짜만 동기, 내용 미도착」 함정(id 세션 4형)을 diff 실물 대조로 통과시켰다. 작업량의 8할은 **직답 블록 49개 신설**과
**질문형 H2 재작성**(10/56 → 49/56 = 88%), 그리고 **「二四法则」 표기 통일**이었다.
회차 1·2의 판정(「경화의 본체는 드리프트가 아니라 직답 블록일 수 있다」)이 **세 번째로** 맞았다.

**가장 값진 검출 3개**
1. **`2 和 4 法则`이 검색어가 아니었다.** 레포 19건의 지배 표기가 EN 직역이었고, 자동완성·경쟁 글 제목에 0건이었다.
   두 회차를 이월한 미결을 실측 한 번으로 끝냈다 — **지배 표기를 근거로 삼으면 안 된다**는 실증.
2. **`pot-odds` 「一步步教你」 절이 자기 글의 경고를 위반**하고 있었다(딜러 렌즈 단독). 8줄 위에서 「후속 벳이 있으면
   단일 카드로 계산하라」고 못 박고, 바로 아래 5단계 예시에서 ×4를 적용해 「跟」으로 닫았다. **D유형은 수학 렌즈를
   몇 개 돌려도 안 나온다**는 것의 재확인.
3. **거울쌍 게이트가 내 결함을 잡았다.** 「zh-hant에만 19.6%」로 뜬 갈림이 실은 **이번 B 구간이 §13 검산 수치를
   요약체로 바꾸며 날린 자리**였고, 같은 유형이 `pot-odds`에도 있었다. 상대 레인 미경화로 인한 정상 갈림 속에
   내 결함이 섞여 있었다.

🔴 **경계**: 반영이 만든 새 결함이 **15건**으로 회차 1(5) · 회차 2(6)의 두 배를 넘었다. 반영량에 비례한다 —
**2차 교열 패스를 건너뛰면 이번 회차는 15건을 그대로 배포했을 것이다.**

---

## 9. 회차 4 마감 보고 (2026-09-08)

```
회차 4 · 전략 8편 · 커밋 818c08d0
드리프트: 미반영 6 → 반영 6 / 이미 반영 2 / 순수 메타 2(소급 안 함)
키워드: winnable 11건 흡수 · 카니발 4건 양보(什么时候 3bet·什么时候弃牌·c-bet 策略·谁先行动 제거) ·
        표기 교정 3건 · 함정 5건 기록 · 「전략 축 서제스트 0」 실측
렌즈 5종: 지적 83 · 반영 52 · EN-먼저 9 · 헤드/타회차 이관 4 · 판단 유보 2
2차 교열: 지적 13(전부 «반영이 새로 만든 것») · 반영 13
§13 손검산: 핸드 9건 + 계산 29건 전건 통과(34% 세트 vs 넛플러시 = C(45,2) 990 중 341승 = 34.44% 전수 열거)
거울쌍: 8편 판정 · zh-hant만 보유 0편(수치 증발 사고 없음) · 🔴 내 쪽 결함 1(AA 만석 off-by-one, 수학 렌즈가 검출)
게이트: audit 🔴 0(56/56) · cjk 0 · hangul 0 · meta 0 · seo-sync 0 · drift 전략 5/5 ✅ · 빌드 exit 0(708 페이지)
FAQ 스키마: 빌드 산출물 Question = 소스 Q, 8편 전부 일치(15/8/10/9/10/15/14/12)
헤드 요청: §5-12 EN-먼저 9건 · §5-13~15 태그·표기 판정 · §5-16 sitemap · §5-6·9 이월(打公共牌·인용부호)
```

**이 회차가 실제로 무엇을 했나**: 드리프트는 6건뿐이었고, 작업량의 8할은 **직답 블록 68개 신설**(2 → 70)이었다.
회차 1·2·3의 판정(「경화의 본체는 드리프트가 아니라 직답 블록일 수 있다」)이 **네 번째로** 맞았다.
질문형 H2는 이미 68%라 대량 개작이 불필요했던 첫 클러스터다.

**가장 값진 검출 4개**
1. **`AA 만석 30.87%`가 상대 수 off-by-one이었다.** 「满9人桌」은 상대 **8명 = 34.5%**인데 30.87%는 상대 9명(10인) 값이다.
   zh 단독 수치라 거울쌍도 형제 글도 못 잡았고, **수학 렌즈가 MC 40만 회를 직접 돌려야 보였다.**
2. **`VPIP 25–30%`가 같은 문장의 「弃 80%」와 산수로 모순**이었다(=VPIP 20%). 형제 글 3편과도 어긋났다.
   **딜러·수학 2렌즈 독립 검출** — D유형과 수치가 겹치는 자리는 두 렌즈가 다 본다.
3. **문장파손 11곳이 전부 «콜론에서 끊는» 같은 손동작이었다**(네이티브·교열 수렴). 「这一点」·「两个字」·「就在这里」가
   가리킬 것 없는 빈 손가락으로 남았다. **기계 게이트가 원리상 못 보는 유형**이다 — 문법도 맞고 수치도 맞다.
4. **FAQ 질문이 문자 단위로 3편에 중복**돼 있었다(`扑克里什么时候该弃牌？` = strategy·when-to-fold·bubble).
   태그는 양보시켰는데 **FAQ 질문은 그대로 둬서** 같은 Question을 JSON-LD로 동시 발행하고 있었다 — SEO 렌즈 단독.

🔴 **경계**: 반영이 만든 새 결함이 **13건**으로 회차 3(15)과 같은 수준이다. 편수가 8로 가장 많은데도 비율은 낮아졌으나,
**유형이 새로웠다 — «축어중복을 지우려다 원점 복귀»가 4건**이고 그중 2건이 하이라이트 자산 복원이었다.
2차 교열을 건너뛰었으면 figcaption 오타(`牌桌上牌桌上`)까지 그대로 배포됐을 것이다.
