# SEO·애널리틱스 진단 2026-08-28

> 이전 회차: `docs/seo-report-2026-08-12.md` · 수치 추세는 `docs/analytics-log.md`(자동 누적) ·
> 원본 스냅샷은 `docs/analytics-snapshots/2026-08-26.json`
> 시각화 보고서(사장님 공유용): https://claude.ai/code/artifact/0d02921e-089c-4896-93dc-b800242200b3

**측정 창** — GA 확정분 2026-07-30~08-26(28일) vs 07-02~07-29 · GSC 07-27~08-23 vs 06-29~07-26
**출처** — GA4 Data API(529721248) · GSC API · DataForSEO SERP Live(모바일·2410·ko) · 라이브 DOM(Playwright)

---

## 0. 한 줄

참여율은 떨어지지 않았다. **화면의 59.4%는 실제로 65.8%**이고, 6.4포인트는 전부 측정 아티팩트였다.
같은 창에서 오가닉 세션은 **1,095 → 2,184로 2배**, ko 노출은 **12,731 → 26,899(+111%)**다.

---

## 1. 🔴 참여율을 읽는 법이 바뀐다 (재사용 규율)

| 단계 | 값 |
|---|---:|
| GA 화면 그대로(7/31~8/27) | 59.4% |
| ① 미처리 하루(8/27) 제외 | 62.1% |
| ② `landingPage=(not set)` 121세션 제외 | **65.8%** |
| 직전 28일을 같은 기준으로 | 69.0% |

- **① 미처리일**: 8/27은 세션 170·PV 364(세션당 2.14)인데 «참여 세션 8». PV 2장 이상이면 정의상 참여
  세션이므로 **구조적으로 불가능** = 집계 미완료. GA4 「지난 28일」 기본 창은 **항상** 이 날을 끝에 문다.
- **② `(not set)` 랜딩**: `session_start` 121 · `user_engagement` **113** · `scroll` 8 · `click` 7인데
  **`page_view`가 0건**. 10초 넘게 읽고 클릭까지 한 **사람**인데 GA가 «참여 0»으로 센다(121 중 참여 판정 3).
  유입 = google 55 · **네이버 47** · bing 11 / 한국 99(82%). 직전 창 55 → 이번 137로 **2.5배 증가 중**.
  원인은 미확정(프리렌더·인앱 브라우저 후보) → **별건**. 메모리 `ga4-pageview-loss-not-set-landing`.
- 실하락 3.2p의 정체는 **신규 유입 2배에 따른 희석**이다. 반증: **데스크톱 신규는 유입 87% 증가에도
  76.0% → 78.8%로 상승**했다. 콘텐츠 품질 저하가 아니다.

→ 이 보정 3종은 **`scripts/analytics-snapshot.mjs`에 코드로 박았다**(`npm run analytics`).
   손으로 창을 고르면 회차마다 기준이 달라져 비교가 성립하지 않는다.

## 2. 🔴 싱가포르 봇 — 번역본 통계의 46%

세션당 정확히 **1.00 PV**(사람 1.98) · **466PV 전체의 참여시간 합계 27초** · Chrome+Macintosh 단일 ·
100% `(direct)` · 데스크톱 465 : 모바일 2. 헤드리스 브라우저다.

- 번역본 PV **837 중 388(46%)** · Direct 채널 830세션 중 465(56%)
  → **Direct 참여율 22.2%는 사이트 문제가 아니다.**
- **걷어내면 번역본 PV당 체류 0:34 → 1:03**(ko 0:58). **해외 독자가 한국 독자보다 오래 읽는다.**
- 패턴: 전수 크롤이 아니라 **298페이지 · 5일 주기 · 영어 편중**(en 61페이지에 180PV = 페이지당 2.95회,
  ko는 1.13회). 재방문 상위가 `showdown-rules`(14) `split-pot-rules`(11) `strategy`(9) `game-order`(8)
  `donk-bet-strategy`(6) — **영어 신규·경화분**이다.
- 🔴 **차단 금지.** AI 답변엔진 계열일 가능성이 있어 차단하면 인용 기회를 스스로 끈다.
  Googlebot이 아니라 **순위·색인·크롤예산에 영향 0**. 정체 확인은 **Vercel Firewall 관측(UA·IP)**에서만
  가능하다 — 정적 페이지라 런타임 로그에 안 남는다.
- ⚠ **해상도 1366×1366을 지문으로 쓰지 마라.** 게이트 첫 실행에서 90개국에 걸려 오탐 90건이었다.
  판별력은 «PV/세션 ≈1.0 + PV당 체류 ≈0»이라는 **행동**에 있다(스크립트가 이 기준으로 자동 감시).

## 3. 로케일별 실 독자 (봇 제외)

| 로케일 | 실PV | 봇PV | PV당 체류 | 비고 |
|---|---:|---:|---:|---|
| zh-hant | 38 | 25 | **1:04** | 대만 30PV(1:15). **가장 건강** |
| en | 257 | 176 | **1:00** | 미국 113PV(1:18) |
| zh | 88 | 46 | 0:52 | 중국 71PV |
| id | 32 | 35 | 0:49 | 인도네시아 25PV |
| fr | 17 | 11 | 0:50 | 프랑스 14PV |
| de | 15 | 42 | 0:44 | 🟠 **독일 본토 10PV에 0:14** (스위스 3PV는 0:56) |
| ja | 24 | 1 | 0:41 | |
| **ko(대조)** | 4,788 | 80 | **0:58** | |
| vi·fil·ms·hi | 0~1 | 전량 | — | 🔴 **실독자 0 — 이 로케일 지표로 판단 금지** |

- 번역본 GSC: 노출 2,252 · 클릭 20 · **노출 페이지 49 → 132(2.6배)**. pt·zh-hant·fr·vi·tr·ru·pl이
  직전 창엔 노출 0이었다가 이번에 전부 등장. 경화분이 색인에 반영되는 중.
- **번역본에서 클릭이 나는 공식** = ⓐ10위 안에 든 자리 또는 ⓑ**우리만 원본 데이터를 가진 대회·일정**.
  `zh-hant/tournaments` **r24.6인데 CTR 10.6%**(「台灣德州撲克比賽2026」 r8.2) · `id/blog/holdem-blind-meaning` CTR 14.3%.

## 4. 「홀덤사이트」 — 판단 대기 #4 종결 + 이번 회차 조치

한국어 노출 26,899 중 **9,624(36%)**가 이 계열 두 쿼리인데 클릭은 **29건**. DFS SERP 실측으로 갈랐다.

- **「온라인 홀덤사이트」 6,654노출·클릭 2 → 목표에서 제외.** 실측 1~10위가 보드게임아레나·피망·한게임·
  유튜브·레딧 = **«접속할 게임»**. 정보 사이트로는 구조적으로 못 이긴다. 우리는 홈이 17위.
- **「홀덤사이트」 2,970노출·클릭 27 → 페이지 선택 문제.**
  홈 2,399노출 **CTR 0.4%** vs `/ranking` 1,170노출 **CTR 1.5%**. 쿼리 단위 2,970 < 페이지 단위 합 3,569 →
  차이 **599건 = 한 검색결과에 우리 두 장이 같이 뜬 횟수**(카니발 산술 증거).
- **원인**: `f1e50c11`(**2026-06-21** «homepage to community feed») 이전 홈에 «안전한 홀덤사이트를 직접
  검증해 순위를 공개합니다»·«🏆 추천 온라인 홀덤 사이트 랭킹»이 있었다. **내용은 `/ranking`으로 갔는데
  순위는 홈에 남았다.** 라이브 실측: 홈 링크 370개 중 `/ranking` **0개**, 본문 «홀덤사이트» **0회**.
- ✅ **`/ranking` 자체는 결함 없음**(라이브 확인): canonical 자기참조 · `index, follow` ·
  JSON-LD ItemList+FAQPage+BreadcrumbList 정상. 2026-08-02 canonical 사고(`components/seo.tsx` 주석)는
  이미 수리돼 있다.

**이번 회차 조치 (2026-08-28)**
1. 홈 `KO_PAGE_TEASERS`에 **`page:ranking` 카드 신설** — 홈에서 `/ranking`으로 가는 유일한 경로.
   제목 「홀덤사이트 안전하게 고르는 법」으로 앵커에 검색어를 담았다.
   산출물 검증: 홈 HTML의 `/ranking` 링크 **0 → 4개**, 「홀덤사이트」 **0 → 4회**.
2. 허브 3곳(`glossary`·`hands`·`strategy`) 앵커 **「추천 사이트」/「TOP5 홀덤 사이트」 →
   「홀덤사이트 고르는 법」/「안전 기준·먹튀 경고 신호」**.
   ⚠ 표기는 **공백 없는 「홀덤사이트」**다(노출 2,970 vs 「홀덤 사이트」 287 — 실측 지배형).
   ⚠ «추천/TOP5»를 뺀 건 SEO 때문만이 아니다 — `/ranking`이 스스로 JSON-LD에 «정보 제공 목적,
   순위·보증 아님»을 선언하는데 앵커가 보증으로 읽히면 그 선언과 어긋난다.
3. 🔴 **noindex는 쓰지 않기로 확정.** 홈 noindex는 사이트가 무너지고, `/ranking` noindex는
   **지금 나오는 클릭 18건을 스스로 끄고** CTR 0.4%짜리 홈만 남긴다 = 지금보다 나빠진다.

**다음 측정**: 앵커 변경 효과는 색인 재크롤 후에 나온다. `npm run analytics`를 주 1회 돌려
`docs/analytics-log.md`에서 홈↔`/ranking` 분배가 바뀌는지 본다. 기준선 행 = **2026-08-26**.

## 5. `/en/blog` — 초판 진단 철회

초판에 «목록 페이지가 개별 글의 자리를 빼앗는다(카니발)»고 적었는데 **틀렸다.**

- 「who bets first in texas holdem」 등에서 **개별 글은 노출 0건**이다. 경쟁 자체가 없다.
  목록이 밀어낸 게 아니라 **아무도 순위가 없어 구글이 관련 있는 목록을 8~9페이지에 끼워 넣은 것**이다.
- 목록이 잡는 43개 쿼리 순위가 **전부 38~92위**(평균 r71.7·클릭 0). **목록을 죽여도 개별 글이 안 올라간다.**
- 43개 중 **약 2/3는 우리가 안 다루는 주제**(`quarter poker`·`zeebo theorem`·`20 40 stud`·`big o` 계열).
  고칠 것이 없다.
- **진짜 항목은 하나**: 질문 변형 6종 = 51노출(`who bets first in texas holdem` 23 · `who goes first in
  poker` 13 · `in texas holdem who bets first` 7 · `who bets first in poker` 4 · `poker turn order` 2 ·
  `poker who goes first` 2). 답을 가진 `/en/blog/holdem-game-order`는 **28일 구글 노출 0건**이다.
  - title `How to Play Texas Hold'em: The Order of Play…` / seoTitle `Lost on When to Bet? —…` /
    태그엔 `who acts first in poker`가 있으나 **제목 어디에도 who bets/goes/acts first가 없다.**
  - → **처방은 목록이 아니라 game-order의 H2·FAQ에 질문형을 그대로 심는 것**(CLAUDE.md §17 미준수 자리).
- 한국어 `/blog` 목록은 「홀덤솔루션 bo_table」 하나(27노출)뿐 — 스팸성 쿼리라 조치 불요.

## 6. 상위권인데 클릭 0 — AI 개요 가설 기각

**실측한 5개 쿼리 전부 AI 개요가 없었다.** 원인은 둘이다.

| 쿼리 | 노출 | GSC 순위 | 실측 | 사정 |
|---|---:|---:|---:|---|
| 버블구간 | 215 | r3.4 | 13위 | **주식 용어**(네이버 프리미엄 «AI 섹터 역사적 버블 구간»·트레이딩뷰·나무위키 버블보블) |
| 블라인드스틸 | 409 | r5.2 | **5위** | 순위는 진짜. SERP에 **창문 블라인드·강철·기업리뷰앱 «블라인드»** 혼재 |
| 홀덤 족보 순위 | 201 | r10.6 | 13위 | 2ace·네이버블로그·한게임·피망이 1~6위 점유 |
| 킹하이·팟리밋·테이블리밋·사이드팟 | 281 | r8.8~9.9 | — | 전부 **모바일 100% · 단일 명사 · 클릭 0** |

- **패턴**: 맥락이 붙은 문장형은 팔린다(홀덤 bb 계산 32.8% · 홀덤 족보 같을 때 11.3% ·
  국내 홀덤 대회 참가 방법 41.0%). **맨 단어는 안 팔린다.**
- 🔴 **GSC 평균순위와 실측이 크게 어긋난다**(버블구간 r3.4↔13위 · 온라인홀덤사이트 r6.2↔17위).
  단일 명사 키워드는 **채택 전 SERP 실측 필수**. 위 6종 **905노출은 개선 여지가 아니라 잡음**이니
  추적에서 뺀다.

## 7. 장단점

**강점** — `/tournaments` 411세션·77.9%·1:53 / 「국내 홀덤 대회 참가 방법」 1위 **CTR 41.0%** /
`/pub` 87.5%·3.08PV / `/strategy` 6:08 / `/hand-chart` 5:10 /
`texas-holdem-rules-for-beginners`(en 4:25, zh·ja에서도 각 언어 1위 = 가장 널리 읽히는 단일 글).

**약점** — `/ranking` 참여율 73.1%인데 체류 **0:23** / `/blog/holdem-blind-meaning` **42.1%**
(세션 50+ 랜딩 중 최저, 「홀덤 bb 계산」 CTR 32.8%로 들어와서 이탈) / `/calculator` 1:10·1.40PV /
`/en/blog/holdem-tiebreak-rules` 45.0%·0:38 / 이미지 검색 1,598노출·5클릭 / 독일 실독자 0:14.

**요약** — **«언제·어디서·얼마»는 전부 이기고, «무엇이다»는 노출만 쌓인다.** 다국어도 같다 —
zh-hant에서 유일하게 클릭이 나는 자리가 **대만 대회 일정**이다.

## 8. 미해결로 넘기는 것

- `page_view` 유실 121세션의 원인 규명(별건)
- en `holdem-game-order`에 질문형 심기(§17)
- 독일어 도입부 점검(표본 10PV라 확정 아님)
- 🟠 **GA4 속성 시간대 확인 필요** — 오가닉 피크가 0~9시, 저점이 12~16시로 뒤집혀 있다.
  미 태평양시 가정 시 한국시간 오후 4시~새벽 1시로 자연스러워진다. **확인 전까지 시간대별 리포트를
  판단 근거로 쓰지 마라.**
