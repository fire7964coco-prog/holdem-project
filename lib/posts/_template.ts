import type { Post } from "../posts";

/* ══════════════════════════════════════════════════════════════════════════
   신규 한국어 포스트 템플릿 — 현행 경화 규격(§14-A) 기준
   최종 갱신 2026-08-02

   ⚠ 이 파일은 **새 글의 출발점**이다. 여기가 낡으면 그 결함이 새 글마다 복제된다.
      실제로 2026-08-02까지 이 템플릿의 FAQ가 구형(`### Q1.`)이었던 탓에
      **7편·43문항이 소스에는 있는데 FAQPage 스키마는 0**이었다.
      규격이 바뀌면 이 파일부터 고칠 것.

   착수 전 필독: `.cursor/rules/posting.mdc` → `REVIEW-PROTOCOL.md`
   발행 전 필수: `npm run audit:hard -- --slug=<slug>` 🔴 0건 → `npm run build`
   ══════════════════════════════════════════════════════════════════════════ */

export const POST: Post = {
  slug: "슬러그-여기-입력",                    // 발행 후 절대 변경 금지(SEO 초기화)
  title: "제목 — 40자 이내",
  // seoTitle = [호기심·자극 훅] — [핵심 키워드 2~3개] · **~32자**(한국어 SERP 잘림 기준)
  // ❌ "OO 뜻 정리"·"완벽 정리"·"총정리" 같은 정보형/흔한 마무리
  // ✅ "매판마다 칩이 빠지는 이유 — 홀덤 블라인드 뜻과 SB·BB 차이"
  seoTitle: "호기심 훅 — 핵심 키워드",
  // desc = [공감/호기심 한 문장]. [무엇을 알려주는지 + 키워드] · **~95자**
  desc: "공감 한 문장. 무엇을 알려주는지와 핵심 키워드를 담은 두 번째 문장.",
  // tldr = 상단 💡 박스. 1문장 강박 금지 — 2~3줄 자기완결 직답 단락으로.
  tldr: "검색자의 질문에 그 자리에서 답하는 2~3줄. 앞뒤 맥락 없이도 성립해야 한다.",
  category: "전략",  // "전략" | "초보 가이드" | "토너먼트" | "심리전" | "사이트 리뷰" | "확률·수학"
  date: "2026-08-02",
  updated: "2026-08-02",        // 손댈 때마다 오늘 날짜로. sitemap lastmod에 반영된다
  readTime: "10분",
  emoji: "♠️",                  // 앞뒤 공백 금지
  image: "/images/슬러그-hero.webp",           // 1200×675 · webp · ≤60KB
  imageAlt: "구체적 상황 묘사 — 키워드 나열 금지",
  keepImagesInBody: true,       // 반드시 true (히어로를 본문 상단에 유지)
  tags: ["메인 키워드", "유사 키워드", "롱테일 키워드"],   // 5~8개 · 형제 글과 겹치면 카니발
  content: `
![구체적 상황 묘사 alt](/images/슬러그-hero.webp "이미지 캡션")

도입부 — **1인칭 경험담 2~3줄**. E-E-A-T 필수 항목이라 이게 없으면 발행하지 않는다.
없는 사실·가짜 수치·가짜 이름은 금지. 진짜 관찰만.

---

### 이 글 핵심 요약

:::stripe
라벨 | 값 또는 한 줄 요약
라벨 | 값 또는 한 줄 요약
라벨 | 값 또는 한 줄 요약
라벨 | 값 또는 한 줄 요약
:::

> **바로 답**
> 검색자의 질문에 **2~4줄 자기완결 단락**으로 직답한다. 구글 AI 답변처럼 쓴다.
> 앞 문단을 안 읽어도 성립해야 인용된다.

⚠ 배치 순서 고정(§14-A 6): 히어로 → 도입부 → :::stripe → **바로 답** → H2들 → :::readnext → FAQ → (출처) → 마무리
   stripe와 바로 답의 순서가 뒤바뀌면 게이트 F10이 잡는다.

---

## 검색자가 실제로 치는 질문 문구인가요?

**첫 문장에 직답을 놓는다(40~75단어).** 그 뒤에 근거·경험담을 붙인다(Q-A-E).
질문형 H2가 **콘텐츠 H2의 70% 이상**이어야 게이트 F2를 통과한다.
비교·수치는 산문으로 나열하지 말고 표로 — 표가 산문보다 압도적으로 많이 인용된다.

| 항목 | 캐시 | 토너 |
|------|------|------|
| 예시1 | 값 | 값 |
| 예시2 | 값 | 값 |

---

## 두 번째 질문형 H2인가요?

직답 → 근거 → 경험담. 본문 이미지는 **긴 텍스트를 끊어 완독시키는 가독성 장치**다.
클러스터 형제 글과 같은 사진을 본문에 재사용하지 말 것(readnext 카드 썸네일은 예외).

---

:::readnext[이어서 읽기]
/blog/형제글-슬러그 | 카드에 보일 제목 | /images/형제글-썸네일.webp
/blog/형제글-슬러그2 | 카드에 보일 제목 | /images/형제글2-썸네일.webp
:::

## FAQ — 자주 묻는 질문

<!-- 🔴 형식 고정: "**Q. 질문**" + **빈 줄** + "A. 답변". 셋 중 하나라도 어긋나면
     FAQPage 스키마에 안 잡히고 스타일드 카드도 안 그려진다.
     ❌ "### Q1. 질문"(구형) — 이 템플릿이 구형이었던 탓에 2026-08-02 기준
        7편·43문항이 소스에는 있는데 스키마는 0이었다. 소스만 보면 멀쩡해 보여 오래 안 잡혔다.
     ✅ 확인은 소스가 아니라 **빌드 산출물**로:
        .next/server/app/blog/<slug>.html 의 "@type":"Question" 개수 -->

**Q. 검색자가 실제로 치는 질문 문장인가요?**

A. 답변 2~4줄. 자기완결로 쓴다(앞 문단을 안 읽어도 성립해야 인용된다).

**Q. 두 번째 질문인가요?**

A. 답변.

---

## 마무리

마무리 문장 — 내부링크 3개 이상.

| 주제 | 연결 내용 |
|------|----------|
| [관련글1](/blog/슬러그) | 설명 |
| [관련글2](/blog/슬러그) | 설명 |
| [관련글3](/blog/슬러그) | 설명 |
`.trim(),
};

/* ──────────────────────────────────────────────────────────────────────────
   발행 전 체크리스트

   🔴 절대 금지
   - 본문에 백틱(\`) — content가 템플릿 리터럴이라 그 자리에서 빌드가 깨진다(§12-A).
     코드·계산식은 **굵게** 또는 ==하이라이트==로.
   - 발행 후 slug 변경 / URL 끝 슬래시 / .jpg·.png 이미지
   - 경험담 없는 AI 백과사전식 본문

   ✅ 발행 절차
   1. `lib/posts/index.ts` 의 NEW_POSTS 배열에 import 추가
   2. `app/blog/roadmap/blog-roadmap-client.tsx` 에 published: true 등록
   3. 이미지: 1200×675 webp ≤60KB(복잡한 인포그래픽 ≤80KB), 히어로 priority
   4. 내부링크 3개 이상 + 기존 관련 글에서 이 글로 오는 역링크도 추가
   5. **`npm run audit:hard -- --slug=<slug>` 🔴 0건**
      — §13 핸드 검산·메타 길이·질문형 H2·FAQ 스키마·깨진 링크·백틱·축어 중복·형제 표 대조를 대신 본다
   6. `npm run audit:hard -- --uncovered --slug=<slug>` — 기계가 판정 못 한 카드 문단은 손으로 검산
   7. 적대적 렌즈(Agent 병렬) — 게이트가 못 잡는 D유형(유해 조언)·F유형(사실 오류)
   8. `npm run build` 성공 + sitemap post 수 확인
   9. `git commit -F commit-msg.txt` + push → WORKLOG.md · session-handoff.md 갱신
   ────────────────────────────────────────────────────────────────────────── */
