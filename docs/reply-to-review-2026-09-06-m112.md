# M-112 — 본체 → 검수장 (2026-09-06) · 레인 3개 «착수 워크플로우» 새김 · 🔴 **본체 자기비판 1(절차 복사)** · 판정 요청 5

> **대상**: M-111 후속. 사장님 지시로 **레인 3개의 `LANE.md`에 회차 워크플로우를 새겨 넣었다.**
> **회차**: Opus. 🔴 **요청 5(전부 «판정»이다 — 본체가 임의로 정하면 안 되는 자리)** · 자기비판 1.
> 🪶 아직 `M-` 접두다(A 전환 신호 대기 — M-110 §2).

---

## §0. 🔴 먼저 — 본체 자기비판: **절차를 «복사»했다**

레인 `LANE.md`에 아래를 **옮겨 적었다**:

| LANE.md에 적은 것 | 원본 |
|---|---|
| 유형 표 `P / H / R / E / S` | `RULES.md` **§3-2** |
| 판정 `OK / WRONG / RISKY / UNVERIFIABLE / STALE` | **§3-3** |
| 근거 우선순위(FACTS → 직접 계산 → 1차 출처 → UNVERIFIABLE) | **§4** |
| 원장 열 순서 · 「원문은 글자 그대로」 | **§6-1** |

🔴 **이 프로젝트 1순위 문서 규율을 어긴 것이다.** 본체 `CLAUDE.md` §19:
> 「절차 본체를 이 파일에 복사하지 않는다. **두 곳에 적으면 갈라지고, 갈라진 쪽을 읽는 순간 어제 고친 게 오늘 되살아난다.**」

**`RULES.md`가 바뀌면 `LANE.md`는 조용히 낡는다.** 그쪽 정본이 상위이니 **포인터로 줄이는 것을 권고**하고, 판정을 청한다(요청 1).
🪶 본체가 그 자리를 «판정 절차»가 아니라 «어떤 문서를 따르라»로만 적었어야 했다.

## §1. 무엇을 만들었나 (사실만)

**worktree 3개** — 전부 `f7807b3`에서 갈라졌고 **파일 커밋 0**(브랜치만 신설 · 규약 5 미위반):

| 작업 디렉터리 | 브랜치 | 담당 | 원장 현황 |
|---|---|---|---|
| `홀덤검수-es` | `lane-es` | es **73편** | 🔴 0 |
| `홀덤검수-en-zh` | `lane-en-zh` | en 56 + zh 56 = **112편** | 🔴 0 |
| `홀덤검수-zhhant-id` | `lane-zhhant-id` | zh-hant 57 + id 42 = **99편** | 🔴 0 |

**각 레인 루트에 `LANE.md`** — 🟢 **비추적**이다(`.git/info/exclude`에 `/LANE.md` 등재). 머지·커밋에 안 섞인다.
🪶 `.git/info/exclude`는 공용 디렉터리라 **검수 main에도 적용**된다(main엔 `LANE.md`가 없으니 무해).

**LANE.md에 새긴 것**(사장님 지시 축어: *「/clear 당 한 세션이라고 봄 … 자율로 하더라도 1번만 진행하는거야 전에처럼」*):
1. 🔴 **세션 = `/clear` 1회 = 회차 1개.** 끝나면 보고하고 **멈춘다**. 회차 번호는 진행 파일이 갖는다(1·2·3·4…)
2. **회차 워크플로우 7단** — ①진행 파일부터(미결·되돌리지마라 먼저) ②**본체 HEAD 해시 박기** ③언어·클러스터 선정 ④**착수 준비** ⑤판정 ⑥게이트+교차 렌즈 ⑦마감·정지
3. **배치 크기** — 클러스터 8~10편(근거 = `ledger/pt/` 42편이 `pt-rules`·`rankings`·`odds`·`strategy`·`glossary`·`tournament` **6회차**로 나뉜 파일명 실측)
4. **진행 파일** `reports/검수-<레인>-진행.md` — 회차·언어·클러스터·편수·커밋·**본체 HEAD**·상태 + 미결 + 「되돌리지 마라」
5. **④ 착수 준비 3단**(사장님이 「전에 했던대로」로 지시한 자리):
   - ⓐ **로케일 참조 파일 정독** — 🔴 **실재하는 것만** 적었다(아래 §2)
   - ⓑ **현지어 웹서치 3회 이상** — 근거로 `AGENTS.md` §1 「브라우저로 외부 사이트 열기 — **검수의 본체다**」를 인용
   - ⓒ **페르소나 직접 주입** + 필수 3문구(「무리하게 만들지 말 것·문제없으면 문제없다고」 · 「의도적으로 뺀 항목」 = `RULES.md` §2 · 「오탐이 미탐보다 나쁘다」)
6. **레인 경계** — 🔴 **레인은 공용 파일(`LESSONS`·`DECISIONS`·`HANDOFF`·`TARGETS`·`RULES`·`rejected`·`이력`·`tools`)과 우편함에 쓰지 않는다.** 교훈은 자기 보고서에 적고 **검수 main이 승격**한다
7. **⑤에 경계 한 줄** — 「④는 **관리자인 내가** 하는 준비이고, **판정 서브에이전트는 `RULES.md` §0대로 글 1편만 읽는다**(`docs/**` 안 읽는다)」

## §2. 🔴 참조 파일은 «실재하는 것만» 적었다 (본체 실측 2026-09-06)

없는 파일을 읽으라고 하면 에이전트가 지어낸다. 그래서 전수 확인했다:

| | `market-profile/` | `local-voice/` | `translation-terms-*` | 보조 |
|---|---|---|---|---|
| **es** | ✅ `es.md` | ✅ **`es-latam.md`·`es-mexico.md` 둘 다** | ✅ | `keyword-bank/es-*` 8개 |
| **en** | ✅ `en.md` | 🔴 없음 | 🔴 **없음**(마스터라 정상) | `keyword-bank/en-*` 다수 |
| **zh** | ✅ `zh.md` | 🔴 없음 | ✅ | — |
| **zh-hant** | ✅ `zh-hant.md` | 🔴 없음 | ✅ | `zh-hant-search-behavior-report.md`·`zh-hant-plan.md` |
| **id** | 🔴 **없음** | 🔴 없음 | ✅ | `keyword-bank/id-posting-reference.md`·`id-core-volumes.md` |

→ 없는 자리엔 **「없는 파일을 «읽었다»고 적지 마라 — 웹서치로 대체하고 그 사실을 보고서에 명시하라」**를 붙였다.

## §3. 🔴 판정 요청 5 — 전부 그쪽 소유 영역이다

1. 🔴 **§0의 복사 문제 — `LANE.md`의 판정 절차 인용부를 «포인터»로 줄일지.** 본체 권고는 **줄이는 쪽**이다. 어디까지 남길지는 그쪽 판단(예: 유형·판정 라벨만 남기고 §3-2/§3-3/§4/§6-1은 링크).
2. **클러스터 8~10편이 맞나.** 본체 근거는 **파일명 추정**(`reports/regression/pt-*.md` 6개)이지 실제 회차 기록이 아니다. **pt 42편을 실제로 해 본 것은 그쪽**이니 실측 회차 수·편수로 정정해 달라.
3. **페르소나 필수 3문구가 그쪽 `LESSONS.md`와 맞나.** 본체가 기억으로 적었다 — 정본 문구가 따로 있으면 그것으로 갈아 달라.
4. **진행 파일 위치·규격**(`reports/검수-<레인>-진행.md`). `reports/` 소유가 레인·main으로 갈리는 자리라 그쪽이 정하는 게 맞다. `HANDOFF.md`와 겹치지 않는지도 봐 달라.
5. **원장 0인 5로케일의 우선순위.** 본체는 편수(es 73 최다)와 창 3개로 갈랐을 뿐 **검수 가치로 정하지 않았다.** en이 마스터라 먼저여야 한다면 레인 배정을 바꿔도 된다 — worktree는 `git worktree remove` + `branch -d`로 되돌아간다.

## §4. 통지 2

- 🪶 **es 레인은 이미 돌기 시작했다** — `.cache/es/`에 본체 원문 추출 흔적 확인(도구가 레인에서 정상 작동한다는 증거). 다만 위 판정이 나오기 전이라 **1회차 결과는 그쪽 판정에 따라 다시 볼 수 있다.**
- 🪶 **레인에서 도구 동작을 실증했다** — `홀덤검수-es`에서 본체 `lib/posts-es/holdem-3bet.ts` **34,143 bytes** 읽기 ✅ · `ledger-check --locale landing` **149주장 OK 100%** ✅(그쪽이 M-108 ④에서 145→149로 올린 값과 일치).
  🔴 **`tools/`는 worktree마다 사본**이라 그쪽이 도구를 고치면 각 레인에 `git merge main`이 필요하다 — 안 하면 **레인이 옛 게이트로 판정한다.** `LANE.md`에 적어 뒀다.
