# 세션 인수인계 (Session Handoff)

> **사용법**: 작업 마칠 때 이 파일 업데이트 → 다음 세션 시작 시 AI가 읽고 바로 이어받음
> 작업 시작 전 `CLAUDE.md` → `session-handoff.md` → `WORKLOG.md` 순서로 읽을 것.

---

## ▶▶▶▶▶▶▶▶▶ 새 세션 START HERE

> **현재 상태 (2026-08-09 마감)** · build **69 blog + 457 intl** (총계 불변) · GTO 솔버 ✅ 13/13 완결
> 오늘 배포 7건: `3ae4178`(다국어 B1) · `c88d567`(APT 인천) · `8474ee8`(KO 잔여 S1) ·
> `e077492`+`4249165`(**S2** 포스트플랍 6편 39건) · `93e16a6`(**S3** 프리플랍 8편 38건) ·
> `242c92a`(**S4** 계산·룰 13편 29건)
> → **KO 잔여 수정은 S5만 남았고, S5는 8/16 이후다.** 지금 할 수 있는 건 ② 다국어 B2다.

### ▶▶ 다음 세션은 여기서 시작 — **다국어 동기화 B2**

```
1. git pull
2. docs/multilang-sync-plan.md §0-D 「S2-B1 적용 완료」 표를 먼저 읽는다 (교훈이 거기 있다)
3. B2 = C7(hand-rankings 케이스 킹 반증) · C8(icm 3bb 버튼 이유) ·
        C10(starting-hands-chart 차트 평균 주장) · C11(texas-…-for-beginners 하이카드 설명)
   값 치환이 아니라 **문장 수정**이다. 미판정 3항목(C5·C6·C9)도 같이 판정한다.
4. 항목마다 전 로케일 원문을 다시 훑는다 → 편집 → audit:hard --locale=<code> → build → 커밋
5. C7을 끝내면 hand-rankings의 masterUpdated를 2026-07-19로 올린다(일부러 보류해 둔 것)
```

🔴 **S1 판정표를 믿지 마라.** B1에서 판정표가 **7건을 놓친 것**을 원문 대조로 잡았다(아래 ② 참조).
🔴 **한글·다국어 파일을 PowerShell로 치환하지 마라** (CLAUDE.md §16). Node `.mjs` + `readFileSync(p,"utf8")`.
🔴 **미러 8종 소급은 아직 하지 마라** — KO 재판정 2차가 수렴한 뒤 워크플로우 8번에서 일괄.

#### S4에서 새로 배운 것 (KO 수정 트랙 전체에 유효)

🔴 **`19.6%`의 절반은 «맞는 값»이었다.** `outs-calculation`의 「Rule of 2 and 4 정확도 검증」 표는
   열 제목이 **"Rule of 2 (턴)"**이라 19.6 · 8.7 · 17.4 · 32.6이 전부 정답이다. 고쳤으면 멀쩡한 표를
   망가뜨렸다. 고친 건 **「플랍」이 명시된 문단뿐**이다 → 47분모(9아웃 19.15% · 8아웃 17.02% · 4아웃 8.51%).
🔴 **수정이 새 오류를 만들었다 — S2·S3·S4 3연속.** split-pot에서 「키커가 A·K인 포카드 보드」라고 썼는데
   K 키커가 안전한 건 **A 네 장이 다 깔린 A-A-A-A-K뿐**이다(9-9-9-9-K는 A를 든 쪽이 이긴다).
   되읽기 패스에서 잡았다. **편집한 문단은 반드시 다시 읽어라.**
🔴 **원장 수치를 그대로 본문에 옮기지 마라.** ak-offsuit 에퀴티를 전수 열거(1,712,304 보드 × 13페어)로
   재계산했더니 원장과 달랐다 — AKo vs 22 = **47.35%**(원장 47.0) · AKs−AKo 격차 **2.73~5.61%p**(원장 3.1~4.7).
   §2-A의 「콤보 수를 베끼지 마라」가 에퀴티에도 그대로 적용된다. 검수장 재판정 요망(리포트 §2).
🔴 **전체 게이트가 초록이어도 «경화 목록 밖 글»은 검사조차 안 된다.** **편집한 글은 `--slug`를 따로 돌려라.**
🔴 **한 항목이 글 전체에 퍼져 있을 수 있다** → grep 전수 검색 후 착수. **자체 검수는 하지 마라**(재판정은 검수장 몫).

---

## ▶ 지금 굴러가는 트랙이 둘이다 (헷갈리지 말 것)

| 트랙 | 지시 문서 | 남은 것 | 우선순위 |
|---|---|---|---|
| ① KO 잔여 수정 (검수장 인계) | `docs/kr-remediation-spec.md` | **S5뿐 — 8/16 이후** | 대기(막힘) |
| **② 다국어 동기화** | **`docs/multilang-sync-plan.md`** | B2 · B3 · B4 | **지금 이것** |

두 트랙은 **같은 파일을 안 건드린다** — ①은 KO만, ②는 로케일만. 다만 ①이 끝나야 ②의 «미러 소급»(워크플로우 8번)을 할 수 있다.

---

## ① KO 잔여 수정 — 153건을 5세션으로 (S1~S4 완료 · **142건 처리 · 남은 건 S5 11건뿐**)

| 세션 | 묶음 | 편 | 건 | 상태 |
|---|---|---:|---:|---|
| S1 | 레인지 % | 5 | 36 | ✅ `8474ee8` (34건 편집 · 2건 기적용) |
| S2 | 포스트플랍 — position · check-raise · cbet · overbet · value-bet-sizing · bluffing | 6 | 39 | ✅ `e077492`·`4249165` (37건 편집 · 1건 기적용 · STALE 1건 선제처리) |
| S3 | 프리플랍·사이징 — 3bet · raise-how-much · blind-steal · pocket-aces · pocket-kings · bankroll · button-position · small-blind-role | 8 | 38 | ✅ `93e16a6` (38건 전부 편집 · 기적용 0건) |
| S4 | 계산·룰 정의 13편 — hand-rankings · confusing · rules · pot-odds · outs · ak-offsuit · blind-meaning · check-meaning · game-order · probability · split-pot · tiebreak · vs-7poker | 13 | 29 | ✅ `242c92a` (29건 전부 편집 · 기적용 0건) |
| **S5** | tournament-how-to-enter | 1 | 11 | **8/16 이후** — STALE #19가 APT 종료로 만료 |

- **분할 축은 «파일»이다.** A층(RISKY)과 B층(유해조언)이 같은 문장을 지적한 중복이 최소 6건이라, 따로 돌리면 같은 문장을 두 번 열게 되고 두 번째 편집이 첫 번째를 깨뜨린다.
- 표준 문안(키커 · 레인지% 절차 · 헤즈업 단서 · **분모 규칙**)은 전부 `kr-remediation-spec.md` §2에 있다.
- 🔴 **`19.6%`를 일괄 치환하지 마라** — 턴 국면에서는 맞는 값이다(플랍 9/47=19.15% vs 턴 9/46=19.6%). S4에서 문단마다 국면 확인.
- 🔴 **원장 콤보 수를 베끼지 마라** — S1에서 `starting-hand-range` #14의 「70콤보」가 낡은 값이었다(현재 Tier 1 = 40콤보).

---

## ② 다국어 동기화 — B2부터

```
docs/multilang-sync-plan.md §0-D 「S2-B1 적용 완료」 표를 먼저 읽어라 (교훈이 거기 있다)
B2 = C7 · C8 · C10 · C11 — 값 치환이 아니라 문장 수정이다
항목마다 전 로케일 원문을 다시 훑는다 → 편집 → audit:hard --locale=<code> → build → 커밋
```

### 🔴 B1이 남긴 가장 중요한 교훈 — **S1 판정표를 믿지 마라**

B1에서 **판정표가 7건을 놓친 것을 원문 대조로 잡아냈다.**
`es`의 `+Con kicker` · `zh`의 `+用踢脚` · `ja`의 `80〜86%`(물결표가 `–`가 아니다) —
전부 **S1 스캔 정규식이 라틴 표기에 치우쳐** 흘린 것이다.
E2는 6→**10개**, E3는 3→**5개**, C4는 「ja엔 해당 문장 없음」이라 적혀 있었는데 **ja에 2곳 있었다.**

→ **B2·B3도 표의 로케일 목록을 그대로 쓰지 말고, 항목마다 전 로케일 블록을 통째로 뽑아 눈으로 비교하라.**
   방법: 스크래치패드에 `dump-block.mjs`류(모든 `lib/posts-*/`에서 같은 블록을 출력) 20줄이면 된다.

### 착수 전 반드시 알 것

1. 🔴 **한글·다국어 파일을 PowerShell로 치환하지 마라** (CLAUDE.md §16).
   Node `.mjs`에서 `readFileSync(p, "utf8")`. B1은 드라이런 → 건수 검증(기대치 불일치면 중단) → `--apply` 순서로 했다.
2. 🔴 **E1(사이드팟)은 값 교체가 아니라 «문장 재작성»이다** — 22개 언어로 한 문장씩 써야 한다. **별도 세션.**
3. **한국어 원본은 B1 7항목 전부 정상이었다.** B2도 KO를 먼저 확인하고 들어갈 것(소급 대상인지 아닌지가 갈린다).
4. **`masterUpdated` 보류 규칙** — `hand-rankings`(C7 대기)·`all-in-rules`(E1 대기)는 일부러 안 올렸다.
   B2에서 C7을 끝내면 `hand-rankings`를 `2026-07-19`로, B3에서 E1을 끝내면 `all-in-rules`를 `2026-07-25`로 올려라.

### 남은 작업

| 배치 | 항목 | 파일 | 성격 |
|---|---|---:|---|
| **B2** | C7 케이스 킹 반증(`hand-rankings`) · C8 3bb 버튼 이유(`icm`) · C10 차트 평균 주장(`starting-hands-chart`) · C11 하이카드 설명(`texas-holdem-rules-for-beginners`) | ~19 | 문장 수정 |
| **B3(별도)** | E1 사이드팟 「× 콜러 수」 → 「각 콜러가 맞춘 만큼」 | 22 | 문장 재작성 |
| **B4** | 재발 방지 — 드리프트 감지를 `scripts/`에 넣고 `audit:hard`에 연결 | — | 기계화 |

### 아직 판정 안 끝난 3항목 (B2 하면서 같이)

`C5`(머킹 공개 요청권한) · `C6`(핸드포핸드 동시탈락) · `C9`(보드 플러시 개선 조건) —
셋 다 **`de`는 오류 확인**됐고 나머지 로케일만 미확인이다.

### B1에서 함께 고친 것 — 게이트 오탐 2종 (자가테스트 76/76)

- 구성 설명 가드가 **라틴 연결어만** 봐서 ms `bersama sepasang`·hi `और एक pair`를 놓쳤다
- 🔴 **프랑스어 족보명이 하나도 등록돼 있지 않아 `fr` 6편이 «원리상 미검사»였다**
  (`Quinte Flush`가 `flush`에 걸려 로열·스트레이트 플러시를 전부 「플러시」로 오독)
  → 다른 소수 언어도 같은 상태일 수 있다. **커버리지 출력의 「시나리오를 못 잡은 글」을 언어별로 확인할 것.**

### 한 줄 요약

EN 마스터에서 **「치명 3 + 정확성 8 + 하모나이징 11 + 내부 불일치 12」**를 고쳤고 커밋 메시지에
**"(전 언어 소급 예정)"**이라고 적어 뒀는데 **그 소급이 안 됐다.**
**드리프트 104편 · 추적 불가(masterUpdated 필드 없음) 123편.**

| 슬러그 | 뒤처진 언어 | 성격 |
|---|---:|---|
| `holdem-all-in-rules` | **20** | 🔴 **계산 오류 3건**(사이드팟 「콜러 수만큼 곱」→「각 콜러가 맞춘 만큼」 · 누적 가주표 $25→$24 · 오버사이즈드 칩룰) |
| `holdem-showdown-rules` | 20 | 정확성·경험담 |
| `holdem-blind-meaning` / `holdem-game-order` / `texas-holdem-rules-for-beginners` | 19씩 | 정확성 |
| `apt-incheon-2026-guide` | 7 | 대회 결과(8/16 종료 후 묶어서) |

🔴 **이건 SEO가 아니라 정확성 작업이다.** 번역본 노출은 de 278 · ja 40 · id 20 · es 19이고
**전 언어 클릭 합계가 2**다. 85편을 고쳐도 트래픽은 안 움직인다.
고치는 이유는 **틀린 계산이 20개 언어로 배포돼 있고 우리가 그걸 안다**는 것뿐이다.

⚠ 방법은 **재번역이 아니라 «값 패치»**다 — 숫자·계산·룰만 교체하고 문장·훅·H2는 건드리지 않는다.
상세·금지사항·검증 명령은 전부 `docs/multilang-sync-plan.md`에 있다.

### 함께 정리된 것 (2026-08-09)

- ✅ **「홀덤 gto 표」 처리 완료** — 조사 중 **「홀덤 차트」 480**(12개월 +121%, 미공략)을 발견해
  `/hand-chart`(제목이 볼륨 0 키워드였다)와 `/solver`에 역할 분리로 배분. 커밋 `eab1766`.
  근거 없던 「GTO」 주장은 제거했다(우리 솔버 SB 46.6% vs 차트 56%). 실측은 키워드 뱅크 §3-C.
- ✅ 솔버 URL 13줄 전달 완료 · 솔버 쪽 [해설 보기] 역링크로 내부링크 순환 완성
- ⏸ **펍 지역 페이지**(`/pub/*` 9개 색인 탈락)는 **사장님 지시로 보류**
- **「gto wizard」 월 4,400**은 여전히 브랜드 키워드라 직접 조준은 사장님 판단 필요(변동 없음)

---

## ▶▶ ★시리즈에서 확립한 방법 — 다음에도 쓸 것

1. 🔴 **레인지 전수 검산 (이번에 확립, 가장 값이 컸다).**
   `.solver-captures/<key>-oop.png`·`-ip.png`의 13×13 매트릭스를 텍스트로 옮겨 보드 블로커를 반영해
   콤보를 직접 세면 **솔버 출력과 소수점까지 일치한다.** ⑫에서 22개 값, ⑬에서 11개 값이 전부 맞았고
   같은 레인지로 ⑪ 보드를 넣으면 538/525가 재현된다. **내 전사 누락(J5s) 1건을 3콤보 차이로 잡아냈다.**
   → 지난 핸드오프의 「⑫⑬은 넓으니 콤보 분해를 시도하지 마라」는 **틀린 조언이었다.** 분해된다.
2. 🔴 **형제 글 인용은 `spec §4-B` 표에서만 가져온다.** 카드·무늬까지 원문 복사.
   ★이번에 새로 배운 것: **기존 글을 고치면 그 글을 인용한 새 글이 낡는다.**
   ⑪에서 「6-5-4처럼」을 지웠더니 ⑫의 축어 인용이 원문과 어긋났다(SEO·교열 렌즈가 동시에 잡음).
3. 🔴 **2차 교열 패스를 절대 건너뛰지 마라.** 1차 지적을 반영하다가
   「탑 페어가 오버 페어를 이긴다」는 **족보 오류를 새로 만들었다**(탑 페어 7은 오버 페어에게 진다).
   수정은 그 자체가 결함 주입 공정이다.
4. **적대검수는 렌즈를 갈라야 값이 나온다.** 이번에도 기계 게이트는 처음부터 🔴 0건이었는데
   4렌즈가 **사실 오류 2건 + 용어 오류 1건 + 유해 조언 4건**을 잡았다.
   한 렌즈만 잡은 항목: 실전=체크레이즈 오용 / 검산=「A를 든 조합」 라벨 / 교열=인용 낡음.
5. ⚠ **PowerShell로 한글 문서를 일괄 치환하지 마라.** 문서 편집은 Edit 도구. CLAUDE.md §16.

---

## ▶ 백로그 — 두 트랙 밖에서 아직 안 끝난 것

> 이전 핸드오프 「🚀 새 세션은 이 순서로」 표에 묻혀 있던 **미완 항목만** 건져 올렸다
> (완료·오해로 판명된 항목은 아카이브로 내렸다). 급하진 않으나 잊으면 안 되는 것들이다.

| # | 할 일 | 메모 |
|---:|---|---|
| 1 | **`/win-rate-quiz` 본문 0줄** | 도구는 완성(6인→실전모드→검수→모바일). **남은 건 본문·FAQ 스키마뿐.** `priority 0.8`로 색인 요청 중인데 h1+부제만 있다 |
| 2 | 프로덕션 E2E 마감 | 검수 세션에 `BASE=https://www.holdemmaster.com`으로 요청 |
| 3 | 다국어 정책 페이지 | `/privacy`·`/terms`·`/contact`가 한국어뿐인데 링크는 영어 라벨로 걸려 있다 |
| 4 | `/solver` desc에 「프로그램」·「계산기」 | 두 단어가 메타에 없다(「무료」는 이미 있음). 볼륨 140·20. **글 안 쓰고 160자 안에서 끝나는 개선** |
| 5 | 리버 승부 해설 | 복기 표에 승률만 있다. "키커로 갈렸다"를 `score5`의 키커로 자동 생성 가능 |
| 6 | `es` 국기 판단 | **사장님 결정 필요** |
| — | ⏸ 펍 지역 페이지(`/pub/*` 9개 색인 탈락) | **사장님 지시로 보류** — 건드리지 말 것 |

### ⏰ 시한이 박힌 것

`docs/update-calendar.md`가 본체다. 현재 최대 3건 —
**8/16 APT 인천 종료**(→ S5 착수 가능 + 결과 아카이브 전환 + 미러 7종) ·
**10월 초 JOPT 오사카 바이인 공개**(글에 약속함) · **12/31 K-ETA 면제 만료**(한국 관련 전 언어 영향).

---

## ▶ 상시 함정 (트랙 무관 · 매 세션 유효)

🔴 **CLAUDE.md §8의 KO 필라 슬러그 목록이 실제 파일과 다르다.** 링크 적을 땐 반드시 grep하라.
   `holdem-pot-odds`(X) → **`holdem-pot-odds-calculation`** ·
   `holdem-position-play`(X, EN·JA·ES 전용) → **`position-is-everything-in-holdem`**
   2026-08-05에 이것 때문에 두 번 404 직전까지 갔다. (전체 대조표는 CLAUDE.md §8 안에 있다.)

🔴 **PowerShell로 한글 파일을 일괄 치환하지 마라** (CLAUDE.md §16). 문서 편집은 Edit 도구,
   스크립트로 해야 하면 `.mjs`에서 `readFileSync(p,'utf8')`.

🔴 **`vercel.json`에 buildCommand가 명시돼 있어 prebuild·postbuild 훅이 프로덕션에서 안 돈다.**
   그래서 `public/sitemap.xml`이 레포에 커밋돼 있다. 빌드 파이프라인을 손댔으면 라이브 curl로 확인하라.

🔴 **전체 게이트가 초록이어도 «경화 대상 목록 밖 글»은 검사조차 안 된다.**
   `npm run audit:hard`의 「41/41편 0건」은 그 41편에 대한 진술일 뿐이다.
   **편집한 글은 반드시 `--slug=<slug>`를 따로 돌려라.** (2026-08-09 S3에서 실제로 걸렸다.)

---

## 📦 과거 로그는 아카이브로 옮겼다 (2026-08-09)

**`docs/handoff-archive-2026-08-09.md`** — 2026-07-13 ~ 08-08 세션 로그 전체(약 3,980행).

이 파일이 **407KB로 불어나 Read 도구 상한(256KB)을 넘겼다.** CLAUDE.md가 "작업 전 반드시 읽어라"고
지정한 파일인데 **열리지가 않는 상태**였다. 살아 있는 항목은 전부 위로 올렸고 완료분만 내렸다.

> 앞으로도 **이 파일은 «지금 할 일»만 담는다.** 세션이 끝나면 완료분은 `WORKLOG.md`에 적고
> 여기서는 지운다 — 이 파일에 로그를 쌓지 마라. 같은 일이 반복된다.
