# 🧰 queue 회차 Q6-b 브리프 — 인용부호·괄호 스윕 + 남은 헤드 판정 4건

> 규격 = `docs/hardening-protocol.md` §3(A 준비) · 레인 규율 = §7-F · 회차 표 = `docs/harden-queue-진행.md` §1.
> **전 구간 Opus**(판정·치환 · 새 문장 0). 재료 = 회차 표 Q6-b 행 + `docs/harden-queue-진행.md` §5 Q6a-7 잔여 3건.
> 🔴 **모든 수치는 2026-09-16 이 회차가 직접 잰 값이다**(도구 = `scripts/`가 아닌 1회용 분류기 · 방법 §1-B).
> 회차 표에 적힌 값(926 · 594 · 13편 · 10:15:16)은 **가설로 취급하고 전건 재측정**했다(프로토콜 §3-③-0-B).

---

## 0. 이 회차가 받은 재료와 «아직 열려 있나» 판정

| # | 재료(회차 표 문구) | 재측정 결과 | 판정 |
|---|---|---|---|
| ① | zh 본문 직선 `"` **926** + `「」` **594** → 정본 `“ ”` | 직선 **924**(462쌍) · `「」` **592**(296쌍) · 기존 `“”` **420**(210쌍) | 🟢 **열려 있다 — 이 회차의 본체** |
| ② | ja 전각（） **368** — 스윕 «안 함» 판정 | 전각 **374** · 반각(CJK 포함) **1,865** = 반각 정본 | 🟢 **판정 유지 · 스윕 안 함**(§3) · 🆕 zh·zh-hant 괄호도 같이 판정(§3-B) |
| ③ | zh-hant 상단 `快速解答` 상자 **13편**(GTO 시리즈 규격과 대조) | **14편**(라벨 이형 포함) · en 24 · ja 25 · zh 24 | 🔴 **이미 닫힌 판정이었다** — zh-hant 정본은 「상단 블록은 넣지 않는다」이고 `de353c38`(09-09)이 12개를 지웠다. 남은 14편은 **다른 규격**(GTO 13 + wsop 1) → **손대지 않는다**(§4) |
| ④ | zh-hant `holdem-probability` FAQ **10 : 15 : 16** | **zh-hant 16 · en 15 · zh 16 · ja 10** | 🔴 **두 겹으로 닫혀 있었다** — zh-hant는 `3f7e8b2e`(09-10)이 10→16 · **ja 10은 `locale-intentional-diffs.md:117`(09-09 헤드 판정)이 「결함 아님·새로 만들지 않는다」로 닫았다** → **아무것도 등재하지 않는다**(§5) |
| ⑤ | 부분문자열 태그 쌍(zh-hant `德州撲克牌型機率` ⊃ `牌型機率`) | 같은 유형이 en **78쌍** · zh **72** · zh-hant **115** · ja **106**. 다국어 태그는 **링크 없는 칩 + `meta keywords`** | 🟢 **손대지 않는다 · 라벨은 좁혀 등재**(§6) |
| ⑥ | zh `holdem-pot-odds:70` H2의 직선 인용부호 | 같은 유형 H2 = 직선 **12자리**(9편) + `「」` **7자리**(4편) | 🟢 ①의 부분집합 — 따로 손대지 않는다 |

---

## 1. 본체 = zh 인용부호 스윕

### 1-A. 정본(이미 판정됨 — 재론하지 않는다)

`docs/translation-terms-zh.md:174`(2026-09-14 queue Q6-a 헤드 판정):
> 🔴 **인용부호 정본 = `“ ”`(간체 표준) · 안쪽 겹침은 `‘ ’`** … 🔴 **직선 `"`는 «라틴 축어 인용»에만 남긴다** … 코퍼스 소급 스윕은 **queue Q6-b**가 한다.

### 1-B. 실측(2026-09-16 · 방법)

분류기가 `content` 템플릿 리터럴을 꺼내 **① HTML 태그 안 ② 마크다운 링크 괄호 `](…)` 안 ③ 그 밖(=본문)** 으로 가른다.
🔴 **링크 괄호 마스크는 «짝 맞는 괄호»로 세어야 한다** — 첫 `)`에서 끊는 정규식은 `](/images/x.webp "ボタン(ミシシッピ)…")`처럼
**제목 안에 괄호가 있는 자리**에서 닫는 따옴표를 본문으로 흘린다(ja `holdem-straddle:79`가 그 실례 · 1차안이 «홀수 1»로 나온 원인).

zh 본문 직선 `"` 분해 — **4,618 = 태그 3,208 + 링크 제목 486 + 본문 924**(합이 정확히 맞는다 · 486 = Q6-a가 «이미지 title 486 제외»라 적은 그 값).

| 종류 | zh | zh-hant | ja |
|---|---|---|---|
| `“ ”` 쌍 | **210** | 0 | 0 |
| `「 」` 쌍 | **296** | **1,262** | **1,371** |
| 직선 `"` 쌍(본문) | **462** | **5** | **6** |
| 『 』 쌍 | 0 | 0 | 11 |

→ **zh만 세 갈래**다. zh-hant·ja는 사실상 100% `「」`이고 직선은 5~6쌍만 남아 있다.
🔴 **표는 전부 «스윕 전(HEAD)» 기준이다** — 1차안이 ja 칸에만 «이 회차가 2자리를 고친 뒤» 값(4)을 적어 기준 시점이 섞여 있었다(렌즈 4).

### 1-C. 치환 규칙

**본문·독자 노출 필드의 인용부호 쌍 전부 → `“ ”`.** 대상 = `content` + `tldr`·`title`·`seoTitle`·`desc`·`imageAlt`(메타 **15쌍 / 14행** — 1차안의 «13자리»는 틀렸다).

🔴 **예외의 «기준»은 «라틴 축어»가 아니라 «zh·zh-hant·ja 셋이 동형으로 직선인 자리»다**
(2026-09-16 렌즈 3종이 각각 독립으로 같은 지적을 했다 — 이름을 «축어»로 붙이면 다음 세션이 **동급으로 축어인데 `“ ”`로 간 자리**
— `“Qualify for as Little as $0.50”`(WSOP 공식) · `“Aug 05 01:48 AM”` · `“Unlimited Re-Entry”` · `“originally from China but now residing in the United States”` —
**를 되돌린다.** 형제 로케일 실측이 그 자리를 `「」`로 감싼다: `posts-zh-hant/wsop-2026-tournament-guide.ts:219·378` · `posts-ja/…:408` · `posts-zh-hant/wpt-australia-2026-guide.ts:173·363`.)

🔴 **그 기준으로 남긴 3자리**(되돌리지 마라):
1. `apt-incheon-2026-guide` **`*"with the APT turning twenty in 2026"*`** — APT 공식 발표 축어(zh-hant·ja 동형).
2. `apt-incheon-2026-guide` **`安塔纳斯·"Tony G"·古加`** — 라틴 이름 안의 별명(zh-hant·ja 동형).
3. `holdem-tiebreak-rules` **`==g:*"In button games with 2 or more high or low hands…"*==`** — WSOP Rule 73 축어(zh-hant·ja 동형).

🔴 **나머지 라틴 인용은 전부 `“ ”`로 간다** — 형제 로케일 실측이 근거다:
`Choose Your Own Adventure` → zh-hant 「」 · `re-opening the bet` → ja 「」 · `I want to see that hand` → zh-hant·ja 「」 ·
`show one, show all` → zh-hant·ja 「」 · `In The Money` → zh-hant 「」 · `future game`·`Malmuth–Harville` → zh-hant 「」.
**즉 «라틴이라서 직선»이 아니다 — 형제들은 라틴 용어·구절도 자기 로케일 인용부호에 넣는다.** 직선이 남은 자리는 위 3자리 부류뿐이다.

🪶 **`‘ ’`(안쪽 겹침)는 이번 스윕에서 발화하지 않는다** — 보이는 중첩이 **0**이다.
유일한 중첩 후보 `holdem-glossary:20`의 「… [cooler](/zh/blog/holdem-cooler "thumb:…")」는 안쪽 따옴표가 **마크다운 링크 제목(구문)**이라 독자에게 안 보인다.

### 1-D. 하지 말 것

- 🔴 **머리 주석(한국어 JS 주석)의 `「」` 126쌍(252자)을 건드리지 마라**(1차안의 «141자리»는 **주석 126 + 메타 15**를 합친 수였다 — 그 15는 §1-C가 «스윕 대상»으로 옳게 지정한 자리다 · 2차 교열) — 독자가 안 보고, 한국어에선 `「」`가 정상이며, 그 안에 **키워드 뱅크·파일 경로·앱 UI 축어**가 들어 있다.
- 🔴 **`app/zh/`(솔버 랜딩·앱 카피)의 `「」` 11자리는 이번 회차 밖이다** — ① 정본 문서가 `translation-terms-zh.md`(포스트 용어)이고 ② 앱은 별도 트랙(`docs/solver-landing-playbook.md`)이며 ③ **포스트가 앱 라벨을 축어로 인용**한다(`「手牌/听牌」面板` 등) → 앱 문자열을 건드리면 인용 축어가 깨진다(ja 전각（） 판정과 같은 논리). → 진행 파일 §5에 등재.
- 🔴 **HTML 속성·마크다운 링크 제목의 직선 따옴표는 구문이다**(3,208 + 486). 바꾸면 빌드·렌더가 깨진다.
- 🔴 **`updated`를 올리지 마라.** 🔴 **근거를 «기계적 변경»이라 적지 마라 — `settled-decisions` §1-C 축어는 「내용을 안 바꿨으면 `updated`도 올리지 마라 / 내용을 바꿨으면 반드시 올려라」이고 축은 «내용»이다**(렌즈 4가 재인용의 재인용을 잡았다 · §12-B).
  이 회차가 그 축을 통과하는 이유: **명제·값이 불변**이고, `git diff -U0 | grep -E '^[-+]\s*(updated|masterUpdated|slug|image|tags):'` = **0건**이다.
  🔴 **«글자 수·표시 폭도 전부 불변»이라고 적지 마라 — 1차안이 그렇게 적었고, 뒤이은 «공백 24칸 삭제»가 그 근거를 거짓으로 만들었다**(2차 교열).
  `「」`↔`“”`는 1:1 전각 치환이라 폭이 같지만, **공백 24칸은 본문 글자를 실제로 지웠다**(9편). 그래도 **명제·값은 안 바뀌므로 결론은 선다** — 틀린 것은 결론이 아니라 근거였다. 이 레포에서 재론을 부르는 건 늘 틀린 근거 쪽이다.
  🪶 프로토콜 §7-F의 「구두점·별표만이면 안 올린다」는 **EN 한정**(근거가 「EN을 올리면 8로케일 `masterUpdated`가 가짜 드리프트」)이라 zh엔 그대로 적용되지 않는다 — 위 «내용» 축으로 판정한다.
  🔴 **`check:stamp`는 커밋 전엔 이 자리를 «미검사»다**(`scripts/check-stamp.mjs` 머리글 L16이 「커밋 안 된 작업 트리 변경」을 자기 사각으로 적어 뒀다) → **커밋 뒤에 돌려 `cosmetic`을 확인한다.**

---

## 2. 왜 `“ ”`인가 (재론 금지 · 근거만 남긴다)

Q6-a 판정의 근거 그대로: ① 간체(GB) 표준이 `“”`이고 `「」`는 대만·홍콩 관례라 **zh-hant와 갈라 두는 편이 낫다**
② zh 레인이 2026-07에 올린 「본문 ASCII ↔ 직답 블록 `「」` ↔ beginners `“”` 세 갈래」 요청(`docs/harden-zh-진행.md` §2·§5-9)의 답이다.
🪶 이번 실측이 그 판정을 보강한다 — zh 코퍼스는 이미 `“”`를 **210쌍** 갖고 있고, 라틴 용어를 CJK 인용부호에 넣는 관례도 **45자리**(`“”` 10 + `「」` 35)로 직선(33)보다 많다.

---

## 3. 괄호 판정 — 세 로케일 전부 «스윕 안 함»

### 3-A. ja (Q6-a J5 판정 유지)

전각 `（` **374** ↔ 반각 `(`(일본어 감쌈) **1,865** → 정본은 반각이고 `translation-terms-ja.md:18`은 이미 정정됐다.
**소급 스윕은 안 한다**: ① 두 폭 다 통용 = 독자 가치 ~0 ② 🔴 **앱 UI 축어 인용이 섞여 있다**(`blind-battle-cbet:232` 「OOP (SB（オープンレイザー）)」 = `app/ja/solver/solver-client.tsx:220·262` 글자 그대로) — 일괄 치환이 축어를 깨뜨린다.

### 3-B. 🆕 zh·zh-hant (이번 회차 신규 판정)

| 로케일 | 전각 `（` | 반각 `(`(CJK 포함) | 판정 |
|---|---|---|---|
| zh | **3,027** | **26** | 전각 정본 · 반각 26은 **결함 아님** |
| zh-hant | **3,077** | **17 → 수리 후 15** | 전각 정본 · **17 중 2를 고쳤고**(아래) 남은 **15는 결함 아님** |

🔴 **1차안은 「43자리 전부 수식·좌석 라벨」이라 적었다 — zh에는 맞고 zh-hant에는 틀렸다**(렌즈 4가 뒤집었고, 전건 재판정으로 범위를 다시 좁혔다).

**전각이 «내용 종류와 무관한» 정본이다**(실측): 전각 괄호 안 내용 = zh CJK 1,378 · 라틴/숫자 1,134 · 혼합 356 / zh-hant 1,348 · 1,273 · 312.
**즉 «라틴이라서 반각»이 아니다.** 반각이 정당한 자리는 **파일 안 패턴**으로만 정해진다:

| 자리 | 판정 | 근거 |
|---|---|---|
| **수식** `(底池 + 下注额)`·`(补牌 − 8)`·`(胜率 × 底池)` (zh 16 · zh-hant 3) | ✅ 결함 아님 | 산문 괄호(전각 109·101)와 **다른 구조**다 — `跟注% × [胜率 × (底池 + 下注额)]`처럼 식 안에 산다 |
| **좌석 역할 라벨** `OOP (BB 跟注方)`·`IP (BTN 开池方)` (zh 9 · zh-hant 9) | ✅ 결함 아님 | **거울쌍이 9 ↔ 9로 동형**이고 `app/zh/solver/solver-client.tsx:228` 라벨(`OOP: BB 跟注方 · IP: BTN 开池方`)을 가리키는 UI 참조 구문이다 |
| **출처 도장** `*(2026-08-15 查证)*` (각 1) | ✅ 결함 아님 | 인용 각주 서식 |
| 🔴 **CJK 병기 2자리** `## M值(哈靈頓分區)` · `\| **單色(同花面)** \|` (zh-hant) | **결함 → 전각으로 고쳤다** | 그 **파일 자신의 패턴을 깬다**(short-stack 반각 23자리 중 22가 영어 병기 `(push-fold)`·`(Nash equilibrium)` · continuation-bet 13 중 12가 `(C-bet)`·`(float)`) **+ zh 거울쌍이 전각**(`M值（Harrington 分区）`·`单色（同花面）`) |
| 🪶 `## …行不通 (Blackjack vs 撲克)` (zh-hant) | ✅ 유지 | `holdem-card-counting`은 **H2 4개가 전부 영어 병기 반각**이다(`(Can You Count Cards)`·`(Is It Legal)`·`(Seven Card Stud)`) — 파일 안 일관 패턴이고 zh엔 대응 병기가 없다 |
| 🪶 `綠區(20+)、黃區(10~20)…死區(小於 1)` (zh-hant `holdem-short-stack:230`) | ✅ 유지 · 등재 | 앞 넷이 숫자 범위라 **나열 안 일관**이 앞선다(선례 = 진행 파일 §3 「표 안 일관이 형제 글과의 일관보다 앞선다」). zh 거울쌍은 전각이지만 **표기 «형식» 갈림은 허용**(선례 `4:1` ↔ `4 比 1`) |

→ **스윕 안 함**(형식 일괄 치환 금지) · **게이트 신설도 안 한다** · 고친 것은 **파일 자신의 패턴을 깨는 2자리**뿐.

---

## 4. zh-hant 상단 `快速解答` 상자 — 전제가 뒤집혔다

**원 안건**(`docs/handoff-archive/2026-09-10-session-handoff-full.md:184` zh-hant ⑩):
> 상단 `快速解答` 블록은 **넣지 않는다**(tldr이 이미 「一句話答案」 상자 → 상자 셋) — 회차 7 소급 때 회차 1·2의 상단 블록 12개를 뺄지 함께.

**실측(2026-09-16 · 첫 `## ` 앞에 있는 `> **<라벨>**` 블록)**:

| 로케일 | 라벨 고정 계수(1차안) | **라벨 불문 재계수** | 어떤 글인가 |
|---|---|---|---|
| **en**(마스터) | 23 | **24** | GTO 13 + apt·ept·blind-meaning·flush-vs-straight·position-play·positions·reading-the-board·split-pot-rules·korea-poker-marathon·wsop·wpt |
| **ja** | 24 | **25** | 같은 13 + 12 |
| **zh** | 23 | **24** | 같은 13 + 11 |
| **zh-hant** | 13 | **14** | **GTO 13 + `wsop-2026-tournament-guide`(라벨 `一句話總結`)** |

🔴 **1차안의 근거 ②·③은 렌즈 4가 뒤집었다. 결론(13편을 빼지 않는다)만 남고 «10편 갭을 메운다»는 폐기한다.**

**(a) 측정이 라벨 하나에 묶여 각 로케일 1편씩을 놓쳤다** — 라벨 불문으로 다시 세면 en **24** · ja **25** · zh **24** · zh-hant **14**.
놓친 것은 전부 `wsop-2026-tournament-guide`의 라벨 이형(`Quick Answer` 대문자 · `一言まとめ` · `一句话总结` · `一句話總結`).
→ 🔴 **「zh-hant 13 = GTO 13편뿐」은 거짓이다**(14편이고 14번째는 GTO가 아니다). **라벨 문자열 기반 계수는 이형을 조용히 삼킨다**(`check:answer-echo`·`check:structure`도 같은 사각을 공유한다).

**(b) 「상단 블록을 넣지 않는다」는 zh-hant의 «현행 정본»이고 이미 집행됐다** — 1차안이 근거로 든 `docs/harden-brief/zh-hant-규칙.md` §①은 **그 정본이 폐기시킨 회차 1 판**이다.
- 정본 = `docs/local-voice/zh-tw.md:45`(회차 3 A 구간 판정): *「🔴 상단 블록은 넣지 않는다 … 렌더러가 `tldr`을 「一句話答案」 상자로 이미 그린다 → 화면에 요약 상자가 셋이 된다. **`tldr` + 각 H2 블록**, 이 둘만 둔다.」*
- 집행 = **`de353c38`(2026-09-09 · zh-hant 회차 7 C)** 커밋 제목 「**상단 블록 12 삭제**」. 그 커밋이 만진 파일은 **23개**이고, 커밋 본문이 「42편 전체가 「tldr + 각 H2 블록」으로 통일됐다」고 **스스로 선언**한다(축어 · 42편 귀속은 커밋의 말이지 이 브리프의 실측이 아니다 · 2차 교열).

**(c) 「EN이 23편 가졌으니 EN-먼저」는 방향이 반대다** — EN은 **H2마다 직답 블록을 달지 않는다**(`> **라벨**` 총량 en **28** ↔ ja 407 · zh 405 · zh-hant 399 · H2는 넷 다 630~660개).
EN에선 머리말 블록이 **유일한 블록**이라 상자가 셋이 될 수 없다. 「상자 셋」 조건에 걸리는 쪽은 머리말 + H2 블록을 **둘 다** 가진 **zh·ja**다.

→ 🔴 **판정 = ① zh-hant 상단 블록 14편은 그대로 둔다**(GTO 13편은 시리즈 규격 · wsop 1편은 이형 라벨)
**② «zh-hant가 10편 모자란다»는 결손이 아니라 로케일 정본의 집행 결과다 — Q6-c에 집필 과제로 넣지 않는다.**
③ 🔴 **그 편차가 `docs/locale-intentional-diffs.md`에 등재돼 있지 않아서 이 회차가 다시 열었다**(grep 0건) → **§5 헤드 요청 = 등재해 닫아 달라.**
 이 회차 자신이 「등재해 닫지 않으면 다음 회차가 또 집는다」(`docs/harden-ja-진행.md:221`)의 실례다.

---

## 5. zh-hant `holdem-probability` FAQ — 이미 닫혀 있었다

실측 FAQ 문항 수(`**Q.` 개수): **zh-hant 16 · en 15 · zh 16 · ja 10**.
이력 = `git show <커밋>:lib/posts-zh-hant/holdem-probability.ts | grep -c '^\*\*Q\.'` →
`f7efb019`(09-08) 10 → `ced2c49a`(09-10) 10 → **`3f7e8b2e`(09-10) 16**(커밋 제목이 「fix(zh-hant): FAQ 파리티 14문항」) → 이후 16 유지.
→ **「zh-hant 10」은 09-10에 닫힌 값이고, 재료 목록이 낡아 있었다**(프로토콜 §3-③-0-B의 다섯 번째 실례).

🔴 **«ja 10 ↔ en 15를 등재하자»는 1차안은 철회한다 — 그 판정은 2026-09-09에 이미 닫혔다**(렌즈 4).
`docs/locale-intentional-diffs.md:117` (헤드 판정 09-09):
> `holdem-probability` FAQ 「쿼드 에이스 확률」·「플랍 적중 32%」(ja · pt) — **EN에만 있고 ja·pt FAQ 세트에 없다** … ✅ **결함 아님** — 로케일 FAQ 세트가 EN과 다르게 구성됨(ja 회차 3 · pt 08-25 재저작). **새로 만들지 않는다**

같은 판정이 `docs/harden-ja-진행.md:221`에도 있다(「`check:structure`의 faq −N 6편(… probability …)은 결함이 아니다」).
🔴 **이것이 이 회차가 «닫힌 판정을 다시 연» 두 번째 자리다**(첫째 = §4 zh-hant 상단 블록). 둘 다 **재료 목록이 낡았고 착수 전 `grep`을 안 했기 때문**이다 — 프로토콜 §3-③-0-B·③-0-C가 정확히 이 유형이다.
🪶 덤: 1차안이 적은 「ja에 없는 문항 6개」도 틀렸다 — **7개**이고 빠뜨린 하나가 하필 그 등재가 이름으로 지목한 **`four of a kind (quad aces)`**다.

→ **판정: 아무것도 등재하지 않는다. FAQ 축은 닫혀 있다.**

---

## 6. 부분문자열 태그 쌍 — 결함 아님 · 종결

- **다국어 태그는 링크가 없다**: `components/intl-blog-post-client.tsx:276`·`components/tournament-guide-post.tsx:177`이 클릭 불가 `<span>` 칩으로 렌더하고 **로케일 태그 인덱스가 없다** → 나눠 가질 순위 자체가 없다.
  🔴 **단 «태그는 meta keywords로만 쓰인다»는 레포 전체로는 거짓이다**(렌즈 4 정정) — **한국어** 인덱스는 `app/blog/blog-index-client.tsx:143`이 태그 «필터»를 갖고 `lib/blog-filters.ts:13-19`가 `/blog?tag=<태그>`를 `router.push`한다. **그 경로는 KO 전용**이고 이번 판정 대상(zh-hant)과 무관하다.
- 다국어 쪽 다른 쓰임은 `lib/intl-blog-page.tsx`의 **`meta keywords`·`openGraph.tags`**인데, 이 레포 정본이 이미 「구글은 meta keywords를 2009년부터 무시한다」(`lib/intl-locale-layout.ts:35`)로 판정해 두었다.
- **같은 유형이 코퍼스 전반이다**: 부분문자열 쌍 = en **78** · zh **72** · zh-hant **115** · ja **106**(태그 총수 417·445·456·453).
  🪶 1차안의 61/55/83/57은 **과소 계수**였다 — 한 줄 정규식이 **여러 줄에 걸친 `tags:` 배열을 로케일당 26~30편씩 조용히 버렸다**(렌즈 4). **결론은 오히려 강해진다** — EN 마스터조차 78쌍이다.
- 문제로 지목된 쌍(`牌型機率` ⊂ `德州撲克牌型機率`)은 **서로 다른 글**(hand-rankings ↔ probability)에 있어 한 화면에 같이 뜨지도 않는다.

→ **판정: 손대지 않는다.** 🔴 **단 라벨을 좁혀 등재한다**(렌즈 3): **«태그 기제는 카니발 «경로»가 아니다»**까지가 증명된 것이고,
**«두 글이 `牌型機率` 쿼리에서 실제로 겹치는가»는 별건**이다(판별은 쿼리단위 GSC 산술 + 라이브 SERP — 메모리 `seo-tool-vs-blog-cannibalization`). 넓게 닫으면 진짜 겹침이 나와도 「종결」에 막힌다.

---

## 7. 실행 순서

0. (완료) 전제 재측정 — §0 표.
1. **zh 스윕 실행** — 분류기 마스크로 본문 인용부호 쌍 → `“ ”`(예외 3자리) · 메타 5필드 `「」` → `“ ”`.
   🔴 **파일 쓰기는 «인코딩 먼저 → tmp → rename»**(`settled-decisions` §3-E · Q6-a가 게이트 파일을 0바이트로 날린 사고).
2. **치환 불변식 검산** — 파일마다 ① 인용부호를 뺀 나머지 **문자 시퀀스가 동일**한가 ② `「`·`」`·직선 본문 잔량이 기대치(0·0·6)인가 ③ 태그·링크 제목 따옴표 개수가 **3,208 / 486 그대로**인가.
3. **게이트** — `audit:hard --locale=zh` · `check:cjk` · `check:seo-sync` · `check:meta` · `check:answer-echo`(스윕 전후 비교 · echo 판정이 뒤집히면 안 된다) · `check:structure` · `check:stamp`(🔴 `cosmetic`으로 나와야 한다) · `check:number-format --locale=zh` · `check:drift` · `check:mirror-pair` · `check:faq-schema` · `check:directives` · `npm run build`.
4. **렌즈 4종 + 2차 교열**(Opus 서브 · 한 메시지 병렬) — §5-③. 이 회차 특유 항목:
   - **zh 간체 네이티브**: 치환이 «인용이 아닌 자리»를 물지 않았나 · `“ ”`가 어색한 자리(강조·서명·수식)는 없나 · 앱 축어 인용이 깨지지 않았나.
   - **수학·딜러**: 인용 안에 든 수치·카드·룰 번호가 그대로인가(§13은 언어 불변).
   - **SEO/GEO**: `seoTitle`·`desc`·`title` 변경분이 길이·CTR을 해치지 않나 · FAQ 정형 `**Q.`+빈줄+`A.` 보존.
   - **교열(diff)**: 지적마다 ①원본 유래 / ②이번 치환 유래 표기 · 「문제없으면 없다고 써라 · 오탐보다 오통과가 나쁘다」.
5. **전건 원문 판정 → 2차 교열 → 커밋 → 진행 파일 §1·§2·§3·§4·§5 갱신 → 보고**.

---

## 8. 🆕 zh-hant·ja «혼입» 4자리 — 같은 축이라 같은 회차에서 닫았다

zh 스윕을 하면서 형제 두 로케일을 같은 분류기로 재면 **직선 `"`가 CJK 본문을 감싼 자리**가 4쌍 남아 있었다.
각 로케일 정본이 이미 그것을 «혼입»이라 부른다(`docs/translation-terms-zh-hant.md:329` 「引號는 **「」** 통일(""는 혼입)」).

| 로케일 | 자리 | 전 | 후 | 근거 |
|---|---|---|---|---|
| zh-hant | `apt-incheon-2026-guide:117` | `多數報導只寫"主賽送 3 個席次"` | `「主賽送 3 個席次」` | 정본 = `「」` |
| zh-hant | `apt-incheon-2026-guide:204` | `"1,281 人次"不等於` | `「1,281 人次」不等於` | 정본 = `「」` |
| ja | `holdem-drawing-odds:20` | `「…って"分かった"んだ?」` | `『分かった』` | 🔴 **`「」` 안쪽이다** — ja 코퍼스 정본 = `『』`(`holdem-when-to-fold` 「…では『自分に絡んだか』」 선례 11자리) |
| ja | `holdem-hand-rankings:112` | `> **最も多い"クーラー"**` | `> **最も多い「クーラー」**` | 정본 = `「」` |

**마감 상태(2026-09-16)** — CJK 본문에 남은 직선 인용부호 = **10쌍 · 전부 라틴 축어**:
zh 3(APT 발표·Tony G·Rule 73) · zh-hant 3(같음) · ja 4(같음 + WSOP 공식 `"Qualify for as Little as $0.50"`).
→ **세 로케일이 같은 규칙 위에 섰다**: 인용부호는 각 로케일 정본(`“”`/`「」`/`「」`), 직선은 **영문 1차 출처 축어**에만.


---

## 9. 렌즈 5종 + 이 회차의 «검산 정직성»

**렌즈 5종(전부 Opus 서브 · 한 메시지 병렬) → 2차 교열 1종.** 값어치가 난 자리는 전부 «한 렌즈만 잡은 것»이었다:

| 렌즈 | 그 렌즈만 잡은 것 |
|---|---|
| **zh 간체 네이티브** | 🔴 **전각 인용부호 옆 공백 24자리**(직선이 반각이라 데리고 있던 것 · 편집 전 `「」` 296자리의 **0개**가 공백을 가졌다) — **불변식이 원리상 못 보는 자리**다. 대표: `。” 他亮出了葫芦` · FAQ 질문줄 `扑克里的 “cards speak” 是什么意思？` |
| **수학·딜러/TD** | §13 **19자리 전건 재검산**(보드플레이·카운터핏·키커·더티아웃·2008 WSOP Mabuchi/Phillips) · WSOP 73/84/90/97/100.b/103/126.a·b·c · TDA 12/13-A/43-A **레포 1차 출처 축어 대조** · 🔴 **CRLF 함정**(아래) |
| **SEO·GEO** | 🔴 **정본 문서가 스윕 결과와 반대로 남아 있었다**(`translation-terms-zh.md:174~178`) · 🪶 **`a-high-board-cbet:156`·`k-high-board-cbet:177`의 readnext 앵커가 «편집 전부터» 전각 `“每个翻牌都 c-bet”`이었다** = 메타를 바꾼 것이 새 불일치를 «연» 게 아니라 이미 있던 것을 **닫았다**(반대 논거를 무너뜨린 실측) |
| **판정 적대** | 🔴 **닫힌 판정 2건 재개방**(§4 zh-hant 상단 블록 · §5 ja FAQ) · 🔴 **§3-B zh-hant 반각 괄호 오판** · 라벨 고정 계수가 로케일마다 1편씩 삼킨 것 · 태그 계수 절반 누락(여러 줄 `tags:`) · **게이트 사각 목록** |
| **교열(diff)** | TS 파서 57편 에러 0 · 렌더러 정규식 8종 스팬 전후 동일 · CRLF/BOM/백틱 2개 전수 · 🔴 **스윕 스크립트 불변식의 `strip()`이 `"`도 지워서 «TS 필드 구분자가 `“`로 바뀌어도» 안 걸린다**(별도 검사로 0건 확인) |

🔴 **이 회차의 검산이 «정직하지 않게» 적힐 뻔한 자리 3개 — 그대로 남긴다**:
1. **불변식은 최종 산출물에서 성립하지 않는다.** 스윕 뒤 **공백 24자리 삭제 패스**가 돌았으므로 「인용부호를 뺀 나머지 문자 시퀀스 동일」은 **8편에서 깨진다**. 그 8편은 **공백 차이만**임을 별도 검사로 확인했다(`adv-whitespace-diff`). 「불변식 0건」으로 적으면 미검사를 검증으로 적는 것이다.
2. 🔴 **CRLF 함정** — `core.autocrlf=true`라 `git show HEAD:<file>`(LF) ↔ 워킹트리(CRLF)를 그냥 비교하면 **42/42 전부 «불변식 위반»으로 보인다.** 줄끝 정규화 먼저. 교차 증거 = `git diff --stat`이 621/621(전체 행 아님).
3. **`strip()` 불변식이 원리상 못 보는 것**: ① 구문 따옴표(`"`)가 인용부호로 바뀐 경우 ② `”…“` 방향 역전 ③ TS 필드 구분자 훼손. 셋 다 **문자 시퀀스는 동일하게 유지된다** → 별도로 쟀다(HTML 속성 3,208/1,579 · 링크 제목 486/177 · 필드 구분자 547 · 방향 역전 0 · TS 파서 에러 0 · `npm run build` ✅).

🔴 **게이트가 원리상 못 보는 자리**(렌즈 4 · 진행 파일 §2로): `check:mirror-pair`는 **구두점·괄호 폭 갈림을 안 본다**(§3-B가 닫는 축을 확인해 줄 게이트가 없다) · `check:cjk`는 `.tsx` JSX 전용이라 포스트 `content`는 대상 밖 · `check:answer-echo`·`check:structure`는 **라벨 문자열 기반**이라 이형 라벨을 같이 놓친다 · `check:directives`·`check:faq-schema`는 `.next` 산출물을 읽으므로 **빌드가 편집보다 뒤여야** 유효하다 · 🔴 **인용부호 정본을 지키는 게이트는 없다**(→ 진행 파일 §5 게이트 신설 요청).


---

## 10. 2차 교열이 잡은 «수리가 만든 새 결함» 4건 (전부 문서 쪽 · 포스트는 0)

프로토콜 §5-⑤(「1차 렌즈의 수리가 3건 이상이면 2차 교열 필수」) — 이번 회차 수리는 4건(공백 24 · zh-hant 괄호 2 · 형제 혼입 4 · 문서 3)이었다.

| # | 무엇 | 처리 |
|---|---|---|
| ① | 🔴 **`translation-terms-ja.md`의 「『』 선례 11자리」가 겹치지 않는 두 집합을 합쳤다** — 실측 `『』` 15 = 작품명 11 + **이미지 title 속성 안 3**(지목한 `holdem-when-to-fold`가 여기다) + **「」 안쪽 겹침 1**. 그 1자리는 **이 회차가 만든 첫 사례**이지 선례가 아니다 | 문서 정정(편집 자체는 표준 용법이라 유지) |
| ② | 🔴 **§1-D의 스탬프 근거 「글자 수·표시 폭 전부 불변」이 수리 A로 거짓이 됐다** — 공백 24칸은 실제로 글자를 지운다 | 근거 문장 교체(결론 유지) |
| ③ | 🟠 **「머리 주석 141자리」는 주석 126 + 메타 15를 합친 수** — §1-C(«메타는 바꾼다»)와 §1-D(«건드리지 마라»)가 같은 15쌍을 두고 갈렸다 | 126으로 정정 |
| ④ | 🟠 **§3-B의 «zh-hant 17»은 수리 전 값** — 2자리를 고쳤으니 남은 건 15 | 표 정정 |

🪶 **포스트 쪽 지적은 0이었다**(전수 확인): 인용부호 안쪽 라틴 띄어쓰기 보존 · 마크다운 구문 스팬 621행 전수 대조 차이 0 · 백틱 개수 50파일 동일 · 수리 14행 전부 «공백 외 글자 변화 없음» · 스탬프 필드 변경 0.
🪶 2차 교열이 **수리 B를 더 강하게 지지하는 근거**도 찾았다: `holdem-short-stack` H2 괄호 5개가 수리 후 **«CJK=전각 / 라틴=반각»으로 전부 성립**하고(L36 `（幾個大盲算短碼）`가 같은 파일 선례), `holdem-continuation-bet`의 같은 표 다른 셀이 전부 전각(`**你（加注方）**`)이다. 🪶 zh 쪽은 **라틴 `Harrington`도 전각 괄호**에 넣는다 — 「라틴이라서 반각」이 아님을 한 번 더 보인다.

## 11. 이 회차의 실제 범위 (브리프 선언과 트리를 맞춘다 · 2차 교열 지적)

| 자리 | 편수 | 비고 |
|---|---|---|
| `lib/posts-zh/` | **43편** | 스윕 42 + **`k-high-board-cbet` 1편**(스윕 대상 아님 · **원래부터 전각이던 `“ ”` 옆 공백 2칸만** 지웠다 — 수리 A가 diff 범위가 아니라 코퍼스 규칙으로 돌았기 때문이다. 타이포그래피상 옳지만 «42편»이라고 적으면 개수가 어긋난다) |
| `lib/posts-zh-hant/` | **3편** | apt-incheon(혼입 2) · short-stack(괄호 1) · continuation-bet(괄호 1) |
| `lib/posts-ja/` | **2편** | drawing-odds · hand-rankings |
| `docs/translation-terms-{zh,zh-hant,ja}.md` | 3 | 정본 갱신(스윕 결과·기준·공백 규칙) |
| 🆕 `scripts/check-locale-hygiene.mjs` | 1 | **인용부호 정본 게이트 신설**(적대 렌즈 지적 「정본을 지키는 게이트가 없다」). 셀프테스트 **25/25** · 전 코퍼스 **🔴 0**. 🔴 첫 실행이 잡은 것은 코퍼스가 아니라 **내 면제 로직**이었다(면제를 문자 단위로 걸어 Rule 73 축어의 **닫는 따옴표만** 3로케일에서 잡혔다) → 면제를 «쌍» 단위로 |
| 브리프 | 1 | 이 파일 |
