# AGENTS.md — HoldemMaster

Codex(GPT-6 Astra 포함)는 이 파일을 프로젝트 시작 지침으로 사용한다.
프로젝트 운영 규칙의 정본은 `CLAUDE.md`다. 파일 이름과 관계없이 읽고 적용한다.
규칙 전문이나 현재 작업 상태를 여기 복사하지 않고, 아래 경로의 최신 원문을 따른다.

## 세션 시작

1. 루트 `CLAUDE.md`를 **전문 읽는다**. 요약이나 이전 세션의 기억으로 대신하지 않는다.
2. 그 문서의 **「세션 시작 시」 절차를 그대로 수행한다**:
   - `session-handoff.md`를 통독한다. 출력이 잘렸으면 남은 부분까지 읽는다.
   - `git status`를 확인한다. 미커밋 수정이 있으면 `git diff --stat`으로 변경 규모를 확인하고 기존 작업을 보존한다.
   - `WORKLOG.md`는 통독하지 않는다. 필요한 경위만 최상단 일부 또는 작업 대상 slug로 검색한다.
   - 지난 핸드오프 아카이브(`docs/handoff-archive/`)는 통독하지 않는다. 경위를 검색해 찾을 때만 해당 부분을 읽는다.
3. 실제 요청에 필요한 문서는 `CLAUDE.md` **§19 라우팅**과 핸드오프의 해당 작업 인계 링크를 따라 읽는다.
   확정 결정을 재검토하기 전에는 `docs/settled-decisions.md`, 시한 항목을 판단할 때는 `docs/update-calendar.md`의 원문을 확인한다.
4. **현재 사용자 지시를 우선한다.** 「읽고 대기」 요청이면 문서를 읽고 대기한다.
   핸드오프의 「다음 할 일」만으로 집필·수정·배포를 시작하지 않는다.

## 작업별 필독

- 모든 언어의 포스트 작성·검수·수정 전: `.cursor/rules/posting.mdc`.
- 검수 요청 시: `REVIEW-PROTOCOL.md` 전문.
- 그 밖의 작업: `CLAUDE.md` §19에서 연결하는 해당 정본 문서.
- 폐기된 문서나 과거 인계 내용을 현재 규칙으로 복원하지 않는다. 상세 절차는 위 정본에서 읽는다.

## Codex에서 적용하는 방법

- 한글 문서는 UTF-8로 읽는다. PowerShell에서는 `Get-Content -Encoding UTF8`을 사용한다.
  편집은 패치 도구 등 UTF-8을 보존하는 방식으로 한다(`CLAUDE.md` §16).
- 문서의 Claude 전용 도구 이름은 현재 제공되는 동등한 도구에 대응하되,
  원문 확인·검산·독립 검수 같은 요구사항은 유지한다. 사용할 수 없는 도구나 모델을 사용했다고 보고하지 않는다.
- 읽은 파일과 수행한 검증만 완료로 보고한다. 시작 준비와 실제 작업 착수를 구분한다.

## 세션 마감

- `CLAUDE.md` §12의 3단 마감을 따른다. 핸드오프는 다음 세션이 바로 이어갈 짧은 알림장이다.
- 완료 이력은 `WORKLOG.md`, 상시 규칙은 해당 정본 MD, 장기 미결의 상세는 해당 대기열에 둔다.
  핸드오프에 누적하거나 같은 내용을 여러 문서에 복사하지 않는다.
