# 클로드 디자인 1차 프롬프트 — 「디자인하지 마라, 판정만 해라」

> 출처 = `docs/ux-layout-brief-2026-08-17.md` §4-B 골격 + **2026-08-19 라이브 재실측**.
> 재측정 이유: 08-17 인벤토리는 그 후 `hr` 중복 제거(§3-1)·박스 가시성 복구(§3-3)가 반영되기 전 값이라
> 그대로 넣으면 **이미 고친 걸 다시 지적받는다.**
> 측정 = Playwright · 라이브 · 390×844 · `/blog/holdem-tournament-how-to-enter`.
>
> 🔴 **아래 코드블록 전체를 claude.ai/design 입력창에 붙여넣는다.**

```
[맥락]
holdemmaster.com — 포커 전략·룰·대회 가이드를 읽는 텍스트 사이트다.
커머스도 대시보드도 아니다. 독자 목적은 하나 — "질문에 답을 얻는 것".
모바일 유입이 약 68%다.
아래 인벤토리는 실제 라이브 페이지를 모바일 390×844로 직접 잰 값이다. 추정이 아니다.

[현황] /blog/holdem-tournament-how-to-enter (모바일 오가닉 2위 글) · 2026-08-19 실측
문서 전체 높이 20,325px = 모바일 화면 24.1개

■ 구간 A — 읽기 전 (0~764px · 0.9화면)
   빵부스러기                                        38px
   히어로 (카테고리 배지 · "17분 읽기" · 작성일 · 업데이트일 · 제목)  318px
   「한 줄 정답」 요약 블록                            277px

■ 구간 B — 읽는 중 (764~13,688px · 15.3화면)
   문단 116개
   H2 12개 (전부 질문형)
   인용 블록 13개
   표 7개 (가로 스크롤 래퍼 안에 있지만 실제로는 넘치지 않음)
   이미지 4개
   디렉티브 박스 12개 (팁·노트·인용강조 등)
   필라 허브 카드 1개
   구분선(hr) 1개

■ 구간 C — 글이 끝난 뒤 (13,688~20,325px · 7.9화면)
   FAQ 10문항                    2,628px
   「관련 글」 표                   719px
   이전 글 / 다음 글 카드            213px
   「함께 읽으면 좋은 글」 카드 목록   1,057px
   작성자 소개 (경력·전문성)          435px
   공유 버튼 (트위터·페북·링크복사)    114px
   커뮤니티 가입 CTA                187px
   푸터                           844px

■ 상시 고정 (스크롤해도 화면에 남는 것)
   상단 섹션 내비  62px
   하단 탭바 (피드·채팅·이벤트·프로필)  62px
   → 합계 124px = 세로 화면의 15%
   그 밖에 「다음 글 읽기」 바 69px 이 평소 숨어 있다가 조건부로 나타난다

[요청]
디자인하지 마라. 시안도 이미지도 코드도 만들지 마라. 먼저 판정만 해라.

독자를 세 구간으로 나눠라 — ①읽기 전 ②읽는 중 ③다 읽은 후.
각 요소를 구간에 배정하고, 요소마다 이 질문에 한 줄로 답해라:
  "이게 없으면 독자가 무엇을 못 하나?"

그리고 답이 안 나오는 요소를 따로 모아라.
"있으면 좋다"·"신뢰를 준다"·"참여를 유도한다"는 답이 아니다.
독자가 못 하게 되는 구체적인 행동을 대지 못하면 답이 안 나온 것이다.

표 한 장으로 답해라. 열은 [요소 / 구간 / 없으면 못 하는 것 / 판정].

[제약]
- 새 웹폰트를 들이지 마라.
- 다크 배경을 쓰지 마라. 따뜻한 크림 라이트가 이 사이트의 차별점이다.
- 기존 URL·slug·라우트를 바꾸는 제안을 하지 마라.
- 「삭제」보다 「접기」·「순서 바꾸기」를 먼저 검토해라.
  구간 C에는 검색 노출을 담당하는 자산이 섞여 있어 지울 수 없는 것이 있다.
```

---

## 08-17 인벤토리와 달라진 것 (재측정 값)

| 항목 | 08-17 | 08-19 | 해석 |
|---|---|---|---|
| 구분선 `hr` ↔ `h2` | 12 ↔ 12 | **1 ↔ 12** | §3-1 중복 제거가 라이브에 반영됨 ✅ |
| 표 7개 | 전부 가로 스크롤 · 최대 516px | **전부 안 넘침** (330~344px / 컨테이너 346px) | 해소됨 ✅ |
| 문서 전체 | 19,945px (23.6화면) | 20,325px (24.1화면) | 본문이 커졌다 |
| **글 끝난 뒤** | 3,418px (4화면) | **6,637px (7.9화면)** | 🔴 08-17은 FAQ~관련글까지만 셌다. **작성자·공유·커뮤니티·푸터 3,219px가 빠져 있었다** |

🪶 **오탐 하나 걸러냈다** — 하단에 「다음 글 읽기」 바(z-50 · 69px)와 탭바(z-40 · 62px)가 같은 좌표에 있어
겹침으로 보였으나, 실제 합성값이 `opacity:0 · pointer-events:none`이었다. **조건부 표시라 겹침이 아니다.**
([[review-mechanize-not-repeat]]의 거울상 — 「있다」 보고 전에도 탐지 방법을 의심하라.)

---

# 2차 프롬프트 — 판정 정정 + 구간별 순서안

> 1차 판정표(2026-08-19 수신)의 **「답없음」 4건이 레포 실측과 어긋나** 그대로 두면
> 그 위에 px 예산이 세워진다. 정정을 먹이고 순서안을 받는다.
> 🔴 아래 코드블록 전체를 **같은 대화에 이어서** 붙여넣는다(맥락 유지).

```
판정 잘 받았다. 다만 코드베이스를 실측하니 「답없음」 4건이 사실과 달랐다.
아래를 반영해서 다시 판정하고, 그다음 순서안을 내라.

[정정 1] 이전 글/다음 글 — 「단독형」이 아니다
  날짜순 이웃이 아니라 클러스터 커리큘럼이 고른 것이다(course.prevSlug/nextSlug).
  이 글의 이전=「대회 바이인·참가비」, 다음=「토너먼트 vs 캐시게임」으로 학습 순서가 있다.
  관련 카드의 유니크 목적지를 6편 → 56편으로 늘린 작업의 산출물이다.
  → 판정을 「유지」로 바꾸고 다시 배정하라.

[정정 2] 표의 가로스크롤 래퍼 — 한 뷰포트만 본 판정이었다
  내가 준 인벤토리가 390px 실측값이었다. 360·320px 기기와
  독일어처럼 단어가 긴 로케일(이 사이트는 18개 언어 공용)에서는 넘친다.
  래퍼는 안 넘칠 때 비용이 0이고 넘칠 때만 작동한다 → 제거 이득 0, 리스크만 있다.
  → 「유지」.

[정정 3] 작성자 소개 — 이 요소의 독자는 사람만이 아니다
  판정 기준이 「독자 행동」이라 원리상 못 잡는 자리다. 이 사이트는 작성자 경력
  (WSOP 취재·12년)으로 검색엔진에 전문성을 신고하고 있고, 그게 순위 근거의 일부다.
  → 압축하되 경력·전문성 문구는 남기는 선으로 다시 판정하라.

[정정 4] 「관련 글」 표 vs 「함께 읽으면」 카드 — 전제가 바뀌었다
  둘의 목적지가 완전한 포함 관계였다(이전/다음 2 ⊂ 카드 3 ⊂ 본문 표 9).
  같은 2편이 한 화면에 세 번 나왔고, 카드 1,057px가 새 목적지를 1개만 추가했다.
  → 이건 방금 코드로 고쳤다. 카드는 이제 「본문·이전/다음에 없는 곳」만 고른다
    (실측: 카드 3장이 전부 신규 목적지로 교체됨. 전 로케일 176편 중복 0).
  → 따라서 본문 표(9개 목적지, 필라 허브 링크 포함)는 「유지」,
    카드도 이제 값을 하므로 «중복이라 압축»이 아니라 순수 px 판단으로 다시 보라.

[추가 사실] 목차는 새로 만들 것이 아니다
  TableOfContents 컴포넌트가 이미 있고 규칙 페이지 3곳에서 돌고 있다.
  블로그에 연결만 하면 된다.

[요청]
정정 반영한 판정표를 다시 내고, 이어서 구간별 순서안을 표로 내라.
순서안의 열은 [순서 / 요소 / px 예산 / 근거].
구간 A는 첫 화면(844px) 안에 무엇이 들어가야 하는지를 명시하라.
구간 C는 현재 6,637px다 — 목표 px를 먼저 적고 그 안에서 배분하라.

[제약] (1차와 동일)
- 새 웹폰트 금지. 다크 배경 금지. URL·slug·라우트 변경 금지.
- 삭제보다 접기·순서 바꾸기를 먼저 검토.
- 아직 시안은 만들지 마라. 순서와 px까지다.
```

---

# ★ 확정 순서안 (2026-08-19 · 정정 7건 반영 후)

> 🔴 **이 표가 오늘의 산출물이다.** 시안은 이걸 그린 것일 뿐이고, 코드로 내릴 때의 기준도 이것이다.
> 총계 **20,325px → 15,494px** (24.1화면 → 18.4화면 · **−23.8%**).
> **삭제된 링크 목적지 0개 · 삭제된 색인 자산 0개** — 전부 접기·순서·압축으로만 줄였다.

## 구간 A — 첫 화면 844px 안 (가용 782 = 844 − 상단 바 62)
근데 
| 순서 | 요소 | px | 근거 |
|---|---|---:|---|
| A1 | 빵부스러기 | 38 | 8/5에 「구조화 데이터만 있고 사람이 볼 경로가 없다」로 넣은 것 — 지우면 회귀 |
| A2 | 제목 | 120 | 이 글이 내 질문의 답인지 판별하는 유일한 단서 |
| A3 | 메타 1줄(업데이트일·17분) | 40 | 룰 현행성 + 읽기 결정. **카테고리 배지·작성일만** 흡수 |
| A4 | 「한 줄 정답」 | 240 | 스크롤 0에서 답 획득. 277→240은 내부 여백만 |
| A5 | 본문 첫 H2 | 344 | 남는 공간 전부 — 첫 화면에 본문이 시작돼야 스크롤 유인이 생긴다 |
| **합계** | | **438** | 764 → 438 (−326) |

🪶 **목차는 예산 0이다.** 상단 바의 `📚 목차 12`가 문서 px를 안 쓰면서 15화면 어디서든 닿는다.
🔴 **−326px은 현행 여백 131px을 거의 다 걷는 전제다.** 크림 라이트에서 여백은 구분 신호 자체라,
답답해 보이면 계획 실패가 아니라 이 전제가 빡빡했던 것 — **A를 470~490으로 풀면 된다.**

## 구간 B — 읽는 중

| 순서 | 요소 | px | 근거 |
|---|---|---:|---|
| B1 | 문단 116 · H2 12 · 표 7(**래퍼 유지**) · 정보형 이미지 · 출처 인용 | 12,300 | 본문 — 손대지 않음 |
| B2 | 디렉티브 3~4개 | (내포) | 함정 회피용만. 8개는 본문 문단 편입 — 12개면 강조가 아니라 배경 |
| B3 | 필라 허브 카드 → C5로 이동 | −180 | 읽는 중 이탈 유발. 목적지는 C3 표에도 있다 |
| B4 | `hr` · 강조용 인용 · 장식 이미지 제거 | −440 | |
| **합계** | | **12,300** | 12,924 → 12,300 |

**상시 UI** — 🔴 두 바의 성격이 정반대다(실측):

| 바 | 내용물 | 처리 | 회수 |
|---|---|---|---:|
| 상단 62px | 뒤로 · 토너먼트 허브 · 러닝맵 · **📚목차12** · 바로가기 | **유지** (전부 읽기 도구) | 0 |
| 하단 62px | 피드 · 채팅 · 이벤트 · 프로필 | **읽는 중 숨김** (삭제 아님 — 다른 페이지에선 쓴다) | 62 = 7.3% |

체감 길이: 12,924÷720 = **17.9화면** → 12,300÷782 = **15.7화면** (−12%).
🔴 손댄 뒤 `node scripts/scan-mobile-fold.mjs` 재실행 필수(셸 높이가 바뀐다).

## 구간 C — 목표 2,800px (현재 6,637px · −58%)

순서 원칙: **잔여 질문 해결 → 커리큘럼 다음 단계 → 넓은 탐색 → 검증 → 행동.**

| 순서 | 요소 | px | 근거 |
|---|---|---:|---|
| C1 | FAQ 10문항 **전부 접힘** | 640 | 2,628→640. 문항 제목 10개는 보이므로 스캔 가능. **삭제 아님 → 색인 자산 보존** |
| C2 | 이전/다음 카드 | 240 | 213→**240 증액** — 커리큘럼 라벨(이전 강/다음 강)이 들어가야 순서가 읽힌다 |
| C3 | 「관련 글」 본문 표 | 520 | 719→520. **행 높이만** 압축, 목적지 9개 + 필라 허브 전부 유지 |
| C4 | 「함께 읽으면」 카드 3장 | 480 | 1,057→480. 카드당 352→140. 썸네일 대신 제목+한 줄 |
| C5 | 필라 허브 카드 (B에서 이동) | 180 | 주제 전체 지도 — 커리큘럼 다음 단계보다 뒤 |
| C6 | 작성자 | 160 | 435→160. **「WSOP 취재·12년」 문구와 프로필 링크는 존치**, 소개 산문만 삭제 |
| C7 | 공유(링크 복사) | 56 | 114→56. 트위터·페북은 OS 공유시트와 중복 |
| C8 | 커뮤니티 CTA | 160 | 187→160. 마지막 행동은 하나만 |
| C9 | 푸터 | 320 | 844→320. 정책·연락처 도달만 보장 |
| **합계** | | **2,756** | |

**별도 — 보류 1건**: 「다음 글 읽기」 바 69px(스크롤 60% 발현)은 **이탈 예정 독자용**이고
C2는 **완독자용**이다. 대상군이 달라 「중복이니 무료」가 아니다 → **측정 후 판단.** C 합계에 미포함.

## 시안 단계 제약 (어기면 폐기)

1. **브랜드색을 통일하지 마라** — 트위터·페북·네이버·카카오 고유색은 톤이 바뀌어도 안 따라와야 맞다
2. **골드 두 개를 합치지 마라** — `--primary`(크림용)와 `--gold-dark-rgb`(다크 표면용)는 의도된 쌍
3. **색은 토큰을 감싸서** — `hsl(var(--card))` · 투명도는 `hsl(var(--primary) / 0.3)`.
   토큰이 HSL 3요소라 `var(--primary)`를 색 자리에 그대로 쓰면 **무효 CSS가 되어 조용히 무시된다**
4. 새 웹폰트 금지 · 다크 배경 금지 · URL·slug·라우트 변경 금지

---

## 왜 4건이 틀렸나 (다음 사람을 위해)

판정 기준 「없으면 독자가 무엇을 못 하나」는 강력하지만 **두 곳이 사각지대**다:

1. **독자가 사람이 아닌 요소** — 작성자 소개(E-E-A-T)·구조화 데이터. 행동 언어로 물으면 항상 「답없음」이 나온다.
2. **화면 밖의 사실** — 이전/다음이 커리큘럼인지 날짜순인지, 표 래퍼가 다른 뷰포트에서 작동하는지는
   **인벤토리에 안 적히면 알 수 없다.** 내가 390px 값만 준 것이 정정 2의 원인이다.

→ 다음에 인벤토리를 만들 땐 **①요소의 «출처»(하드코딩인가 로직 산출인가) ②다른 뷰포트·로케일에서의 거동**을 함께 적는다.
