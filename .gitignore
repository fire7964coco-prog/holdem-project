public/images/.compressed
scripts/.pf-equity-cache-*.bin

node_modules
.next
out
build
.env
.env*.local
*.log

# GSC 서비스계정 키·원시 데이터 (커밋 금지)
*gsc*key*.json
*service-account*.json
docs/gsc-tracking/data/
# Claude Code — 개인 설정(절대경로·머신별 허용목록)은 제외, 스킬/MCP 설정은 커밋
.claude/settings.local.json
.playwright-mcp/
.html-baseline.json
.html-baseline-dom.json

# 커밋 메시지 임시 파일 (§16 — 한글·여러 줄 메시지는 git commit -F 로 넣는다)
commit-msg.txt

# 솔버 라이브 캡처 원본(PNG) — webp만 커밋한다
.solver-captures/
.solver-captures-*/

# hookify 로컬 규칙 (.local = 커밋하지 않는다)
.claude/*.local.md
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# 검수용 임시 원문 추출 폴더 — 남의 PDF 원본이 들어가므로 절대 커밋하지 않는다
.wpt-check/
.solver-captures*/
