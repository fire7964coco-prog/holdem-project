# 세션 인수인계 (Session Handoff)

> **사용법**: 작업 마칠 때 이 파일 업데이트 → 다음 세션 시작 시 AI가 읽고 바로 이어받음
> 작업 시작 전 `CLAUDE.md` → `session-handoff.md` → `WORKLOG.md` 순서로 읽을 것.
> 🔴 **이 파일은 «지금 할 일»만 담는다.** 완료분은 `WORKLOG.md`에 적고 여기서 지운다 — 로그를 쌓지 마라.

---

## ▶▶▶▶▶▶▶▶▶ 새 세션 START HERE

> **상태 (2026-08-09 마감 · 커밋 `6094060`)**
> build **69 blog + 457 intl**(불변) · KO 게이트 41/41편 🔴 0건 ·
> 게이트 **25개 로케일 `--locale` 전부 🔴 0건** · selftest 76/76 ·
> 드리프트 **104** · 추적불가 **84**
>
> ✅ KO 잔여 = **S5(11건) 하나, 8/16 이후 잠금**
> ✅ 다국어 **B1·B2·B4 완료** — 남은 배치는 **B3 하나**
> 상세는 `WORKLOG.md` 2026-08-09 (5)~(9).

### ▶▶ 다음 세션은 여기서 시작 — **다국어 B3 (E1 사이드팟, 22개 언어)**

```
1. git pull
2. npm run check:drift                     ← 지금 지점을 먼저 찍는다 (104 / 84)
3. docs/multilang-sync-plan.md §0-D 「B2 적용 완료」 절 정독 (교훈 3개가 거기 있다)
4. 대상: lib/posts-<locale>/holdem-all-in-rules.ts — en·zh-hant 제외 22개 언어
5. 전 로케일 «해당 줄»을 통째로 출력해 눈으로 판정 → 언어별로 문장 작성 → 되읽기
6. npm run audit:hard -- --locale=<code>  (편집한 로케일 전부) → npm run build → 커밋 → push
7. 끝나면 all-in-rules의 masterUpdated를 EN updated(2026-07-25)로 올린다 — E1 대기로 보류해 둔 것
```

**E1이 무엇인가** — `holdem-all-in-rules`의 사이드팟 계산 문장:

> ❌ 수정 전: *"자기가 낸 것 **× 콜한 사람 수**"* → 100 × 2 = **200**
> ✅ 수정 후: *"**내 벳 + 각 상대가 맞춘 만큼**"* → 100 + 100 + 100 = **300**

★**치환이 아니라 «문장 작성»이다.** 22개 언어로 한 문장씩 새로 써야 해서 별도 세션으로 남겼다.
★여러 로케일이 **같은 글 안에서 자기모순**이다 — 문장은 「× 콜러 수」(200)인데 바로 아래 표·FAQ는
「100 × 3 = **300**」이라고 적는다. **문장만 고치지 말고 그 글의 표·FAQ까지 같이 맞춰라.**

### ▶ B3에 끼워 처리 — 검수장 재검수(2026-08-10) §3 인계 1건

`holdem-reading-the-board` **FAQ 2곳**에 C9와 같은 기준점(「**보드 최저 플러시 카드보다** 높아야」) 누락.
**EN 마스터부터 잔존** — B2가 만든 오류 아님(B2는 표 행만 고쳤고 FAQ는 범위 밖이었다). 본체 세션에서 재확정함.

- ① FAQ「플러시 가능 판별」(en L266): "only a higher card of that suit improves it" — 기준 없음. 표(L139)는 정확
- ② FAQ「보드가 플러시면 누가 이기나」(en L270): "whoever holds the highest card of that suit in their hand" —
  보유 최고 무늬 카드가 보드 최저보다 낮으면 개선 실패·전원 스플릿이라 거짓이 되는 경우 존재
- 처리: **EN 먼저 정정 → 보유 8로케일**(en de es id ja pt zh zh-hant) 같은 세션에서 소급 후 masterUpdated 정리
  (B2가 de·id·pt에 방금 07-19를 삽입했다 — EN만 고치고 끝내면 드리프트가 다시 벌어진다)
- ⚠ 검수 보고서 §3의 반례(보드 K-J-9-6-2♠ + 5♠)는 **그 자체가 틀렸다**(5♠>2♠라 개선 성공·단독 승리).
  올바른 반례 = 보드 K-J-9-6-**5**♠에서 **2**♠만 든 플레이어(개선 실패 → 전원 스플릿). 결함 판정은 유효

### ▶ B3 이후 남는 것 (성격이 다르다 — 착수 전 판단 필요)

`holdem-showdown-rules`·`texas-holdem-rules-for-beginners`의 **드리프트 잔존분**.
EN이 07-19에 넣은 경험담·구조 변경이 19~20개 언어에 안 왔다. B2에서 **항목별 결함만** 고쳤고
masterUpdated는 **일부러 안 올렸다(46건)** — 지금 올리면 추적기가 거짓말을 한다.
이건 «항목 소급»이 아니라 «판 전체 재동기화»다.

---

## ② 다국어 동기화 — 지금 이 트랙이다

`docs/multilang-sync-plan.md`가 본체다. **재번역이 아니라 «패치»** — 숫자·계산·룰만 바꾸고
문장·훅·H2는 건드리지 않는다(현지 재저작 자산을 훼손하지 말 것).

### 🔴 착수 전 반드시 알 것

**1. 판정표를 믿지 마라 — 두 배치 연속으로 과소했다.**
   B1: E2 6→**10** · E3 3→**5** · C4 「ja엔 없음」인데 **ja에 2곳**
   B2: C11 6→**23** · C5 2→**23** · C7 3→**8** · C9 1→**5** · C6 1→**4**

**2. 🔴 「틀린 패턴」을 정규식으로 찾지 마라.** 찾을 것은 «결함»이 아니라 «그 주장을 담은 줄»이다.
   결함을 검색하면 표기 편차에 전부 흘린다 — B1 `es +Con kicker`·`zh +用踢脚`·`ja 80〜86%`(물결표),
   B2 `fr de la table`·`he בשולחן`·`fil sa mesa`. **두 배치에서 같은 방식으로 6건을 놓쳤다.**
   → 항목마다 **해당 줄을 전 로케일에서 통째로 출력해 눈으로 판정**한다. `.mjs` 20줄이면 된다.

**3. 🔴 본문에 결함이 없으면 FAQ·tldr·표를 봐라.** B2 C5가 그랬다 — 본문은 24개 전부 정상인데
   **FAQ만 22개가 틀려** 같은 글 안에서 자기모순이었다.
   고칠 땐 그 로케일의 **자기 본문 표현을 그대로 옮긴다**(새 번역을 만들지 마라).

**4. 🔴 EN이 항상 정답은 아니다.** B2 C7에서 **EN 마스터가 틀렸고** `zh`·`zh-hant`가 옳았다.
   소급 전에 EN 문장도 한 번은 의심하라(전수 열거로 확정했다).

**5. 🔴 미러는 KO 번역본이 아니라 «EN 마스터 파생본»이다.** KO에서 고친 결함이 미러엔 아예 없는 경우가
   많고, 반대로 EN 계열에만 있는 결함이 있다. **KO diff를 옮기려 들지 말고 로케일 원문부터 읽어라.**
   슬러그도 다르다 — KO `holdem-pot-odds-calculation` ↔ 미러 `holdem-pot-odds` ·
   KO `holdem-rules` ↔ `texas-holdem-rules-for-beginners`. **대조 전에 매핑부터 확인.**

**6. 🔴 한글·다국어 파일을 PowerShell로 치환하지 마라**(CLAUDE.md §16). Node `.mjs` + `readFileSync(p,"utf8")`.
   순서는 **드라이런 → 건수 검증(기대치와 어긋나면 중단) → `--apply`**.
   ★이 절차가 08-09에 실제로 오적용을 두 번 막았다(24 vs 기대 16 → 규칙을 좁혀 16/16).

**7. masterUpdated는 «남은 항목이 없을 때만» 올린다.** 남은 게 있는 채로 올리면 드리프트 추적이
   거짓말을 한다. 현재 보류분 = `all-in-rules`(E1 대기) · `showdown-rules`·`texas-…-for-beginners`(46건).

### 이 작업의 성격 (기대치 조정)

**SEO가 아니라 정확성 작업이다.** 번역본 노출은 de 278 · ja 40 · id 20 · es 19이고 **전 언어 클릭 합계가 2**다.
고쳐도 트래픽은 안 움직인다. 고치는 이유는 **틀린 계산이 20개 언어로 배포돼 있고 우리가 그걸 안다**는 것뿐이다.

드리프트 상위: `holdem-all-in-rules` **20**(→ B3) · `holdem-showdown-rules` 20 ·
`holdem-blind-meaning`·`holdem-game-order`·`texas-holdem-rules-for-beginners` 19씩 ·
`apt-incheon-2026-guide` 7(8/16 이후).

---

## ① KO 잔여 수정 — S5만 남았다 (8/16 이후)

`docs/kr-remediation-spec.md`가 본체다. S1~S4 정산·표준 수치·교훈은 전부 그 파일 §4에 있다.

| 세션 | 묶음 | 편 | 건 | 상태 |
|---|---|---:|---:|---|
| S1~S4 + 재판정 | — | 36 | 147 | ✅ 완료 |
| **S5** | `holdem-tournament-how-to-enter` | 1 | **11** | 🔒 **8/16 이후** — STALE #19가 APT 인천 종료로 만료 |

착수 전 `홀덤검수/reports/fix-handoff-remaining-2026-08-09.md` §2-보·§3을 읽을 것.

---

## ▶ 백로그 — 두 트랙 밖에서 아직 안 끝난 것

| # | 할 일 | 메모 |
|---:|---|---|
| 1 | **`/win-rate-quiz` 본문 0줄** | 도구는 완성(6인→실전모드→검수→모바일). **남은 건 본문·FAQ 스키마뿐.** `priority 0.8`로 색인 요청 중인데 h1+부제만 있다 |
| 2 | 프로덕션 E2E 마감 | 검수 세션에 `BASE=https://www.holdemmaster.com`으로 요청 |
| 3 | 다국어 정책 페이지 | `/privacy`·`/terms`·`/contact`가 한국어뿐인데 링크는 영어 라벨로 걸려 있다 |
| 4 | `/solver` desc에 「프로그램」·「계산기」 | 두 단어가 메타에 없다(「무료」는 이미 있음). 볼륨 140·20. **글 안 쓰고 160자 안에서 끝나는 개선** |
| 5 | 리버 승부 해설 | 복기 표에 승률만 있다. "키커로 갈렸다"를 `score5`의 키커로 자동 생성 가능 |
| 6 | `es` 국기 판단 | **사장님 결정 필요** |
| 7 | 🪶 `holdem-implied-odds` 표 헤더 `(1 card)` → `(turn → river)` | 8종 전부. 세트 행 `~11.8% flop`만 국면이 어긋난다(값은 전부 맞음). 검수장 승인부기 — **전용 커밋 금지, 그 파일 만질 일 있을 때 얹을 것** |
| — | ⏸ 펍 지역 페이지(`/pub/*` 9개 색인 탈락) | **사장님 지시로 보류** — 건드리지 말 것 |

### ⏰ 시한이 박힌 것

`docs/update-calendar.md`가 본체다. 현재 3건 —
**8/16 APT 인천 종료**(→ S5 착수 + 결과 아카이브 전환 + 미러 7종) ·
**10월 초 JOPT 오사카 바이인 공개**(글에 약속함) · **12/31 K-ETA 면제 만료**(한국 관련 전 언어 영향).

---

## ▶ 상시 함정 (트랙 무관 · 매 세션 유효)

🔴 **편집한 글은 `--slug=<slug>`(또는 `--locale=<code>`)를 따로 돌려라.** 전체 게이트가 초록이어도
   «경화 대상 목록 밖 글»은 **검사조차 안 된다.** 「41/41편 0건」은 그 41편에 대한 진술일 뿐이다.

🔴 **커버리지 출력을 반드시 읽어라.** 「시나리오를 못 잡은 글」의 0건은 **검증이 아니라 미검사**다.
   B1에서 **프랑스어 족보명이 하나도 등록돼 있지 않아 `fr` 6편이 «원리상 미검사»**였던 게 드러났다
   (`Quinte Flush`가 `flush`에 걸려 로열·스트레이트 플러시를 전부 「플러시」로 오독).
   **다른 소수 언어도 같은 상태일 수 있다 — 언어별로 확인할 것.**

🔴 **편집한 문단은 반드시 다시 읽어라 — 수정이 그 자체로 결함 주입 공정이다.** S2·S3·S4·재판정까지
   **4연속으로** 새로 쓴 문장이 새 오류를 만들었다. 되읽기 패스에서 전부 잡혔다.

🔴 **원장·인계서의 수치를 그대로 본문에 옮기지 마라. 직접 계산하라.**
   `ak-offsuit` 에퀴티를 전수 열거로 재계산했더니 원장이 틀렸다 — AKo vs 22 = **47.35%**(원장 47.0).
   ★B2 C7도 전수 열거(990조합)로 **EN 마스터의 오류**를 잡았다. 콤보 수·에퀴티 둘 다 해당된다.

🔴 **`19.6%`를 일괄 치환하지 마라.** 국면에 따라 맞는 값이다 — 플랍 9/47 = **19.15%** / 턴 9/46 = **19.6%**.
   표 제목·바로 위 문장까지 읽고 국면을 확정한 뒤 손대라(`outs`의 「Rule of 2 (턴)」 표는 전부 정답이었다).

🔴 **CLAUDE.md §8의 KO 필라 슬러그 목록이 실제 파일과 다르다.** 링크 적을 땐 반드시 grep하라.
   `holdem-pot-odds`(X) → **`holdem-pot-odds-calculation`** · `holdem-position-play`(X) → **`position-is-everything-in-holdem`**
   (08-05에 이것 때문에 두 번 404 직전까지 갔다. 전체 대조표는 CLAUDE.md §8.)

🔴 **PowerShell로 한글 파일을 일괄 치환하지 마라**(CLAUDE.md §16). 문서 편집은 Edit 도구,
   스크립트로 해야 하면 `.mjs`에서 `readFileSync(p,'utf8')`.

🔴 **`vercel.json`에 buildCommand가 명시돼 있어 prebuild·postbuild 훅이 프로덕션에서 안 돈다.**
   그래서 `public/sitemap.xml`이 레포에 커밋돼 있다. 빌드 파이프라인을 손댔으면 라이브 curl로 확인하라.

⚠ **형제 글을 인용할 땐 원문을 복사하라**(카드·무늬까지). 그리고 **기존 글을 고치면 그 글을 인용한 글이 낡는다.**

---

## 📦 과거 로그는 아카이브로

**`docs/handoff-archive-2026-08-09.md`** — 2026-07-13 ~ 08-08 세션 로그 전체(약 3,980행).
GTO 솔버 시리즈(✅13/13)·경화 스프린트 등 완료 트랙의 방법론은 `docs/gto-solver-series-spec.md`와
`docs/kr-remediation-spec.md` §4에 남겼다. 세션 로그는 `WORKLOG.md`가 본체다.
