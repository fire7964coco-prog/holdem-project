/**
 * 족보 10단계 데이터 — 서버(page.tsx의 JSON-LD)와 클라이언트(hands-client.tsx의 표·카드)가
 * **같은 배열 하나**를 본다.
 *
 * ★2026-08-13 분리 이유: 스키마가 클라이언트에만 있어 실제로는 출력되지 않았다.
 *   `components/seo.tsx`는 `schema` prop을 받기만 하고 렌더하지 않는다(구조분해에 없다) —
 *   라이브 HTML의 ld+json이 루트 layout의 WebSite 1개뿐이었던 원인이다.
 *   JSON-LD는 서버에서 내보내야 크롤러가 본다. 그래서 데이터를 여기로 빼고
 *   page.tsx가 ItemList·BreadcrumbList를 직접 렌더한다.
 *
 * ★prob = 홀덤 실제 조건인 **7장 기준** 확률이다(홀카드 2장 + 보드 5장에서 최강 5장).
 *   필라 `lib/posts.ts` holdem-hand-rankings의 itemList 2026-08-01 검산본을 그대로 이식했다 —
 *   C(52,7)=133,784,560 전수 census로 검산된 값이라 여기서 다시 계산하지 않는다.
 *   🔴 값을 고칠 일이 생기면 **필라와 이 파일을 반드시 함께** 고칠 것. 한 사이트가 같은 족보에
 *   다른 숫자를 말하면 E-E-A-T가 무너진다(CLAUDE.md §13).
 *
 * ⚠ 강약 순위와 출현 빈도는 뒤집힌다: 하이카드는 10위지만 7장에서는 17.4%로
 *   투페어(23.5%)·원페어(43.8%)보다 드물다. 5장 기준(하이카드 50.1%)과 정반대다.
 */
export type HandRank = {
  rank: number;
  name: string;
  description: string;
  /** 홀덤 7장 기준 출현 확률 */
  prob: string;
  example: string[];
  /** UI 전용 — 카드 테두리/배경 */
  color: string;
};

/**
 * FAQ — 화면(`hands-client.tsx`)과 FAQPage JSON-LD(`page.tsx`)가 **같은 배열**을 본다.
 * 스키마에만 있고 본문에 없는 정보는 LLM이 못 읽는다(posting.mdc §GEO).
 *
 * 🔴 **카니발 방지 규율**: 여기 답은 «표를 보는 그 자리에서 끝나는 판정»까지만 쓴다.
 *   근거·확률 유도·실전 판정은 필라(blog/holdem-hand-rankings)와 전용 글이 소유하고,
 *   각 항목은 «즉답 + 자세히는 저쪽» 구조로 링크를 건다.
 *
 * ⚠ 기준은 «문장 수»가 아니라 «깊이»다 (2026-08-13 갱신).
 *   딜러 검수에서 쇼다운 분쟁을 부르는 불완전한 답 2건(키커 범위·플러시 비교)이 나와
 *   오해를 막는 문장은 늘렸다. **늘려도 되는 건 «판정에 필요한 조건»이고,
 *   늘리면 안 되는 건 «왜 그런가»다** — 후자를 여기 쓰면 두 페이지가 같은 쿼리를 다시 나눠 먹는다.
 */
const PILLAR = "/blog/holdem-hand-rankings";

export const FAQS: { q: string; a: string; href?: string; linkLabel?: string }[] = [
  {
    q: "플러시와 풀하우스 중 어느 것이 더 강한가요?",
    a: "풀하우스(4위)가 플러시(5위)보다 강합니다. 7장 기준으로 풀하우스는 2.60%, 플러시는 3.03%라 풀하우스가 더 드뭅니다.",
    href: `${PILLAR}#초보가-가장-많이-틀리는-비교는`,
    linkLabel: "초보가 가장 많이 틀리는 비교 보기",
  },
  {
    q: "스트레이트와 플러시 중에는 뭐가 더 강한가요?",
    a: "플러시가 강합니다. 7장 기준 플러시 3.03% vs 스트레이트 4.62%로 플러시가 더 드물기 때문입니다.",
    href: "/blog/holdem-flush-vs-straight",
    linkLabel: "플러시 vs 스트레이트 자세히 보기",
  },
  {
    q: "스트레이트에서 A는 어떻게 쓰나요?",
    a: "양쪽 끝에 다 씁니다. A-K-Q-J-10이 가장 높은 스트레이트, A-2-3-4-5(휠·백스트레이트)가 가장 낮은 스트레이트입니다. 단 K-A-2처럼 A를 사이에 두고 이을 수는 없습니다.",
    href: `${PILLAR}#카드-한-장씩의-강도는-어떻게-되나`,
    linkLabel: "카드 한 장씩의 강도 보기",
  },
  {
    /**
     * 🔴 이 답은 필라의 「바로 답」 블록과 **축어가 겹쳐 있었다**(2026-08-13 적대 검수 적발).
     *   같은 도메인에 동일 문장이 둘이면 어느 쪽이 인용될지 구글이 고른다 —
     *   필라의 그 자리는 인용당하라고 만든 자리이므로 이쪽 어순·용어를 바꿔 끊었다.
     * 🔴 그리고 «족보에 쓰이지 않은 나머지 카드»는 6·7번째 카드까지 세는 것으로 읽혔다.
     *   홀덤은 7장 중 **딱 5장**만 쓴다. 딜러 렌즈가 쇼다운 분쟁 1위로 꼽은 오해다.
     */
    q: "족보가 서로 같으면 누가 이기나요?",
    a: "먼저 족보를 이루는 카드를 비교하고, 그것까지 같으면 남은 자리를 채우는 카드(키커)로 갈립니다. 비교는 다섯 장까지만 하고 6·7번째 카드는 아무리 높아도 보지 않습니다. 팟을 나누는 건 다섯 장이 하나도 빠짐없이 겹칠 때뿐입니다.",
    href: "/blog/holdem-tiebreak-rules",
    linkLabel: "키커·스플릿 판정 순서 보기",
  },
  {
    /**
     * 🔴 「만들기 어려운 족보가 더 강하다」는 **5장 기준에서만 참**이다(2026-08-13 수학·SEO 렌즈 동시 적발).
     *   이 페이지가 싣는 7장 확률에서는 하이카드 17.4%가 원페어 43.8%·투페어 23.5%보다 드문데도 더 약하다.
     *   기준을 명시하지 않으면 바로 위 표가 이 문장을 반박한다.
     */
    q: "홀덤 족보를 빨리 외우는 방법이 있나요?",
    a: "앞 글자만 이어서 «로·스·포·풀·플·스·트·투·원·하»로 외우면 됩니다. 다만 서열의 근거는 카드 5장 기준 희귀도라, 만들기 어려운 족보가 그대로 더 강하다는 것만 이해하면 굳이 암기하지 않아도 됩니다.",
    href: `${PILLAR}#홀덤에서-각-족보는-실제로-얼마나-나오나`,
    linkLabel: "5장·7장 기준을 나란히 보기",
  },
  {
    q: "보드에 있는 카드만으로 이겨도 되나요?",
    a: "됩니다. 홀 카드를 1장만 쓰거나 아예 쓰지 않아도 상관없습니다. 7장 중 가장 좋은 5장이면 되기 때문입니다. 보드 5장이 그대로 최강 5장이면 남은 사람 전원이 팟을 나눕니다.",
    href: `${PILLAR}#실제-보드에서-내-패는-무엇이-되나`,
    linkLabel: "보드에서 내 패 읽는 법 보기",
  },
  {
    /**
     * 딜러·SEO 렌즈 공통 지적: 세븐포커와의 진짜 차이는 «이름»이 아니라 «동점 처리(무늬)»다.
     * ⚠ 「동점일 때」를 반드시 남길 것 — 빼면 「홀덤은 (원래) 팟을 나누는 게임」으로 읽힌다(교열 렌즈 적발).
     *   무늬가 홀덤에서 어떻게 처리되는지는 바로 아래 FAQ가 소유하므로 여기선 대조까지만 한다.
     */
    q: "세븐포커와 홀덤은 족보가 같나요?",
    a: "10단계 구성은 같습니다. 다만 동점일 때 세븐포커는 무늬로 승자를 가리지만 홀덤은 무늬를 따지지 않습니다. 마운틴·백스트레이트처럼 부르는 이름도 다릅니다.",
    href: "/blog/holdem-vs-7poker-hand-rankings",
    linkLabel: "세븐포커와 홀덤 족보 비교",
  },
  {
    /**
     * ⚠ 앵커 주의: 처음엔 «홀덤펍에서는 족보를 뭐라고 부르나»로 걸었는데 그 섹션은
     *   로티플·쿼드·보트 같은 **별명 표**일 뿐 무늬 얘기가 한 글자도 없다(교열 렌즈 적발).
     *   필라에 이 질문 전용 h3가 따로 있다 — 스다하클 표 + WSOP 룰 + «무늬는 자리를 정할 때만 쓴다»까지 있다.
     */
    q: "무늬(수트)에도 순서가 있나요?",
    a: "공식 텍사스 홀덤에서 무늬는 승패를 가리지 않습니다. 플러시끼리는 숫자로만 비교하고, 다섯 장이 같으면 무늬와 무관하게 스플릿입니다. 홀덤에서 쓰는 「스다하클」은 일부 매장의 하우스 룰입니다.",
    href: `${PILLAR}#무늬수트-순서-한국과-국제-규칙이-다릅니다`,
    linkLabel: "무늬 서열이 어디에 쓰이는지 보기",
  },
];

export const HANDS: HandRank[] = [
  {
    rank: 1,
    name: "로열 플러시 (Royal Flush)",
    description: "같은 무늬의 10, J, Q, K, A — 포커 최강의 패.",
    prob: "0.0032%",
    example: ["A♠", "K♠", "Q♠", "J♠", "10♠"],
    color: "border-primary shadow-[0_0_30px_rgba(212,175,55,0.4)] bg-primary/10",
  },
  {
    rank: 2,
    name: "스트레이트 플러시 (Straight Flush)",
    description: "같은 무늬의 연속된 카드 5장. 로열 플러시 다음으로 강한 패.",
    prob: "0.0279%",
    example: ["9♥", "8♥", "7♥", "6♥", "5♥"],
    color: "border-primary/70 bg-card",
  },
  {
    rank: 3,
    name: "포카드 (Four of a Kind)",
    description: "동일한 숫자 4장. 포카드 중에선 숫자가 높을수록 강합니다.",
    prob: "0.168%",
    example: ["8♣", "8♦", "8♥", "8♠", "K♥"],
    color: "border-border bg-card",
  },
  {
    rank: 4,
    name: "풀하우스 (Full House)",
    description: "트리플(3장)과 원페어(2장)의 결합. 트리플 숫자를 먼저 비교하고, 그것까지 같으면 페어 숫자로 갈립니다.",
    prob: "2.60%",
    example: ["Q♠", "Q♥", "Q♦", "5♣", "5♠"],
    color: "border-border bg-card",
  },
  {
    rank: 5,
    name: "플러시 (Flush)",
    /**
     * 🔴 원래 「숫자 무관, 같은 무늬 5장. 최고 카드 숫자가 높을수록 강합니다.」였다.
     *   딜러·수학 렌즈가 동시에 지적한 자리 — 플러시끼리 붙는 판은 거의 보드에 같은 무늬가
     *   3~4장 깔린 상황이라 **최고 카드를 서로 공유**한다. 그때는 둘째·셋째 카드로 내려가야 하는데
     *   옛 문장은 「최고 카드만 보면 된다」로 읽혀 A하이 플러시끼리 스플릿으로 오독됐다.
     *   요약 표에는 첫 문장만 잘려 나가므로(hands-client.tsx의 split) 첫 문장에서 「숫자 무관」도 뺐다.
     */
    description: "같은 무늬 5장 — 숫자는 이어지지 않아도 됩니다. 플러시끼리는 최고 카드부터 다섯 장을 차례로 비교하므로, 보드에 같은 무늬가 깔렸다면 내 카드가 몇 번째로 높은지가 승부를 가릅니다.",
    prob: "3.03%",
    example: ["A♦", "J♦", "8♦", "6♦", "2♦"],
    color: "border-border bg-card",
  },
  {
    rank: 6,
    name: "스트레이트 (Straight)",
    description: "무늬 무관, 연속된 숫자 5장. A는 최고(A-K-Q-J-10) 또는 최저(A-2-3-4-5)로 사용 가능.",
    prob: "4.62%",
    example: ["7♠", "6♥", "5♣", "4♦", "3♠"],
    color: "border-border bg-card",
  },
  {
    rank: 7,
    name: "트리플 / 쓰리카드 (Three of a Kind)",
    description: "동일한 숫자 3장. 세트(포켓페어+커뮤니티 1장)와 트립스(커뮤니티 페어+홀카드 1장)로 나뉩니다.",
    prob: "4.83%",
    example: ["J♣", "J♠", "J♥", "A♦", "4♠"],
    color: "border-border bg-card",
  },
  {
    rank: 8,
    name: "투페어 (Two Pair)",
    description: "서로 다른 숫자의 페어 2개. 높은 페어 → 낮은 페어 → 키커 순으로 비교합니다.",
    prob: "23.5%",
    example: ["10♠", "10♥", "8♣", "8♦", "A♠"],
    color: "border-border bg-card",
  },
  {
    rank: 9,
    name: "원페어 (One Pair)",
    description: "동일한 숫자 2장. 페어 숫자 → 키커 순으로 우열을 가립니다.",
    prob: "43.8%",
    example: ["K♠", "K♦", "9♥", "6♣", "2♠"],
    color: "border-border bg-card",
  },
  {
    rank: 10,
    name: "하이카드 (High Card)",
    /**
     * ⚠ 첫 문장에 «비교 규칙»을 반드시 남길 것 — 요약 표는 description의 첫 문장만 잘라 쓴다
     *   (hands-client.tsx의 split). 한때 「아무 조합도 만들어지지 않은 상태」로만 끝내
     *   10개 행 중 이 행만 판정 정보가 없는 셀이 됐다(교열 렌즈 적발).
     */
    description: "아무 조합도 없을 때 가장 높은 카드부터 차례로 비교합니다. 같으면 다음 카드로 내려가고, 에이스 하이가 가장 강합니다.",
    prob: "17.4%",
    example: ["A♣", "Q♠", "9♥", "5♦", "3♣"],
    color: "border-border bg-card",
  },
];
