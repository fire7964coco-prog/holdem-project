# pt-BR 경화 키워드 뱅크 — 42편 재조준 (2026-08-25 실측 개시)

> 도구: **라쿠 `search-volume-history`**(Brazil/Portuguese · 48개월 · requestId **1256433**) +
> **lowfruits**(Brazil·Portuguese·MaxDA25 · 시드 10개 — 사장님 실행분 순차 편입) + exa/firecrawl 현지 서치.
> 파서: scratchpad `lf_parse.py`(inlineStr 대응 · winnable = top3 약점≥1 or top10 약점≥3).
> ⚠ lowfruits 계정 제외어 `Reddit, Quora, Amazon, UK, near me, excel, math` — pt 시드 무해 판정(08-25).

---

## 0. 헤드라인 발견 (경화 방향을 바꾸는 것들)

1. 🔴 **«maos DO poker» 8,100 > «maos DE poker» 3,600** — 관사 하나가 볼륨 2.2배.
   월별 시계열이 서로 달라 **별도 시리즈**(구글이 정규화 안 함 — fr range poker와 반대).
   현행 pt `holdem-hand-rankings`의 태그·본문은 «maos de poker» 위주 → **do 형태 편입 필요.**
2. 🔴 **`sequencia poker` 27,100** — 이 클러스터 최대 문자열. ⚠ **의도 확인 필수**(자릿수 함정):
   pt에서 sequência = ①스트레이트(족보) ②순서. SERP 실측 후 조준 결정(flush-vs-straight ·
   hand-rankings 어느 쪽 몫인지). 동의어 축: `jogadas de poker` 6,600 · `combinacoes poker` 4,400 ·
   `cartas do poker` 2,900 — 족보 수요가 «maos» 문자열 밖에 3배 더 있다.
3. 🟢 **lowfruits 약점 축 = «레인지·도구»** — `range de maos de poker`(10 · 약점6/2 · 최저DA 11!) ·
   `tabela range de maos poker` · `range de mãos poker pdf`(40 · DA16) · `matriz de maos` ·
   `site para treinar maos de poker`(약점4/1) · `analisar maos`(FS) · `leitor/replayer de maos`.
   → **starting-hands-chart(레인지)와 솔버 랜딩(treinar·analisar)이 먹을 자리.**
4. `piores mãos do poker`(약점4/1·DA5·FS) · `maos de poker melhor a pior`(FS — 우리 글 구조 그대로) ·
   `maos suited poker` · `maos marginais poker` — hand-rankings 경화 때 H2/FAQ 편입 후보.
5. 노이즈 제외 확정: `balatro`(비디오게임) · `red dead redemption 2` · `pokerstars`(브랜드) ·
   `em ingles`(영어 자료 수요 — 스킵).

## 1. 라쿠 볼륨 — rules + hand-rankings 클러스터 (Brazil 48m · 요청 1256433)

| 키워드 | 볼륨 | 메모 |
|---|---:|---|
| `sequencia poker` | **27,100** | ⚠ 의도 실측 후 배정(§0-2) |
| `regras do poker`/`regras poker` | **12,100** | rules 필라 정면 · 12m −11% |
| `como jogar poker`/`poquer` | **9,900** | 12m **+13%** 상승 · rules-for-beginners 몫 |
| `maos do poker` | **8,100** | 🔴 do 형태가 본체(§0-1) |
| `jogadas de poker` | 6,600 | 족보 동의어 · 광고경쟁 95 |
| `combinacoes poker` | 4,400 | 족보 동의어 |
| `maos de poker` | 3,600 | 현행 태그 축 |
| `cartas do poker` | 2,900 | 족보/카드 혼합 의도 추정 |
| `poker como jogar` | 2,400 · `full house poker` 2,400 · `flush poker` 1,600 | 어순 변형·개별 족보 |
| `poker texas holdem` | 1,300 | |
| `all in poker` | 880 · `dealer poker` 880 | all-in-rules·game-order 몫 |
| `regras texas holdem` | 390 · `flop turn river` 260 · `aprender a jogar poker` 260 | |
| `poker para iniciantes` | 210 · `mao mais forte do poker` 170(12m +23%) · `poker regras basicas` 140(+24%) | |
| `small blind big blind` 110 · `showdown poker` 110 · `kicker poker` 110 | blind·showdown·kicker 몫 |
| `empate no poker` 30 · `desempate poker` 30 · `ordem das maos de poker` 30 · `ordem de jogadas poker` 50 | tiebreak·game-order |
| **null**: rodadas de apostas poker · o que e blind no poker · ordem do jogo poker · o que ganha no poker · mesa de poker regras | 죽은 표기 — 산문만 |

## 2. lowfruits 시드별 수확 (Analyzed only · 순차 편입)

### 시드 1 `maos de poker` (341행 · analyzed 133) + 시드 2 `* maos de poker` (106행 · analyzed 35)

**winnable(약점 실측)** — 전부 hand-rankings/starting-hands/도구 축:
`range de maos de poker`(vol 10 · 약점 6/2 · DA11) · `range de mãos poker pdf`(40 · 3/0 · DA16) ·
`tabela range de maos poker`(10 · 4/1 · DA13) · `matriz de maos poker`(4/2 · DA13) ·
`site para treinar maos de poker`(4/1 · DA16) · `analisar maos de poker`(4/2 · FS) ·
`piores mãos do poker`(4/1 · DA5 · FS) · `maos suited poker`(4/1) · `maos marginais poker`(4/2 · DA5) ·
`melhores mãos do poker pré flop`(3/2 · DA23) · `leitor de maos poker`(5/1 · DA11) ·
`replayer de maos poker`(3/2 · DA10) · `maos de poker melhor a pior`(1/1 · FS) ·
`maos de poker tabela`(1/1 · FS) · `tabela de maos de poker`(1/1 · FS) · `piores maos de poker`(3/1)

**볼륨 있는 서제스트**: `rank de maos poker` 320(FS) · `maos de poker ordem`/`em ordem` 90+90 ·
`maos do poker tabela` 50 · `todas as mãos de poker` 40+10 · `melhores maos de poker` 30+30 ·
`ordem das maos de poker` 30 · `mãos de poker portugues` 30 · `maos do poker em portugues` 30 ·
`mãos de poker texas hold em` 30 · pdf 10+10 · `nome das maos de poker` 10 · `forca das maos` 10

**질문(FAQ 후보)**: quais são as melhores mãos · quais maos de poker jogar · quais as maos de poker ·
quais sao as maos de poker

⚠ 시드 1 미분석 208행 — 볼륨 표기가 없어 누락 판정 불가(도구가 삼킨 게 아니라 분석 안 한 것).
필요시 lowfruits에서 해당 행 추가 분석.

### 시드 3 `regras do poker` (360행 · analyzed 174)

- **코어 정면 축**: `regras do poker`=`regras poker`=`poker regras`=`regras para poker` **각 12,100**
  (전부 동일 시리즈 — 라쿠 §1과 일치 · DA30 · 약점 0 = 정면은 경쟁 보통, 롱테일에서 이긴다).
- **winnable — 전부 우리 rules 클러스터 글의 H2/FAQ 몫**:
  `regras poker texas holdem desempate`(10 · DA13 → **tiebreak**) · `regras do poker small blind`(4/2 ·
  FS → **blind-meaning**) · `regras poker big blind small blind`(FS) · `regras poker empate`(FS) ·
  `regras do poker all in`(FS → **all-in-rules**) · `regras do poker flush`(**DA2** · FS) ·
  `regras do dealer poker`(FS → **game-order**) · `regras raise poker`(10 → **betting-actions**) ·
  `regras poker cash game`(4/2 · DA6) · `regras torneio poker`(10 · DA6 → **tournament 클러스터**) ·
  `regras do poker fichas`(4/1)
- **볼륨 롱테일**: `regras do jogo de poker` 계열 260×4 · `poker regras cartas` 210 ·
  `poker 5 cartas regras` 210 · `regras do poker texas hold em` 170 · `regras basicas` 140×3 ·
  `quais são as regras do poker` 70(질문·FS) · pdf/para imprimir 다수(10)
- 🪶 **변형 게임 축 = 신규 글 후보로만 기록**(코퍼스 밖 — 경화에 안 끼움): omaha 110+50+40+40
  (약점 2 · DA14 — 가장 유망) · poker fechado(5-card draw) 20 · caribenho·stud·chines·dados
- 노이즈 제외: strip poker(110 — 톤 밖) · RDR2 · pokerstars 브랜드

### 시드 6 `poker para iniciantes` (124행 · analyzed 23)

- winnable = **학습 자료·수단 수요**: `poker para iniciantes pdf`(3/1 · DA20) · `livro`(5/1 · DA11) ·
  `aula`(2/1) · `dicas de poker para iniciantes`(1/1) · `app`(2/1) · `poker online para iniciantes`(2/1)
  → **rules-for-beginners** H2/FAQ 편입 + «연습은 트레이너» CTA 명분(솔버 랜딩 연계)
- `melhor site de poker para iniciantes`(2/1) — 사이트 추천 의도, 우리 톤에서는 «어디서 배우나»로만

### 시드 4·5·7~10 — 대기 (도착 순 편입)

## 3. 다음 실측 할 일

- [ ] `sequencia poker` 27,100 SERP 의도 실측(스트레이트 vs 순서) → 배정
- [ ] `maos do poker` vs `maos de poker` SERP 동일성 확인 → 태그·H2 표기 결정
- [ ] rules 클러스터 회차 착수 시: exa로 브라질 상위글 패턴(Kill Tilt 브라질판? — 현지 매체 조사)
- [ ] 시드 3~10 편입
