# 🇨🇳 zh(간체) 키워드 뱅크 — 트랙 종결 (경화 회차 7)

> 실측일 **2026-09-09** · 레인 `Holdem-zh` / 브랜치 `harden-zh`
> 대상 = 클러스터가 아니라 **회차 1~6이 «다음 회차 / 트랙 종결 회차»로 넘긴 미결**이다.
>
> 도구·규율: 구글 자동완성 `client=chrome&hl=zh-CN&gl=SG`(+`gl=MY` 검증표본) · 코퍼스 grep · EN↔zh 링크 계수.
> 🔴 **볼륨 절대값을 글·문서에 쓰지 마라** — 신마 데이터는 상대 비교 전용(프로토콜 §7-B).
> 🔴 **여기서 «0»은 «자동완성에 없다»는 뜻이지 «검색량 0»이 아니다**(메모리 `absence-may-be-the-standard`).
> 그래서 이 회차는 **0을 근거로 «제거»한 것은 근사 중복 1건뿐**이고, 나머지 0은 «판정 종결·재론 금지»로만 쓴다.

---

## 0. 이 회차의 한 줄 결론 — **넘겨받은 표기 미결 6개 중 «SEO 축»은 하나뿐이었다**

회차 1~6이 「볼륨 실측으로 정리하라」며 넘긴 표기 미결을 전부 재니, **5개는 자동완성 축 자체가 없었다.**
축이 없는 자리는 «어느 표기가 이기나»가 아니라 «내부 일관성·독자 이해»가 판정 기준이다 — **판정 기준이 바뀐다.**

| 넘겨받은 미결 | 실측 결과 | 판정 |
|---|---|---|
| `德州扑克现金桌` 근거가 얇다(회차 5) | 🟢 **SG 3건 + MY 3건 실재**(`技巧`·`规则`·`ptt` · `澳门`·`济州岛` 지역형까지) | **H2 병기 확정** — 이 회차 유일한 «SEO 축» |
| `德州扑克赔率表` ↔ `底池赔率计算`(회차 3) | `底池赔率计算` 🟢 실재 / `德州扑克赔率表` **0 + 함정**(`德扑保险赔率`) | **`德州扑克赔率表` 제거** |
| `短码策略`·`短码怎么打` 앵커 없음(회차 5) | 둘 다 **0 + 함정**(断码·短信验证码·短裤打版) · 살아 있는 것은 **`德州扑克 短码`·`德州短码`** | **앵커 부여** |
| tiebreak 앵커 12갈래(회차 6) | `平局`·`谁赢`·`踢脚牌`·`牌型一样谁赢` **전부 0** · 살아 있는 것은 **`<牌型>怎么比大小`** | **SEO 축 아님 → 카드 라벨만 통일, 본문 앵커는 유지** |
| `德州扑克怎么打` 귀속(회차 4) | `怎么打` **0** · `怎么玩` **도 0** | **양쪽 다 죽은 축 — 셔플 금지, 현행 유지** |
| string bet 역어 3갈래(회차 6) | `串注` **0 + 함정**(串珠·注音) · `分段下注` **0 + 함정**(分段 english) | **SEO 축 아님 → 코퍼스 지배·정본으로 판정** |
| card-counting 21点 절(회차 3) | `德扑算牌和21点区别` **0** · 살아 있는 것은 **`21点 算牌`**(=블랙잭 축, 우리 것 아님) | **SEO 근거로는 기각 · 블록은 §14-A 일관성 근거로만** |

---

## 1. 자동완성 실측 원문 (SG · 2026-09-09)

🔴 **표기 규율(회차 6 §7)**: 아래 «실측 원문» 칸은 **구글이 돌려준 문자열 그대로**다.
자동완성이 넣는 공백은 **구글의 정규화이지 사용자의 타건이 아니다** — 채택 표기는 «판정» 칸을 쓴다.

### 1-A. 살아 있는 축 (real completions)

| 시드 | 실측 원문 | 건수 | 판정 |
|---|---|---:|---|
| `德州扑克现金桌` | 德州扑克现金桌技巧 · 德州扑克现金桌规则 · 德州扑克现金桌ptt · 济州岛 德州扑克现金桌 · 澳门 德州扑克现金桌 | **5** | 🟢 채택 — `德州扑克现金桌` |
| `德州扑克 现金` | 德州扑克现金桌 · 德州扑克现金桌技巧 · 德州扑克现金桌规则 · 德州扑克现金 · 德州扑克现金桌ptt · 德州扑克 现金局 · 德州扑克 现金版 · 德州扑克 线上 现金 | **8** | 🟢 `现金桌`가 `现金局`보다 **앞선다**(1·2·3위 전부 现金桌) |
| `底池赔率计算` | 底池赔率 · 底池賠率怎麼算 · 底池賠率算法 · 底池赔率计算 | **4** | 🟢 유지 |
| `底池赔率怎么算` | 底池賠率怎麼算 · 底池赔率 · 底池賠率算法 · 底池赔率计算 | **4** | 🟢 유지 |
| `底池赔率` | 德州扑克 底池赔率 · poker 底池赔率 · 底池賠率怎麼算 · 底池赔率计算 | **4** | 🟢 유지 |
| `短码 德州` | 德州扑克 短码 · 德州撲克 短碼 · 德州短码 · 短碼 德州 | **4** | 🟢 **앵커형만 산다** |
| `德州扑克两对怎么比` | 德州扑克两对怎么比大小 · 德州两对怎么比大小 | **2** | 🟢 local-voice §7 재확인 |
| `德州扑克 算牌` | 德州扑克 算牌器 | **1** | 🟡 얇다 |
| `德州扑克 straddle` | 德州扑克 straddle 意思 · straddle 扑克 · straddle德州扑克意思 · poker straddle意思 | **4** | 🟢 현행 태그 유지 |
| `21点 算牌` | 21点 算牌 违法 · ptt · 电影 · 赌场 · 法 · 程式 · 澳门 21点 算牌 | **7** | 🔴 **블랙잭 축 — 우리 것이 아니다** |

### 1-B. 죽은 축 (0 real · 반환된 것은 전부 무관 폴백)

`德州扑克平局` 0 · `德州扑克 平局` 0 · `平局判定` 0 · `德州扑克 比大小` 0 · `踢脚牌` 0(→踢脚线 인테리어) ·
`德州扑克 踢脚` 0 · `德州扑克 平局规则` 0 · `牌型一样谁赢` 0(→牌告/牌品) · `德州扑克 谁赢` 0 ·
`德州扑克 怎么比大小` 0 · `短码策略` 0 · `短码怎么打` 0 · `德州扑克短码怎么打` 0 · `德扑短码` 0 ·
`德州扑克赔率表` 0 · `底池赔率对照表` 0 · `德州扑克底池赔率` 0 · `德州扑克怎么打` 0 · `德州扑克怎么玩` 0 ·
`串注` 0 · `德州扑克 串注` 0 · `分段下注` 0 · `德扑算牌和21点区别` 0 · `德州扑克 21点` 0 · `德州扑克 房规` 0 ·
`德州扑克现金局` 0

### 1-C. `gl=MY` 검증표본 (4건)

| 시드 | SG | MY | 일치? |
|---|---|---|---|
| `德州扑克现金桌` | 技巧·规则·ptt·济州岛 | 技巧·规则·ptt·济州岛 | 🟢 **완전 동일** |
| `短码 德州` | 德州扑克 短码 · 德州短码 · 短碼 德州 | 德州扑克 短码 · **短牌 德州扑克** · 短碼 德州 | 🟡 1건 갈림 |
| `德州扑克赔率表` | 0 | 0 | 🟢 동일(둘 다 죽음) |
| `底池赔率计算` | 🟢 4건 | **0** | 🔴 **갈렸다** |

🔴 **`底池赔率计算`이 SG 실재 / MY 부재로 갈린 것은 회차 5의 `首尔 马拉松`에 이은 «SG·MY 완전 동일» 두 번째 반증**이다.
「SG·MY는 문자 단위로 같다」는 회차 2·3·4의 다섯 번 실증은 **개념 축에서만 참**이고,
**절차·계산 축과 지역명 축에서는 깨진다.** → **검증표본을 개념어로만 뽑지 마라.**

---

## 2. 함정 (이 회차 신규 5건)

| # | 문자열 | 실제로 무엇을 부르나 | 근거 |
|---|---|---|---|
| 1 | `踢脚牌` | **인테리어 걸레받이**(`踢脚线`) — english·安装费用·砖·铝合金·柜子 | SG 자동완성 5/5 |
| 2 | `短码怎么打` | **断码**(재고 떨이) · **短信验证码** · **短裤打版** | SG 5/5 · 회차 5 §1-E(문자 단축번호)의 새 변이 |
| 3 | `德州扑克赔率表` | **德扑保险赔率**(올인 보험) — 우리 축이 아니다 | SG·MY 양쪽 |
| 4 | `串注` | **串珠**(비즈) · **注音**(주음부호) | SG 6/6 |
| 5 | `牌型一样谁赢` | **牌告·牌品·牌位表·刁牌** — `牌`가 광고판/인품으로 샌다 | SG 5/5 |

🪶 함정 5는 **우리 글의 title 축어**(`牌型一样谁赢？德州扑克平局与踢脚牌规则`)가 자동완성에서 오염된 사례다.
제목을 바꾸라는 뜻은 아니다 — 제목은 CTR 훅이고 이 축은 애초에 검색량이 얇다. **태그로 올리지 마라**는 뜻이다.

---

## 3. 배정·양보표 (이 회차가 손대는 태그만)

| 태그 | 현 소유 | 판정 | 근거 |
|---|---|---|---|
| `德州扑克赔率表` | `holdem-pot-odds` | 🔴 **제거** | 0 + 함정 3 · `底池赔率计算`과 근사 중복(회차 3 미결) |
| `底池赔率计算` | `holdem-pot-odds` | 🟢 **유지(승자)** | SG 실재 |
| `短码策略` | `holdem-short-stack` | 🔴 **`德州短码`로 교체** | 0 + 함정 2 · `德州短码`는 SG·MY 양쪽 실재 |
| `短码怎么打` | `holdem-short-stack` | 🟡 **`德州扑克短码怎么打`로 앵커화** | bare는 함정 2에 걸린다 · 앵커형은 글 title 축어와 일치 |
| `德州扑克 短码` | `holdem-short-stack` | 🟢 유지 | SG·MY 실재 · **띄어쓰기는 CJK↔CJK라 붙임이 정본**(회차 6 §7) → `德州扑克短码` |
| `德州扑克现金桌` | `holdem-tournament-vs-cash-game` | 🟢 유지 + **H2로 승격** | SG 5 · MY 4 · 본문 근거가 블록 괄호 1회뿐(회차 5 미결) |
| `德州扑克怎么打` | 없음(미배정) | ⛔ **배정하지 않는다** | 0 · `怎么玩`도 0 — 셔플할 이유가 없다 |
| `底池赔率对照表` · `德州扑克底池赔率` | `holdem-pot-odds` | 🟡 **관찰만**(건드리지 않음) | 0이지만 근사 중복이 아니다 → «0 = 제거»로 확대 적용 금지 |

---

## 4. 앵커 표기 실측 — `holdem-tiebreak-rules`(회차 6이 넘긴 12갈래)

### 4-A. 코퍼스 실측 (2026-09-09 · grep 전수)

**본문 인라인 앵커 12자리 · readnext 카드 라벨 5자리 · 합계 12종**

| 표기 | 자리 |
|---|---|
| `德州扑克平局怎么判` | kicker L76 · L187 · **카드 L222** |
| `平局判定规则` | kicker L295 · reading L158 |
| `分胜负规则` | glossary L159 · L337 |
| `踢脚牌与平局规则` | flush **카드 L232** · probability L221 |
| `踢脚牌与平局判定规则` | reading **카드 L314** · beginners L395 |
| `摊牌时哪手牌赢(？)` | cooler L102 · **카드 L165** |
| `踢脚牌与比牌规则(指南)` | hand-rankings L205 · **카드 L324** |
| `扑克平局与踢脚牌规则` · `平局与踢脚牌指南` | flush L170 · L290 |
| `平局与踢脚规则` · `踢脚与平局规则` | split-pot L84 · L288 |
| `德州扑克花色分大小吗` | hand-rankings L73 |

### 4-B. 판정 — **SEO 축이 아니다. 카드 라벨만 통일한다**

🔴 후보 표기 전부가 자동완성 **0**이다(§1-B). 살아 있는 것은 `<牌型>怎么比大小` 하나인데
그것은 **개별 족보 질의**(`两对怎么比大小`)이지 이 글 전체를 부르는 이름이 아니다.
→ **어느 앵커를 골라도 순위 효과를 살 수 없다.** 그러면 판정 기준은 «내부 일관성」과 «독자가 목적지를 알아보는가»다.

| 자리 | 처방 | 근거 |
|---|---|---|
| **readnext 카드 라벨 5자리** | 🔴 **`德州扑克平局怎么判` 하나로 통일** | 카드는 **고정 UI 슬롯**이라 5종이 그냥 불일치다. 이 표기는 ① 코퍼스 최다(3자리) ② **글 자신의 desc 축어**(「这篇讲清德州扑克平局怎么定胜负」)와 가장 가깝다 |
| **본문 인라인 앵커 12자리** | 🟢 **현행 유지** | 문장마다 문맥이 다르다. 특히 `德州扑克花色分大小吗`(hand-rankings L73)는 **그 글의 특정 FAQ를 정확히 가리키는 좋은 앵커**다 — 통일하면 정보가 준다. 회차 6의 「2자리만 고치면 셔플」 우려는 **카드 슬롯에는 해당하지 않는다**(5자리 전건이라 셔플이 아니다) |

🪶 **재론 금지**: 이 축은 실측으로 닫혔다. 다음 회차·다른 로케일이 「앵커 통일로 순위를 올리자」고 다시 열지 마라 —
**표기를 고르는 근거가 애초에 없다.** 근거가 생기려면 `平局` 계열에 자동완성이 붙어야 한다.

---

## 5. EN↔zh 내부링크 계수 대조 (전 56편 · 기계 실측)

메모리 `translation-link-structure-equals-en`(「개수(count)로 검증」)를 처음으로 **전 코퍼스에 기계 적용**했다.
스크립트 = 스크래치패드(레인은 `scripts/` 금지) · 본문 인라인 `](/{locale}/blog/<slug>` 만 센다(카드·readnext 제외).

**갈림 18편 / 56편.** 그중 **zh가 EN보다 적은 자리는 3건뿐**이고 나머지 15편은 zh가 경화 중에 더한 것이다(정상).

| 슬러그 | 갈림 | 판정 |
|---|---|---|
| `holdem-3bet` | `3bet-pot-cbet` **EN1 / zh0** | 🔴 **드리프트 미반영** — §6 |
| `holdem-position-play` | `low-board-check-raise` **EN1 / zh0** | 🔴 **드리프트 미반영** — §6 |
| `wsop-2026-tournament-guide` | `holdem-tournament-vs-cash-game` **EN1 / zh0** | ⛔ **경화 대상 밖**(§7-A) — 헤드 요청으로 |

🟢 나머지 15편(zh 초과)은 전건 «경화가 더한 링크»로 확인했다 — `holdem-tiebreak-rules`가 `split-pot`을 EN4/zh6으로
부르는 식이다. **EN 1:1 원칙은 «대상·구조»에 걸리지 «zh가 더 걸면 안 된다»는 뜻이 아니다.**

---

## 6. 🔴 이 회차 최대 발견 — **`check:drift` ✅ 56/56은 «날짜 진실»이지 «내용 진실»이 아니다**

회차 6 마감 보고는 *「미러가 EN 마스터를 처음으로 완전히 따라잡았다(drift ✅ 56/56)」*로 닫았다.
그런데 §5의 기계 대조가 **EN에 있고 zh에 없는 내용 2건**을 잡았다.

| 자리 | EN이 언제 넣었나 | zh `masterUpdated` | 왜 게이트가 못 봤나 |
|---|---|---|---|
| `holdem-3bet` 요약 6번째 항목(3bet 팟 SPR 4.7 · 팟 2.6배 · `3bet-pot-cbet` 링크) | **2026-08-20** `cd9ec3c0` | **2026-09-06** | `masterUpdated` > EN `updated` → **게이트는 ✅로 본다** |
| `holdem-position-play` 过牌加注 문단의 `low-board-check-raise` 링크 | **2026-08-20** `1d0245c1` | **2026-08-27** | 같음 |

**둘 다 «날짜는 최신인데 내용이 안 왔다».** 프로토콜 §3-③이 경고한 바로 그 유형(id 세션 4 실증)이
**날짜를 올린 뒤에도** 살아남는다는 뜻이다 — 한 번 `masterUpdated`를 올려 버리면 **diff 방식으로는 영영 못 찾는다.**

🔴 **처방(다음 로케일·다음 트랙이 그대로 쓸 것)**:
**드리프트 판정에 «구조 계수»를 한 겹 더 붙여라.** 날짜도 diff도 못 보는 자리를
«EN에 있고 나에게 없는 링크·표 행·리스트 항목의 개수»는 본다. 값이 싸다(전 코퍼스 0.5초).
