# 세션 인수인계 (Session Handoff)

> **사용법**: 작업 마칠 때 이 파일 업데이트 → 다음 세션 시작 시 AI가 읽고 바로 이어받음
> 작업 시작 전 `CLAUDE.md` → `session-handoff.md` → `WORKLOG.md` 순서로 읽을 것.
> 🔴 **이 파일은 «지금 할 일»만 담는다.** 완료분은 `WORKLOG.md`에 적고 여기서 지운다 — 로그를 쌓지 마라.

---

## ▶▶▶▶▶▶▶▶▶ 새 세션 START HERE

### 지금 트랙 = **검수장 인계 4단계** (사장님 지시로 de보다 우선) · **2단계 A층 진행 중 — 8/15건 · 다음 = 묶음 5**

> ✅ 1단계(미러 전파 47곳) `a66ef97` · ✅ **2단계 묶음 1~4** = `3d51c1c` `2b4d6cd` `77de935` `0c35439`
> 🔴 **아직 push 안 했다.** 검수장이 단계 단위로 재검수하므로 **묶음 7까지 끝난 뒤 한 번에** 푸시한다.
> 남은 것은 **A층 묶음 5·6·7(7건) → 3단계 B층 7건 → 4단계 C층 42건**. 상세는 아래 「▶ 검수장 인계 트랙」.
>
> **de 경화 Session 3(Odds & Mathe 6편)은 그 뒤다.** 착수 준비는 이미 끝나 있다 — 필독 4종 통독 완료,
> 대상 6편 스코핑 완료(아래 「▶ de 경화」에 스코핑 결과를 적어 뒀다). **다시 조사하지 마라.**

```
npm run audit:hard -- --locale=<code>     # 편집한 로케일마다 «따로» (전체 초록은 진술이 아니다)
npm run build && npm run check:hreflang && npm run check:faq-schema && npm run check:drift
node tools/ledger-check.mjs --locale ja    # ← 검수 폴더에서 실행 (본문 변경 0편이어야 정상)
```

**이 세션의 본체** = **A층 묶음 5·6·7**(7건). 착수표·정정 방향은 아래 「▶▶ 2단계(A층)」에 있다.

### 🔴🔴 묶음 1~4가 증명한 것 — 5~7도 그대로 겪는다

1. 🔴🔴 **원장 수치를 옮겨 적지 마라. 네 번 재계산해서 세 번 어긋났다.**
   · 22 vs AKo 원장 53.0 → **52.65%** · 22 vs AKs 49.9 → **50.11%**(부호가 뒤집힌다)
   · 모노톤 플랍 셋 원장 29.0 → **34.44%** — 원장은 **아웃 카운팅의 하한**을 적었다
   · 버블 JJ $EV +0.9~+2.6 → **재현 불가**(모델 파라미터 미기재). 수치를 빼고 구조만 고쳤다
   ★**발원지는 FACTS §3 표 자체였다** — 12항목 중 10항목이 «무늬 겹침 없는 단일 조합»값이었다.
   검수장이 전면 정정했고 **신설 규칙**이 섰다: **핸드타입 주장 = 가중평균이 기본값. 단일 조합값 인용 금지.**
   (글이 무늬를 명시한 실례일 때만 그 조합값을 쓴다)
2. ★**검산 도구는 이미 있다. 다시 만들지 마라.** → `홀덤검수/tools/equity.mjs`
   `node tools/equity.mjs selftest` · `import { equity } from …` → `equity(["As","Ah"],["Ks","Kh"])`
   7장 평가기 + C(48,5) 완전열거. ICM·버블팩터가 필요하면 `WORKLOG.md` 2026-08-11 (2)의 식을 쓴다.
3. 🔴 **되읽기 패스가 매 묶음 «내가 만든» 결함을 잡았다**(묶음1 4건 · 묶음4 2건). 빼지 마라.
   ★**신설 문단이 특히 위험하다** — 사전엔 있고 **코퍼스엔 없는 낱말**이 새어 들어온다
   (de `Jamming-Range` · id 명사 `jam`=「시계」 · pt `range de jam`). **문단을 새로 쓰면 그 낱말을 grep해 확인하라.**
4. 🔴 **앵커는 언제나 인계 메모보다 많다.** 묶음 1~4 실측 **124곳**(메모 암시 64곳의 약 2배).
   한 주장이 **본문·표 행·체크리스트·FAQ·링크카드 캡션**에 흩어져 있다.
5. **de·id·pt는 FAQ 문항이 7개뿐**이라 「FAQ에도 있다」가 해당 없는 경우가 잦다.
   **없는 문항을 만들지 마라.** (`es 10 · zh 10 · en 9 · ja 9 · zh-hant 9 · de 7 · id 7 · pt 7`)
6. ⚙ **날짜는 `.mjs` 말고 Edit 도구로 바꿔라.** 외부 스크립트가 파일을 쓰면 하네스가
   **수정된 파일 전문을 컨텍스트에 덤프**한다(묶음 2·3·4에서 매번 터졌다). Edit는 안 그런다.

### ✅ Session 2 빚 청산 — 적대 QA **4렌즈** 완료 (네이티브·교열·딜러·SEO)

빚이던 ⑤단계를 돌렸고 결과를 전부 반영했다(`e52ac4a` 정확성 · `a9494ea` 구조).
🟠 **남은 것 하나**: `holdem-hand-rankings` **1인칭 EN 12 : DE 3** — EN 경험담이 미러에 안 왔다.
소급할지는 판단이 필요하다(**없는 사실을 지어내지 말 것**).

### 🔴🔴 Session 3~6이 반드시 가져갈 것 — 4렌즈가 실제로 증명한 5가지

1. **§13 계산에 사람 시간을 쓰지 마라.** 세 렌즈가 «독립적으로» 전건 재계산했고
   (네이티브 16 · 딜러 36 · SEO 전건) **오류 0건**, 기계 게이트와 일치했다.
   그 층은 이미 막혀 있다 — 사람은 아래 2~4에만 쓴다.
2. 🔴 **새 결함 유형: 「계산은 맞는데 홀덤에서 그 상황이 성립 불가」.**
   게이트는 베스트5를 «주어진 카드로» 검산할 뿐, 그 배치가 **가능한지**는 안 본다. 이번에 4건:
   · 서로 다른 무늬 플러시 2개(보드 5장에 두 무늬가 동시에 3장 불가) ×2
   · `K-K-K-A-2` vs `K-K-K-Q-J`(둘째 킥커가 2면 보드가 KKK22 → 풀하우스)
   · 「vier Asse를 이기는 건 SF **와 Royal**」(에이스가 넷 다 나갔으니 로열 불가)
   ★**형제 글이 그 불가능을 정확히 반박하는 문장을 이미 갖고 있던 경우가 2건**이었다(자기모순).
   → **다음 게이트 확장 1순위**(아래 「게이트 확장 후보」).
3. 🔴 **D유형은 계산이 아니라 «조언»이 틀린다.** `split-pot`의 팁이 EV를 거꾸로 가르쳤다
   (확정 촙 콜의 순이득 = **+P/2**인데 폴드를 권했다 — 매몰비용 오류).
   **수학 검산 렌즈를 몇 개 돌려도 안 나온다.** 딜러 렌즈만 잡았다.
4. 🔴 **독일어는 «독일어 낱말로 쓴 영어 문장»에서 샌다.** 오독 3건이 나왔다 —
   `jede Straße`(street를 족보 Straße로! 하필 Straße를 가르치는 클러스터의 도입부) ·
   `unentschiedener Spieler`(=「우유부단한」) · `mieten`(=임차하다).
   어휘 교정이 아니라 **문장 단위 재작성**이 필요한 자리다.
5. ★**렌즈끼리 서로를 고친다 — 한쪽만 들었으면 다른 쪽 결함이 굳었다.**
   오드칩에서 교열·SEO는 「형제 글처럼 `unentschiedenen`을 넣어라」, 네이티브는 「그 단어가 오독」이라 했다.
   → `ersten am Chop beteiligten Spieler`로 양쪽 충족. **렌즈 수가 아니라 렌즈 «종류»가 성과를 가른다.**
   ⚠ 그리고 **검수자도 틀린다**: 딜러가 「네가 뽑는 건 Neun-hoch Flush」라 했으나 **리버가 A♥면 A-하이**다.
   취지만 살려 표현을 고쳐 반영했다.

**렌즈 프롬프트 4장치**(빼면 오탐이 쏟아진다): 의도적 제외 목록 · 「문제없으면 문제없다고 써라」 ·
확신도 표기 필수 · 「렌즈 밖이어도 사실이 이상하면 보고」. 교열 렌즈엔 **diff**와
「①원본 유래 / ②이번 편집 유래」 구분을 요구한다(안 하면 「내가 만든 오류」만 올라온다).

### 상태 (2026-08-11 마감 · **A층 묶음 1~4 후에도 지표는 전부 불변** — 실측 재확인)

| 지표 | 값 |
|---|---|
| build | **69 blog + 457 intl** (불변) |
| `audit:hard` | 25개 로케일 `--locale` 전부 🔴 **0건** |
| `check:de-style` | 셀프테스트 **99/99** · 42편 🔴 **1건** = `apt-incheon`(Session 6 몫) |
| `check:faq-schema` | 셀프테스트 **16/16** · **455/455편 · 25개 로케일 🔴 0건** ✅ |
| `check:hreflang` | 481페이지 · 50세트 · 🔴 0건 |
| 미러 드리프트 | **90** · 추적불가 **81** (✅ **239**) |

⚠ **드리프트 90은 «옳은» 숫자다.** FAQ 스키마 복구로 EN 2편의 `updated`를 올렸는데
**미러의 masterUpdated는 일부러 안 올렸다** — 그 변경은 형식(FAQ 마크업)뿐이고
`texas-…-beginners`의 **내용 재동기화는 미결**이라, 올리면 진짜 드리프트가 숨는다.
「판 전체 재동기화」 트랙에서 한 번에 정리할 것.

⚠ **08-10 (15)에서 EN `holdem-tournament`의 `updated`를 올렸지만 세 수치는 그대로다** — 미러 4편의
`masterUpdated`를 **같은 커밋에서 함께** 올려 드리프트 오탐을 만들지 않았고, de·id·pt는 원래 추적불가였다.
이 글은 8개 로케일에만 있다. **EN 마스터의 `updated`를 올릴 땐 미러 상향을 같은 커밋에 묶어라.**

**끝난 트랙**: KO 잔여 = S5 하나(8/16 잠금) · 다국어 B1~B4 · 다국어 FAQ 스키마(25편 전건) ·
de 표기 42편 일괄(§7-10 확정) · **de Session 1~2(적대 QA 4렌즈 포함 완결)**.
경위는 `WORKLOG.md` 2026-08-10 (1)~(13).

### 🔧 게이트 확장 후보 (다음에 게이트를 손댈 때)

| 검사 | 왜 | 난이도 |
|---|---|---|
| 🔴 **«구성 가능성» 검사** | 위 2번. 베스트5 계산은 맞는데 **그 배치가 홀덤에서 불가능**한 예시를 4건 놓쳤다. 최소한 「서로 다른 무늬의 플러시 2개」는 **정규식으로도 잡힌다**(보드 5장 제약) | 낮음~중 |
| **H2↔FAQ 축어 중복** | 정본 §7-6인데 기계가 안 본다. Session 1·2에서 **11쌍**이 나왔다(tiebreak 4·kicker 2·flush 3·reading 2) | 낮음 |
| **질문형 H2 비율** | `audit:hard`는 KO만 본다. de 필라가 **10%**였는데 아무도 몰랐다 | 낮음 |
| **직답 블록 유무** | `hand-rankings`·`kicker`에 없었다. ★`tldr`은 **화면에 렌더되지 않는다**(JSON-LD 전용) — 있다고 착각하기 쉽다 | 낮음 |
| **이미지 title 누락** | 없으면 `<figcaption>`이 안 생긴다. `reading-the-board` 3장이 그랬다 | 낮음 |

---

## ▶ de 경화 — Session 2~6

### 클러스터별 (승인된 순서)

| # | 클러스터 | 편 | 이 순서인 이유 | 상태 |
|---|---|---:|---|---|
| 0 | 문체 게이트 | — | 검사 없이 42편을 손대면 같은 실수를 반복한다 | ✅ |
| 1 | **Regeln** | 6 | `poker regeln` 90,500/월 · 필라라 용어가 여기서 확정 | ✅ |
| 2 | **Handreihenfolge** | 6 | 대결표 7→14행 · FAQ 18문항 소급 · masterUpdated 3편 · **적대 QA 4렌즈** | ✅ |
| **3** | **Odds & Mathe** | **6** | `Faustregel` 표기 최적화(15편에 이미 있는데 최적화만 안 됨) + 국면형 H2 | ◀ **다음** |
| 4 | Strategie | 8 | `poker strategie` 1,300/**LDA 6** — 볼륨 대비 SERP 최약 | |
| 5 | Glossar | 6 | 토너먼트·온라인 약어표(ATS·ITM·GTD·PLO·Cap·Overlay) | |
| 6 | Turniere | 9 | `/de/tournaments`와 상호 연결 + 라이브이벤트 4편 · **apt-incheon 🔴 3건이 여기** | |

### ★ Session 3의 본체 = §5-A #5 — 「이름표」 유형, **세 번째 반복이다**

`holdem-probability`의 H2를 **국면형(nach dem Flop)·Faustregel형**으로 재배치하는 일이다.
상위어는 LDA 27~34 벽이고 국면형·계산형은 **LDA 9~15**다. `Faustregel`은 **15편에 이미 있는데
최적화만 안 됐다**(정본 §7-5에 실증). 콘텐츠는 있고 **독일 검색자가 쓰는 낱말만 없는** 상태 말이다.
실적: Session 1 `blinds tabelle` · Session 2 `Kombinationen` 4,400/LDA12 · `Wertetabelle` 260 ·
`karten reihenfolge` 880 — **≈3,000/월을 H2·표 이름 교체만으로** 열었다.
→ **착수하면 먼저 「이미 있는데 이름이 없는 것」부터 훑어라.** 새로 쓰는 것보다 ROI가 높고 §13 리스크가 0이다.

### ✅ Session 3 착수 준비는 끝나 있다 (2026-08-11 · 다시 조사하지 마라)

필독 4종 통독 완료(`translation-terms-de.md` §7 · `de-core-volumes.md` **727행 전문** ·
`de-dach.md` · `market-profile/de.md`). 베이스라인: `check:de-style --cluster=Odds` **🔴 0 · 🟠 3**
(D9 `masterUpdated` 없음 = `outs`·`equity`·`drawing-odds`) · `audit:hard --locale=de` 42/42 🔴 0.

**대상 6편을 열어 본 결과 — 핸드오프가 적어둔 「Faustregel 표기」보다 손볼 게 더 있다:**

1. 🔴 **질문형 H2 비율이 클러스터 전체에서 낮다** — `probability` **0/8** · `drawing-odds` **0/6** ·
   `equity` 1/8 · `implied-odds` 2/8 · `pot-odds` 2/9 · `outs` 2/7.
   Session 2의 `hand-rankings`(10%)와 같은 상태이고, **`audit:hard`는 이 항목을 KO에만 적용**해서 안 걸린다.
2. 🔴 **클러스터 안에서 용어가 갈라져 있다** — `probability`는 「Regel der **2 und 4**」,
   `pot-odds`·`outs`는 「Regel der **4 und 2**」. 정작 독일 검색어는 **`Faustregel`**(30/LDA15)이고
   15편에 그 낱말이 이미 있는데 **H2·FAQ가 안 쓴다.** 「이름표」 유형 3번째 반복이 맞다.
3. **국면형 축이 통째로 비었다** — 상위어 `poker wahrscheinlichkeiten` 480은 **LDA 27~34 벽**인데
   `nach dem Flop` 계열은 **LDA 9**. `probability`의 H2 8개 중 국면형 **0개**.
4. **H2↔FAQ 준중복이 3편**(전부 Faustregel 문항). 정본 §7-6 위반이고 기계가 안 본다.
5. `masterUpdated` 3편은 **드리프트를 닫은 뒤에** 찍는다(Session 2 교훈 3).

**세션당 고정 6단계**: ①정본 재확인 → ②**기계 게이트 먼저** → ③편집
→ ④**되읽기 패스** → ⑤**독일 네이티브 적대 QA**(2렌즈: 네이티브 + 교열) → ⑥빌드·게이트·커밋·WORKLOG

### 🔴🔴 착수 전 «리서치 통독»은 선택이 아니다 (2026-08-10 Session 2에서 대가를 치름)

Session 2는 `de-core-volumes.md` **730행 중 ~150행만** 읽고(핸드오프가 가리킨 곳만) 진행했고,
규정된 **「글마다 WebSearch PAA」를 한 번도 안 돌렸다.** 결과:

- **`pokerblätter`(720/LDA20, 독일 고유 표기)가 본문에 0회**였다 — 태그에만 있었다. 경쟁사 5곳은 제목에 쓴다.
- **PDF 어포던스 누락** — `poker reihenfolge pdf` 260/LDA11 등 **3개 시드에서 반복** 확인된 수요인데다,
  SERP 상위 4곳(PokerOlymp 포함)이 전부 PDF를 걸고 있었다. **de 글은 「영어 PDF」를 걸고 있었다.**
- `poker kombinationen` **4,400**/LDA12 · `straße bube dame könig as 2` 40/LDA5 등 누락.

★**교훈: 인계서의 「§5-A 실행 항목」은 리서치의 «요약»이지 대체물이 아니다.**
클러스터 착수 전 **키워드 뱅크를 해당 클러스터 관점에서 통독**하고(다른 클러스터 시드에도 교차 항목이 있다 —
족보 재료가 시드③ `poker regeln`에 있었다), **글마다 WebSearch로 현지 SERP를 실제로 본다.**
SERP는 볼륨표가 못 주는 걸 준다 — 경쟁사가 **무엇을 제공하는지**(PDF)와 **어떤 낱말을 쓰는지**(Pokerblätter·„Poker"=Vierling·Skat 대비).

**▶ 착수 전 읽을 것**(순서 고정):
1. `docs/translation-terms-de.md` ★**정본** — §7 실측 문체 · **§7-9 Bet/Raise 성**(재조사 금지)
2. `docs/keyword-bank/de-core-volumes.md` — §5 **실행 항목 표**(어느 글에 뭘 넣을지 이미 정해져 있다)
3. `docs/local-voice/de-dach.md` · `docs/market-profile/de.md`
4. 게이트를 손대게 되면 `docs/de-style-gate-spec.md` §6·**§6-B**(오탐 원인 **12종** + 채택 안 한 규칙)

### 🔴 Session 1이 값비싸게 배운 것 — 그대로 반복될 것들

1. **「게이트 🔴 N건」은 «게이트가 넓은 만큼만» N건이다.** Session 0의 6건은 틀린 값이 아니라 **좁은 값**이었다 —
   규칙을 넓히자 같은 42편에서 성 오류 **24곳** · 인용부호 **59곳**이 더 나왔다(셀프테스트 61→87).
   **새 결함 «유형»을 만나면 규칙을 넓히고 셀프테스트를 올려라.**
2. **되읽기 패스에서 «내가 새로 만든» 오류를 게이트가 못 잡으면, 그게 게이트를 넓히라는 신호다**
   (내가 써 넣은 `eine ganze Orbit` → `Orbit` 목록 추가 + 형용사 슬롯 신설).
3. 🔴🔴 **게이트는 «성»을 보지 «격»을 안 본다.** 여성 `eine Raise`는 1·4격이 동형이라 격 오류가
   **가려져 있었고**, 게이트 지시대로 `ein Raise`로 고치는 순간 1격이 노출돼 동격의 격이 깨졌다.
   **일괄 치환 뒤에는 그 문장을 사람이 다시 읽어라.** 생략 명사도 같다
   (`dein Re-Raise ist die dritte **[Bet]**` — 주어를 보고 `der dritte`로 고치면 뜻이 바뀐다).
4. **기계 게이트가 원리상 못 보는 것에 사람 시간을 써라.** 네이티브 QA가 잡은
   `mehrstöckige Aggression`(= 「여러 «층»으로 된」 = 건물)이 그 예다. 게이트로는 영원히 안 걸린다.
5. **매 실행 «미판정» 줄을 읽어라** — `prüfen` 16곳 · 천단위 쉼표 2곳 ·
   **`deine/keine + 남성 명사`**(복수와 동형이라 기계가 못 본다. `deine Re-Raise` 2곳은 사람이 잡았다) ·
   **관사 없는 형용사**(`als kleine Re-Raise`).

**드리프트의 정체를 찾는 법(Session 1에서 확립)** — 셋을 순서대로 돌리면 나온다.
① **섹션별 줄 수** EN↔DE 대조(어긋난 섹션만 펼쳐 읽는다) ② **1인칭 문장 수** 대조
(EN 07-19가 넣은 **경험담이 안 온 게** 드리프트의 실체였다) ③ **FAQ 질문 목록** 대조.
이 셋으로 `game-order` 4곳·`texas-…-beginners` 4곳·`blind-meaning` 2곳·`showdown-rules` 2곳·
**`all-in-rules` 2곳(masterUpdated가 이미 올라가 있던 편!)**을 잡았다.

### 🔴 Session 2가 값비싸게 배운 것 — Session 3~6이 그대로 겪을 것

1. 🔴 **「신설하라」고 적힌 항목이 이미 있었다.** 대결표는 7행이 이미 있었고, 핸드오프만 「신설」이라고 적혀 있었다.
   **착수 전 대상 파일을 여는 것만으로 세션의 정의가 바뀐다.** 인계서를 작업 지시서로 읽지 마라.
2. 🔴 **섹션 수가 EN과 맞아도 동기화된 게 아니다.** 드리프트 4편 모두 **섹션 수는 정확히 일치**했고,
   **FAQ 질문 목록 대조만이** 18문항 누락을 잡았다. 3단(섹션·1인칭·FAQ) 중 **FAQ 목록이 주력**이다.
3. 🔴 **masterUpdated는 드리프트를 «닫은 뒤에» 찍는다.** 먼저 찍으면 그 날짜가 거짓 증거가 되고,
   다음 세션은 그 글을 «최신»으로 믿고 건너뛴다.
4. **개수를 맞추려고 준중복 FAQ를 넣지 마라.** tiebreak는 14:15로 남겼다 — EN 쪽이 자기 안에서 중복이다.
5. **H2↔FAQ 축어 중복은 게이트가 안 본다.** 이번에 6쌍이 **완전 일치**였다(tiebreak 4·kicker 2).
   클러스터를 열 때마다 **H2 목록과 FAQ 목록을 나란히 출력해 눈으로 대조**하라.

### §5-A 실행 항목 — 남은 것만

| 대상 | 할 일 | 세션 |
|---|---|---|
| `holdem-probability` | H2를 **국면형**(nach dem Flop)·**Faustregel형**으로 — 상위어는 LDA27~34 벽, 국면형은 9~15 | **3** |
| `holdem-glossary` | **「Turnier- & Online-Abkürzungen」 표** — ATS·ITM·GTD·PLO·Cap·Overlay·Rebuy(전부 LDA 5~35) | 5 |
| 전 42편 | **§7 문체 적용** — 이게 경화의 본체다 (성·인용부호는 ✅ 42편 완료) | 전 세션 |

🔴 **채택 금지**(근거 있음): 합법성 판정 · 봇/RTA · 책 PDF · Würfelpoker/Kniffel · rote Karten(축구) · 소매 · 타언어.

**불변**: §13 수치·카드·계산 / slug / 이미지 / 내부링크 구조(EN 1:1)
**masterUpdated**: 없는 24편은 채운다. 단 **`showdown-rules`·`texas-…-for-beginners`는 올리지 마라** —
「판 전체 재동기화」 미결 표시다. ⚠ **de 쪽 델타는 Session 1에서 이미 다 닫았다**(그 트랙 실행 시 de는 제외 가능).

### 🟠 이월된 자잘한 것

- **FAQ↔H2 축어 중복** — `showdown-rules`에 준일치 5건 남음(EN 마스터에서 상속).
  정본 §7-6은 중복 금지다 → **삭제가 아니라 «다른 실검색 문형»으로 재구성**할 것.
  (완전 일치 2건 + `blind-meaning` 1건은 Session 1에서, tiebreak 4·kicker 2는 Session 2에서 해소)
- **`masterUpdated` 없는 글 21편** — Session 2에서 24 → 21. 클러스터를 열 때 그 6편의 드리프트를
  3단(섹션·1인칭·**FAQ 목록**)으로 닫고 찍으면 세션마다 자연히 줄어든다.
- 🖼 **`holdem-split-pot-suit-equals.webp` 교체 필요** — 이미지가 「Pik-Royal-Flush = Herz-Royal-Flush」인데
  그 장면은 홀덤에서 **이중으로 불가능**하다(로열 하나가 보드 5장을 다 먹는다). 본문은 고쳤고 alt도 그에 맞춰야 한다.
  → 「**같은 스트레이트, 다른 무늬 = Chop**」으로 다시 만들 것. 이미지 제작이라 별도 작업.
- **적대 QA가 낸 «낮음» 등급 소소한 어법 ~12건** — `totes Gewicht`·`Nachbarschaft`·`Mobiliar`·
  `Ein-Zeilen-Definition`·`nur zum Kicken`·`Chip Rest`(→`Chip-Rest`)·`vs`→`vs.`·`#1-3`→`#1–3` 등.
  급하지 않다. **그 파일을 다음에 열 때 함께** 처리(전용 커밋 금지).
- **SEO 내부 논리가 본문에 노출** 3곳 — `split-pot`「Beides wird gesucht」·`kicker`「die Konkurrenz in
  Absätzen vergräbt」·「von einem schwammigen unterscheidet」. 독일 전략 매체 톤과 어긋난다(실독 12편에 이런 문장 0).

---

## ▶ 검수장 인계 트랙 — **4단계 중 1단계 완료**

검수장(`C:\Users\하봄\Downloads\홀덤검수`)이 ja 38편 1,380주장을 판정했다. WRONG 9건은 `0a6f5c0`으로,
그 미러 전파 47곳은 `a66ef97`으로 끝났다. 남은 것은 **RISKY 64건(A·B·C층)**이다.
푸시하면 검수장이 해시 변화를 감지해 재검수한다(확인: `node tools/ledger-check.mjs --locale ja`).

### 🔴 단계별 실측 규모 — 「건수」가 아니라 «로케일 × 건수»로 재라

| 단계 | 층 | 주장 | 로케일 분포 | 실제 수정 지점 | 상태 |
|---|---|---:|---|---:|---|
| 1 | 확정 오류 미러 전파 | 6종 | 6편 전부 8개 로케일 | **47곳**(실측) | ✅ `a66ef97` |
| **2** | **A층 — 독자 손실** | **15** | 11편 전부 8개 로케일 | **124곳**(묶음1~4 실측) | ◀ **8/15 · 묶음 5부터** |
| 3 | B층 — 일본 관할·법률 | 7 | `tournament-vs-cash-game`만 **13개** | ≤71 (**ja 단독 가능성 큼**) | |
| 4 | C층 — 사실·정의·모순 | 42 | 🔴 `game-order`·`betting-actions`·`texas-…-beginners`가 **25개 로케일** | ~**418곳** | |

**C층이 큰 건 건수가 아니라 저 3편이 25개 언어에 깔려 있어서다** — 그 3편에 걸린 5건만으로 125곳이다.
메모: `홀덤검수/reports/fix-handoff-ja-risky64-2026-08-10.md`(A·B·C층 전부 · 정정값과 계산 근거 포함).

### 🔴🔴 1단계가 값비싸게 배운 것 — 2~4단계가 그대로 겪는다

1. 🔴 **인계 메모의 편수는 «검수장이 본 범위»지 «결함의 범위»가 아니고, 그나마도 과소하다.**
   「43~45곳」이 실측 **47곳**이었다(`positions`가 최대 12 → **13** — es가 본문 + FAQ **2개** = 3곳).
   네 배치 연속 과소다. **착수 전에 앵커로 전 로케일을 grep해 직접 세라.**
2. 🔴 **메모의 슬러그를 그대로 쓰지 마라 — KO 것이 섞여 있다.** A층·C층의
   `when-to-fold-preflop-holdem`은 **KO 슬러그**고 미러는 **`holdem-when-to-fold`**다.
   미러는 KO 번역본이 아니라 **EN 파생본**이라 슬러그가 갈린다.
3. **「직접 확인 필요」로 미확정 처리된 자리도 대개 결함이다.** `3bet` zh-hant가 그랬다
   (문구만 「後面沒人下注」로 달랐다). 미확정을 «해당 없음»으로 읽지 마라.
4. **이미 옳은 로케일이 실제로 섞여 있다.** `rake` 캡 인과의 `zh` · `positions`의 `de`(단락 자체가 없음).
   **기계 치환을 했으면 둘 다 망가뜨렸다.**
5. ★**새 문장을 만들지 말고 형제 글 표현을 복사하라.** de는 street를 **`die Street`**로 쓴다 —
   Session 2가 대가를 치른 `Straße` 오역을 피할 근거는 **코퍼스에만** 있었다. es `calle` · pt `streets` ·
   zh/zh-hant는 `bad-beat` 캡션의 「翻牌后…最后行动」.
6. ★★**금지 규칙을 만나면 그 «이유»를 먼저 확인하라.** 「ja를 건드리면 원장 해시가 깨진다」는
   **본문에만 해당**한다 — 해시는 `tools/post-source.mjs`의 **desc+tldr+content**만 본다.
   그래서 ja `masterUpdated`만 올려 **드리프트 오탐을 막으면서** 재검수는 유발하지 않았다(본문 변경 0편 실측).
   규칙을 글자대로 따랐으면 ja 6편이 가짜 드리프트로 남았을 것이다.
7. **`updated`는 내용이 실제 바뀐 편만, `masterUpdated`는 필드가 이미 있는 곳만.**
   없는 자리에 새로 박으면 🟠 추적불가를 거짓 초록으로 덮는다.

### ▶▶ 2단계(A층) — **묶음 5부터 시작한다**

지시서 2종을 **둘 다** 읽어라. 두 번째가 갱신본이고 우선한다.
① `홀덤검수/reports/fix-handoff-ja-risky64-2026-08-10.md` 「A층」(원 지시)
② 🔴 `홀덤검수/reports/fix-handoff-facts3-and-bundle4-7-2026-08-11.md` (**FACTS §3 정정 통지 + 묶음 4~7 갱신 지시**)
완료분 보고: `홀덤검수/reports/fix-done-a-layer-bundle1-3-2026-08-11.md`

**11편 전부 8개 로케일**(`de en es id ja pt zh zh-hant`) — 한 건이 8곳이 아니라 **평균 15곳**이다.

| 묶음 | 편 (미러 슬러그) | 건 | 상태 · 메모 |
|---|---|---:|---|
| 1 | `holdem-short-stack` | A1 #26 · A2 #25 | ✅ `3d51c1c` — **28곳** |
| 2 | `holdem-when-to-fold` | A3 #24 · A4 #27 | ✅ `2b4d6cd` — **32곳** |
| 3 | `holdem-icm` | A10 #2 · A11 #20 | ✅ `77de935` — **24곳** |
| 4 | `holdem-bubble` | A12 #20 · A13 #22 | ✅ `0c35439` — **40곳** (+FACTS §3 후속 2곳) |
| **5** | `holdem-split-pot-rules` #12 · `holdem-fish` #13 | 2 | ◀ **다음.** 찹 확정 시 **폴드 금지**(EV `p(찹)·P/2 − p(패)·B`) · LAG/TAG **분리**(「벳을 존중」은 TAG에만, LAG엔 콜·3벳 확대). **두 건 다 표 행 + FAQ에 반복** |
| 6 | `holdem-card-counting` #13 · `holdem-probability` #45 | 2 | 같은 결함(×4를 1장 스팟에). **플랍→턴 9/47 = 19.15% · 턴→리버 9/46 = 19.57%** — 🔴 **일괄 치환 금지**(국면이 다르면 분모가 다르다). ×4는 「올인이거나 리버까지 볼 때」만. 반복 자리 = `probability` 예제 본문+표 캡션 / `card-counting` 요령 목록+FAQ |
| 7 | `holdem-strategy` #9 · `holdem-rake` #32 · `holdem-drawing-odds` #17 | 3 | strategy: 예외에 **BB 디펜스** 추가(2.5bb 오픈 → 필요 ≈27%, 표준 디펜스의 30~50%가 플랫콜) + **본문↔FAQ 정합**(FAQ는 이미 셋마이닝 콜을 인정한다). rake: 「상위 레이트로」에 **뱅크롤·에지 조건**(본문이 이미 「같은 실력·같은 에지」를 전제했다). drawing-odds: 35%는 코인플립 아님 → **「3회에 1회에 가깝다」** |

**착수 시 지킬 것**

**묶음마다 고정 절차** (1~4에서 이대로 돌려 🔴 0을 유지했다)

1. 원장 근거 읽기 — `홀덤검수/ledger/ja/<slug>.md`의 해당 번호 행(계산·출처가 그대로 있다)
2. **수치를 직접 재계산** — `홀덤검수/tools/equity.mjs`. 원장 값은 «참고»지 «인용원»이 아니다
3. 앵커로 **8개 로케일 전수 grep** → 로케일별 개별 판정(**기계 치환 금지**)
4. **EN 마스터 먼저** 고치고 → 미러 7종. 새 문장을 만들지 말고 **형제 글 표현을 복사**
5. **되읽기 패스**(§위 3번) → 6. 게이트 → 7. `updated`/`masterUpdated` → 8. 커밋
   - `npm run audit:hard -- --locale=<code>` **8종 전부** · `npm run check:de-style` · `npm run build`(69+457 불변)
   - `updated`는 내용이 바뀐 편 전부 / `masterUpdated`는 **필드가 이미 있는 곳만**(없는 데 새로 박지 마라)
   - EN 마스터의 `updated`를 올리면 미러 `masterUpdated` 상향을 **같은 커밋에** 묶어라(드리프트 오탐 방지)

- 🔴 **A층은 «조언»을 다시 쓰는 일이라 1단계보다 훨씬 느리다.** 묶음 하나가 8개 로케일 × 3~5앵커다.
- 묶음 하나 끝날 때마다 커밋하되 **푸시는 2단계 전체(묶음 7)가 끝난 뒤 한 번**.
- 🔴 **「이미 옳은 로케일」이 섞여 있다** — 1단계 2곳 · 묶음 1에서 de·id·pt는 해당 FAQ 자체가 없었다.
- 마무리: `ledger-check --locale ja`로 **무효가 A층 15건과 1:1**인지 확인하고
  `홀덤검수/reports/fix-done-…` 메모를 갱신해 올린다(묶음 1~3분은 이미 올렸다).

### 🔴 이 트랙이 증명한 것 — 다음에 정정 지시를 받으면 그대로 적용할 것

1. 🔴🔴 **한 사실이 8개 로케일에 복제돼 있으면, 한 로케일만 고치는 건 «고친 것»이 아니라 «어긋나게 만든 것»이다.**
   검수장은 **ja 원장만** 갖고 있어 ja만 잡았다. WPT Seminole 실례 2건은 EN 마스터 포함 **7개 로케일에 그대로** 살아 있었다.
   → **정정 절차에 「미러 전수 grep」을 붙여라.** 지시서의 편수는 «검수장이 본 범위»지 «결함의 범위»가 아니다.
2. **grep 히트를 그대로 믿지 마라.** `181` 히트 중 `wsop-2026-guide`는 준우승 상금 **$256,181**이었다.
   KO 트랙엔 이 실례가 아예 없었다. 실제 대상은 정확히 7편이었다.
3. 🔴 **오류가 「계산」이 아니라 「메커니즘 서술」에 몰린다** — 9건 중 **6건이 결론은 맞는데 이유가 틀린** 문장이었다
   (버튼이 액션을 닫는다 / 두 로열은 다른 무늬로만 / 캡이 «안 오른다»).
   **숫자를 보는 게이트가 원리상 못 잡는 층**이다. `audit:hard --locale=ja`는 이 9건 전부에 초록이었다.
4. **같은 오류가 본문과 FAQ 두 곳에 박힌 경우가 있다**(`positions` 187행 + 218행).
   한쪽만 고치면 반대쪽이 남는다 — **정정 전에 같은 주장을 글 전체에서 grep**하라.
5. ★**`masterUpdated`는 「있는 로케일만」 올린다.** 필드가 없는 곳에 새로 박으면
   **EN 경화를 흡수했는지 모르는 채 🟠 추적불가를 ✅로 덮는 것**이다. **추적불가가 거짓 초록보다 낫다.**
   (Session 2 교훈 3번과 같은 원리 — 먼저 찍으면 다음 세션이 그 글을 「최신」으로 믿고 건너뛴다)

### 이 트랙에 함께 얹을 잔여 3건 (전용 커밋 금지 — 해당 파일을 열 때 처리)

| # | 할 일 | 메모 |
|---:|---|---|
| 1 | `holdem-game-order` **번 카드 내용을 ja·id에 보충** → **그다음** `masterUpdated: 2026-07-19` | EN이 07-19(`1bb851c`)에 넣었는데 안 왔다. ja는 본문에 「バーン」이 **0회** — FAQ 한 줄이 아니라 **개념이 통째로** 빠졌다. バーンカード는 실검색어. **4단계 C층 `game-order #31`과 같은 파일이다 — 함께 열어라** |
| 2 | `holdem-blind-meaning` ja | 같은 상태(`updated` 07-12 < EN 07-19 · `masterUpdated` 없음). ja 「추적불가 2」가 이 둘이다 |
| 3 | 🪶 `holdem-tournament` **de·id·pt의 `masterUpdated`** | EN 07-19 대조를 한 번 하면 참값을 넣을 수 있다. **4단계 C층 `tournament #11·#13`과 같은 파일이다** |

### 🟠 FAQ 문항 드리프트 — 「개수 일치」를 목표로 삼지 마라

`texas-…-beginners`는 **EN 12 : ja 13**으로 **ja에만 있는 초과분**이다(「チップなしでも遊べますか」).
현지 롱테일을 EN에 없어도 넣는 건 **이미 확립된 방침**이라 드리프트가 아니라 의도된 현지화다 → **그대로 둔다.**

같은 글 FAQ 수가 로케일마다 흩어져 있다 — `es 12 · zh 12 · de 8 · zh-hant 8 · en 8 · ja 7 · pt 7 · id 7`.
→ 「판 전체 재동기화」를 열면 **«EN에 있는데 현지에 없는 항목» 단방향으로만** 훑어라. **초과는 정상이다.**

---

## 🔴 사장님 판단 대기

### ⓪ ~~검수장 ja RISKY 64건~~ → **2026-08-11 진행 결정**

검수장 판정은 **64건 전부 수정 대상 · 기각(오탐) 0건**이었고, 사장님이 4단계 순서로 진행을 지시했다.
→ 진행 상황은 위 「▶ 검수장 인계 트랙」. **판단 대기 항목이 아니다.**

**남은 판단 하나**: 4단계 C층 `starting-hands-chart #13`은 UTG **13%** 수치와 핸드 목록이 충돌한다.
조합수로는 13%가 맞다(144콤보=10.86%). **수치를 고칠지 목록을 고칠지**는 SEO·전략 판단이라 그때 물을 것.

### ① ~~de 코퍼스 «42편 일괄» 2건~~ → **2026-08-10 확정·완료**

정본 §7-1 기준으로 **`„…“` · ` – `** 확정, 42편 일괄 적용(인용부호 300쌍 · 대시 2,369곳).
게이트 **D11·D12** 신설로 되돌아감을 막는다. 근거는 `translation-terms-de.md` **§7-10**(재조사 금지).

### ② de 콘텐츠 신설

칩 분배 계산기(`verteilung rechner` LDA**7** · 비대체 자산 · KO에도 없어 역수입 가능) ·
세금 FAQ 흡수(승인됨, 미착수 · BFH X R 8/21 1차출처 확보) ·
툴 브랜드 대응(gto wizard 2,400 · equilab LDA3) → **솔버 다국어화(2주 뒤) 시 함께 결정**.

### ④ 🪶 PDF 용지 규격 — ko·ja·zh도 A4 시장이다

`generate-beginner-pdf.mjs`는 `@page { size: Letter }`가 하드코딩돼 있었다(미국 규격).
2026-08-10에 **언어별 `pageSize`를 도입하고 de만 A4**로 했다 — 기존 4개 산출물은 건드리지 않았다.
ko·ja·zh를 A4로 재생성할지는 판단 사항이다(바이너리 diff · 이미 배포된 파일).
`node scripts/generate-beginner-pdf.mjs ko ja zh` 한 줄이면 되고, CONTENT에 `pageSize: "A4"`만 추가하면 된다.

### ⑤ 🪶 PDF 수요는 족보 밖에도 있다

`poker wahrscheinlichkeiten pdf`(시드⑨) · `poker strategie pdf` 10/18(시드⑩) · `poker starthände tabelle` 110/**LDA5**.
확률·전략·스타팅핸드 클러스터를 열 때 **그 글에 맞는 인쇄물**을 붙일지 검토할 것
(`poker-starting-hands-chart.pdf`는 이미 있고 de 글이 링크한다).

### ③ 다국어 «판 전체 재동기화» — 할지 말지부터

`holdem-showdown-rules`·`texas-holdem-rules-for-beginners` 2편.
EN이 07-19에 넣은 경험담·구조 변경이 19~20개 언어에 안 왔다.
«항목 소급»이 아니라 판 전체를 다시 맞추는 작업이라 배치 방식이 다르다.
**노출 미미**(전 언어 클릭 합계 2) — **정확성 명분뿐**이다.

착수한다면 `docs/multilang-sync-plan.md`가 본체이고, 아래 7개는 **B1·B2·B3에서 실제로 대가를 치른 것**이다:

1. **판정표를 믿지 마라 — 세 배치 연속으로 과소했다.** B1 E2 6→**10** · B2 C11 6→**23** · B3 「22개」→**23개**.
2. 🔴 **「틀린 패턴」을 정규식으로 찾지 마라.** 찾을 것은 «결함»이 아니라 **«그 주장을 담은 줄»**이다.
   결함을 검색하면 표기 편차에 전부 흘린다(`ja 80〜86%` 물결표 · `he בשולחן`). → 항목마다 해당 줄을
   **전 로케일에서 통째로 출력해 눈으로 판정**한다. `.mjs` 20줄이면 된다.
3. 🔴 **본문에 결함이 없으면 FAQ·tldr·표를 봐라.** B2 C5는 본문 24개 전부 정상인데 **FAQ만 22개가 틀려**
   같은 글 안에서 자기모순이었다. 고칠 땐 **그 로케일의 자기 본문 표현을 그대로 옮긴다**(새 번역 만들지 마라).
4. 🔴 **EN이 항상 정답은 아니다.** B2 C7은 **EN 마스터가 틀렸고** `zh`·`zh-hant`가 옳았다.
5. 🔴 **미러는 KO 번역본이 아니라 «EN 마스터 파생본»이다.** 슬러그도 다르다 —
   KO `holdem-pot-odds-calculation` ↔ 미러 `holdem-pot-odds` · KO `holdem-rules` ↔ `texas-holdem-rules-for-beginners`.
   **대조 전에 매핑부터 확인.**
6. 🔴 **치환은 `.mjs`로**(§16). 순서는 **드라이런 → 건수 검증(기대와 어긋나면 중단) → `--apply`**.
   ★08-09에 오적용을 두 번 막았고, 08-10 FAQ 배치 46편도 이 절차로 한 건도 안 어긋났다.
7. **masterUpdated는 «남은 항목이 없을 때만» 올린다.** ★역도 참이다 —
   **올라간 masterUpdated도 증거가 아니다**(`all-in-rules`가 07-25인 채 비어 있었다).

---

## ① KO 잔여 수정 — S5만 남았다 (8/16 이후)

`docs/kr-remediation-spec.md`가 본체다. S1~S4 정산·표준 수치·교훈은 그 파일 §4에.

| 세션 | 묶음 | 편 | 건 | 상태 |
|---|---|---:|---:|---|
| S1~S4 + 재판정 | — | 36 | 147 | ✅ 완료 |
| **S5** | `holdem-tournament-how-to-enter` | 1 | **11** | 🔒 **8/16 이후** — STALE #19가 APT 인천 종료로 만료 |

착수 전 `홀덤검수/reports/fix-handoff-remaining-2026-08-09.md` §2-보·§3을 읽을 것.

---

## ▶ 백로그 — 두 트랙 밖

| # | 할 일 | 메모 |
|---:|---|---|
| 1 | 프로덕션 E2E 마감 | 검수 세션에 `BASE=https://www.holdemmaster.com`으로 요청 |
| 2 | 다국어 정책 페이지 | `/privacy`·`/terms`·`/contact`가 한국어뿐인데 링크는 영어 라벨 |
| 3 | 리버 승부 해설 | 복기 표에 승률만 있다. "키커로 갈렸다"를 `score5`의 키커로 자동 생성 가능 |
| 4 | `es` 국기 판단 | **사장님 결정 필요** |
| 5 | 🪶 `holdem-implied-odds` 표 헤더 `(1 card)` → `(turn → river)` | 8종 전부. 세트 행 `~11.8% flop`만 국면이 어긋난다(값은 전부 맞음) — **전용 커밋 금지, 그 파일 만질 일 있을 때 얹을 것** |
| 6 | 🪶 FAQ 섹션이 아예 없는 2편(`ru`·`tr` 각 1편) | `check:faq-schema` 커버리지에 «미판정»으로 출력된다. **FAQ를 새로 쓸지는 SEO 판단** |
| — | ⏸ 펍 지역 페이지(`/pub/*` 9개 색인 탈락) | **사장님 지시로 보류** — 건드리지 말 것 |

### ⏰ 시한이 박힌 것

`docs/update-calendar.md`가 본체다. 현재 3건 —
**8/16 APT 인천 종료**(→ S5 착수 + 결과 아카이브 전환 + 미러 7종) ·
**10월 초 JOPT 오사카 바이인 공개**(글에 약속함) · **12/31 K-ETA 면제 만료**(한국 관련 전 언어 영향).

---

## ▶ 상시 함정 (트랙 무관 · 매 세션 유효)

🔴 **편집한 글은 `--slug=`(또는 `--locale=`)를 따로 돌려라.** 전체 게이트가 초록이어도
   «대상 목록 밖 글»은 **검사조차 안 된다.** 「N편 0건」은 그 N편에 대한 진술일 뿐이다.

🔴 **커버리지 출력을 반드시 읽어라.** 「시나리오를 못 잡은 글」의 0건은 **검증이 아니라 미검사**다.
   B1에서 **프랑스어 족보명이 하나도 등록돼 있지 않아 `fr` 6편이 «원리상 미검사»**였던 게 드러났다.
   **다른 소수 언어도 같은 상태일 수 있다.**

🔴 **「없다」를 보고하기 전에 «탐지 방법»부터 의심하라.** 08-10에만 두 번 당했다 —
   hreflang을 소문자로 grep해 「전 로케일 0개」라 오판했고(실제는 `hrefLang` camelCase라 정상),
   FAQ 스키마를 `.ts` **원본 텍스트**로 세어 「41편」이라 오보했다(파일이 **CRLF**라 정규식이 안 걸렸다).
   ★**소스 raw로 감사하지 마라 — 판정은 «파싱된 객체» 또는 «빌드 산출물»로.**

🔴 **「별도 검사가 있다」는 주석은 그 검사의 존재 증거가 아니다.** `canonical-check.mjs`가
   「다국어는 hreflang이 본다」고 적어 뒀는데 그 검사는 없었다. `audit:hard --schema`도
   「스키마를 본다」지만 **한국어 산출물만** 봤다. 커버리지 구멍은 «검사한다고 적힌 곳»에 숨는다.

🔴 **편집한 문단은 반드시 다시 읽어라 — 수정이 그 자체로 결함 주입 공정이다.**
   S2·S3·S4·재판정 **4연속**, 그리고 de Session 1에서도 **3종**(산수·격·생략명사)이 나왔다.

🔴 **한 글을 고치기 전에 «그 사실이 몇 개 로케일에 복제돼 있는지» grep하라.**
   한 로케일만 고치면 나머지와 어긋난다 — 08-10에 ja만 고쳐 **ja가 혼자 맞는 상태**를 만들었다가
   같은 날 7개 로케일(EN 마스터 포함)을 뒤따라 고쳤다. **인계서의 편수는 «검수장이 본 범위»지 «결함의 범위»가 아니다.**
   그리고 **grep 히트를 그대로 믿지 마라** — `181`이 준우승 상금 `$256,181`이었다.

🔴 **원장·인계서의 수치를 그대로 본문에 옮기지 마라. 직접 계산하라.**
   `ak-offsuit` AKo vs 22 = **47.35%**(원장 47.0) · B2 C7은 990조합 전수 열거로 **EN 마스터의 오류**를 잡았다.

🔴 **`19.6%`를 일괄 치환하지 마라.** 국면에 따라 맞는 값이다 — 플랍 9/47 = **19.15%** / 턴 9/46 = **19.6%**.

🔴 **CLAUDE.md §8의 KO 필라 슬러그 목록이 실제 파일과 다르다.** 링크 적을 땐 반드시 grep하라.
   `holdem-pot-odds`(X) → **`holdem-pot-odds-calculation`** · `holdem-position-play`(X) → **`position-is-everything-in-holdem`**

🔴 **PowerShell로 파일을 치환하지 마라**(CLAUDE.md §16). 문서는 Edit 도구, 스크립트는 `.mjs`.
   ★08-10에 `node -e "…$1…"`의 **`$1`을 PowerShell이 빈 변수로 먹어** 프런트매터가 뭉개지고
   **빌드가 깨졌다**(에러가 webpack 번들 덤프로 나와 원인 파악에만 시간이 들었다).
   ★**CRLF 파일을 `\n` 기준으로 치환하면 CR이 남는다** — 줄끝을 파일에서 읽어 복원하라.

🔴 **`vercel.json`에 buildCommand가 명시돼 있어 prebuild·postbuild 훅이 프로덕션에서 안 돈다.**
   그래서 `public/sitemap.xml`이 레포에 커밋돼 있다. 빌드 파이프라인을 손댔으면 라이브 curl로 확인하라.

⚠ **형제 글을 인용할 땐 원문을 복사하라**(카드·무늬까지). 그리고 **기존 글을 고치면 그 글을 인용한 글이 낡는다.**

---

## 📦 과거 로그는 아카이브로

**`docs/handoff-archive-2026-08-09.md`** — 2026-07-13 ~ 08-08 세션 로그 전체(약 3,980행).
완료 트랙의 방법론은 `docs/gto-solver-series-spec.md`·`docs/kr-remediation-spec.md` §4에 남겼다.
세션 로그는 `WORKLOG.md`가 본체다.
