# 세션 인수인계 (Session Handoff)

> **사용법**: 작업 마칠 때 이 파일 업데이트 → 다음 세션 시작 시 AI가 읽고 바로 이어받음
> 작업 시작 전 `CLAUDE.md` → `session-handoff.md` → `WORKLOG.md` 순서로 읽을 것.
> 🔴 **이 파일은 «지금 할 일»만 담는다.** 완료분은 `WORKLOG.md`에 적고 여기서 지운다 — 로그를 쌓지 마라.

---

## ▶▶▶▶▶▶▶▶▶ 새 세션 START HERE

> **상태 (2026-08-09 마감)** · build **69 blog + 457 intl**(불변) · KO 게이트 41/41편 🔴 0건 · selftest 76/76
> ✅ **KO 잔여 수정 트랙 확정** — S1~S4 완료(142건) + 재판정 3회차분 WRONG 4건 정정 + 미러 소급까지 끝났다.
> **남은 건 S5(11건) 하나이고 8/16 이후**다. 상세는 `WORKLOG.md` 2026-08-09 (5)~(8).

### ▶▶ 다음 세션은 여기서 시작 — **다국어 동기화 B2**

```
1. git pull
2. npm run check:drift          ← 지금 지점을 먼저 찍는다 (현재 드리프트 104편 · 추적불가 105편)
3. docs/multilang-sync-plan.md §0-D 「S2-B1 적용 완료」 표를 읽는다 (교훈이 거기 있다)
4. B2 = C7(hand-rankings 케이스 킹 반증) · C8(icm 3bb 버튼 이유) ·
        C10(starting-hands-chart 차트 평균 주장) · C11(texas-…-for-beginners 하이카드 설명)
   ★값 치환이 아니라 «문장 수정»이다. 미판정 3항목(C5·C6·C9)도 같이 판정한다.
5. 항목마다 전 로케일 원문을 다시 훑는다 → 편집 → audit:hard --locale=<code> → build → 커밋
6. C7을 끝내면 hand-rankings의 masterUpdated를 2026-07-19로 올린다 (일부러 보류해 둔 것)
```

---

## ② 다국어 동기화 — 지금 이 트랙이다

`docs/multilang-sync-plan.md`가 본체다. **재번역이 아니라 «패치»** — 숫자·계산·룰만 바꾸고
문장·훅·H2는 건드리지 않는다(현지 재저작 자산을 훼손하지 말 것).

| 배치 | 항목 | 파일 | 성격 |
|---|---|---:|---|
| **B2** | C7 `hand-rankings` · C8 `icm` · C10 `starting-hands-chart` · C11 `texas-holdem-rules-for-beginners` | ~19 | 문장 수정 |
| **B3(별도)** | E1 사이드팟 「× 콜러 수」 → 「각 콜러가 맞춘 만큼」 | 22 | 문장 재작성 — **한 문장씩 22개 언어라 별도 세션** |
| ~~B4~~ | ✅ 완료 `861e4c7` — `npm run check:drift` + `audit:hard --locale`에 상시 연결 | — | 기계화 |

**아직 판정 안 끝난 3항목**(B2 하면서 같이): `C5`(머킹 공개 요청권한) · `C6`(핸드포핸드 동시탈락) ·
`C9`(보드 플러시 개선 조건) — 셋 다 **`de`는 오류 확인**됐고 나머지 로케일만 미확인.

### 🔴 착수 전 반드시 알 것 5가지

1. **판정표를 믿지 마라.** B1에서 표가 **7건을 놓친 것**을 원문 대조로 잡았다 —
   `es` `+Con kicker` · `zh` `+用踢脚` · `ja` `80〜86%`(물결표가 `–`가 아니다). 스캔 정규식이 라틴 표기에 치우쳐 흘린 것이다.
   E2는 6→**10**, E3는 3→**5**, C4는 「ja엔 없음」이라 적혀 있었는데 **ja에 2곳** 있었다.
   → **표의 로케일 목록을 그대로 쓰지 말고, 항목마다 전 로케일 블록을 통째로 뽑아 눈으로 비교하라.**
   `dump-block.mjs`류 20줄이면 된다(스크래치패드).
2. 🔴 **미러 slug가 KO와 다르다.** 08-09 미러 소급에서 실제 결함이 **이름이 달라 안 보이던 곳**에 있었다 —
   KO `holdem-pot-odds-calculation` ↔ 미러 **`holdem-pot-odds`**, KO `holdem-rules` ↔ **`texas-holdem-rules-for-beginners`**.
   **슬러그 대조 전에 매핑부터 확인하라.**
3. 🔴 **미러는 KO 번역본이 아니라 «EN 마스터 파생본»이다.** KO에서 고친 결함이 미러엔 아예 없는 경우가 많고,
   반대로 EN 계열에만 있는 결함이 있다. **KO diff를 그대로 옮기려 들지 말고 로케일 원문부터 읽어라.**
4. 🔴 **한글·다국어 파일을 PowerShell로 치환하지 마라**(CLAUDE.md §16). Node `.mjs` + `readFileSync(p,"utf8")`.
   순서는 **드라이런 → 건수 검증(기대치와 어긋나면 중단) → `--apply`**.
   ★08-09에 이 절차가 실제로 오적용을 막았다(24줄 vs 기대 16 → 규칙을 좁혀 16/16).
5. **KO 원본을 먼저 확인하고 들어가라.** B1 7항목은 KO가 전부 정상이었다(소급 대상인지 아닌지가 여기서 갈린다).
   `masterUpdated` 보류분: `hand-rankings`(C7 대기) · `all-in-rules`(E1 대기).

### 이 작업의 성격 (기대치 조정)

**SEO가 아니라 정확성 작업이다.** 번역본 노출은 de 278 · ja 40 · id 20 · es 19이고 **전 언어 클릭 합계가 2**다.
85편을 고쳐도 트래픽은 안 움직인다. 고치는 이유는 **틀린 계산이 20개 언어로 배포돼 있고 우리가 그걸 안다**는 것뿐이다.

드리프트 상위: `holdem-all-in-rules` **20**(🔴 계산 오류 3건) · `holdem-showdown-rules` 20 ·
`holdem-blind-meaning`·`holdem-game-order`·`texas-holdem-rules-for-beginners` 19씩 · `apt-incheon-2026-guide` 7(8/16 이후).

### B1에서 함께 고친 게이트 오탐 2종 (자가테스트 76/76)

- 구성 설명 가드가 **라틴 연결어만** 봐서 ms `bersama sepasang`·hi `और एक pair`를 놓쳤다
- 🔴 **프랑스어 족보명이 하나도 등록돼 있지 않아 `fr` 6편이 «원리상 미검사»였다**
  (`Quinte Flush`가 `flush`에 걸려 로열·스트레이트 플러시를 전부 「플러시」로 오독)
  → 다른 소수 언어도 같은 상태일 수 있다. **커버리지 출력의 「시나리오를 못 잡은 글」을 언어별로 확인할 것.**

---

## ① KO 잔여 수정 — S5만 남았다 (8/16 이후)

`docs/kr-remediation-spec.md`가 본체다. S1~S4 정산·표준 수치·교훈은 전부 그 파일 §4에 있다.

| 세션 | 묶음 | 편 | 건 | 상태 |
|---|---|---:|---:|---|
| S1~S4 | — | 32 | 142 | ✅ 완료 (`8474ee8`·`e077492`·`4249165`·`93e16a6`·`242c92a`) |
| 재판정 | 내 수정이 만든 WRONG 4건 + 🟡 1건 | 4 | 5 | ✅ 완료 (`5ac1a25`·`4b695f6`) |
| **S5** | `holdem-tournament-how-to-enter` | 1 | **11** | 🔒 **8/16 이후** — STALE #19가 APT 인천 종료로 만료 |

착수 전 `홀덤검수/reports/fix-handoff-remaining-2026-08-09.md` §2-보·§3을 읽을 것.

---

## ▶ 백로그 — 두 트랙 밖에서 아직 안 끝난 것

| # | 할 일 | 메모 |
|---:|---|---|
| 1 | **`/win-rate-quiz` 본문 0줄** | 도구는 완성(6인→실전모드→검수→모바일). **남은 건 본문·FAQ 스키마뿐.** `priority 0.8`로 색인 요청 중인데 h1+부제만 있다 |
| 2 | 프로덕션 E2E 마감 | 검수 세션에 `BASE=https://www.holdemmaster.com`으로 요청 |
| 3 | 다국어 정책 페이지 | `/privacy`·`/terms`·`/contact`가 한국어뿐인데 링크는 영어 라벨로 걸려 있다 |
| 4 | `/solver` desc에 「프로그램」·「계산기」 | 두 단어가 메타에 없다(「무료」는 이미 있음). 볼륨 140·20. **글 안 쓰고 160자 안에서 끝나는 개선** |
| 5 | 리버 승부 해설 | 복기 표에 승률만 있다. "키커로 갈렸다"를 `score5`의 키커로 자동 생성 가능 |
| 6 | `es` 국기 판단 | **사장님 결정 필요** |
| 7 | 🪶 `holdem-implied-odds` 표 헤더 `(1 card)` → `(turn → river)` | 8종 전부. 세트 행 `~11.8% flop`만 국면이 어긋난다(값은 전부 맞음). 검수장 승인부기 — **전용 커밋 금지, 그 파일 만질 일 있을 때 얹을 것** |
| — | ⏸ 펍 지역 페이지(`/pub/*` 9개 색인 탈락) | **사장님 지시로 보류** — 건드리지 말 것 |

### ⏰ 시한이 박힌 것

`docs/update-calendar.md`가 본체다. 현재 3건 —
**8/16 APT 인천 종료**(→ S5 착수 + 결과 아카이브 전환 + 미러 7종) ·
**10월 초 JOPT 오사카 바이인 공개**(글에 약속함) · **12/31 K-ETA 면제 만료**(한국 관련 전 언어 영향).

---

## ▶ 상시 함정 (트랙 무관 · 매 세션 유효)

🔴 **편집한 글은 `--slug=<slug>`를 따로 돌려라.** 전체 게이트가 초록이어도 «경화 대상 목록 밖 글»은
   **검사조차 안 된다.** `audit:hard`의 「41/41편 0건」은 그 41편에 대한 진술일 뿐이다(08-09 S3에서 실제로 걸렸다).

🔴 **편집한 문단은 반드시 다시 읽어라 — 수정이 그 자체로 결함 주입 공정이다.** S2·S3·S4·재판정까지
   **4연속으로** 내가 새로 쓴 문장이 새 오류를 만들었다(가장 최근: split-pot 「키커가 A·K인 포카드 보드」 —
   K 키커가 안전한 건 A 네 장이 다 깔린 `A-A-A-A-K`뿐이다). 되읽기 패스에서 전부 잡혔다.

🔴 **원장·인계서의 수치를 그대로 본문에 옮기지 마라. 직접 계산하라.**
   `ak-offsuit` 에퀴티를 전수 열거(C(48,5)=1,712,304 보드 × 13페어, `_equity.ts` best7fast)로 재계산했더니
   원장이 틀렸다 — AKo vs 22 = **47.35%**(원장 47.0). 콤보 수·에퀴티 둘 다 해당된다.

🔴 **`19.6%`를 일괄 치환하지 마라.** 국면에 따라 맞는 값이다 — 플랍 9/47 = **19.15%** / 턴 9/46 = **19.6%**.
   표 제목·바로 위 문장까지 읽고 국면을 확정한 뒤 손대라(`outs`의 「Rule of 2 (턴)」 표는 전부 정답이었다).

🔴 **CLAUDE.md §8의 KO 필라 슬러그 목록이 실제 파일과 다르다.** 링크 적을 땐 반드시 grep하라.
   `holdem-pot-odds`(X) → **`holdem-pot-odds-calculation`** · `holdem-position-play`(X) → **`position-is-everything-in-holdem`**
   (08-05에 이것 때문에 두 번 404 직전까지 갔다. 전체 대조표는 CLAUDE.md §8.)

🔴 **PowerShell로 한글 파일을 일괄 치환하지 마라**(CLAUDE.md §16). 문서 편집은 Edit 도구,
   스크립트로 해야 하면 `.mjs`에서 `readFileSync(p,'utf8')`.

🔴 **`vercel.json`에 buildCommand가 명시돼 있어 prebuild·postbuild 훅이 프로덕션에서 안 돈다.**
   그래서 `public/sitemap.xml`이 레포에 커밋돼 있다. 빌드 파이프라인을 손댔으면 라이브 curl로 확인하라.

⚠ **형제 글을 인용할 땐 원문을 복사하라**(카드·무늬까지). 그리고 **기존 글을 고치면 그 글을 인용한 글이 낡는다.**

---

## 📦 과거 로그는 아카이브로

**`docs/handoff-archive-2026-08-09.md`** — 2026-07-13 ~ 08-08 세션 로그 전체(약 3,980행).
GTO 솔버 시리즈(✅13/13)·경화 스프린트 등 완료 트랙의 방법론은 `docs/gto-solver-series-spec.md`와
`docs/kr-remediation-spec.md` §4에 남겼다. 세션 로그는 `WORKLOG.md`가 본체다.
