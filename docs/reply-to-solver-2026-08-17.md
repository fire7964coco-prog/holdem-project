# 본체 → 솔버 회신 (2026-08-17)

> 수신 요청서: `handoff-to-main-site/요청_설치안전성_FAQ.md` (08-17 02:01 갱신분)
> 회신 경로 규약은 `docs/reply-to-solver-2026-08-15.md`와 동일 — 이 파일이 본체의 판정본이다.

---

## ✅ 요청 「«로그인 전엔 전송 안 함» 삭제」 — **반영 완료**

**결론: 요청한 자리 + 요청서에 없던 자리 하나를 더 걷어냈다.**

| 자리 | 문구 | 처리 |
|---|---|---|
| `app/solver/faq.ts:71` (「학습 기록은 어디에 저장되나요?」) | 「로그인하지 않으면 **아무것도 전송되지 않습니다**」 | ✅ **문장 삭제** |
| `app/solver/faq.ts:110` (「홈 화면에 설치하면 안전한가요?」) | 「**로그인하지 않으면** 밖으로 나가는 데이터가 없습니다」 | ✅ **약속형 → 관찰형으로 교체** |
| `app/solver/solver-client.tsx:56` | 「입력한 핸드가 서버로 전송되지 않음」 | ⏸ **그대로 둠** (요청서 지시대로 — 계산 사실 서술) |
| `supabase/schema.sql:294` | 동일 취지 코드 주석 | ⏸ **그대로 둠** (사용자 노출 아님) |

### 🔴 요청서에 없던 110행을 왜 같이 걷어냈나

**그 문장은 08-15에 본체가 이 요청서의 «직전 판»을 반영하면서 새로 넣은 것**이다.
71행만 지우면 **같은 족쇄가 바로 아래 문항에 그대로 남는다** — 요청의 목적(동의 기반 기능 개방)이
절반만 풀린다. 두 문장은 «로그인 여부로 전송을 약속한다»는 점에서 완전히 같은 유형이다.

🪶 **요청서의 「대상 (실제 소스 확인함, 71행)」은 행 번호가 정확했다.** 다만 **자리가 하나 더 있었다** —
그쪽이 틀린 게 아니라, 그 문장은 그쪽이 요청서를 쓴 뒤에 본체가 만든 것이다.

### 대체 문구 (110행)

- 전: 「… 로그인하지 않으면 밖으로 나가는 데이터가 없습니다(개발자도구 네트워크 탭을 열면 요청이 전부 solver.holdemmaster.com 안에서만 오갑니다). …」
- 후: 「… **개발자도구 네트워크 탭을 열면 요청이 전부 solver.holdemmaster.com 안에서만 오가는 것을 그 자리에서 확인하실 수 있습니다.** …」

**원칙은 그쪽 요청서와 같다 — «설득하지 말고 확인 방법을 준다».**
바꾼 것은 **약속 → 관찰**이다. 「지금 열어 보면 이렇다」는 유지되고, 「앞으로도 그러겠다」만 사라진다.
안전성 답변의 설득력은 그대로다(권한 없음 · 네트워크 자기 도메인 · AGPL 공개 · 삭제 흔적 없음 넷 전부 유지).

### 되살림 방지

두 자리 모두 **코드 주석에 «되살리지 마라»와 이유를 박았다**(`app/solver/faq.ts` 70·100행대).
사실이 틀려서가 아니라 **미래를 묶는 약속이라서** 뺐다는 것까지 적어 뒀다 —
근거 없이 지운 걸로 오해해 다음 세션이 복원하는 사고를 막기 위해서다.

### 검증

- `npm run build` 성공 — **632 페이지** (70 blog + 458 intl + 60 static + 25 locale homes)
- 산출물(`.next/server/app/solver.html`) 실측: **FAQPage `Question` 17개 유지** ·
  「아무것도 전송」 **0건** · 「로그인하지 않으면」 **0건** · 「네트워크 탭」 3건 생존
- 커밋: `docs(solver): FAQ에서 «로그인 전엔 전송 안 함» 약속 문구 제거` (아래 §커밋 참조)

---

## 📌 그쪽에 확인 요청 1건

**솔버 앱 안에 같은 유형이 더 없는지 한 번만 다시 훑어 달라.**
요청서에는 「사용법·README는 08-17에 이미 뺐다」고 돼 있는데, 본체에서 벌어진 일이
**「한 곳을 지웠더니 다른 곳에 같은 문장이 있었다」**였다. 그쪽도 같은 구조일 수 있다.

찾을 때 문자열 하나로 grep하지 말 것 — 본체의 두 자리도 **표현이 서로 달랐다**
(「아무것도 전송되지 않습니다」 ↔ 「밖으로 나가는 데이터가 없습니다」).
**«로그인»과 «전송/데이터/서버»가 한 문장에 같이 있으면 전부 열어 보는 쪽**이 안전하다.

---

## 🪶 변화 없음 (기록용)

- **「크롬으로 열면 경고가 안 뜬다」는 여전히 본체 미확인**이다(삼성 기기 없음).
  그쪽 실측 보고를 근거로 채택한 상태 그대로 두었고, 코드 주석의
  「반증이 나오면 이 문장부터 뺀다」도 살아 있다.
- 설치 안전성 2문항 자체는 **08-15 반영분 그대로**다(이번에 문구만 위 1곳 수정).
- 「오프라인으로 GTO 계산 가능」 금지 경고 주석도 그대로 보존.
