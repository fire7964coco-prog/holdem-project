# 검수 프로토콜 — 3층 게이트

> **이 파일이 검수 절차의 본체(single source of truth)다.**
> 사장님이 "검수 프로토콜 읽고 검수해"라고 하면 **이 파일 전체를 Read 하고 그대로 따른다.**
> `.claude/skills/review-post/SKILL.md`와 `CLAUDE.md` §14는 이 파일을 가리키기만 한다 — 내용을 복사해 두지 말 것(갈라진다).
>
> 적용 대상: **신규 포스팅 직후 검수 · 기존 글 검수 · 경화 · 번역본 검수** 전부.
> 최종 갱신 2026-07-31

## ★ 이 프로토콜의 전제 (2026-07-31 확립 — 먼저 읽어라)

**같은 검수를 여러 번 반복해도 검출률은 오르지 않는다.** 같은 방식의 통독은 N번이 1번과 같고,
오히려 문장이 익숙해져 검출률이 떨어진다. 실제로 "여러 번 검수했는데 또 오류가 나온" 원인이 이것이었다.

**그리고 읽어서 통과시킬 수 있는 검사는, 그럴듯하고 틀린 글이 반드시 통과한다.**
`A♠2♦ vs K♠K♦ / 보드 Q-J-10-9-8 → 스플릿`은 읽으면 참으로 읽힌다. 7장을 실제로 조합해야만 거짓이 드러난다.

→ 그래서 **읽기로 검증되는 것과 계산으로만 검증되는 것을 분리**하고, 후자는 기계에 맡긴다.

| 층 | 무엇 | 누가 |
|---|---|---|
| **1층** | §13 검산 · 메타 · 링크 · 축어중복 · 표 교차대조 | **기계** (`npm run audit:hard`) |
| **2층** | 기계가 판정 못 한 카드 문단 | **사람** (`--uncovered`) |
| **3층** | D유형(전략적 유해 조언) · F유형(사실 오류) | **적대적 렌즈** (Agent) |

---

## 0. 대상 파일 찾기

slug가 어느 트랙인지 먼저 확인 — 위치마다 규칙이 다르다:
- `lib/posts.ts` = 한국어 LEGACY (구조/slug 변경 금지, **오타·사실오류·링크 수정은 허용·권장**)
- `lib/posts/[slug].ts` = 한국어 NEW
- `lib/posts-en/[slug].ts` = 영어 / `lib/posts-{ja,zh,es,...}/` = 다국어

```
Grep: slug: ['"]<slug>['"]   → 파일·라인 확정 후 Read
```

---

## 1층 — 기계 게이트 (가장 먼저, 스킵 금지)

```bash
npm run audit:hard -- --slug=<slug>
```

🔴 **0건이 될 때까지 고친다.** 이 스크립트가 아래를 전부 본다 — **손으로 다시 세지 마라**:

| 코드 | 검사 |
|---|---|
| F1~F13 | 바로 답 라벨 · 질문형 H2 70% · FAQ Q/A 짝 · seoTitle/desc 길이 · **깨진 링크·trailing slash 301** · 백틱(§12-A) · 이모지 · updated · §17 금지어("총정리" 등) · imageAlt · **필라 역링크(F12)** · **본문 나눗셈 재계산(F13)** |
| H1~H6 | **§13 자동 판정** — 7장 → 베스트5 계산 → 글의 주장과 대조 / 카드 중복 / 5장 족보 예시 / 홀카드\|보드 표 |
| E1·E2 | 직답 삽입이 만든 축어 중복 · FAQ↔H2 중복 |
| C1·C2 | 형제 글 표 교차 대조 (자동 대조 불가 쌍은 육안 대상으로 올라온다) |

### 다국어판 검사 — `--locale=<code>`

```bash
npm run audit:hard -- --locale=en     # ja zh zh-hant es de pt id 도 동일
```

⚠ **로케일 모드는 "언어 불변 항목"만 본다** — §13 핸드 검산(H1~H6)과 산수 계산(F13), 표 교차 대조(C1·C2).
**메타 길이·"바로 답" 라벨·질문형 H2·§17 금지어는 한국어 기준이라 적용하지 않는다**(EN은 seoTitle ~55자·desc ≤160자로 기준 자체가 다르다).

> ★ **KO를 기준으로 번역이 맞는지 따지지 말 것** (사장님 지시 2026-07-31).
> KO판과 다국어판은 뼈대가 비슷해도 **내용은 달라도 된다.** 훅·H2·예시·경험담은 각 시장에 맞게 재저작된 것이다.
> 기계로 검수할 수 있는 건 **숫자와 계산**뿐이다.

**게이트를 의심하면 검증부터 한다**: `npm run audit:hard:selftest` (2026-08-06 기준 **64/64**. 픽스처를 추가하면 이 수도 올라간다 — 중요한 건 전부 통과인지다).
과거 실제 사고(2026-06-23 tiebreak 스플릿 오판)를 잡는지, 알려진 오탐 패턴에 안 울리는지 고정돼 있다.

### ★ C1/C2(형제 대조)는 `--slug` 모드에서도 돈다 (2026-08-01 수정)

`--slug=<slug>`로 돌리면 게이트는 그 1편만 보지만, **클러스터 대조는 소속 형제 전원을 자동으로 끌어와**
내 글이 낀 쌍만 보고한다. 출력 첫 줄에 `대상: <slug> ↔ [클러스터] 형제 N편`이 찍히는지 확인할 것.

> ⚠ 그 전까지는 `--slug` 모드가 **표를 0개 수집**해 늘 "0쌍"이었고, 출력은 "헤더가 서로 달라 매칭 실패"라는
> **틀린 원인**을 붙였다. 그 오진이 핸드오프에 "매칭 규칙을 느슨하게 고쳐야 한다"로 기록돼 있었다.
> 실제로 전체 실행에선 25쌍이 멀쩡히 돌고 있었다 — **검사가 죽은 게 아니라 정작 쓰는 순간에만 죽어 있었다.**
> 교훈: **게이트가 "0건"이라고 할 때 그게 "안 봤다"인지 "보고 통과"인지부터 갈라라.**

- 대상 글이 `CLUSTERS`(scripts/audit-hardening.mjs) 정의에 없으면 형제 대조를 **시도조차 안 한다** —
  이 경우 그렇게 명시된다. 새 글을 경화 대상에 넣을 땐 `CLUSTERS`에도 추가할 것.
- `ℹ 행 키가 안 맞아 대조 못 한 행 N개`는 **검증되지 않은 행**이다. 0건이 이 공백을 덮지 않는다.

### 🔴 게이트가 틀렸다고 판단되면 — 원문을 먼저 본다
오탐이면 **파서를 고치고 자가 테스트에 픽스처를 추가한다.** 그냥 무시하고 넘어가지 마라.
실제 오탐 사례: 표 두 열 병합 · `페어 없음`/`플러시 아님`의 부정 · 주장 탐색이 다음 줄 침범 ·
스타팅 핸드 나열 · 구성 설명(`트리플+원페어`) · 화살표 `→`를 이모지로 오판.

---

## 2층 — 커버리지 공백을 사람이 메운다

출력 맨 아래 **§13 커버리지**를 반드시 읽는다.

> ⚠ **"시나리오를 못 잡은 글"의 0건은 검증이 아니라 미검사다.**

```bash
npm run audit:hard -- --uncovered --slug=<slug>
```

기계가 판정 못 한 카드 문단이 원문 그대로 나온다. **한 문단씩 직접 검산한다**:
1. 7장(또는 그 시점의 카드 전부) 나열
2. 베스트 5장을 명시적으로 조합
3. 글의 주장(승자·족보·드로우·보드 텍스처)과 대조

### 함정 체크리스트
- 보드에 스트레이트가 깔렸을 때 홀카드가 **그 위로 더 높은 스트레이트**를 만드는가 (2026-06-23 사고)
- "스플릿"이면 양쪽이 **정말 동일한 5장**인가
- 키커: 보드 카드가 자동으로 베스트5에 들어가 키커가 바뀌는가
- 휠(A-2-3-4-5)은 가장 낮은 스트레이트 / 홀덤에 무늬 서열 없음
- **★블로커** — 아웃츠를 셀 때 그 카드가 **상대에게 더 좋은 패를 주지 않는지**.
  2026-07-31에 내가 여기서 틀렸다: 트리톤 핸드 턴 82%를 77%로 계산했는데,
  9♥·10♥·8♥는 상대에게 플러시를 주므로 아웃츠가 아니었다. **글이 맞고 검수자가 틀렸다.**
- 5장 기준 vs 7장 기준 확률 혼용 → [[poker-probability-5card-vs-7card]]

---

## 3층 — 적대적 렌즈 (게이트가 원리상 못 잡는 것)

**게이트가 초록이라고 검수가 끝난 게 아니다.** 아래 둘은 자동화 불가다:
- **D유형 — 전략적 유해 조언**: 문법도 산수도 멀쩡한데 실전에서 손해. (예: 나를 이기는 레인지를 "밸류"라고 가르침)
- **F유형 — 사실 오류**: 대회·법·통계. §12-B(원문 직접, 요약 금지)로만 막는다.

Agent 툴로 **렌즈를 다르게 준 페르소나를 병렬** 실행한다. **페르소나 수가 아니라 렌즈 다양성이 성과를 가른다** — 수학 검산관 4명을 돌리면 0건이다.

| 렌즈 | 이 렌즈로만 보이는 것 |
|---|---|
| WSOP TD 출신 수학 검산관 | 공식 재유도 · 표 전 칸 재계산 |
| **현역 딜러/플레이어** | **실전 유해 조언** (다른 렌즈는 전부 놓친다) |
| 한국어 SEO·GEO 편집자 | 태그 카니발 · 썸네일 맥락 · imageAlt |
| **교열자 — `git diff`를 준다** | **내 편집이 만든 축어 중복·조건 불일치** |

### 프롬프트에 반드시 넣을 4가지 (효과 검증됨)
1. **"의도적으로 뺀 항목" 목록** — 출처 섹션 없음 · slug/이미지 불변 · 백틱 없음 등.
   안 적으면 "출처를 달아라"류 오탐이 쏟아진다.
2. **"문제없으면 문제없다고 쓸 것 / 오탐이 미탐보다 나쁘다"** — 할당량 압박 제거
3. **확신도(높음·중간·낮음) 필수 표기** — 페르소나끼리 갈리는 지점이 진짜 쟁점이다
4. **"읽다가 사실이 이상하면 SEO 밖이어도 보고하라"**

★ **교열 렌즈에는 최종본이 아니라 `git diff`를 준다.** 물어야 할 것은 "이거 괜찮나?"가 아니라
**"내 이번 편집이 무엇을 깨뜨렸나?"**다. 검수 작업 자체가 결함 주입 공정이다.

> ⚠️ **단, 이 설계는 측정 편향을 만든다 — 프롬프트에서 반드시 상쇄할 것.**
> 교열 렌즈는 diff만 보므로 **원리상 "이번 편집의 결함"만 보고한다.** 원본에 원래 있던 결함은
> 시야 밖이다. 이대로 보고서를 쓰면 실제 비율과 무관하게 **매번 "내가 만든 오류"가 머리에 온다.**
>
> → 렌즈 프롬프트에 **"각 지적이 ①원본 유래인지 ②이번 편집 유래인지 표기하라"**를 넣는다.
> → 사장님 보고서에는 **원본 결함과 편집 결함을 같은 표에 나란히** 적는다. 누가 만들었는지가 아니라
> **무엇이 어떻게 고쳐졌는지**가 보고의 본체다. 자책 문장을 WORKLOG·handoff에 남기지 말 것 —
> 다음 세션이 그걸 읽고 같은 프레임을 재생산해 루프가 된다(2026-08-02 사장님 지적으로 확인).

★ **검수자도 틀린다.** 페르소나 지적을 그대로 반영하지 말고 **1차 출처·직접 계산으로 판정**한다.

---

## 4. 이미지·속도 (기계가 안 보는 항목)

- **깨진/플레이스홀더**: 1KB 안팎 webp = 빈 파일 → 재제작 필요, 사용자에게 보고
- **용량**: 60KB(복잡한 인포그래픽 80KB) 초과 → 재압축 (§15 — Windows 파일 락: readFileSync→tmp→rename)
- **형식**: webp만, 1200×675, 히어로 `priority={true}`
- **맥락**: **Read로 이미지를 직접 열어** 본문과 맞는지·한글 오버레이 없는지(다국어 공용)·
  인포그래픽이면 **철자 육안 검수**(§9-1). 클러스터 형제 글과 본문 사진 재사용 금지
- **속도**: LCP·이미지 치수(CLS). 필요 시 `npm run psi <url>` → [[perf-lessons-lcp-and-bundle]]

---

## 5. 마무리

- [ ] `npm run audit:hard -- --slug=<slug>` 🔴 0건
- [ ] `npm run build` 성공 + sitemap post 수 확인
- [ ] (FAQ 손댔으면) `npm run audit:hard -- --schema`
      ※ 산출물의 `"@type":"Question"`은 이스케이프돼 0으로 세진다. `acceptedAnswer ÷ FAQPage`가 맞다
- [ ] `git commit -F commit-msg.txt` + push (한글·여러 줄 메시지는 here-doc이 깨진다)
- [ ] `WORKLOG.md` 최상단 · `session-handoff.md` 갱신
- [ ] 임시 파일(`*.mjs`·`commit-msg.txt`) 삭제

## 검수 우선순위
GSC 노출 높은 글 → 전략 글(핸드예시 많아 오류 위험) → 토너먼트 가이드(날짜·운영사 노후화).


---

# 상시 함정 · 렌즈 운용 (2026-08-11 `session-handoff.md`에서 승격)

> 아래는 특정 트랙이 아니라 **매 검수에 유효한 규율**이다. 인계서에 있으면
> 트랙이 끝날 때 같이 지워지므로 검수 본체인 여기로 옮겼다.
> 다국어 경화 트랙 고유의 교훈은 `docs/lessons-i18n-hardening.md`에 있다.

### 🔧 게이트 확장 후보 (다음에 게이트를 손댈 때)

| 검사 | 왜 | 난이도 |
|---|---|---|
| 🔴 **F13이 콤마 소수점을 못 본다** | `PCT_RE = /=\s*\**\s*(?:약\s*)?([\d.]+)\s*%/g` — 캡처에 `,`가 없어 **`= 19,1%`를 매칭조차 안 한다.** 즉 **de·id·pt의 「= N,N%」 주장은 F13 기준 미검사**고 그 자리의 0건은 검증이 아니다. 2026-08-11 실측: 전 로케일 51건(재계산 가능 15건) — **손검산 결과 불일치 0**이라 지금 숨은 오류는 없지만 구멍은 실재한다. 고칠 때 **천단위 쉼표와 소수 쉼표를 가르는 조건**과 셀프테스트 픽스처를 같이 넣을 것 | 낮음 |
| 🔴 **«구성 가능성» 검사** | 게이트는 베스트5를 «주어진 카드로» 검산할 뿐 **그 배치가 홀덤에서 가능한지**는 안 본다. de 4렌즈가 4건을 잡았다(서로 다른 무늬 플러시 2개 ×2 · `K-K-K-A-2`(둘째 킥커 2 → 보드가 KKK22 = 풀하우스) · 「vier Asse를 이기는 건 SF **와 Royal**」). 최소한 「서로 다른 무늬 플러시 2개」는 정규식으로도 잡힌다 | 낮음~중 |
| **H2↔FAQ 축어 중복** | 정본 §7-6인데 기계가 안 본다. de Session 1·2에서 **11쌍**(tiebreak 4·kicker 2·flush 3·reading 2) | 낮음 |
| **질문형 H2 비율** | `audit:hard`는 KO만 본다. de 필라가 **10%**였는데 아무도 몰랐다 | 낮음 |
| **직답 블록 유무** | `hand-rankings`·`kicker`에 없었다. ★`tldr`은 **화면에 렌더되지 않는다**(JSON-LD 전용) — 있다고 착각하기 쉽다 | 낮음 |
| **이미지 title 누락** | 없으면 `<figcaption>`이 안 생긴다. `reading-the-board` 3장이 그랬다 | 낮음 |

---

---

### ★ 적대 QA 렌즈 운용 (de Session 2에서 효과 검증)

- **§13 계산에 사람 시간을 쓰지 마라.** 세 렌즈가 독립적으로 전건 재계산했고(네이티브 16 · 딜러 36 · SEO 전건)
  **오류 0건**, 기계 게이트와 일치했다. 그 층은 이미 막혀 있다.
- 🔴 **D유형은 계산이 아니라 «조언»이 틀린다.** `split-pot`의 팁이 EV를 거꾸로 가르쳤다
  (확정 촙 콜의 순이득 = **+P/2**인데 폴드를 권했다 — 매몰비용 오류). **수학 검산 렌즈를 몇 개 돌려도 안 나온다.**
  딜러 렌즈만 잡았다. → **렌즈 «수»가 아니라 렌즈 «종류»가 성과를 가른다.**
- ★**렌즈끼리 서로를 고친다.** 오드칩에서 교열·SEO는 「형제 글처럼 `unentschiedenen`을 넣어라」,
  네이티브는 「그 단어가 오독」이라 했다 → `ersten am Chop beteiligten Spieler`로 양쪽 충족.
- ⚠ **검수자도 틀린다**: 딜러가 「네가 뽑는 건 Neun-hoch Flush」라 했으나 **리버가 A♥면 A-하이**다. 취지만 살려 반영했다.
- **프롬프트 4장치**(빼면 오탐이 쏟아진다): 의도적 제외 목록 · 「문제없으면 문제없다고 써라」 ·
  확신도 표기 필수 · 「렌즈 밖이어도 사실이 이상하면 보고」. 교열 렌즈엔 **diff**와
  「①원본 유래 / ②이번 편집 유래」 구분을 요구한다(안 하면 「내가 만든 오류」만 올라온다).

---

## ▶ 상시 함정 (트랙 무관 · 매 세션 유효)

🔴 **편집한 글은 `--slug=`(또는 `--locale=`)를 따로 돌려라.** 전체 게이트가 초록이어도
   «대상 목록 밖 글»은 **검사조차 안 된다.** 「N편 0건」은 그 N편에 대한 진술일 뿐이다.

🔴 **커버리지 출력을 반드시 읽어라.** 「시나리오를 못 잡은 글」의 0건은 **검증이 아니라 미검사**다.
   B1에서 **프랑스어 족보명이 하나도 등록돼 있지 않아 `fr` 6편이 «원리상 미검사»**였던 게 드러났다.
   **다른 소수 언어도 같은 상태일 수 있다.**

🔴 **「없다」를 보고하기 전에 «탐지 방법»부터 의심하라.** 08-10에만 두 번 당했다 —
   hreflang을 소문자로 grep해 「전 로케일 0개」라 오판했고(실제는 `hrefLang` camelCase라 정상),
   FAQ 스키마를 `.ts` **원본 텍스트**로 세어 「41편」이라 오보했다(파일이 **CRLF**라 정규식이 안 걸렸다).
   ★**소스 raw로 감사하지 마라 — 판정은 «파싱된 객체» 또는 «빌드 산출물»로.**

🔴 **「별도 검사가 있다」는 주석은 그 검사의 존재 증거가 아니다.** `canonical-check.mjs`가
   「다국어는 hreflang이 본다」고 적어 뒀는데 그 검사는 없었다. `audit:hard --schema`도
   「스키마를 본다」지만 **한국어 산출물만** 봤다. 커버리지 구멍은 «검사한다고 적힌 곳»에 숨는다.

🔴 **편집한 문단은 반드시 다시 읽어라 — 수정이 그 자체로 결함 주입 공정이다.**
   KO S2·S3·S4·재판정 4연속 · de Session 1 3종 · A층 묶음 1·4·5 전부에서 나왔다.

🔴 **한 글을 고치기 전에 «그 사실이 몇 개 로케일에 복제돼 있는지» grep하라.**
   08-10에 ja만 고쳐 **ja가 혼자 맞는 상태**를 만들었다가 같은 날 7개 로케일(EN 마스터 포함)을 뒤따라 고쳤다.
   그리고 **grep 히트를 그대로 믿지 마라** — `181`이 준우승 상금 `$256,181`이었다.

🔴 **원장·인계서의 수치를 그대로 본문에 옮기지 마라. 직접 계산하라.**
   `ak-offsuit` AKo vs 22 = **47.35%**(원장 47.0) · B2 C7은 990조합 전수 열거로 **EN 마스터의 오류**를 잡았다.

🔴 **`19.6%`를 일괄 치환하지 마라.** 국면에 따라 맞는 값이다 — 플랍 9/47 = **19.15%** / 턴 9/46 = **19.57%**.

🔴 **CLAUDE.md §8의 KO 필라 슬러그 목록이 실제 파일과 다르다.** 링크 적을 땐 반드시 grep하라.
   `holdem-pot-odds`(X) → **`holdem-pot-odds-calculation`** · `holdem-position-play`(X) → **`position-is-everything-in-holdem`**

🔴 **PowerShell로 파일을 치환하지 마라**(CLAUDE.md §16). 문서는 Edit 도구, 스크립트는 `.mjs`.
   ★08-10에 `node -e "…$1…"`의 **`$1`을 PowerShell이 빈 변수로 먹어** 프런트매터가 뭉개지고
   **빌드가 깨졌다**(에러가 webpack 번들 덤프로 나와 원인 파악에만 시간이 들었다).
   ★**CRLF 파일을 `\n` 기준으로 치환하면 CR이 남는다** — 줄끝을 파일에서 읽어 복원하라.

🔴 **`vercel.json`에 buildCommand가 명시돼 있어 prebuild·postbuild 훅이 프로덕션에서 안 돈다.**
   그래서 `public/sitemap.xml`이 레포에 커밋돼 있다. **푸시 커밋에 sitemap을 빠뜨리지 말고**,
   빌드 파이프라인을 손댔으면 라이브 curl로 확인하라.

⚠ **형제 글을 인용할 땐 원문을 복사하라**(카드·무늬까지). 그리고 **기존 글을 고치면 그 글을 인용한 글이 낡는다.**

---
