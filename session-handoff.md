# 세션 인수인계 (Session Handoff)

> **사용법**: 작업 마칠 때 이 파일 업데이트 → 다음 세션 시작 시 AI가 읽고 바로 이어받음
> 작업 시작 전 `CLAUDE.md` → `session-handoff.md` → `WORKLOG.md` 순서로 읽을 것.
> 🔴 **이 파일은 «지금 할 일»만 담는다.** 완료분은 `WORKLOG.md`에 적고 여기서 지운다 — 로그를 쌓지 마라.
> (2026-08-14 전면 정리: 종료된 트랙 6개를 걷어내고 608행 → 이 크기로 줄였다.
> 걷어낸 트랙의 경위·교훈은 전부 `WORKLOG.md`에 있다. 방법론은 아래 「계속 유효한 규율」에 남겼다.)

---

## ▶▶▶▶▶▶▶▶▶ 새 세션 START HERE

### ★ 이번 세션의 본체 = **`/solver` 랜딩 페이지 다듬기** (사장님 지시 2026-08-14)

**대상 파일**: `app/solver/page.tsx`(31행 · 메타데이터) · `app/solver/solver-client.tsx`(306행 · 본문)

#### 착수 전에 반드시 읽을 것

1. 🔴 **`app/solver/page.tsx`의 주석을 먼저 읽어라.** 키워드 배분 근거가 코드 주석에 박혀 있다 —
   「gto 표」 단독 조준 금지(자동완성이 **만화 GTO**다) · `/hand-chart`와의 역할 분리
   (그쪽은 「홀덤 차트」 480, 여기는 「홀덤 gto 차트」) · 사이트명 직접 붙이지 말 것(title template 중복).
   **모르고 고치면 카니발을 다시 만든다.**
2. `docs/keyword-bank/ko-gto-solver.md` §4-1 — 볼륨 근거.
3. [[seo-tool-vs-blog-cannibalization]] 규율: **도구 페이지를 밀 땐 «블로그가 이기고 있나»를 먼저 본다.**

#### 실측 상태 (2026-08-14)

| 항목 | 값 |
|---|---|
| 구조 | `h1` 1개 ✅ · **H2 5개** (사용법 / GTO 트레이너 / 교육 예제 13스팟 / 함께 쓰면 좋은 도구 / FAQ) |
| FAQ | **8문항** (홀덤 GTO 표 어디서 · 솔버가 뭔가 · 무료인가 · 설치하나 · GTO Wizard와 차이 · 트레이너 뭔가 · 왜 EV 손실로 채점 · 어떤 상황 분석) |
| 스키마 | **FAQPage만** (Article·WebApplication 없음 — 얹을지는 판단 사항) |
| 🔴 **GSC 28일** | **쿼리 1개 · 노출 1 · 32위** — 진입로가 아직 아무것도 못 잡았다 |

#### 노리는 키워드 (실측 볼륨)

`홀덤 gto` **390**(+9.9%) · `홀덤 gto 표` **210** · `홀덤 gto 프로그램` **140**(+27.5%) ·
`gto 차트` 90 · `홀덤 솔버` 70 · `홀덤 gto 차트` 20 · `홀덤 gto 계산기` 20

#### 🔴🔴 ★2026-08-15 실측 — **이 축의 유입은 100% 네이버다. 구글이 아니다**

GA4 랜딩 기준(28일): `/solver` 유입 **19세션 전부 네이버** — 모바일 네이버 referral 13(평균 1분 53초)
· 네이버 오가닉 6(**평균 6분 36초**) · 구글 **0**. 랜딩 평균 체류 **3분 22초** · 참여율 **68.4%**.

> ⚠ **GA4 표의 「활성 사용자당 평균 참여 시간 12초」를 그대로 읽지 마라.** 그건 «그 페이지를 본
> 모든 세션»의 값이고 페이지가 포그라운드에 있던 시간만 센다. CTA가 새 탭이라 솔버로 보내는
> 순간 계측이 멈춘다 — **잘 보낼수록 짧아 보이는 지표**다. 판단은 `node scripts/ga-page.mjs solver`의
> **랜딩(entrance) 기준**으로 하라.
> 🔴 **솔버 하위도메인에는 GA4 태그가 아예 없다**(본체는 `G-CHSBJJDC14`). 그래서 솔버 체류가
> 통째로 안 잡힌다. 「추천 제외 목록」에 solver를 넣은 것도 지금은 효과가 없다(무해).

**네이버 웹사이트 영역 순위 (2026-08-15 실측)**

| 쿼리 | 볼륨 | 우리 순위 | 뜨는 URL |
|---|---:|---:|---|
| 홀덤 gto 표 | 210 | **1위** | `/solver` |
| 홀덤 gto 프로그램 | 140 | **2위** | `/solver` |
| 홀덤 솔버 | 70 | **2위** | `/solver` |
| 홀덤 spr | 90 | **2위** | — |
| gto 솔버 | 70 | **3위** | `/solver` |
| 홀덤 gto | 390 | 5위 | `/solver` |
| 홀덤 포지션 | 390 | 5위 | — |
| gto 차트 | 90 | 6위 | (상위가 TradingView·investing = 금융 의도) |
| **동크벳** | **320** | **없음** | 색인은 됐다(「동크벳 안좋은 이유」로는 잡힌다) |
| **포커 솔버** | 50 | **없음** | 랜딩에 08-14에 넣었다 — 재크롤 대기 |

★ **거의 모든 쿼리에서 대표 URL이 `/solver` 랜딩이다** — 「랜딩이 13편과 경쟁하면 안 된다」는
사장님 우려는 네이버에서는 **이미 해결돼 있다**. 랜딩이 이기고 있다.

**기술 점검 — 결함 없음**: `naver-site-verification` 메타 ✅ · robots.txt 전체 허용 + 사이트맵 선언 ✅ ·
미색인 5편도 사이트맵 포함 ✅ · 라이브 200 + `index, follow` ✅.
**GTO 13편 네이버 색인 = 8/13.** 미색인 5편(①②③⑤⑥)은 **결함이 아니라 크롤 대기**다(발행 08-08).

> 🔴🔴 **2026-08-15 발견 — 네이버에 등록된 호스트가 우리 정본과 다르다**
> 서치어드바이저 사이트 목록에 등록된 것은 **`https://holdemmaster.com`(www 없음)**인데
> 우리 정본은 canonical·사이트맵 전부 **`https://www.holdemmaster.com`(www)**이다.
> 웹마스터도구의 「요청」은 **등록된 호스트 하위 URL만** 받으므로, 지금 상태로 www URL을
> 넣으면 거부될 수 있다. **미색인 5편의 유력한 원인이다.**
> → **`https://www.holdemmaster.com`을 사이트로 추가 등록**하고 거기서 수집 요청·사이트맵 제출.
>   소유 확인은 「HTML 태그」로 즉시 통과한다(www 페이지에 인증 메타가 이미 있다:
>   `naver-site-verification` = `b978aa0f38cbfd49ce7e06253e0bcb6f9e2efd18`).
>
> 🔴 **사장님만 할 수 있는 것**
> ① **네이버에 www 호스트 추가 등록** (위 건. 이게 먼저다)
> ② **미색인 6개 「웹 페이지 수집 요청」** — GTO 5편 + **`/hand-chart`**
>    (`a-high-board-cbet` · `k-high-board-cbet` · `broadway-board-strategy` ·
>     `monotone-board-strategy` · `paired-board-strategy` · `/hand-chart`)
>    🪶 `/hand-chart`도 네이버 미색인이다 — 「홀덤 차트」 480을 노리는 페이지인데 빠져 있었다.
>    ⚠ 공식 가이드 원문: 「최소 1일에서 몇 주」 소요 · **같은 URL 반복 요청 불필요** · 노출 보장 아님.
> ③ **사이트맵 제출** — `https://www.holdemmaster.com/sitemap.xml`
>    (가이드 원문: 「노출량이 적다면 사이트맵 피드 제출을 권장」. RSS는 불필요 — 가이드도 사이트맵 권장)
> ④ 🟠 **Vercel 도메인 리다이렉트 307 → 308(Permanent)**
>    실측: `holdemmaster.com` → `www` 가 **307 Temporary**다(Server: Vercel).
>    `vercel.json`에는 `permanent: true`(=308)로 적혀 있는데 **도메인 설정이 먼저 이긴다** —
>    [[vercel-buildcommand-skips-npm-hooks]]와 같은 계열의 함정이다.
>    → Vercel → 프로젝트 → Settings → Domains → `holdemmaster.com` → Redirect를 **Permanent**로.
>    급하진 않다(canonical이 www를 가리켜 실무 영향은 작다). 다만 정본 이전 신호가 «임시»로 남는다.
>
> 🪶 **GA4 교차 도메인은 하지 않기로 했다**(2026-08-15 사장님 판단) — SEO 영향이 없기 때문이다.
>   대가: 「랜딩이 몇 %를 솔버로 보내는가」는 영영 측정 불가로 남는다. 필요해지면 그때 붙인다.

#### ✅✅ GEO 경화까지 완료 (2026-08-14) — 경위는 `WORKLOG.md` 「2026-08-14 (4)」

**전제 확인됨**: 솔버 본체는 라이브 `meta robots noindex`다 → 이 축의 노출은 전부 랜딩이 받는다.
H2 5→**12**(질문형+직답) · 표 0→**5** · FAQ 8→**14** · 본문 3,100→**9,609자** ·
제목에 「프로그램」·desc에 「한국어」 투입. 키워드 근거 전문 = `docs/keyword-bank/ko-gto-solver.md` §6.

> 🔴 **다음에 이 축을 만질 때 먼저 읽을 것 셋**
> ① **「솔버」 단독은 방탈출 카페다**(솔버 건대 1호점·루시드드림 + sat/ode 솔버). 「gto」 단독이
>    만화·페라리인 것과 같은 계열 — **앞에 홀덤/포커/gto가 붙은 형태만 쓴다.**
> ② **자동완성 API는 `oe=UTF-8` 필수.** 응답이 `charset=EUC-KR`이라 안 붙이면 한글이 전부 깨진다
>    (문서의 「client=chrome이면 UTF-8」은 **거짓이라 고쳤다**). 수확 1회를 이걸로 날렸다.
> ③ **구글 SERP·PAA는 이 환경에서 429로 막혀 있다.** 라쿠 `question-search`도 한국어 0건이다.
>    → 자동완성 원문 + 라쿠 볼륨 두 경로가 현재 유일한 1차 수단이다.
>
> 🪶 **랜딩에 박힌 「33개 결정 노드」는 트레이너 화면에 표시되는 값이다.** 앱에서 사라지면
>    랜딩에서 **먼저 지울 문장**이다(08-13엔 근거가 없어 뺐다가 08-14에 되살린 이력이 있다).

#### ✅ 1차 다듬기 (2026-08-14) — 경위는 `WORKLOG.md` 「2026-08-14 (3)」

| # | 후보 | 결과 |
|---:|---|---|
| 1 | H2 5개가 전부 비질문형 | ✅ **판단해서 «기계 변환 안 함»으로 종결.** 도구 허브라 질문형은 FAQ 8문항이 받는다(`/pub-promotion` 예외와 같은 계열). 대신 H2 2개를 **키워드로 재조준**했다 — 「홀덤 솔버 사용법」(자동완성 1번) · 「홀덤 GTO 전략」. **근거를 코드 주석에 박아 뒀으니 다시 논의하려면 그걸 먼저 읽어라** |
| 2 | 첫 화면에 「홀덤 gto」 직답 | ✅ **h1 직후 직답 단락 신설.** 없었다(답이 FAQ까지 내려가야 나왔다). 서술은 기존 FAQ에서 옮겨 **새 주장 0** |
| 3 | 스키마 보강 | ✅ FAQPage 하나 → **`@graph` 셋**(WebApplication + FAQPage + BreadcrumbList). FAQ 배열은 `app/solver/faq.ts`로 분리 · 이름·설명은 metadata 상수 재사용 |
| 4 | 13개 스팟 앵커 | ✅ 보드 문자열뿐이던 앵커에 **각 글 seoTitle의 낱말**을 올렸다. `note`의 §4-B 확정 수치는 무변경 |
| 5 | 🪶 `/en/solver` | **아직 열지 마라** — 조건이 «국문 성과 확인 후»다. 시한은 9월 초(아래 「시한」) |

#### ▶ 다음에 볼 것 (이 페이지)

- 🔴 **성과 판정은 9월 초**(아래 「시한」). 앵커·H2 변경은 재크롤에 시간이 걸리니 **4주에 안 움직여도 실패로 단정하지 마라** — 족보 앵커 건과 같은 규율(8주까지).
- 🪶 **역방향 앵커는 아직 균일하다** — GTO 13편이 `/solver`로 거는 앵커가 **전건 「홀덤마스터 GTO 솔버」**(26곳)다. 「무료 GTO 솔버」·「홀덤 GTO 표」로 일부를 갈아 끼울지는 판단 사항(글을 열 때 함께).
- 🪶 「홀덤 gto 표」(210)의 의도는 «프리플랍 레인지 차트»에 가깝고 우리는 포스트플랍 전용이다. **랜딩은 그 갈림을 정직하게 적어 `/hand-chart`로 보낸다** — 순위가 붙었는데 이탈이 높으면 그 자리를 다시 볼 것.

> 🔴 **이 페이지를 손대면 라이브로 확인하라** — `vercel.json`에 `buildCommand`가 명시돼 있어
> **prebuild·postbuild 훅이 프로덕션에서 안 돈다.** [[vercel-buildcommand-skips-npm-hooks]]

---

### 🔴🔴 다만 — **8/16(일)이 이틀 뒤다. 이건 시한이 박혀 있다**

**APT 인천 메인이벤트 최종일이 8/15(내일)라 그날 우승자가 나온다.** 솔버 작업 중이라도
8/16에는 아래 4건을 **하루로 묶어** 처리해야 한다(같은 글·같은 로케일을 여러 번 열지 않기 위해).

| # | 할 일 | 메모 |
|---:|---|---|
| 1 | **`apt-incheon` 2차 훅 전환** — 「남은 일정」 → **「결과·우승자」** | 갈아끼울 자리 5곳(ko 기준)은 `docs/update-calendar.md` 8/16 항목에 박아 뒀다: `seoTitle` 꼬리 · `desc` · `tldr` 첫 문장 · 도입부 첫 문장 · **첫 화면 `:::note` 상태 블록**. ⚠ **8로케일은 1차 전환도 안 됐다**(ko만 했다) · `:::note`의 「8월 13일 현재」 날짜 갱신 필수. ⚠ 훅 앞머리 「한국 여권으론 못 들어간다」는 **그대로 둘 것**(CTR 10.6%·5.4위 · §17 단독 교체 금지) |
| 2 | **`apt-incheon` Country Exclusives** — 「거주국」 → 정본 **국적**(citizen+KYC) | **en + 미러 7종.** 근거 `translation-terms-de.md`. 환산값 $1.684도 같은 글의 $1.703과 충돌 → **1번과 같은 파일이니 반드시 함께** |
| 3 | 🔒 **국적·비자 축**(검수장 순위6) — apt #105·#100 · korea #57·#68·#69 | KO `tournament-entry` 지시서와 **일괄**. 2번과 같은 축이다 |
| 4 | **KO S5** — `holdem-tournament-how-to-enter` 11건 | STALE #19가 APT 인천 종료로 만료된다. 본체 = `docs/kr-remediation-spec.md`, 착수 전 `홀덤검수/reports/fix-handoff-remaining-2026-08-09.md` §2-보·§3 |

🪶 **함께 볼 것**: `holdem-tournament` **딥스택 3중 자기모순**(표 「100–200BB+」 ↔ 본문 「200BB+」 ↔
일반 「100–300BB」) — 남은 로케일 **en es id pt zh zh-hant**, de 수정본을 옮기면 된다
(일반=100–200BB · Deepstack=**기준선 없는 상대 개념**). 08-13에 같은 파일 7로케일을 이미 열었으니
**그 파일을 열 때 함께 닫으면 왕복이 줄어든다.**

---

## ▶ 그 밖에 열려 있는 것

### 🔴 1차 확인이 필요한 잔여 3건 (전부 **출처 확인이 선행**돼야 한다)

| # | 무엇 | 왜 |
|---|---|---|
| 1 | **KO `apt-incheon` 295행 「$3.50 Step Satellite」** | 같은 글 「바로 답」은 「$20 스텝 → $200 메가」로 옳다 — **한 글 안에서 값이 갈린다.** KO 표는 `$3.50 → $35 Mega`를, EN 정본은 `$20 Step → $200 Mega`를 「마지막 경로」라 한다. **Natural8 1차 확인 후** 판단(다국어 4곳은 EN 정본 문형으로 이미 닫았다). 🪶 **8/16에 이 파일을 여니 그때 함께 닫아라** |
| 2 | **`GGPoker SuperSatellites` 고유명** | 08-13에 «주장 제거»로 중립화만 했다. ggpoker.com 404로 1차 실패 — **검수장에 근거 URL 요청** 후 고유명 승격 여부 결정 |
| 3 | `holdem-tournament`의 `:::note` 출처 표기 | **casinos.at·spielbanken-bayern.de를 「abgerufen」(열람함)으로 적어 뒀는데 접근 불가 출처**다(검수장 실측 403). 열람 주장을 뺄지 다른 출처로 바꿀지 판단. 🪶 위 딥스택 건과 **같은 파일** |

### 📬 검수장 회신 잔여 — **#2 하나만 남았다**

| # | 항목 | 왜 아직인가 |
|---|---|---|
| 2 | `hand-rankings` #65 「토너먼트는 딜러 오른쪽 첫 좌석에서 시작」 | 내가 **WSOP Rule 85**를 근거로 박았는데 검수장은 「TDA 2024·RRoP 어디에도 없다」고 했다. **출처가 서로 다르니 WSOP 원문으로 재확인**할 것 — PDF 경로는 아래 「1차 출처」에 확보돼 있어 **분량이 짧다** |

> 🪶 `holdem-bubble #27`(FT버블 테이블 구성)도 남아 있다 — 검수장이 룰 중립안으로 교체했다
> (구안 「an den letzten zwei Tischen」은 TDA에만 맞고 **WSOP Rule 68.a(10인 한 테이블)와 충돌**).
> **그 글을 다시 열 때 닫아라.**

### 적대 QA 잔여 8항목 — 코퍼스 단위라 **사장님 판단이 섞인다**

| 항목 | 실측 | 왜 아직인가 |
|---|---|---|
| `rabattieren` / `Rabatt` | **13곳 / 4편**(outs 5 · position-play 4 · limping 3 · strategy 1) | 네이티브: 독일어에서 **상거래 할인 전용어**(`abwerten`·`discounten`이 실사용). 하우스 표준으로 굳어 있어 **반만 바꾸면 더 나쁘다** |
| `Side Pot` ↔ `Nebenpot` | betting-actions 4 · showdown 10 vs **split-pot 18** · readnext 카드 제목까지 갈림 | 독일 실사용은 압도적 `Side Pot`. 첫 등장만 병기 권고 |
| 드로 라벨 | `Gutshot-Straße`(equity·implied-odds) vs `Gutshot (Inside Straight)`(outs) | 드로 표에 `-Straße`는 **완성된 족보로 오독**된다. outs 표기가 정답 |
| `Leaks` ↔ `Lecks` | equity 1곳만 `Leaks` | 어느 쪽으로 통일할지가 판단 |
| 🔴 `holdem-tiebreak-rules:229` | 「ungerader Chip」 = **오역**(홀수 칩 아님, 나머지 칩) | 9편 밖. `hand-rankings`는 「überzähliger Chip」으로 정정했다 |
| `outs` 19,1% ↔ 19,6% | 같은 글에 나란히 있고 **둘 다 맞다**(플랍→턴 9/47 · 턴→리버 9/46) | 사실오류 아님. 한 줄 단서를 붙일지가 판단 |
| WSOP Rule 96.a | 연속 올인 예외가 `betting-actions`에 없다 | 누락이지 오류 아님 |

🔴 **오탐 2건 — 다음 렌즈도 같은 걸 지적한다. 프롬프트의 «의도적 제외 목록»에 넣어라:**
① **통화 `€10` → `10 €`**(21곳) — DIN 5008로는 네이티브가 옳지만 **`€` 선행이 정본**이다(2026-08-11 확정).
② `equity` 하프팟 flushdraw 35% — 바로 다음 문단이 그 전제를 뒤집는 **의도적 구성**이다.

### ▶ 대기 중인 큰 트랙

| 트랙 | 메모 |
|---|---|
| C. 다국어 «판 전체 재동기화» | 🔴 **사장님 판단 대기 #2.** 노출 미미(전 언어 클릭 합계 2), 정확성 명분뿐. 🪶 **오늘 실측 하나가 붙었다**: `blind-meaning` 표 헤더가 **EN·zh-hant를 뺀 23 로케일 전부** 같은 결함인데 08-14에 5곳만 고쳤다 — 나머지 18곳(`master=07-11`)이 이 트랙 소관이다 |
| es LATAM 보충 패스 | Spain 베이스 42/42는 완결. **Country=Mexico부터, 슬랭 우선** |

---

## 🔴 사장님 판단 대기

| # | 안건 | 메모 |
|---|---|---|
| 1 | 🔴 `starting-hands-chart #13` — **수치를 고칠지 핸드 목록을 고칠지** | UTG **13%** 수치와 핸드 목록이 충돌한다(다른 4곳은 그 핸드들을 폴드하라고 한다). 조합수로는 13%가 맞다(144콤보 = 10.86%). **SEO·전략 판단이라 본체가 못 정한다.** 이것만 정해 주면 C층에서 유일하게 막힌 자리가 풀린다 |
| 2 | 다국어 «판 전체 재동기화» — **할지 말지부터** | 위 표 참조. 착수 시 본체는 `docs/multilang-sync-plan.md` |
| 3 | de 콘텐츠 신설 | 칩 분배 계산기(`verteilung rechner` LDA**7** · 비대체 자산 · KO에도 없어 역수입 가능) · 세금 FAQ 흡수(승인됨, 미착수 · BFH X R 8/21 1차출처 확보) · 툴 브랜드 대응(gto wizard 2,400 · equilab LDA3) → **솔버 다국어화 시 함께 결정** |
| 4 | 🪶 PDF 용지 규격 — ko·ja·zh도 A4 시장이다 | `generate-beginner-pdf.mjs`가 `@page { size: Letter }` 하드코딩이었다. 08-10에 언어별 `pageSize` 도입, **de만 A4**. 재생성은 `node scripts/generate-beginner-pdf.mjs ko ja zh` + CONTENT에 `pageSize: "A4"` 한 줄 |
| 5 | 🪶 PDF 수요는 족보 밖에도 있다 | `poker wahrscheinlichkeiten pdf` · `poker strategie pdf` 10/18 · `poker starthände tabelle` 110/**LDA5** |
| 6 | `es` 국기 판단 | |

---

## ⏰ 시한이 박힌 것

**본체는 `docs/update-calendar.md`다.** 현재 살아 있는 것 —

| 시점 | 무엇 |
|---|---|
| **8/16(일)** | **APT 인천 종료 → 위 4건 묶음** |
| **8/29** | EPT 바르셀로나 종료 → `ept-barcelona-2026-guide` 8개 언어 결과 아카이브 전환 |
| **9/초 (8/29~9/5)** | ★★ **GTO 13편·`/solver` 색인 성과 확인 → T24(솔버 다국어) 착수 판단.** 볼 것 셋은 캘린더에 있다. 🔴 **GA4로 솔버 유입을 볼 거면 구간을 8/14 이후로 잘라라** — 그 전은 자기 참조 referral로 오염돼 있다(8/14에 사장님이 「원치 않는 추천」 설정 완료) |
| **9/5경** | `/hand-chart` 색인 해제 효과 재측정 |
| **9/10경** | 족보 필라 앵커 정비 효과 확인 — 아래 절 |
| **9/6 · 10/23~25** | de `holdem-tournament` SBPM 예선 마감 / 결승. 지나면 「11. Auflage」·개런티 갱신 |
| **10/초** | JOPT 오사카 바이인 공개(글에 약속함) 🔴 **단 대상 파일이 발행 취소돼 없다** — 글을 새로 쓰는 판단이 먼저 |
| **12/31** | K-ETA 면제 만료 — **한국 대회 글 전 언어 영향.** 가장 광범위하다 |

### ⏰ 9/10경 — 족보 필라 앵커 정비 효과 (`/hands` 재측정은 취소됨)

08-13에 §17 금지어 계열 7건을 걷어내고 그 자리를 **「홀덤족보」 붙여쓰기**로 채웠다(0건 → 7건).

```bash
node scripts/gsc-query-pages.mjs "홀덤족보" --days 7        # 8,100 ★우리 진짜 헤드텀(붙여쓰기)
node scripts/gsc-query-pages.mjs "홀덤 족보 순위" --days 7  # 390 (기준선이 있는 쿼리)
```

**기준선(2026-08-09 GSC 7일)**: 필라 「홀덤 족보 순위」 63노출 **12.1위** · 「포커 족보」 23노출 27.8위.

🔴 **「포커 족보」 18,100을 헤드텀으로 세지 마라** — 자동완성 21개에 **홀덤이 하나도 없다**
(마운틴·7포커·5포커·RDR2까지 섞인 **다른 게임들의 볼륨**). 33.7위인 건 **실패가 아니라 의도 불일치**다.
★ 세븐포커 의도는 `holdem-vs-7poker-hand-rankings`(8.9위)가 받을 자산이다.
⚠ 앵커 변경은 재크롤에 시간이 걸린다 — 4주에 안 움직여도 실패로 단정하지 말고 **8주까지** 볼 것.

---

## 🔴 손대면 안 되는 것 / 되살리지 마라

| 무엇 | 결론 |
|---|---|
| **`/hands` 색인** | 🔴 **noindex 유지.** 08-13에 열었다가 같은 날 되돌렸다. 필라가 29.4위 → **12.1위**로 오른 건 «실측된 성과»이고 도구가 9.4위를 회복한다는 건 «가설»이다. **실측된 성과를 가설에 걸지 않는다.** 양쪽 논거는 `app/hands/page.tsx` 상단 주석에 **전부 보존**돼 있다 — 열기 전에 반드시 읽어라. **「도구가 9.4위였다」는 이미 계산에 들어간 관찰이라 새 근거가 아니다** |
| **트레이너 랜딩 `/trainer` 신설** | 🟠 **기각.** ①노릴 키워드 10개 전부 볼륨 데이터 없음 ②이 축은 `/solver`가 이미 노림(홀덤 gto 390 등) ③`/solver` GSC가 쿼리 1·노출 1·32위 — **있는 진입로가 아직 아무것도 못 잡았다.** 대신 **랜딩에 흡수**했다(섹션 1 + FAQ 2문항, 새 URL 0개). 🪶 **재론 조건**: 「gto 트레이너」류에 실측 볼륨이 잡히거나, `/solver`가 「홀덤 gto」(390)에서 실제 노출을 얻은 뒤 |
| **`/pub/*` 11개** | ⏸ **사장님 지시로 보류 — 건드리지 말 것** |
| **`app/home-client.tsx`** | 🪶 **죽은 파일**(어디서도 import 안 됨 · 현재 홈 = `community-home`). 삭제할지 판단 대기. 08-13에 죽은 `schema` prop만 걷어냈다 |

---

## 🔗 솔버 프로그램과의 인계

솔버는 **별도 배포**(`solver.holdemmaster.com`)이고, 그쪽 세션이
`~/Downloads/클로드-프로그램만들기/handoff-to-main-site/`에 요청서를 놓는다.

- 🔴 **요청서의 「배포 대기 중」을 믿지 말고 라이브 번들을 먼저 확인하라.** 지난번엔 이미 배포돼 있었다
  (`curl` → 번들 grep). **순서를 반대로 잡으면 같은 불일치를 반대 방향으로 만든다.**
- 🪶 요청서의 분모·전제도 실측과 갈렸다(「13편 전부」 → 실제 11편 · 「`%팟` 표기」 → 0건).
  **검수장 지시서와 같은 규율을 적용하라 — 수치는 직접 센다.**
- 🪶 역방향 링크는 솔버 쪽 `ARTICLE_URLS`가 갖고 있다 — **포스팅 slug를 바꾸면 그쪽에 알려야 한다.**
- ✅ GA4 「원치 않는 추천」 = **2026-08-14 완료**(사장님 직접). 유입 측정 신뢰 구간은 **8/14부터**다.

### 📤 솔버 세션에 보낼 것 — **PWA 요청서** (2026-08-15 사장님 결정)

본체 = `docs/handoff-to-solver-pwa.md`. **사장님이 그 파일을 솔버 세션에 전달한다.**

**결정: PWA는 «솔버»에 한다. 본체에는 하지 않는다.**
- 솔버는 «열어서 쓰는 무료 도구»라 설치 동기가 실재하고, 트레이너 → 「이 스팟 해설 읽기」로
  본체 13편에 UTM 링크를 거는 **순환이 이미 만들어져 있다.** PWA는 그 입구를 홈 화면에 박는 일이다.
- 본체는 유입 77%가 검색이라 아이콘을 눌러야 할 이유가 약하고, **SSG 630페이지 + 매일 크론 재빌드**라
  서비스워커 캐시가 낡은 HTML을 붙들 위험이 있다 — 솔버 세션의 「위험 0」은 **SPA인 솔버에서만 참**이다.
- 🪶 **푸시는 이번 범위에서 뺀다** — 통로보다 보낼 내용이 먼저다.

**기준선 (2026-08-15 · GA4 28일) — 4주 뒤 이걸로 판정한다**
| 지표 | 값 |
|---|---:|
| 재방문 비율 | **9.8%** (207 / 2,113) |
| 평균 세션 | 신규 167초 · **재방문 385초(2.3배)** |
| DAU / WAU / MAU | 124 / 668 / 2,100 → **DAU/MAU 5.9%** |

> 🔴 **GA4 벤치마크 「포커 및 카지노 게임」 상위 25%(1,100명)를 목표로 삼지 마라.**
> 그 집단은 **게임 앱**이 주력이라 DAU가 구조적으로 높다. 우리 곡선은 동종 **중앙값에 이미 근접**해 있고,
> 30일 활성이 1.1천 → 2.1천으로 **우상향 중**이다. 성장기에는 신규가 분모를 키워 재방문 비중이 낮게 나온다.
> **볼 것은 순위가 아니라 DAU/MAU의 방향이다.**
> ⚠ PWA 단독 효과는 분리 측정 불가 — 같은 기간에 네이버 색인·제목 재조준이 함께 돈다.

**본체가 이어서 할 일(솔버 PWA 배포 «후»)**: `/solver` 랜딩에 설치 안내 + FAQ 15번째 문항.
⚠ **솔버가 실제로 PWA가 된 뒤에 쓴다** — 안 되는 기능을 먼저 적지 않는다.

### 📬 솔버 세션 요청서 4건 — 판정 완료 (2026-08-14) · **회신용으로 그대로 전달 가능**

원본 = `본체세션_전달메시지.txt`(2026-08-13 작성). 🔴 **4건 중 3건이 이미 닫힌 건이었다** —
지시서가 낡는 그 유형이다. 실측 근거를 함께 적는다.

| # | 요청 | 판정 | 근거 |
|---:|---|---|---|
| 1 | [급함] 13편 EV 표기를 bb로 (값 ÷10) · 6편 「%팟」 오표기 | ✅ **이미 완료(08-13)** | 13편 중 **11편의 `EV (bb)` 행이 요청서 표와 값까지 일치**. 나머지 2편은 결함이 아니다 — `donk-bet-strategy`는 **애초에 OOP/IP EV 요약 행이 없는 글**이고 `paired-board-strategy`는 `EV (bb)`가 **열 제목**이다. `%팟` 표기 **0건** · `0.1bb 단위` 라벨 **0건**. 잔존 「벳 18/14/11」은 전건 **주석의 검산 기록**이거나 「3벳 11bb」(bb라 옳다) |
| 2 | [확인] 랜딩 H1이 비어 있음 | ✅ **이미 완료(08-13)** | 라이브 산출물 **h1 정확히 1개**. 형제 허브 7개와 h1이 어긋나 있던 것을 08-13에 h2→h1로 올렸다. 요청서가 본 것은 그 이전 판이다. 🪶 08-14에 title도 바뀌었다 — 「무료 GTO 솔버 — **홀덤 GTO 표 계산 프로그램**」 |
| 3 | [제안] GTO 트레이너 랜딩 신설 | 🟠 **기각 유지 · 랜딩에 흡수** | 08-13 라쿠 실측에서 제안된 후보 **10개 전부 볼륨 데이터 없음**, 08-14 재실측에서도 「gto 트레이너」류는 안 잡힌다. 반면 랜딩이 노리는 축은 실재한다(홀덤 gto 390 · 홀덤 gto 표 210 · 홀덤 gto 프로그램 140). → **얇은 새 URL 대신 랜딩을 두껍게** 했다: H2 「GTO 트레이너는 어떻게 채점하나요?」 + FAQ 2문항. 🪶 **재론 조건**: 「gto 트레이너」류에 실측 볼륨이 잡히거나, `/solver`가 「홀덤 gto」에서 실제 노출을 얻은 뒤 |
| 4 | [정보] UTM 부착 완료 · GA4 추천 제외 확인 요청 | ✅ **완료(08-14, 사장님 직접)** | 측정 신뢰 구간은 **8/14부터**. 그 전은 자기 참조 referral로 오염돼 있다 |

**측정할 때 쓸 UTM 표** (솔버 세션이 붙여 배포함 · `utm_source=solver` · `utm_medium=referral`):

| `utm_content` | `utm_campaign` | 도착지 | 이 값이 크면 |
|---|---|---|---|
| `navbar` · `about-credit` | community | 홈 | — |
| `about-landing` | landing | `/solver` | **앱에 온 사람이 설명을 더 원한다** |
| `preset-card` · `preset-preview` | article | 해설 13편 | 예제 → 해설 흐름이 돈다 |
| `trainer-feedback` | article | 해설 13편 | **트레이너 → 블로그 순환이 돈다** |
| `static-fallback` | (각각) | 홈·`/solver`·해설 13편 | JS 미실행 유입 |

> ⚠ **저쪽 폴더(`클로드-프로그램만들기`)는 읽기 전용이다.** 회신은 이 표를 사장님이 전달하는 방식으로 한다.

---

## ▶ 백로그 (두 트랙 밖)

| # | 할 일 | 메모 |
|---:|---|---|
| 1 | 프로덕션 E2E 마감 | 검수 세션에 `BASE=https://www.holdemmaster.com`으로 요청 |
| 2 | 다국어 정책 페이지 | `/privacy`·`/terms`·`/contact`가 한국어뿐인데 링크는 영어 라벨 |
| 3 | 리버 승부 해설 | 복기 표에 승률만 있다. 「키커로 갈렸다」를 `score5`의 키커로 자동 생성 가능 |
| 4 | 🪶 FAQ 섹션이 아예 없는 2편(`ru`·`tr` 각 1편) | `check:faq-schema` 커버리지에 «미판정»으로 출력된다. **FAQ를 새로 쓸지는 SEO 판단** |
| 5 | 🪶 저가치 스키마 잔여 | `/about` `/contact` `/privacy` `/terms` `/login`에 WebPage/AboutPage/ContactPage 정도. **검색 수요가 거의 없어 우선순위 최하** |

---

## 📚 계속 유효한 규율 (트랙과 무관하게)

> 상시 함정·렌즈 운용 = `REVIEW-PROTOCOL.md` 맨 아래 · 다국어 경화 교훈 = `docs/lessons-i18n-hardening.md`.
> **트랙 착수 전 둘 다 읽어라.**

### ⚙ 미러 작업 4+1단계

0. 🔴 **어느 로케일이 «정답»을 갖고 있는지 먼저 찾는다. EN이라는 보장이 없다** —
   `equity`·`flush-vs-straight`·`all-in-rules`는 **de 정본**, `positions`·`implied-odds`는 **ja 정본**,
   `texas-…-beginners` #28·#29도 **de 정본**이었다(EN 포함 24곳이 안 따라와 있었다).
   정답 보유가 EN이 아니면 **EN을 그쪽에 맞춰 고친 뒤** 전파한다.
1. **EN 마스터에서 자리를 특정한다.** EN도 같이 고치고 `updated` 갱신.
2. **전 로케일 «동일 상대행»에서 원문을 뽑는다** — 번역본은 EN과 문단 순서가 평행하다.
   단 **FAQ 구역은 로케일마다 개수가 달라 어긋난다** → 문자열로 잡을 것.
3. **로케일별로 개별 작성.** 각 로케일의 **기존 어휘를 재사용**해 최소 침습으로.
4. **결함 문자열을 grep해 «잔존 0»을 확인.** 그 다음 게이트 → 빌드 → 커밋.

### 🔴 반복해서 값을 한 규율 다섯

1. **「N곳」을 가정하지 마라.** 실측에서 계속 깨졌다 — `split-pot #21` **ja 1곳** · `fish #30` **pt 1곳** ·
   `short-stack #1` 7곳(EN이 이미 옳았다) · `card-counting #20` **8로케일 × 2자리** ·
   `beginners #28·#29` **24로케일 × 2자리**. **언제나 직접 grep해 세고, 로케일별로 개별 판정하라.**
2. 🔴 **미탐을 «부재»로, 히트를 «존재»로 읽지 마라.** 08-14 하루에 세 번 걸릴 뻔했다 —
   betting-actions에서 ar·fa·he·pl·th 5곳이 「없음」으로 나왔으나 **전부 보유**했고(영문 `big blind`만 본 정규식),
   번카드 스캔의 fr=6·tr=4는 **전건 오탐**이었다(tr `yaklaşık`=대략). **그 자리를 직접 열어라.**
3. 🔴 **`masterUpdated` 스탬프를 신뢰하지 마라.** 같은 07-19 스탬프를 단 **de·es·zh가 두 건 다 미반영**이었다.
   스탬프는 «그 리비전을 봤다»는 뜻이지 «전부 반영했다»가 아니다 — **드리프트는 문자열로 세라.**
4. 🔴 **지시서·원장도 낡는다.** C층 잔여 6편 중 **셋이 이미 닫힌 건**이었다. **고치기 전에 실측하라.**
   그리고 **「N곳이 비어 있다」를 결함으로 읽기 전에 «정본이 그렇게 정한 것 아닌가»를 먼저 보라**
   (선례 커밋을 `git log -S`로 판다 — 이 레포는 커밋 메시지에 «왜 안 바꿨는지»를 적는다).
5. 🔴 **한정어를 «덧붙이는» 정정은 금지** — 거짓 주장 문자열이 살아남아 게이트에도 안 잡힌다.
   단 기존 서술이 «참이지만 불완전»하면 덧붙이는 게 맞다. 그리고 **기존 서술을 «빼는» 정정이 더 안전하다**
   (근거를 새로 지어내다 사고가 난 게 누적 4건).

> 🔴 **부재는 «누락»이 아니라 «해당 없음»일 수 있다 — 미러에서 새 문항을 만들지 않는다.**
> 판정이 끝난 자리는 **`docs/locale-intentional-diffs.md`**(append-only)에 한 줄씩 쌓는다.
> 그 파일에 **없다 = 「아직 판정 안 함」**이지 「차이 없음」이 아니다.

### 🔴 검수장이 준 대체 수치도 검산하라

「라이브는 핸드당 약 2분」의 근거로 WSOP Rule 126.c·TDA RP-8 B를 쓸 뻔했는데, 그 2분은
**버블 핸드포핸드에서 «시계를 깎는 규정값»**이지 실측 소요시간이 아니다.
**규정값을 실측으로 둔갑시키면 §12-B 위반이다.** → 이미 검증된 형제 로케일의 실무 범위를 채택했다.
🪶 같은 유형: **TDA RP-8 B 원문은 「2-minutes per hand」**로, 지시서의 「최대 3분」과 다르다.

### de 키워드 조사 도구 주의

🔴 라쿠 **서제스트는 시드 언어를 못 따라간다**(`vierling` → 미국 DB로 새서 미시간 식당·야구선수).
**지역·언어를 명시하는 `search-volume-history`(German/Germany)가 정답이다.**
그리고 조회한 독일 포커 키워드가 **12개월 −23~−69%로 전부 하락 추세**다 — 신규 투자 판단 시 감안할 것.

### 1차 출처 — 확보해 둔 경로 (재조사 금지)

> 🟢 **de 관련은 §5 「다시 파지 마라」 13축을 먼저 읽어라**
> (`홀덤검수/reports/fix-handoff-de-S1-S3-2026-08-12.md`) — 검수장이 «찾아봤고 없다»를 **근거와 함께**
> 박아 뒀다. **재조사에 시간 쓰지 마라.**
> ⚠ 1차 접근이 여럿 막혀 있다: `worldpokertour` 403 · `pokerstars` 404 · `casinos.at` 403 ·
> `spielbanken-bayern.de` 403 · `ggpoker.com` 404. **막힌 걸 「확인했다」로 적지 마라**(위 잔여 #3이 그 사고다).

- **WSOP 2026 공식 룰북**: `wsop.gg-global-cdn.com/wsop/pdfs/2026-WSOP-Tournament-Rules.pdf`
  → `pdftotext -layout`. 쓴 조항: 72·75·84·85·90(a/b.1/d)·95·96·97·99·100.b·103·116·117·119 + 용어집 DEALER BUTTON
  · **126 전문**: 126.a 「the clock being **paused**」 · 126.c 「each hand will run **2 minutes** off the clock,
  **regardless of how long the hand actually takes**」 · 126.d 배치 아닌 **매 핸드** · 126.e 「Blinds will **continue to increase**」
- ⚠ **`pokertda.com`은 이 환경에서 접속 불가**(curl·Playwright·WebFetch 전부 ECONNREFUSED).
  TDA 원문은 **버지니아 주 규제기관 공개 PDF**로 확보했다 —
  `ris.dls.virginia.gov/uploads/11VAC20/dibr/Poker Tournament Directors Assoc_2019 Rules-20221228170122.pdf`
  (2019 Longform + Illustration Addendum). 쓴 조항: 12·14·16·17·18-A/B·19·20·68 + Addendum 16/18 · **RP-8**
- ⚠ **Robert's Rules는 1차 출처로 쓰지 않았다** — 전문 호스트 `pokercoach.us`가 **Betsperts Media 소유**라
  «저자 원본»이 아니다. 깨끗한 호스트를 찾으면 캐시 캡 「a bet and three raises」를 되살릴 수 있다.

### 🔴 룰 인용 시 두 판을 갈라 써라

- **R1 캡**: 「3 raises → 4」는 **캐시게임에선 3이 맞다**(해제 조건도 캐시는 «핸드» 헤즈업이라 WSOP와 정반대).
- **무늬 서열**: WSOP 용어집 DEALER BUTTON의 「highest card by rank **and suit**」는
  「at the start of a **new game**」(= 캐시 신설 테이블)에만 적용되고 **Rule 85가 토너먼트를 명시적으로 배제**한다.
  → **「캐시 신설 테이블」로 한정해 쓸 것.**

### 적대 검수

- **렌즈 «종류»가 성과를 가른다.** 4렌즈에서 겹친 건 3건뿐이고 나머지는 전부 단독 검출.
  ★**룰북 축어 대조 렌즈**를 따로 두면 «내가 방금 박은 조항 번호»가 걸린다(12곳 중 2곳 불일치, 둘 다 그날 내가 넣은 것).
- **2차 교열 패스는 매번 값을 한다.** 반영이 새 결함을 만든 게 한 세션에 3+3+1건.

### 다음 언어 경화 6단계 (de 6/6에서 확립)

①정본 재확인 → ②**기계 게이트 먼저** → ③편집 → ④**되읽기 패스**
→ ⑤**현지 네이티브 적대 QA 2렌즈**(네이티브 + 교열, 교열엔 `git diff`) → ⑥빌드·게이트·커밋·WORKLOG

★**여섯 세션 전부에서 최대 ROI는 「이름표」 유형이었다** — 콘텐츠는 이미 있고 **현지 검색자가 쓰는
낱말만 없는** 상태. H2·표 이름·태그 교체로 닫히고 **§13 리스크가 0**이다.
실적: `blinds tabelle` · `Kombinationen` 4,400 · `karten reihenfolge` 880 ·
`poker begriffe` 260(vs `glossar` 10 = **26배**) · `pokerturnier` **720**.

> 🔴 **볼륨표는 리서치의 «요약»이지 대체물이 아니다. 글마다 현지 SERP를 실제로 봐라** —
> SERP는 볼륨표가 못 주는 «경쟁사가 무엇을 제공하는지»와 «어떤 낱말을 쓰는지»를 준다.
> 한 세션이 이걸 건너뛰어 `pokerblätter` 720과 PDF 어포던스를 통째로 놓쳤다.

---

## 📊 상태 (2026-08-14 실측)

| 지표 | 값 |
|---|---|
| build | **630 페이지 성공** (69 blog + 457 intl + static 60) |
| `audit:hard` KO | **41/41 · 🔴 0건** |
| `audit:hard` 로케일(en de es id ja pt zh zh-hant) | **전수 🔴 0건** |
| `check:hreflang` | 481페이지 · 50세트 · 🔴 0건 |
| `canonical:check` | KO 104 라우트 · 🔴 0건 |
| `audit:hard:selftest` | **76/76** · `check:de-style` 셀프테스트 104/104 |
| **구조화 데이터** | ✅ **66페이지 완료** (허브 10 + 로케일 50 + 도구 6). 실제 `schema={}` prop 잔존 **0건** |
| **검수장 재검수 원장** | 42편 2,988주장 · OK 2,558 · WRONG 30 · RISKY 116 — **순위1~5 종결.** 남은 WRONG은 🔒8/16 국적축 5건뿐 |
| **§6 축 B (de 키워드)** | ✅ 전부 종료 |
| **C층 트랙** | ✅ **종결 2026-08-14** — 잔여 6편 중 셋은 이미 닫혀 있었고 셋을 고쳤다(총 65곳) |
| **미러 전파 / de 구 검수 / 스키마 / 허브** | ✅ 전부 종료 — 경위는 `WORKLOG.md` |

> ⚠ **검수 폴더(`~/Downloads/홀덤검수`)는 검수장이 동시에 작업 중이다.** 읽기만 하고 **수정하지 마라.**
> 착수 전 `git pull` 필수. 원장을 볼 때는 최신인지 확인할 것.

## 📦 과거 로그는 아카이브로

**`docs/handoff-archive-2026-08-09.md`** — 2026-07-13 ~ 08-08 세션 로그 전체(약 3,980행).
그 이후 세션 로그는 **`WORKLOG.md`가 본체**다.
완료 트랙의 방법론은 `docs/gto-solver-series-spec.md`·`docs/kr-remediation-spec.md` §4에 남겼다.
