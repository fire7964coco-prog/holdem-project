# 내비게이션·동선 실측 진단 + 1차 처방 기록 (2026-08-16)

> 사장님 지시 「사이트 구조 논의」 → 축=내비게이션·동선, 목적=진단부터.
> **4주 뒤(9/13경) 판정의 기준선이 이 파일이다. 지우지 마라.**
> 처방 P1(관련글 로직)은 커밋 `f5a3d847`, 위생은 `f727b515`로 배포됨.

## 1. 한 줄 진단

**최대 유입 관문(블로그 상세, 오가닉 38.2%)이 가장 얕은 동선(1.73 p/s)을 갖고 있었고,
그 격차가 유입의 68%인 모바일에서 최대(1.57 vs 데스크톱 2.06)였다.**

## 2. 기준선 (GA4 Organic Search 28일 · 2026-07-19~08-16 · 총 1,832세션)

| 지표 | 기준선 | 4주 뒤 보는 것 |
|---|---|---|
| KO 블로그 상세 랜딩 | 700세션 · **1.73 p/s** | ≥1.85 기대 |
| ↳ 모바일 | 500 · **1.57** · 참여율 55.8% | 격차 0.49 → ≤0.40 |
| ↳ 데스크톱 | 190 · 2.06 · 81.6% | 회귀 감시(≥2.00) |
| 다국어 블로그 상세 | 131 · **1.27 p/s** | ≥1.45 기대 |
| 블로그 글 → 다음 페이지뷰 | 762뷰: 블로그 55.2% / tournaments 18.6% / 커뮤니티 11.7% | 분산 확대 |
| 관련글 상수 목적지 9편의 내부 유입 | **125세션 (블로그 랜딩의 17.9%)** | 감소해야 정상 (분산의 증거) |
| 관련 카드 유니크 목적지 | 6편 → **배포 후 56편** (빌드 산출물 실측) | — |

**측정법** (이 세션에서 실제 돌린 것):
- `node scripts/ga-fetch.mjs --days 28` — 채널·랜딩 개요
- `node scripts/ga-page.mjs blog/<slug> --days 28` — 「조회 − 랜딩 = 내부 유입」 분리
- **GA4 `pageReferrer` 차원이 살아 있다** — `pagePathPlusQueryString` × `pageReferrer CONTAINS
  holdemmaster.com/blog/` 조합으로 내부 이동 그래프를 직접 센다(고정 스크립트화 후보: `ga-flow.mjs`)

## 3. 무엇을 고쳤나 (커밋 `f5a3d847` · `f727b515`)

1. **관련글 3편** — 기존: KO `filter(같은 category).slice(0,3)` = 배열 앞 3편 고정
   (전략 30편이 전부 같은 3편 · 토너먼트 1번 카드가 작년 wsop-2025). 다국어는 **카테고리
   필터조차 없이** 로케일 배열 앞 3편(EN 43편 전부 동일).
   → `lib/related-posts.ts` 신설: ①클러스터 형제(학습 순서) ②GTO는 전략 필라로
   ③카테고리 **원형 회전** 폴백. 제외 목록: wsop-2025 · holdem-spr(9/13까지, `until` 자동 만료) · noindex.
2. **이전/다음(모바일 sticky 「다음 글」 포함)** — 날짜순 이웃 → 커리큘럼 순서(`courseNeighbors`).
   클러스터 밖 글은 기존 날짜순 폴백(회귀 0).
3. **로케일→클러스터 표 단일화** — `CLUSTERS_BY_LOCALE`을 `lib/pillar-clusters.ts`로
   (`clustersForLocale`). hub-trending의 사본 제거.
4. 위생: 죽은 `components/header.tsx`·`intl-header.tsx` 삭제 + **skip-link를 layout.tsx로 이식**
   (사이트에 0개였다) + 「56편·57편」 하드코딩 → `totalPosts` prop 파생(빌드 실측 71편).

**배포 전 반증을 먼저 돌렸다**: 상수 목적지 9편의 내부 유입이 125세션(17.9%)으로
«관련글 슬롯은 눌리고 있다»가 확인된 뒤에 코드를 썼다. 5% 미만이었으면 이 처방은 기각이었다.

## 4. 남은 처방 (미착수 — 순차 배포로 귀인을 지킨다)

| # | 무엇 | 착수 조건 |
|---|---|---|
| P2 | 대회 가이드 5편(`tournament-guide-post.tsx`)에 하단 탭바·모바일 섹션바 없음(전역 네비 0). 형제 링크가 `hidden sm:flex`라 모바일에서 실종. 없는 탭바 몫 62px 공백 | **P1 배포 +14일(8/30경) 이후.** pv 목표 걸지 마라 — 레이아웃별 실측 차이 없음(1.63 vs 1.60) |
| P3 | 클러스터 고아 편입: 라이브 이벤트 8편 → 토너먼트 필라 `group:"라이브 이벤트"`(EN Live Events 동형) · `texas-holdem-rules-for-beginners` → rules · GTO 13편은 **편입 말고** 미니맵 `fallbackPillarId` · `wsop-2025`는 제외 유지 · **`holdem-spr`은 9/13 이후** | 동일 8/30 이후. 게이트 `check:clusters` 신설 필수(커버리지·유령 slug·중복 소속·**만료된 면제**) — **package.json prebuild와 vercel.json buildCommand 양쪽에** 걸어야 실효(vercel은 npm 훅을 안 탄다) |
| P4 | `intl-footer.tsx` 링크 2개 → `hubPagesFor(locale)`. ⚠ side-rail은 `"use client"`라 **`lib/hub-pages.ts`로 데이터를 빼서** 서버·클라 공용으로 | P2와 함께 가능 |

## 5. 하지 말 것 (근거)

- **카테고리·태그 아카이브 URL 신설 금지** — 태그 199개 중 186개가 1편짜리(thin) +
  `/blog` 목록 자체가 오가닉 9세션(0.5%) + `/hands` 카니발 전례.
- **하단 탭바 「블로그」 탭 부활 금지** — 2026-08-04 `9f8f377d` 사장님 결정. 전제(홈 피드에
  71편이 카드로 전부 있음)가 지금도 참. 필요한 건 «목록»이 아니라 «다음 글»이고 그건 P1이 했다.
- **태그 칩 부활 금지** — 같은 커밋의 사장님 판단(목록으로 되돌리는 동선).
- **`lib/related-posts.ts`를 클라이언트에서 import 금지** — POSTS 전체가 번들에 실린다.
- **관련 카드 셰이프(6필드) 확장 금지** — 필드 하나가 71편×N으로 플라이트를 불린다.
- **R1 판정 전(9/13) 관련글 로직 재변경 금지** — 측정 오염.

## 6. 반증 조건 (9/13경 판정)

관련 카드 목적지가 6→56으로 넓어졌는데도 ① 상수 4편의 내부 유입이 안 줄고
② KO 블로그 상세가 1.73±0.05 안이면 — 「관련글이 관련 없어서 안 눌린다」 가설 기각.
그때는 «말단 글 독자는 답만 얻고 나간다»(의도 교란)를 정설로 채택하고 P3 이하를 재검토한다.
**판정은 비율(p/s·참여율)로. 절대 페이지뷰는 SEO 성장과 섞인다.**

## 7. 진단에서 나왔지만 처방 안 한 관찰 (후속 판단용)

- 1024~1279px 구간: 블로그 글의 전역 이동이 `BlogTopBar` 2버튼뿐인데 **href가 동일**(둘 다 홈).
- `/blog/roadmap`도 하단 탭바 없음(모바일 전역 네비 = BlogTopBar 2버튼).
- 허브 미등록 20개 로케일의 `/xx/blog`: FixedSideRail만 뜨고 모바일 전역 네비 0.
- `?tag=` 필터는 살아 있는데 진입 링크 0개(무해 — 방치 가능).
- 블로그 검색이 본문 미매칭(title+desc+category+tags만).
- 글별 «내보낸 비율» 실측: 필라 허브 57~79% ↔ 말단·이벤트 글 21~27%
  (단일 의도 교란과 섞여 있어 인과 단정 금지).
