# 🇯🇵 회차 7 브리프 — 횡단 표기·구조 소급 (트랙 종결 회차)

> 규격 = `docs/hardening-protocol.md` §3-⑥. 🔴 **B 구간은 이 파일 하나만 읽는다** — 원문·EN 축어는 전부 여기 옮겨 놨다.
> 작성 = 2026-09-09 A 구간(Opus) · HEAD `d30264df` 기준 실측.

## 0. 이 회차가 왜 «클러스터»가 아닌가

§7-A 클러스터 표는 **42편 6회차로 끝났다**(회차 6에서 완주 · `check:drift --locale=ja` 🔴 **0**).
회차 7은 zh-hant §7-C가 「**트랙 종결 회차(7)에서 소급**」이라 부르는 자리 — 회차 1~6이 «파일 안에서는 일관한데 코퍼스에서 갈린다»며
미결로 넘긴 것을 **자리 단위로** 닫는다. 편수가 아니라 **자리 수**로 관리한다.

🔴 **범위 밖 셋 — 손대지 마라**
1. **🔒 토너먼트 9편**(tournament · tvc · icm · bubble · short-stack · apt-incheon · ept-barcelona · wpt-australia · korea-poker-marathon)
   — GPT 팩 C 미도착으로 **동결**(핸드오프). 원문이 갈리면 A·B 보고서 36건이 통째로 무효다.
2. **GTO 13편 · wsop-2026 · japan-poker-tournaments-guide** — §7-E 대상 밖.
3. **헤드 우편함 회차(MA-121~127)가 가져갈 «사실» 자리** — §6 표 참조. 회차 7은 **표기·구조**만 만진다.

**대상 = 비동결 33편**. 이번 회차가 실제로 여는 자리 = 아래 A~G **약 60자리**.

---

## 1. 드리프트 — 미반영 0 (전수 실측 완료 · 새로 가져올 것 없다)

- `npm run check:drift -- --locale=ja` = **✅ 56 · 🔴 0 · 🟠 0**.
- 🔴 **회차 5 §5-C 2가 지적한 «게이트 사각»(EN이 `updated`를 안 올린 커밋)도 전수로 팠다** — 42편 각각
  `git log --since=<masterUpdated> -- lib/posts-en/<slug>.ts`. 50편에 커밋이 걸렸으나 **전건이 «이미 반영» 또는 «ja 무관»**이었다:
  - `holdem-continuation-bet` ← EN `851d8dc7`(08-20 · masterUpdated 08-19 **이후**) = GTO ⑨ 역링크 1문단 추가.
    → **ja L144에 이미 있다**(회차 4가 열었다 · `3bet-pot-bet-sizing` 링크 + `thumb:` + 98.4%).
  - `holdem-probability` ← EN `1d427c6d`(08-12) = 표 행 분리 `Pair → set / set → full house` → 2행.
    → **ja L137·L138에 이미 있다**(`| ペア → セット |` + `| セット → フルハウス | 7(フロップ)/ 10(ターン) | 33.4% | 21.7% |`).
  - 나머지는 `8d2aba44`(HSL 토큰 · 전 로케일 동시) · `09bd0697`(히어로 중복) · GTO 발행 커밋 등 **ja가 같은 커밋에 포함된 것**.
- 🪶 **결론: 회차 7은 드리프트 이식이 0인 첫 회차다.** B 구간은 «가져오기»가 아니라 «맞추기»만 한다.

## 2. 키워드 — 이번 회차의 축은 «표기»다

산출 = **`docs/keyword-bank/ja-notation.md` 신설**(표기 축 뱅크 · 클러스터 뱅크가 아니다).

🔴🔴 **B 구간이 반드시 알아야 할 것 — 이 회차의 판정은 «다수결»이 아니라 «실측»이다.**
`アンダーザガン` 축에서 **DFS 단독 조회가 반대 결론을 냈다**(`ポーカー アンダー・ザ・ガン` 70 vs `ポーカー アンダーザガン` null →
「中黒가 정답」). 라쿠 `suggest`로 교차하니 뒤집혔다 — 무中黒 **320** · `アンダーザガン ポーカー` **40**이고,
中黒 쪽은 **서제스트 13건 중 포커 의도 0건**(건담 만화 · TV · Deep Purple). **헤드/회차 6의 판정(`daf65be4`)이 옳았다.**
→ 이 축은 **이미 통일 완료**이므로 **B는 손대지 마라.** 되돌리면 볼륨 0 표기로 되돌아간다.

확정된 표기 판정 3건(근거·수치는 뱅크 §1~§3):

| 축 | 🔴 정본 | 근거 | 바꿀 표기 |
|---|---|---|---|
| 6인 테이블 | **`6max`** | `ポーカー 6max` 20 · `ポーカー 6max レンジ` 20 ↔ `6マックス` 서제스트 2건 **둘 다 0** · `6-max` null | `6マックス` 12 · `6-max` 4 |
| 저스테이크 | **`低レート`** | `ポーカー 低レート` 10(12개월 안정) ↔ `低ステークス` null · `ローステークス` 서제스트 3건 전부 null | `低ステークス` 4 · `ローステークス` 3 |
| favorite | **`有利`** | 현지 상위 공기어에 `本命` **0**·`有利` 0, 상위는 `勝率`로 말한다 · bad-beat가 **seoTitle까지** 「圧倒的有利」로 잠김 | `本命` 계열 4 |

## 3. 현지 상위 글 실측 (§3-⑤)

라쿠 `co-occurrence`(`ポーカー バッドビート` · 상위 페이지 본문 · 3크레딧):
공기어 26개 = `ビート` `ハンド` `プレイヤー` `カード` `Bad` `Beat` `解説` **`勝率`** `相手` `用語` `オールイン` `状況` `ドロー` …
🔴 **`本命` 0 · `有利` 0** — 일본 상위 매체는 favorite을 **`勝率(が高い)`**로 표현한다. §5-E 판정의 근거.
🪶 `用語`가 見出し에 4회·3사이트 — glossary가 이미 `ポーカー用語集` seoTitle을 갖고 있어 추가 조치 없음.

---

# 4. B 구간이 할 일 — 자리별 지시

## A. `## FAQ` → `## よくある質問` (비동결 **7편** · 7자리)

**자리**: `holdem-all-in-rules` · `holdem-betting-actions` · `holdem-blind-meaning` · `holdem-drawing-odds` ·
`holdem-game-order` · `holdem-showdown-rules` · `texas-holdem-rules-for-beginners` — 각 1자리.

**근거**: 코퍼스 **33편이 「よくある質問」 · 9편이 「FAQ」**. 회차 3 §5-A 16은 「`drawing-odds`만」이라고 적었으나 **실측은 9편**이다
(그 회차 7편 범위 안에서만 세었기 때문 — 진행 파일 기록이 부정확했다). 「よくある質問」이 일본 웹의 표준 조판이고 다수다.
🔒 `holdem-tournament-vs-cash-game`·`wpt-australia-2026-guide`는 **동결이라 그대로 둔다**(회차 7이 갈림을 9→2로 줄인다).

🪶 **FAQ 스키마는 절 제목이 아니라 `**Q.`+빈줄+`A.` 형식으로 잡힌다**(메모리 `faq-schema-build-output-is-truth`) — 이 변경은 스키마에 영향이 없다. C 구간에서 `audit:hard --schema`로 확인한다.

## B. 6인 테이블 표기 → `6max` (5편 · 16자리)

- `holdem-position-play` **10** · `holdem-when-to-fold` **2** : `6マックス` → `6max`
- `holdem-3bet` 1 · `holdem-blind-meaning` 1 · `texas-holdem-rules-for-beginners` 2 : `6-max` → `6max`

🔴 **일본어 문장에 라틴 `6max`가 박히는 것은 이 코퍼스의 기존 규범이다**(positions 11 · shc 15가 이미 그렇다).
🔴 **H2·seoTitle에 걸린 자리가 있으면 표기만 바꾸고 문형은 건드리지 마라**(§0-3). 앵커 id는 `slugify`가 CJK를 지워 어차피 비어 있다(§5-B 1) — 영향 0.

## C. 저스테이크 표기 → `低レート` (4편 · 7자리)

- `holdem-starting-hands-chart` 2 · `holdem-when-to-fold` 2 : `低ステークス` → `低レート`
- `holdem-betting-actions` 1 · `holdem-strategy` 2 : `ローステークス` → `低レート`

🪶 문맥에 따라 「低レートのライブキャッシュ」처럼 뒤 명사가 붙는 자리가 있다. **조사·접속이 깨지지 않게** 문장 단위로 확인할 것(파일 일괄치환 금지 — 메모리 «정정은 문장 단위»).

## D. favorite 어휘 → `有利` (2편 · 4자리)

**정본 근거**: `holdem-bad-beat`가 seoTitle `勝率80%で負けた — ポーカーのバッドビートとは` · title `圧倒的有利なのに負ける瞬間` ·
desc · tldr · H2 `どれくらい有利なら「本物の」バッドビートか?` · 표 L61 · 직답 L40까지 **전부 「有利」**로 잠겨 있다(제목은 못 바꾼다).

| 파일 | 줄 | 현행 | → 고칠 문안 |
|---|---|---|---|
| `holdem-cooler` | L60 | `あなたは**勝っていた**(多くは大きな本命)のに` | `あなたは**勝っていた**(多くは圧倒的有利)のに` |
| `holdem-cooler` | L68 표 | `あなたは**勝っていた**(本命)` | `あなたは**勝っていた**(有利)` |
| `holdem-glossary` | L163 표 | `**バッドビート** \| 大本命が運のドローに負けること` | `**バッドビート** \| 圧倒的有利だった側が運のドローに負けること` |
| `holdem-glossary` | L299 FAQ | `お金が入ったとき自分が本命だったのに` | `お金が入ったとき自分が圧倒的有利だったのに` |

**EN 축어 대조**(창작 아님을 보증):
- EN cooler L53 `you were **ahead** (often a big favorite)` · L61 표 `You were **ahead** (favorite)`
- EN bad-beat L21 `The key word is *favorite*` · L38 `a strong statistical favorite`
🪶 `holdem-cooler` L76·L176의 「本命」 아닌 자리는 이미 「勝っていた/負けていた」 구조라 건드릴 것 없다.
🔒 `ept-barcelona-2026-guide`의 `本命` 1자리는 **동결편이고 favorite 문맥도 아니다**(대회 우승 후보) — 손대지 마라.

## E. `holdem-kicker` 中黒 나열 해소 (2자리) — §13 인접

| 줄 | 현행 | → |
|---|---|---|
| L8 tldr | `ストレート・フラッシュ・フルハウスにはキッカーがありません` | `ストレート、フラッシュ、フルハウスにはキッカーがありません` |
| L33 stripe | `0 \| ストレート・フラッシュ・フルハウスのキッカー` | `0 \| ストレート、フラッシュ、フルハウスのキッカー` |

🔴 **회차 2의 «오독 위험 낮음» 판정을 뒤집는다.** 근거 = **`ストレートフラッシュ`가 검색량 3,600의 실재 족보명**이고,
일본어 中黒는 외래어 **복합어 내부**에도 쓰인다(`ストレート・フラッシュ` = straight flush로 읽힌다).
즉 이 나열은 «스트레이트플러시·풀하우스 2개»로 읽힐 수 있다 — 같은 글 본문·FAQ 4자리는 이미 **読点**이다(파일 내 갈림이기도 하다).

## F. `holdem-reading-the-board` 4스텝 — 🔴 표기 갈림이 아니라 **오역** (5자리)

**EN 정본은 6자리 전부 동일**: `flush → straight → paired ranks → high card`
(tldr L8 · stripe L38 `4 | scan steps: flush → straight → paired ranks → high card` · 직답 L42 · L207 · FAQ L240 · まとめ L287)

ja는 그 첫 항목 `flush`를 **「スート」(무늬)**로 옮겼다. **무늬는 족보가 아니다** — 족보 사다리에 없는 항목이 섞여 카테고리가 깨진다.
같은 글의 `:::steps`(L83 `フラッシュを確認`) · 직답(L78) · FAQ(L272 `まずフラッシュ`)는 **맞게 「フラッシュ」**다.

| 줄 | 현행 | → |
|---|---|---|
| L8 tldr | `必ず「スート → ストレート → ペア → ハイカード」の順で` | `必ず「フラッシュ → ストレート → ペア → ハイカード」の順で` |
| L40 stripe | `4 \| 読む手順:スート → ストレート → ペア → ハイカード` | `4 \| 読む手順:フラッシュ → ストレート → ペア → ハイカード` |
| L44 상단 블록 | `決まった順——スート、ストレート、ペア、ハイカード——で読み` | `決まった順——フラッシュ、ストレート、ペア、ハイカード——で読み` |
| L236 | `「スート → ストレート → ペア」の読み順の習慣` | `「フラッシュ → ストレート → ペア」の読み順の習慣` |
| L307 まとめ | `2. **順番どおりに読む** — スート → ストレート → ペア → ハイカード` | `2. **順番どおりに読む** — フラッシュ → ストレート → ペア → ハイカード` |

🪶 **L83 steps의 설명문 「7枚全部のスートを数える」는 그대로 둔다** — 거기서는 「スート를 센다」가 **동작**이라 맞다.
🪶 직답 L78의 4번째 항목만 「いちばん強いものを取る」인데 EN은 `high card`다. **이건 `:::steps` 4번째 칸(L86)과 일치시킨 것**이고
블록은 절 본문을 따르는 것이 규율(§3-D)이라 **그대로 둔다** — 통일 대상은 «첫 항목 스ート→フラッシュ» 하나다.

## G. 전각 `％` → 반각 `%` (2편 · 22자리)

- `holdem-game-order` **4** · `texas-holdem-rules-for-beginners` **18**

🔴 **헤드가 이미 판정했다** — 진행 파일 §5 헤드 처리 2026-09-07 (8) **9번 「판정 = 반각」**. 회차 1이 «파일 내 일관»만 지키고 넘겨 **소급이 안 됐다.**
코퍼스 실측 = 반각 **47편** : 전각 **2편**.
🪶 **이 두 편의 전각 `？`·`（）`·`：`는 건드리지 마라** — 그건 아직 헤드 판정 대기다(§5-A 13·14 · 핸드오프 대기열 32). `％`만이다.

---

## 5. §13 자리 — C 구간 손검산 대상

이 회차는 **카드·확률·금액을 하나도 바꾸지 않는다.** 그래도 §13 인접 자리 셋은 마감에서 손으로 본다:

| 자리 | 무엇을 검산하나 |
|---|---|
| `holdem-kicker` L8·L33 (E) | 「ストレート・フラッシュ・フルハウス」가 **3개 족보**인지(2개가 아닌지) — 키커가 없는 족보 목록이 맞는지. 스트레이트·플러시·풀하우스는 5장이 다 정해져 키커가 없다 ✅ / **스트레이트플러시도 키커가 없으므로 어느 독법이든 «참인 문장»이지만, 「ワンペア3枚·ツーペア1枚·スリーカード2枚」와 나란한 목록이라 3개여야 대응이 맞는다** |
| `holdem-reading-the-board` L8·L40·L44·L236·L307 (F) | 스캔 순서가 **찾기 어려운 것 → 쉬운 것**인지: 플러시(7장 무늬) → 스트레이트(랭크 배열) → 페어 계열 → 하이카드 ✅ EN 동형 |
| `holdem-bad-beat` L61 · `holdem-cooler` L68 (D) | 두 표가 **같은 대조**(누가 앞섰나)를 반대 방향으로 적고 있는지 — cooler 표는 「クーラー \| バッドビート」 열 순서, bad-beat 표는 「バッドビート \| クーラー」 열 순서다. **어휘만 바꾸고 열은 건드리지 마라** |

## 6. 이 회차에서 «하지 말 것»

| 자리 | 왜 |
|---|---|
| 🔒 **토너먼트 9편 전부** | GPT 팩 C 대기 · 동결 |
| **`アンダーザガン` 되돌리기** | 실측이 헤드 판정을 지지한다(뱅크 §1). 中黒는 포커 의도 0 |
| **`pot-odds`의 $150↔$100 혼재** | **EN 동형이다**(EN tldr·L19·L41·FAQ191·195가 `$50 into a $150 pot` / L52·L199가 `$100 pot + $50 bet`). 게다가 둘은 «벳 전 팟»과 «벳 후 팟»으로 **각각 참**이고 다른 것을 가르친다 → EN-먼저 등재만, ja 단독 통일 **금지**(§2-C) |
| **`本命` — ept-barcelona 1자리** | 동결편 + favorite 문맥 아님 |
| **전각 `？`·`（）`·`：`** | 헤드 판정 대기(대기열 32) |
| **2인칭 「あなた」 감축** | bad-beat 54 · cooler 36 · position-play 29 — **별도 회차 규모**(§5-D 6) |
| **seoTitle·H2 문형 교체** | §0-3. B는 «표기»만 바꾸고 문형·형태소는 그대로 |
| **헤드 우편함 회차가 가져갈 사실 자리** | betting-actions 50% 룰 «증분» · beginners ×4 조건 · equity 직답 · pot-odds 직답 · showdown 「判別できなくなった時点」 · card-counting 「多くの管轄では」 · blind-meaning 신규 착석 · **reading-the-board 보드 플러시 L158·L296**(정본 = de `fc404ca8`) |

🔴 **F(reading-the-board)와 헤드의 «보드 플러시» 정정은 같은 파일이지만 다른 자리다** — F는 L8·L40·L44·L236·L307(스캔 순서),
헤드는 **L158 표 마지막 행 · L296 FAQ**(「ボードの一番低いカードより高い1枚を持てば上回れる」 = 스트레이트플러시 반례로 승자가 뒤집힌다). **hunk가 겹치지 않는다.** B는 L158·L296을 열지 마라.

## 6-B. B 구간 메모 (Fable · 2026-09-09) — C 구간이 판정할 것

1. 🔴 **브리프 D 오기 정정** — cooler의 `本命`은 4자리 전부 favorite 문맥이었다. L76 「約4対1の本命(およそ80%)」(EN L69 `~4-to-1 favorite (about 80%)`) ·
   L176 「(たいてい大きな本命)」(EN L154 `usually a big favorite`). «해당 없음»이라 적은 것은 틀렸고, 반쪽 적용을 막기 위해 **4자리 전부 `有利`로** 고쳤다
   (L76 「約4対1で有利」 · L176 「たいてい圧倒的有利」 — L60의 「多くは圧倒的有利」와 어형 일치). 편집 후 cooler `本命` **0**.
2. 🟠 **`holdem-hand-rankings` L274·L423 「スート → ストレート → ペア」는 손대지 않았다** — EN 원문이 **`suits, then straights, then pairs`**(L273) ·
   **`suits → straights → pairs`**(L401)라 ja는 축어다. 즉 EN 단계에서 형제 글이 갈린다(reading-the-board는 EN 6자리 전부 `flush →`). → **EN-먼저** 등재 대상.
3. 🪶 B 항목 확장 — 「6マックスと9マックス」처럼 한 문장에 6·9가 짝으로 나와 **9인 표기도 같은 축으로 맞췄다**(`9マックス` 4 · `9-max` 4 → `9max` · 코퍼스 다수 20). position-play L54 표 헤더 「席(9max)」 · beginners L124·L290 · blind-meaning L232 카드.
4. 자기 게이트: `audit:hard --locale=ja` **57/57 🔴 0 · 🟠 2**(기존 C2 2건 그대로) · `check:hangul` 🔴 0 · 편집 16편 본문 백틱 0(파일당 2 = 템플릿 리터럴 여닫이) · `check:drift` ✅56.
   잔존 실측: `6マックス/6-max/9マックス/9-max/低ステークス/ローステークス/％/ストレート・フラッシュ・フルハウス` 전부 **0** · `## FAQ` 잔존 = 🔒tvc·🔒wpt 2편만(동결).

## 7. 자기 게이트 (B 구간 종료 조건)

```bash
npm run audit:hard -- --locale=ja      # 🔴 0
npm run check:hangul                    # 비KO 본문 한글 누출 0
```
+ 편집한 편마다 **백틱 0** 확인(§12-A).
