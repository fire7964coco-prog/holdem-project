# 🇰🇷 계산기 잔여 정정 레인 진행 파일

> 레인: `Holdem-calc-ko` / `harden-calc-ko` · 정본 규율은 루트 `CLAUDE.md`와 `docs/hardening-protocol.md`.

## 1. 회차 표

| 회차 | 범위 | 상태 | 커밋 | 날짜 |
|---|---|---|---|---|
| CALC-KO-1 | 한국어 계산기 SPR·아웃츠·ICM 전수 대조 및 잔여 정정 | ✅ 머지·배포 완료 | `10590ab3` · 배포 `db667c60` | 2026-09-22 |
| CALC-SHARED-2 | EN·10로케일·KO 공용 후속8건 정정(H-2 중복 포함) | ✅ 본체 머지 완료, 배포는 본체 기록 참조 | `4849f3c8` | 2026-09-22 |
| CALC-H1 | K9s·Q9s·J9s T3 등재 및 12사본 설명·권고·요약 동기화 | ✅ 구현·검증 완료, 본체 머지 대기 | 본 회차 커밋 | 2026-09-22 |

## 2. 미결

- CALC-SHARED-2는 본체 머지 완료. H-1은 CALC-H1로 구현·검증 완료 후 본체 머지 대기, H-3 오프수트 확장은 사용자 판단 대기. 97s·86s 등 그 밖의 항목 보강은 오류 정정과 별개의 개선 후보로 남긴다.
- 전체 TypeScript 검사에는 계산기 밖 649개 오류가 있다(2026-09-22 실행, EN glossary Term·다국어 Category·기존 scripts 등). 계산기 변경 파일 오류는 0개이며 이번 회차에서 범위 밖을 고치지 않았다.

## 3. 되돌리지 마라

- Q10에서 닫힌 starting 권고·티어·요약과 `outs.exactNote`를 다시 열지 않는다. 후속 승인된 CALC-H1의 지정 세 핸드·T3 요약 수정은 아래 별도 기록한다.
- `HAND_TABLE`은 CALC-H1의 K9s/Q9s/J9s 등재만 허용했다. `fallbackTier`와 H-3 오프수트 티어 확장은 변경하지 않는다.
- 계산기 메타에 SPR 훅을 되살리지 않는다. 기능 나열과 탭 자체는 유지한다.

## 4. CALC-KO-1 판정·검산

- A 브리프: `docs/harden-brief/calc-ko-1.md`.
- 동기화: A 시작과 C 진입 전에 `git merge main`; 최종 기준은 `58367528`이며 충돌 없이 fast-forward됐다.
- 아웃츠: 플랍→리버·플랍→턴·턴→리버를 분리하고 1~20아웃츠 전수 표를 조합식, `o/47`, `o/46`, Rule of 4·2로 독립 재계산했다. 20행 전건 일치. 대표값은 4아웃츠 `16.5/8.5/8.7%`, 9아웃츠 `35.0/19.1/19.6%`, 15아웃츠 `54.1/31.9/32.6%`다. 두 장을 보게 확정된 상황과 다음 한 장만 보장되는 상황을 문구로 분리했다.
- SPR: 유효 스택 정의를 두 스택 중 작은 쪽으로 바로잡고, 낮은 SPR의 드라이 보드 조건·강한 액션 예외와 중간 SPR의 팟 컨트롤/세미블러프 조건을 명시했다. 대표 입력 `3.9→낮음`, `4.0/7.9→중간`, `8.0/14.9→높음`, `15.0→매우 높음`을 실제 UI에서 확인했다. `3.95→4.0/중간` 오분류는 §5 공용 결함으로 재현했다.
- ICM: Malmuth-Harville 재귀를 독립 계산으로 대조했다. 4인 버블 `60k/40k/30k/20k`, 상금 `500k/300k/200k`에서 `332,634/272,405/228,698/166,263원`, ICM `33.3/27.2/22.9/16.6%`, 차이 `-6.7/+0.6/+2.9/+3.3%p`를 실제 UI에서 확인했다. 3인 딜 `50/30/20`, 상금 `90/40/20만원`의 ICM `617,857/485,000/397,143원`도 독립 재계산과 일치했다.
- ICM 문구: 승리·무승부·패배 후 가치를 실제 확률로 가중하는 콜/폴드 비교, 원시 칩찹 열, `%p`, 미들스택 리스크 프리미엄·극숏 예외, 세이브 앤드 칩찹과 ICM 한계를 반영했다.
- 게이트: `check:calc-parity:selftest` 59/59, `check:calc-parity:all` en+10로케일+ko 12사본 불일치 0, `check:faq-schema`, `check:cjk`, `check:seo-sync`, `audit:hard -- --schema` 통과. `npm run build`는 773개 정적 페이지와 postbuild까지 성공했다. build의 meta 숫자 마감 21건과 hard-audit C2 4건은 기존 경고이며 이번 변경 파일 유래가 아니다.
- 화면: 프로젝트 screen-review로 1440×900·390×844를 촬영했다. 두 폭 모두 페이지 가로 overflow 0px. 390px 아웃츠 3카드와 SPR 탭은 잘림이 없고, ICM 560px 표는 322px 컨테이너 내부에서만 가로 스크롤된다. 브라우저 상호작용과 별도 Playwright 검사 모두 page error 0이었다. 더미 공개 Supabase URL을 쓴 화면 촬영에서 해당 더미 요청과 Google Analytics 요청 실패만 있었으며 제품 코드 오류로 판정하지 않았다.
- 독립 검수: 현재 Codex 세션에서 파생한 읽기 전용 하위 에이전트 3개가 SPR·아웃츠, ICM, 범위·교열을 각각 1차 검수하고 수정 후 2차 및 최종 spot-check를 수행했다. 런타임이 정확한 모델 식별자를 노출하지 않아 그 이상은 기록하지 않는다. 최종 판정은 새 치명·중요·경미 오류 0건이며, 자동 게이트가 KO SPR·아웃츠·ICM 의미를 검사하지 않는 한계는 직접 산수·확률 검산과 대표 입력 UI 확인으로 보완했다.
- 범위: `HAND_TABLE`, `fallbackTier`, starting 티어·집계 요약, EN·공용·다른 로케일, 포스트는 수정하지 않았다. H-3과 H-1/H-2 확대, Equity·공용화·새 기능·SEO 재조준도 하지 않았다.

## 5. 헤드 요청

### CALC-H1 — 지정 세 핸드의 티어 역전 해소

- 브리프 = `docs/harden-brief/calc-h1.md`. 두 구현 HAND_TABLE에 K9s/Q9s/J9s T3 추가, EN+10locale+KO 12사본의 desc/action/T3 summary 동기화.
- 기존 K10s/Q10s/J10s/109s도 T3. 권고는 앞 전원 폴드 시 CO/BTN 오픈과 레이즈를 마주한 판단을 구분하며 무조건 콜·3벳을 명령하지 않는다.
- 실제 두 소스에서 HAND_TABLE/getHandName/fallbackTier를 추출해 169타입 전수 검사: 타입 T1~5 `7/8/9/89/56 → 7/8/12/86/56`, 콤보 `46/44/54/510/672 → 46/44/66/498/672`. 바뀐 것은 지정3타입×4콤보뿐이며 나머지166타입은 불변. 전체1326/T1~4합654(49.3213%) 유지. 정적47→50, 폴백122→119. H-3·폴백 함수는 불변.
- 본체 요청: H-1과 calculator-landing-rewrite §5의 K9s/Q9s 중복을 종결하되 97s·86s 개선 후보까지 완료라고 적지 않는다. 라이브 확인은 Starting Hand 탭에서 K♠9♠·Q♠9♠·J♠9♠ T3와 조건부 문구·요약을 확인한다.
- 자기 UI 12locale390px+EN/KO1440px의42개 카드선택 전부 T3·조건문·overflow0·pageerror0. 스크린리뷰 요소캡처에서 EN/KO/de/ja 가독성 직접 확인. 산출물 `tmp/screen/calc-h1/`(report.json + PNG), 수학 결과 `tmp/calc-h1-math.json` 보존.
- 게이트: calc selftest59/59·parity12/12, build773페이지·postbuild 통과. 보조의 EN/KO/두 구현 독립 검수와 본 담당의 보조작성10locale 독립 검수에서 신규 차단0. 자체 임시 검사 스크립트는 제거하고 빌드 생성 sitemap은 제외했다.

### CALC-SHARED-2 마감 — 아래 공용8건 구현 완료

- 브리프 = `docs/harden-brief/calc-shared-2.md`. 실제 변경 계산기 URL은 KO+EN+de/es/fr/hi/id/ja/ms/pt/zh/zh-hant 12개. 포스트·메타·링크 대상·starting·HAND_TABLE·fallbackTier는 불변.
- 2구현 SPR 원시비율 분류·경계 표시, 정확값 캡션, 11사전 7아웃츠 오예시 제거, ICM 실제 확률 가중/탈락 수령상금, 부호/버블 조건, 지급액 비증가 검증(동액 허용), 플레이어 메달 제거, 동적 percentage-point 단위 완료. H-2는 정확값 캡션 교정으로 함께 닫았다.
- 본체 요청: 머지·빌드·정확12URL 라이브/IndexNow·MB 통지 후 아래8건 대기 해소와 `en-first-queue` H-2를 반영. 새 영구 규율 후보는 SPR 표시용 반올림과 존 판정 분리, ICM share 차이≠행동 EV, 탈락실수령상금·동액상금 허용.
- 검수 중 발견한 선재 화면 결함도 최소 정정: ICM 비율열의 다크 면 글자 대비(`text-foreground`)와 pp 단위 줄바꿈. KO 버블 정적 비율→콜 EV 추론 잔여와 자동 공격확대 조건을 2차 교열에서 해소.
- 검증: 함수추출/독립재귀·조합 156검사, 플랍 셋 풀/쿼드 361/1081=33.395%(고정7=27.8446%), calc selftest59/59·parity12/12, build773+postbuild 통과. 실제 빌드 FAQ207문항을 소스와 전수 일치 확인. 전체 tsc 실패는 §2 별도 기록.
- 화면: 12로케일390px+EN/KO1440px SPR경계·ICM 역순거절/동액허용·플레이어/단위·outs3정확값 검증, 페이지 가로overflow0·pageerror0. EN/KO는 9SPR경계 및 4인 버블 값 확인. 공개 테스트 Supabase 도메인/GA 요청 실패는 제품 오류와 분리.
- 독립 검수: 보조는 EN·공용·KO 수학/실전/교열/범위 diff, 본 담당은 보조작성 10locale diff, 본체는 공용·EN diff와 별도 계산을 확인. 보조 지적 KO동형 조건2건/반영2, 본 담당 fr공백1건/반영1, 최종2차 새차단0. 네 명의 별도 모델 검수를 수행했다고 주장하지 않는다.
- 화면 산출물(비추적): `tmp/screen/calc-shared-tabs/*-{390,1440}-{spr,outs,icm}.png`, `report.json`, `locales.json`; EN 구도·원본seg는 `tmp/screen/calc-shared-en-valid/`. 코드 임시검사 파일은 마감 시 제거, 이미지·검증결과는 헤드 확인용 보존.

### CALC-KO-1에서 분리했던 원래8건 — CALC-SHARED-2로 전건 해소

- SPR는 표시용 0.1 반올림값으로 존을 판정해 실제 3.95~3.99, 7.95~7.99, 14.95~14.99가 상위 존으로 넘어간다. `calculator-tool.tsx`와 KO가 같은 결함이므로 공용 판정·전 로케일 회차가 필요하다.
- Rule of 4·2 설명 바로 아래에 정확값을 함께 보여 주면 9아웃츠의 `36%`와 `35.0%`가 충돌처럼 보인다. 근사치·정확값의 역할을 전 로케일에서 더 분명히 분리할 필요가 있다.
- EN 정적 표의 `7 outs = set to full house/quads`는 두 스트리트 고정 7아웃츠의 27.8%와 맞지 않는다. 플랍 셋은 턴 미스 뒤 새 보드 랭크까지 페어링 아웃이 되어 리버 아웃츠가 10장으로 늘고 실제 두 장 확률은 약 33.4%다. KO는 예시를 비워 두었고 EN·다른 로케일의 같은 행은 공용 회차에서 바로잡아야 한다.
- EN FAQ의 콜/폴드 ICM 비교는 승리·패배 값을 단순 평균하라고 적혀 있다. 실제 승률(무승부 포함) 가중 기댓값으로 바꾸고, `dict.ts`의 설명 누락도 함께 보완해야 한다.
- EN을 포함한 공용 ICM 설명의 `+차이/−차이`와 버블 조언은 부호만으로 행동을 결정하는 듯한 과잉 일반화가 있다. 보통 미들스택의 리스크 프리미엄이 가장 크며 극단적 숏스택은 예외라는 조건을 전 로케일에서 검토해야 한다.
- ICM 입력은 상금 내림차순을 검증하지 않아 1등 상금이 2등보다 작은 구조도 계산한다. 공용 입력 검증이 필요하다.
- ICM 플레이어 아이콘의 메달은 성적 순위가 아니라 입력 순서에 붙어 오해를 부를 수 있다. 공용 UI 표식을 검토해야 한다.
- 동적 차이는 비율의 차이이므로 KO는 `%p`로 바로잡았다. EN·다른 로케일의 `%` 표기에도 같은 의미 정정이 필요한지 공용 회차에서 확인해야 한다.
- 위 항목은 CALC-KO-1 당시 분리했으며, CALC-SHARED-2에서 EN·공용·10로케일·KO에 반영했다.

### 본체 머지 마감 (2026-09-22)

- 사용자 지시로 main fast-forward 머지 `10590ab3`, sitemap `/calculator` 한 곳만09-22 갱신·배포 `db667c60`.
- 본체·독립 검수자 각각 아웃츠20행/60확률칸·ICM 버블/3인 딜을 독립 검산해 일치. 신규 차단0. 본체 build773·calc parity12/12·후속 빌드 게이트 통과.
- 라이브390/1440px: page.content FAQ10개 로컬 빌드와 일치·구 단정 소멸, outs 세 확률/플랍→턴 탭, SPR6경계, ICM4인 입력·원시 칩찹·%p 확인. 페이지 가로 overflow0·pageerror0. MB-077 통지.
- 새 영구 규율 신설 없음. 이미 확립된 한 장/두 장 확률·ICM 가중 기준을 KO에 반영한 회차. 후속8건은 위 §5가 정본이며 queue Q14에 끼워 넣지 않는다.

## 6. 자산 축적 체크

- 키워드·SEO 재조준 없음(범위 밖).
- 의도적 로케일 편차 신설 없음.
- KO 계산기 의미·동작 판정은 A 브리프와 이 진행 파일에만 기록한다.
