# 🇯🇵 ja 회차 8 브리프 — 잔여 미결 소급 II (절 제목 · 표기 축 · 오독 자리)

> 규격 = `docs/hardening-protocol.md` §3-⑥. **B 구간은 이 파일 하나만 읽는다** — 포스트·EN·진행 파일을 다시 열지 마라.
> 작성 = 2026-09-09 A 구간(Opus) · 트리 기준 **`64cc6415`**(헤드 `e47a3f2f` 판정 반영 후).

## 0. 이 회차가 무엇인가

회차 7로 **42/42 클러스터 트랙은 종결**됐다. 회차 8은 클러스터가 아니라 **§2-B~§2-G에 남은 미결 중, 헤드 판정이 끝났거나 레인이 실측으로 닫을 수 있는 것만** 골라 닫는 소급 회차다.
**9편 · 22자리.** EN 이식(드리프트)은 **0** — 아래 §1에서 실물로 확인했다.

🔴 **회차 중 헤드가 `lane:sync --apply`를 돌려 트리가 바뀌었다**(`92ec21e1` → ff `64cc6415`). A 구간이 처음 잰 값 중 **`holdem-probability`의 죽은 앵커는 그 사이 헤드가 고쳤다**(§5-E 11 ✅) → **이번 회차 대상에서 뺐다.** 프로토콜 §3-③-0(브리프 전제도 실측)이 바로 이 유형이다.

## 1. 드리프트 실측 (9편 전건 · 날짜 필드가 아니라 diff·git log로)

`npm run check:drift --locale=ja` = **✅ 56 · 🔴 0**. 날짜 필드만 믿지 않고 편마다 `git log --since=<masterUpdated> -- lib/posts-en/<slug>.ts`를 돌렸다:

| 편 | masterUpdated | EN 이후 커밋 | 판정 |
|---|---|---|---|
| holdem-tournament-vs-cash-game | 2026-09-09 | 없음 | — |
| wpt-australia-2026-guide | 2026-09-09 | 없음 | — |
| ept-barcelona-2026-guide | 2026-09-09 | 없음 | — |
| holdem-fish | 2026-08-30 | `781301e4`(이미지 alt만) | **이미 반영** |
| holdem-glossary | 2026-08-30 | `781301e4`(이미지 alt만) | **이미 반영** |
| holdem-flush-vs-straight | 2026-08-12 | `8d2aba44`(HSL 토큰 · 본문 무관) | **이미 반영** |
| holdem-continuation-bet | 2026-08-19 | `851d8dc7` = EN에 3bet-pot-bet-sizing 링크 1문단 추가 | **이미 반영** — ja L144에 동형 문단 실재(축어 대조 완료) |
| holdem-pot-odds · holdem-kicker · holdem-reading-the-board | 2026-09-06~09 | 헤드 `e47a3f2f`가 09-09에 이미 이식 | **이미 반영** |

→ **미반영 0 · 이미 반영 8 · 편차 0.** 이번 회차는 EN 이식이 없다(회차 7과 같은 성격) → **`masterUpdated`는 올리지 않는다. `updated`만 2026-09-09로 올린다**(§3-H 9 규칙: 렌더되는 문장이 바뀌면 `updated`는 올린다).

## 2. 키워드 실측 — 「レート ↔ ステークス」 축의 「高」 쪽 (§5-E 13 · 헤드가 회차 8 몫으로 넘김)

🔴 회차 7 §0 규율(**「① 앵커형 ② 어순 반대형 ③ 단독형을 다 조회하고, null을 «검색량 0»으로 읽지 마라」** = `rakko-playbook` 함정 17)을 그대로 적용해 **도구 2종 × 3형태**로 다시 쟀다. 회차 7의 §5-B는 DFS 앵커형 한 줄뿐이었다.

| 표기 | DFS(2392/ja · 2026-09-09 · 이 회차 자체 조회) | ラッコ suggest(2026-09-09 · 이 회차) | 판정 |
|---|---|---|---|
| **`高レート`**(단독) | **110**(12개월 90~170 · 안정) | 서제스트 114건 — `高レート麻雀` 140 · `高レートパチンコ` 30 · `高レートスロット` 30 · `ベトナム 高レート スロット` 90 · `高レートとは` 170 | 🔴 **채택.** «도박의 레이트»가 의미장 정중앙 |
| `ポーカー 高レート`(앵커형) | **10**(12개월 전부 10 — 흔들림 없음) | — | 포커 의도 실재 |
| `高レート ポーカー`(어순 반대) | null | — | — |
| `ハイステークス`(단독) | **null** | 서제스트 33건 — **포커 의도 매출 0**: 게임 『フォートナイト ハイステークス』 · 교육 `ハイステークステスト` · `ハイステークス合同会社` · 『ニードフォースピード ハイステークス』 | 🔴 **폐기** |
| `ポーカー ハイステークス`(앵커형) | null | — | — |
| `ハイステークス ポーカー`(어순 반대) | null | — | — |
| 🪶 `ハイステークスポーカー 配信` | — | **10** | **함정** — ヨコサワ의 **방송 프로그램명**이다(`〜 ヨコサワ` · `〜 どこで見れる` · `〜 映画` · `〜 日本人`). «캐시게임 레이트»가 아니라 **콘텐츠 시청 의도** |
| `ハイレート`(단독) | 260 | 서제스트 79건 — **포커 의도 0**: 항공 `ハイレートクライム`(F-15·羽田) · 마작점 상호 `ハイレート坂戸/川越` · 충전기 `ハイレートマックス` · 유희왕 | 🔴 **폐기**(볼륨은 크지만 다른 축) |
| `ミドルレート` | — | 서제스트 **3건 전부 환율**(`為替 ミドルレートとは` · `みずほ銀行 ミドルレート`) | 🔴 **폐기 — 조어하면 FX와 충돌** |
| `ミドルステークス` | null | — | 폐기 |
| `中レート` | **null**(0 아님 — 모름) | — | 🪶 **아래 §3-⑤ 한 자리에만** |
| (참고 · 회차 7) `ポーカー レート` 170 ↔ `ポーカー ステークス` 10 | | | 축 자체가 17배 |

🔴 **회차 7이 「ハイステークス는 일본에서 확립된 복합어」(네이티브 렌즈)라며 남겼는데, 실측은 그 복합어의 의미장이 «포커 레이트»가 아니라 «포트나이트·방송·교육시험·상호»임을 보여 준다.** 유일한 포커 실측치(`〜配信` 10)조차 프로그램 시청 의도다. → **「高レート」로 통일한다.**
🪶 **`中レート`는 검색 슬롯이 아니라 «표 셀의 서술어» 한 자리에만 쓴다**(§3-⑤). 근거: `ミドルレート`는 측정된 함정(FX)이고 `ミドルステークス`는 null이라 **둘 다 못 쓴다**. `低レート`(확정)·`高レート`(이번 확정) 사이가 형태적으로 투명하다. 조어이므로 **다른 자리로 번지지 않게** §3-I에 못 박는다.

## 3. 편별 지시 — 이 목록 밖은 손대지 마라

### ① `holdem-tournament-vs-cash-game` — masterUpdated 2026-09-09 · updated는 올린다

**L401** `## FAQ` → **`## よくある質問`**
- 근거 = §3-H 5(코퍼스 50 대 3) + **헤드 판정 2026-09-09 (8) §5-E 9 = 「よくある質問」**.
- 🔴 이 편은 **이벤트 가이드가 아니다** → ept·wpt처럼 「— 대회명」 접미를 붙이지 마라. **맨 `## よくある質問`**(코퍼스 50편 형).
- 🪶 **앵커**: `#faq` → `#よくある質問`. 코퍼스의 프래그먼트 참조는 **0건**(`grep -rn "](#" lib/posts-ja/` = 0 · 이 회차 재실측). 회차 7이 같은 개칭 7편에서 실해 0을 실증했다.
- 🔴 **이 편은 전각 규약 파일이다**(`？`31 · `（）`13 · `：`10 — §2-E · §5-C 8). **다른 자리의 전각을 반각으로 바꾸지 마라.** 이번 개칭은 문장부호가 없어 규약과 무관하다.

### ② `wpt-australia-2026-guide` — updated 올린다

**L369** `## FAQ — WPT オーストラリア 2026` → **`## よくある質問 — WPT オーストラリア 2026`**
- 근거 = 헤드 판정 §5-E 9 + **거울쌍 선례**: 같은 회차 5의 `ept-barcelona-2026-guide` L508이 이미 `## よくある質問 — EPT バルセロナ 2026`이다. 접미(「— 대회명」)는 **유지**한다.
- 🪶 이 편만 접두가 달랐다 — 두 렌즈가 회차 7에서 독립으로 «미완»을 지적한 자리다.

### ③ `holdem-fish` — updated 올린다 · **3자리**

세 자리 전부 `ハイステークス` → **`高レート`**. 조사(助詞)는 그대로 두고 명사만 바꾼다.

- **L98**(표 「ホエール(クジラ)」 행 3열): `超ルース、ハイステークス、気にしない` → `超ルース、高レート、気にしない`
- **L111**(불릿): `たいてい裕福でハイステークスをゆるく打っています` → `たいてい裕福で高レートをゆるく打っています`
- **L205**(FAQ 답): `たいてい裕福なプレイヤーがハイステークスをゆるくギャンブルしています` → `…が高レートをゆるくギャンブルしています`
- 🔴 **표의 다른 열·수치는 건드리지 마라.** 이 편의 §13 자리(카드 문단)는 이번 diff 밖이다.

### ④ `holdem-glossary` — updated 올린다 · **1자리**

- **L186**(용어표 「ホエール(クジラ)」 행): `高いステークスにいる裕福で弱いレクリエーションプレイヤー` → **`高レートにいる裕福で弱いレクリエーションプレイヤー`**
- 🪶 `レクリエーションプレイヤー`는 **그대로 둔다** — 회차 6 §2-F의 `娯楽勢` 갈림은 이미 해소됐고(코퍼스 `娯楽勢` 0), 이번 축이 아니다.

### ⑤ `ept-barcelona-2026-guide` — updated 올린다 · **1자리**

- **L110**(이벤트표 「向いている人」 열): `ミドルステークスのグラインダー` → **`中レートのグラインダー`**
- 근거 = §2 실측. EN 원문 = `Mid-stakes grinders`(`lib/posts-en/ept-barcelona-2026-guide.ts` L101 축어) — 의미는 «중간 레이트의 그라인더»로 보존된다.
- 🔴 **같은 행의 `**€1,650**`·`8月16日〜22日`은 절대 건드리지 마라.** 표의 다른 6행도 무변경.
- 🔴 **L113 `誰もが狙う本命`은 그대로 둔다** — §3-H 3이 «간판 이벤트» 예외로 남긴 자리다. favorite 축과 무관.

### ⑥ `holdem-reading-the-board` — updated 올린다 · **1자리**

- **L35** `### 結論から` → **`### 数字で見るボードの読み方`**
- 근거(§2-B) = 이 H3 바로 아래가 `:::stripe`이고, stripe가 끝난 **L43에 `> **先に結論**` 블록**이 온다. 「結論から」와 「先に結論」이 두 줄 건너 연속한다. **라벨은 규격이라 고정**이므로 헤딩 쪽을 바꾼다.
- 코퍼스 실측(stripe 앞 H3 31개): 「ひと目で/一目で」 계열 15 · 「数字で(見る)」 계열 5 · **「結論」 계열은 이 편과 ⑦ 둘뿐**. 선례 축어 = `### 数字で見るスターティングハンド`.
- stripe 행이 `5 |` `3 |` `4 |` 숫자 3개라 「数字で見る」가 내용과 맞는다.
- 🔴 **stripe 안의 `4 | 読む手順:フラッシュ → ストレート → ペア → ハイカード` 행은 손대지 마라** — §3-H 7이 확정한 스캔 순서다(`スート` 아님).

### ⑦ `holdem-flush-vs-straight` — updated 올린다 · **1자리**

- **L26** `### まず結論から` → **`### フラッシュとストレート、ひと目で`**
- 근거 = ⑥과 같다(L34에 `> **先に結論**`). 코퍼스 선례 축어 = `### フィッシュとは、ひと目で` · `### クーラーをひと目で` · `### バッドビート、ひと目で`.
- 🔴 **stripe의 `5,108 vs 10,200`·`#5 vs #6`은 손대지 마라**(§13 수치).

### ⑧ `holdem-continuation-bet` — updated 올린다 · **10줄 / 13자리**

`⅓`·`⅔`(U+2153·U+2154) → **`3分の1`·`3分の2`**. 뒤에 붙는 `ポット`·`+`·괄호는 그대로.

| 줄 | 현행 축어 | 고친 뒤 |
|---|---|---|
| L8 tldr | `サイズはドライなら約⅓ポットと小さく、ウェットなら⅔ポット以上と大きく。` | `…約3分の1ポットと小さく、ウェットなら3分の2ポット以上と大きく。` |
| L30 stripe | `⅓ポット` + `｜ ドライボードでの小さな「レンジベット」サイズ` | `3分の1ポット`(뒤 무변경) |
| L61 | `*ほとんどの*フロップで約⅔ポットのCベットを打て` | `…約3分の2ポットの…` |
| L85 표 | `**高頻度で小さく**(⅓)打つ` | `**高頻度で小さく**(3分の1)打つ` |
| L135 | `**大きく——⅔ポット以上**——` | `**大きく——3分の2ポット以上**——` |
| L139 | `- **⅓ポット**のCベットは==$10==` | `- **3分の1ポット**のCベットは==$10==` |
| L140 | `- **⅔ポット**のCベットは==$20==` | `- **3分の2ポット**のCベットは==$20==` |
| L267 FAQ | `小さく——約⅓ポット——` / `大きく——⅔ポット以上——` | 각각 `3分の1`·`3分の2` |
| L304 まとめ | `**ドライは小さく(⅓)、ウェットは大きく(⅔+)。**` | `**ドライは小さく(3分の1)、ウェットは大きく(3分の2+)。**` |

- 🔴 **왜 이 편만인가**: 이 파일은 **같은 개념(베트 사이즈 = 팟 분수)을 두 표기로 동시에** 쓴다 — L130 직답 블록·L134·L144는 이미 `3分の1`/`3分の2`다. **한 파일 안에서 갈린 자리**가 이번 회차의 유일한 분수 결함이다. 코퍼스 전체는 `分の` 142 대 `⅓⅔` 16.
- 🔴 **L44·L251·L301의 `約3分の2`는 «확률»이다**(플롭을 빗나갈 확률). 문자열이 같아도 **다른 개념이니 그대로 둔다** — 바꿀 것이 없다.
- 🔴 **`holdem-pot-odds`의 `¼ ⅓ ½ ⅔ ¾`는 손대지 마라**(§3-I 참조). 그 표는 5분수가 **한 계열로 닫힌 표기 체계**이고 파일 안에서 일관한다.
- **§13**: L136의 `ポット==$30==` → L139 `$10`(30 × 1/3) · L140 `$20`(30 × 2/3). 표기만 바뀌고 **수치는 불변**이어야 한다. C 구간에서 재검산한다.

### ⑨ `holdem-kicker` — updated 올린다 · **1자리**

- **L74**: `ストレート、フラッシュ、フルハウスはすでに5枚すべてを使っているので、キッカーに回すカードが残らない`
  → **`ストレート、フラッシュ、フルハウス、ストレートフラッシュはすでに5枚すべてを使っているので、キッカーに回すカードが残らない`**
- 판정 근거(§2-G 3) = 이 편은 **«완결 규칙을 진술하는 자리»는 4족보**(L54 직답 블록 · L206 FAQ 답 · L69~70 표는 `ストレートフラッシュ / ロイヤルフラッシュ` 행을 따로 갖는다), **«압축 요약»은 3족보**(L8 tldr · L33 stripe · L225 まとめ)로 이미 갈라져 있다. **L74만 «규칙의 도출»을 진술하면서 3족보라 예외**다.
- 🔴 **L8 tldr · L33 stripe · L225 まとめ는 바꾸지 마라** — «압축»이 정본이라는 것이 이번 판정이다(§3-I에 기록).
- 🔴 **같은 줄의 「役の組み合わせ枚数 + キッカー = 常に5枚」 산식과 「ワンペアは2枚使うので、残り3枚がキッカー」는 손대지 마라.**

## 4. 이번 회차가 **하지 않는 것** (근거를 남긴다 — 다음 회차가 다시 열지 않도록)

1. 🔴 **見出し 전각 콜론 `：` 34자리** — **손대지 않는다.** 실측: 전각 헤딩 34(7편) 대 반각 75. **어느 파일도 파일 안에서 갈리지 않는다**(전각 전용 7편 = card-counting 3 · drawing-odds 4 · game-order 9 · outs 5 · positions 3 · tvc 5 · beginners 5 · 나머지는 전부 반각). 「파일 내 일관이면 남긴다」가 이 레인의 규율이다. 🪶 회차 3·4·5가 **세 번** 헤드에 올렸고 아직 답이 없다 → §5-F에 **네 번째로, 판정에 필요한 마지막 조각을 붙여** 올린다.
2. 🔴 **`ハイステークスポーカー`(프로그램명)를 근거로 「ハイステークス는 살아 있다」고 되돌리지 마라** — §2 표의 함정 행이 그것이다.
3. 🔴 **`holdem-probability` 앵커** — 헤드가 `e47a3f2f`로 이미 고쳤다(`#pot-odds` → `/ja/blog/holdem-pot-odds`). 다시 손대지 마라.
4. 🟠 **`あなた` 밀도**(bad-beat 54 · cooler 36 · position-play 29 …) — **EN 동형**(EN "you"는 자연)이라 ja 단독 감축은 갈림을 만든다. §5-F EN-먼저.
5. 🟠 **`holdem-pot-odds`의 「$150のポットに$50コール」(tldr·L20·L216·L220) ↔ 「$100のポットに$50ベット」(L54 직답·L59·L201·L224)** — 둘 다 참이고(벳 후 팟 / 벳 전 팟) **최종 팟 $200·25%로 산수가 완전히 일치**한다(A 구간 재검산 완료). 표기 통일이 아니라 **다리 한 줄**이 처방인데, 그 자리가 §13 수치 문단이라 **렌즈를 태운 뒤 넣는 편이 안전하다** → 회차 9 또는 §5-F. 이번 회차는 **손대지 않는다.**
6. 🟠 **`3bet` L89 中黒** · **`reading-the-board` 4번째 항목 갈림** — §3-H 4·7이 «재론 금지»로 확정한 자리다.

## 5. §13 자리 — C 구간 손검산 대상 (이번 diff가 닿는 문단만)

| 편 | 자리 | 무엇을 검산하나 |
|---|---|---|
| `holdem-continuation-bet` | **L136~L140** | 팟 `$30` → 3分の1 = `$10` · 3分の2 = `$20`. **표기 교체가 금액을 건드리지 않았는지** |
| `holdem-continuation-bet` | L44·L251·L301 | `約3分の2`(확률)가 **무변경**인지 — 사이즈 표기 치환이 확률 문장을 물면 안 된다 |
| `holdem-kicker` | L54·L69~70·L74·L206·L225 | 「조합 매수 + 키커 = 5」가 4족보 목록 추가 후에도 성립하는지(ストレートフラッシュ = 5장 사용 → 키커 0 ✓) |
| `holdem-flush-vs-straight` | L28~L30 stripe | `5,108 vs 10,200` · `#5 vs #6` **무변경** |
| `holdem-reading-the-board` | L37~L40 stripe | `5 / 3 / 4` 행과 스캔 순서 `フラッシュ → ストレート → ペア → ハイカード` **무변경** |
| `ept-barcelona-2026-guide` | L106~L116 표 | `€825 / €1,650 / €2,700 / €3,250 / €5,300 / €10,300 / €100,000` · 날짜 7행 **무변경** |
| `holdem-fish` | L98 표 행 | 다른 3열 **무변경** |

## 6. B 구간 공통 금지

- 🔴 **백틱 금지**(CLAUDE.md §12-A) · slug·이미지 경로·readnext 대상·태그 변경 금지 · 새 출처 창작 금지.
- 🔴 **직답 블록을 새로 만들거나 지우지 마라** — 이번 회차는 직답 신설이 **0**이다(42편 전편이 이미 보유).
- 🔴 **굵게·하이라이트가 줄을 넘게 쓰지 마라** — `b4360cf2`에서 라이브에 `**`가 축어 노출된 사고가 있었다(렌더러가 줄 넘는 강조를 안 받는다).
- 🔴 **`masterUpdated`는 올리지 않는다. `updated`만 `"2026-09-09"`로.** 편마다 Edit(일괄치환 금지 — 프로토콜 §5-⑦).
- 편집이 끝나면 `npm run audit:hard -- --locale=ja`만 돌린다(렌즈는 C 구간).

## 7. 편집 후 자기 점검(B 구간 · 기계)

```
npm run audit:hard -- --locale=ja
grep -rn "^## FAQ" lib/posts-ja/
grep -rn "ハイステークス|高いステークス|ミドルステークス" lib/posts-ja/
grep -rn "U+2153|U+2154" lib/posts-ja/holdem-continuation-bet.ts
grep -rn "^### .*結論" lib/posts-ja/
```

기대값: audit 🔴 0 · `## FAQ`는 `japan-poker-tournaments-guide` 1건만(대상 밖) · 스테이크스 3표기 **0** · cbet 분수기호 **0** · `### …結論`은 `holdem-tournament-vs-cash-game` L41 「15秒で結論」 1건만(stripe 헤딩이 아니라 유지).
