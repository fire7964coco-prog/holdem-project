# de 솔버 랜딩 키워드 뱅크 — `/de/solver`

> 2026-08-24 실측. 도구 = **라쿠 `search-volume-history`**(볼륨 · Germany · 48개월 · requestId 1255693) +
> **DataForSEO Labs**(서제스트·SD · location 2276 · de) + **DataForSEO SERP API**(google.de top-10 + PAA).
> 🟢 **두 벤더 볼륨 일치 8/8** — `gto wizard` 2,400·2,400 · `gto poker` 390·390 · `poker range` 320·320 ·
> `poker solver` 140·140 · `poker trainer` 140·140 · `piosolver` 90·90 · `gto solver` 50·50 · `solver poker` 30·30.
> 🪶 선행 실측 = `de-core-volumes.md`(2026-08-10 · lowfruits LDA 포함) — **큰 그림 일치**(gto poker 390 · poker range 320).

---

## 0. 한 줄 결론

**de는 ja·es·pt 어느 쪽 규율도 그대로 못 옮긴다. 네 가지가 다르다.**
① **어순이 pt와 반대다** — `poker solver` **140** 대 `solver poker` 30 (pt는 `solver poker` 170이 컸다).
② **`solver` 단독 오염은 pt와 같다**(엑셀) — 그러나 **`GTO`는 깨끗하다**(es 과나후아토 같은 동음이의 없음).
③ **`kostenlos` 계열이 볼륨 null**이다 — 무료 수요는 실재하나(Reddit 「Beste (kostenlose) Poker Solver?」가
   top-10에 뜬다) **검색어 문자열로는 안 잡힌다.** 차별화는 카피(«im Browser, ohne Anmeldung»)로.
④ **`postflop` 볼륨 10** — pt(0)와 같이 죽은 축. 살아 있는 것은 **`flop turn river` 390**(pt 260보다 크다).

조준 1순위 = **`gto poker`/`poker gto` 390**(SD 6~25 · 오염 없음 · 제목 편입) +
**`poker range(s)` 320**(SD 6~11 · **플랍 이후로 가른다** — es·pt와 같은 구조) + **`poker solver` 140**(SD 11).

---

## 1. 🔴 de 고유 함정 (2026-08-24 실측)

### ① `solver` 단독 = 독일에서도 «Excel Solver»다

SERP(`solver` · google.de): `de.wikipedia.org`(1위) · `support.microsoft.com`(Excel) · Solver Advisory(기업) ·
Solver Service GmbH · Rheinwerk(Excel 교재) · solverglobal(FP&A) · IMDb(영화).
**PAA**: `Was bedeutet Solver?` · `Wie verwende ich Solver in Excel?`
`was ist ein solver`도 위키·엑셀·SOLIDWORKS 지배(Reddit r/poker가 2위로 끼는 정도).
→ 🔴 **제목·H1·H2에서 `Solver`는 반드시 `Poker`/`GTO`와 붙인다**(pt §1-B-①과 같은 규율·같은 이유).

### ② 어순 — `poker solver` 140 대 `solver poker` 30 (**pt와 반대다**)

| 표기 | 볼륨 | SD(sugg) |
|---|---:|---:|
| **`poker solver`** | **140** | 11 |
| `gto solver` | 50 | 🔴 57(sugg — 참고만. suggestions 엔드포인트 SD는 재현 불안정 실증 있음) |
| `solver poker` | 30 | 11 |
| `poker solver kostenlos` · `gto solver kostenlos/deutsch` | **null** | — |

→ 제목·본문 기본 어순은 **`Poker Solver`**. 🪶 독일어 자연 조어(`GTO-Solver`처럼 하이픈 합성)와 검색어가
   일치하는 행운의 판 — pt의 «제품명≠쿼리» 긴장이 de에는 약하다.

### ③ `kostenlos`·정의형·도구형 롱테일은 거의 전부 볼륨 null

null 실측: `gto poker tabelle`·`gto tabelle(n)`·`gto poker kostenlos`·`was ist gto poker`·`gto strategie`·
`gto rechner`·`poker range lernen`·`was ist eine range poker`·`poker trainer kostenlos`·`poker range tabelle`·
`postflop strategie`·`poker solver deutsch`.
🪶 **2026-08-10 lowfruits에서 `gto poker tabelle`·`gto poker kostenlos`가 LDA 7이었다** — SERP가 약한 것은
사실이나 **볼륨이 없으니 단독 조준은 않고 H2·FAQ가 흡수**한다([[low-volume-longtail-aggregation]]).

### ④ 의도 함정 둘 — 게임이다

| 키워드 | 볼륨 | SERP 실측 |
|---|---:|---|
| `poker simulator` | 390 | 게임 6/10(aarp·xbox·arkadium·spiele-kostenlos) + 트레이너 2 + 리뷰 — **주류가 무료 게임. 조준 금지**(es `simulador` 동일 함정) |
| `poker ohne anmeldung` | 320 | **10/10 무료 게임 플레이.** 조준 금지 — 단 «ohne Anmeldung» **표현 자체는 차별화 카피로 유효** |

### ⑤ 금지 축 (de-core-volumes 시드 ⑪에서 확정 · 재확인)

`poker bot`(170) · `rta poker`(40) 계열 = **온라인 부정행위 도구 검색.** 어떤 각도로도 채택 금지.
서제스트에도 `real-time solver poker`류가 올라온다 — 랜딩은 «실시간 보조가 아니라 공부 도구»라는 한 줄을 FAQ에.

---

## 2. 볼륨표 (Germany · 라쿠 48개월 ↔ DFS 대조)

### 조준 (소유자 판정은 §4)

| 키워드 | 볼륨 | SD(sugg) | 메모 |
|---|---:|---:|---|
| **`gto poker` / `poker gto` / `gto in poker`** | **390** | 6~25 | 🎯 **1순위 — 제목 편입**(pt 선례: 오염 없음). ⚠ 12m 추세 −30%(완만한 하강) |
| **`poker range` / `poker ranges`** | **320** | 6~11 | 🎯 **플랍 이후 한정** — 프리플랍은 chart 소유(§4) |
| **`flop turn river`** | **390** | — | 🎯 2차축(pt 260보다 크다 · comp 1) |
| **`poker solver`** | **140** | 11 | 🎯 어순 정본(대 `solver poker` 30) |
| **`poker trainer`** | **140** | **7** | 🎯 트레이너 절이 받는다. `poker trainer deutsch` 10 · `poker trainer app` 20 |
| `poker hand range` / `poker hands range` | 70 | 8 | 🟢 본문 흡수 |
| `gto solver` | 50 | (57 sugg) | 🎯 |
| `solver poker` | 30 | 11 | 병기 흡수 |
| `gto trainer` | 20 | 24 | |
| `poker simulator kostenlos` | 20 | — | 🟢 12m **+50%** 상승 — FAQ 한 줄로 흡수(게임과 구분 명시) |
| `gto poker bedeutung` | 10 | — | 🟢 **2024-08 첫 등장 신생 쿼리** — FAQ 정의 문항 |
| `gto poker lernen` · `gto poker deutsch` · `poker im browser` · `equity rechner (poker)` | 10 | — | FAQ·본문 흡수 |

### 제외 (이유와 함께)

| 키워드 | 볼륨 | 왜 |
|---|---:|---|
| `poker strategie` | 1,300 | 🔴 `de/holdem-strategy` 소유(seoTitle+태그). ⚠ 추세 급락(4,400→720 · 12m −45%) |
| `poker charts` | 390 | 프리플랍 차트 의도 → `holdem-starting-hands-chart` 방면 |
| `poker simulator` | 390 | 게임 의도(§1-④) |
| `poker ohne anmeldung` | 320 | 게임 의도(§1-④) |
| `poker rechner` | 260 | 계산기 축 — de에 도구 페이지 없음. 앱 내 Equity-Rechner 언급으로만 |
| `preflop charts` · `poker range chart` · `poker preflop tabelle` | 110 · 50 · 10 | 🔴 프리플랍 → chart 소유 |
| `poker software` | 90 | 추세 급락(480→30) + HUD/트래커 의도 혼재 |
| `poker bot` · `rta poker` | 170 · 40 | 🔴 부정행위 축(§1-⑤) |
| `o.— gto wizard` | **2,400** | 브랜드(참고). `gto wizard deutsch` 20~30 · `piosolver` 90 · `wasm postflop` 10 |

---

## 3. SERP 실측 (google.de · 2026-08-24)

### 🟢🟢 `gto solver`·`poker solver` — **«돌아가는» 무료 브라우저 솔버가 top-10에 0개** (pt와 같은 빈자리)

`gto solver`: Reddit 「Bester GTO-Solver?」(1위) · gtowizard(구독) · 앱스토어 ×2 · pokerfirma(리뷰
「Beste Poker Solver 2026」) · github TexasSolver(설치형) · **Reddit 「Completely Free GTO Trainer/Solver」** ·
piosolver(설치형) · simplepoker · 888poker.de(가이드).
`poker solver`: gtowizard · **Reddit 「Beste (kostenlose) Poker Solver?」** · 앱스토어 ×2 · deepsolver(구독) ·
pokerfirma · solvers.poker(리뷰) · youtube · 888poker.de · lucidpoker.
→ **무료 수요가 Reddit 스레드로만 채워지고 있다** = 우리가 들어갈 자리. 경쟁자 제목에 「kostenlos」 전면
   표방은 없음 → **훅 = «kostenlos + im Browser + ohne Anmeldung» 조합**(ja 「登録不要」 판정과 동형).

**PAA 축어**: `Was ist gto wizard?` · **`Was ist gto beim Poker?`** · `Wie wird man gut im Poker?`

### `gto poker`(390) — 정의 콘텐츠 + 도구 혼재

gtowizard(1위) · Reddit FAQ · **888poker.de 「Einführung in GTO Poker」** · Amazon(책) · upswing(EN) ·
youtube · GTO Poker Trainer 앱 · pokerfirma. **PAA**: `Was ist gto im Poker?` · `Was bedeutet gto?`
→ 정의 H2 하나(«Was ist GTO …»)가 필요하되 **도구 관점으로 좁힌다**(ja 「GTOツールとは」 선례 —
   개념 일반 정의는 `holdem-strategy`가 태그를 갖고 있다. §4).

### `poker range`(320) — **top-10이 프리플랍·계산기·핸드리딩. 포스트플랍 0개** (es·pt와 같은 구조)

pokerhandrange.com(계산기) · 888poker.de(상대 레인지 읽기) · poker.de(전략) · openpokertools(도구) ·
youtube · freebetrange(프리플랍 차트) · Reddit · blog.gtowizard(**Preflop** Range Morphology) ·
pokerstars.de(C벳) · poker-toolkit(**Preflop**-Range-Tabellen).
→ **«레인지가 플랍 이후 어떻게 되나»는 비어 있다** — 랜딩의 1순위 서사.

---

## 4. 카니발 — de 43편 전수 grep (2026-08-24)

| 축 | 소유자 | 랜딩의 처리 |
|---|---|---|
| **`poker strategie` 계열** | 🔴 `de/holdem-strategy`(seoTitle+태그 4형) | 가져가지 않는다 |
| **`gto poker`·`poker range` «태그»** | ⚠ `de/holdem-strategy`가 태그로 달고 있으나 **본문 H2 14개 중 GTO 축 0**(GTO 언급 1회 = FAQ 참조점 설명 · Range H2는 «정의» 하나) | **명목 태그다**(ja `holdem-strategy`와 동형). 랜딩이 **도구·포스트플랍 축**을 가져간다. 개념 정의 문장은 랜딩 FAQ에서 짧게, 깊은 정의는 그 글 링크 |
| **`gto preflop charts`·`poker starthände chart`·`preflop charts`·`poker charts`** | 🔴 `de/holdem-starting-hands-chart`(태그 `gto preflop charts` 명시 + 본문 GTO 13회) | **프리플랍 표는 그 글.** 랜딩은 포스트플랍으로 가른다 — **첫 내부링크** |
| `3-bet range`·`linear vs polarisiert range` | `de/holdem-3bet`(태그) | 프리플랍 3벳 축 침범 금지 |
| `c-bet` 계열 | `de/holdem-continuation-bet` | 무관 — 본문 링크로 |
| **`poker solver`·`gto solver`·`poker range` 일반형·`flop turn river`·`poker trainer`** | 🟢 **소유자 0**(43편 태그·seoTitle 전수) | **랜딩이 가져간다** |

🪶 `solver` 언급 10편·`range` 언급 23편이지만 **어느 글도 solver/range를 조준축(제목·태그)으로 갖고 있지 않다**
(strategy의 명목 태그 제외). `holdem-position-play`(solver 6회)는 포지션 축.

---

## 5. 🔴 앱 실측 (`?lang=de` · Playwright · 2026-08-24)

본체 = **`docs/solver-app-verbatim-5langs-2026-08-24.md`**(13스팟 축어 + 판정). 골격만:

- `<html lang="de">` · title **`HoldemMaster GTO Solver – Kostenloser Online-Solver für Texas Hold’em`**
- 히어로 **`GTO-Strategie, direkt im Browser.`** / **`Nichts zu installieren, nichts zu bezahlen.`**
- 네비 `Lernspots ⚡ Sofort` · `GTO-Trainer EV-Note` · `Preflop-Charts Ranges` · `Equity-Rechner Sieg-%`
- 사이드바 `① OOP-Range ② IP-Range ③ Board ④ Bet Sizes Spielbaum ⑤ Berechnen`
- 특징 4칸: `Kostenlos` · `Offline lernen` · `Schnelles Solving` · **`GTO-Trainer — benotet nach
  EV-Verlust im Verhältnis zum Pot`** 🟢(팟 대비 정정본)
- 🔴 **소수 구분자 = 쉼표**(`5,5bb` · `77,9%`) — 랜딩 본문도 독일 표기(쉼표)로 통일한다
- 🔴 앱 결함 4자리: **④ 콜러 우위(8언어 전건)** · ⑦ RP-19 + 조작 지시 · ⑧ RP-03 · ⑩은 de 「weitgehend」로 완화형 🟢
  → **랜딩은 전부 §4-B 정정본을 쓴다**

---

## 6. 조준 확정 (발행 시 갱신)

- **1차**: `gto poker`/`poker gto`(390) — 제목 · `poker range(s)`(320) — H1·H2, 플랍 이후 한정 ·
  `poker solver`(140) — 제목·어순 정본
- **2차**: `flop turn river`(390) · `gto solver`(50) · `poker trainer`(140)+`gto trainer`(20) ·
  `poker hand range`(70) · `gto poker bedeutung`(10·신생) · `poker im browser`(10) ·
  `poker simulator kostenlos`(20·FAQ) · PAA 3문항(`Was ist GTO beim Poker?` 등)
- **제외**: §2 제외표 전부
- **훅**: «Kostenlos · im Browser · ohne Anmeldung/Installation» — 경쟁 top-10 제목에 없는 조합.
  ⚠ 「무료」 단독은 pokerfirma·Reddit이 이미 다룬다 — **«설치도 계정도 없이 지금 바로»**가 차별점
